# MSO 코드베이스 2차 심층 진단 보고서
> 작성일: 2026-05-10
> 조사 방법: 6개 전문 에이전트 병렬 전수 조사
> 제외 항목: V1(보안진단), V2(CODEX_TASKS_V2) 지시 완료 항목

---

## 조사 영역

| 에이전트 | 조사 영역 |
|---------|---------|
| A | Next.js 15/16 특화 문제 |
| B | 데이터 페칭 · 로딩 · 페이지네이션 |
| C | Supabase 패턴 |
| D | 날짜 · 타임존 · i18n |
| E | 환경설정 · tsconfig · next.config |
| F | 테스트 · PWA · 인쇄 스타일 |

---

## 핵심 요약

| 분류 | 항목 | 심각도 | 조치 |
|------|------|--------|------|
| 기능 버그 | 타임존 이중 계산 (date-utils.ts) | 🔴 | 즉시 |
| 기능 버그 | Supabase Realtime 구독 누수 2곳 | 🔴 | 즉시 |
| 보안 | next.config.ts 보안 헤더 전무 | 🔴 | 즉시 |
| 성능 | 게시판 500건 무제한 로딩 | 🟠 | 1주 |
| 코드 품질 | 메신저 silent-fail catch | 🟠 | 1주 |
| 코드 품질 | OP체크 빈 catch 4개 | 🟠 | 1주 |
| 코드 품질 | 구성원현황 입력 유효성 없음 | 🟠 | 1주 |
| UX | error.tsx 전무 | 🟠 | 1주 |
| 성능 | 대형 컴포넌트 dynamic import 미적용 | 🟠 | 2주 |
| 코드 품질 | toLocaleString() 로케일 미지정 | 🟡 | 2주 |
| 코드 품질 | tsconfig 엄격 옵션 부족 | 🟡 | 2주 |
| 성능 | `<img>` → next/Image 미전환 22개 | 🟡 | 2주 |
| 아키텍처 | 'use client' 77.5% 과남용 | 🟡 | 장기 |
| 접근성 | Suspense 경계 전무 | 🟡 | 장기 |
| 테스트 | 인쇄 스타일 미검증 | 🟡 | 장기 |

---

## 1. 기능 버그

### 1-1. 타임존 이중 계산 🔴
**파일**: `app/utils/date-utils.ts` 줄 33-36

```typescript
// ❌ new Date()는 이미 로컬 시간 → 9시간 추가 = KST+9 = UTC+18
const now = new Date();
const koreaTime = new Date(now.getTime() + 9 * 60 * 60 * 1000);
return koreaTime.toISOString().slice(0, 19);
// → 한국 브라우저에서 실제 시간보다 9시간 앞선 값 반환
```

**영향 범위**: 이 함수를 호출하는 근태 기록, 퇴원심사 타임스탬프, 출퇴근 기록 전체

```typescript
// ✅ 수정 — Date.prototype 로컬 getter 직접 사용
const now = new Date();
const pad = (n: number) => String(n).padStart(2, '0');
return `${now.getFullYear()}-${pad(now.getMonth()+1)}-${pad(now.getDate())}T${pad(now.getHours())}:${pad(now.getMinutes())}:${pad(now.getSeconds())}`;
```

---

### 1-2. Supabase Realtime 구독 누수 🔴

#### 게시판.tsx
**파일**: `app/main/기능부품/게시판.tsx` (grep: `supabase.channel`)

두 개의 채널 구독이 useEffect cleanup 없이 존재.  
컴포넌트 재마운트마다 WebSocket 연결 누적 → Supabase 연결 수 초과 에러.

```typescript
// ❌ 현재
useEffect(() => {
  const ch1 = supabase.channel('board-changes').on(...).subscribe();
  const ch2 = supabase.channel('notifications').on(...).subscribe();
  // return 없음
}, []);

// ✅ 수정
useEffect(() => {
  const ch1 = supabase.channel('board-changes').on(...).subscribe();
  const ch2 = supabase.channel('notifications').on(...).subscribe();
  return () => {
    supabase.removeChannel(ch1);
    supabase.removeChannel(ch2);
  };
}, []);
```

#### 업무가이드.tsx
**파일**: `app/main/기능부품/게시판서브/업무가이드.tsx` (grep: `supabase.channel`)

3테이블 postgres_changes 구독 cleanup 없음.

```typescript
// ✅ 수정 — useEffect return 추가
return () => { supabase.removeChannel(channel); };
```

---

## 2. 보안

### 2-1. HTTP 보안 헤더 전무 🔴
**파일**: `next.config.ts`

`headers()` 설정이 아예 없어 아래 헤더가 모두 전송되지 않음:

| 헤더 | 방어 대상 |
|------|---------|
| `X-Frame-Options: DENY` | Clickjacking |
| `X-Content-Type-Options: nosniff` | MIME 스니핑 |
| `X-XSS-Protection: 1; mode=block` | 반사형 XSS |
| `Referrer-Policy: strict-origin-when-cross-origin` | 레퍼러 노출 |
| `Permissions-Policy: camera=(), microphone=()` | 권한 남용 |
| `Strict-Transport-Security` | HTTP 다운그레이드 |

```typescript
// next.config.ts — NextConfig 객체에 추가
async headers() {
  return [{
    source: '/(.*)',
    headers: [
      { key: 'X-Frame-Options', value: 'DENY' },
      { key: 'X-Content-Type-Options', value: 'nosniff' },
      { key: 'X-XSS-Protection', value: '1; mode=block' },
      { key: 'Referrer-Policy', value: 'strict-origin-when-cross-origin' },
      { key: 'Permissions-Policy', value: 'camera=(), microphone=(), geolocation=()' },
      { key: 'Strict-Transport-Security', value: 'max-age=63072000; includeSubDomains; preload' },
    ],
  }];
},
```

---

## 3. 성능

### 3-1. 게시판 500건 무제한 로딩 🟠
**파일**: `app/main/기능부품/게시판.tsx`

```typescript
// ❌ 현재 — .limit 없이 전체 조회
supabase.from('board_posts').select('*').order('created_at', { ascending: false })
```

게시글 누적 시 수백 KB 응답 → 초기 렌더링 지연.

```typescript
// ✅ 20건 페이지네이션
const PAGE_SIZE = 20;
const [page, setPage] = useState(0);

supabase
  .from('board_posts')
  .select('id, title, author_name, created_at, view_count, board_type', { count: 'exact' })
  .order('created_at', { ascending: false })
  .range(page * PAGE_SIZE, (page + 1) * PAGE_SIZE - 1)
```

### 3-2. 대형 컴포넌트 dynamic import 미적용 🟠

초기 번들에 포함된 대형 컴포넌트 (메인 컴포넌트에서 동적 임포트 권장):

| 파일 | 줄수 |
|------|------|
| OP체크.tsx | 3,768 |
| 메신저.tsx | 3,395 |
| 게시판.tsx | 2,975 |
| 근태관리메인.tsx | 2,507 |
| 알림시스템.tsx | 2,353 |

```typescript
// 메인 라우팅 컴포넌트에서
import dynamic from 'next/dynamic';

const OP체크 = dynamic(() => import('./기능부품/OP체크'), { ssr: false });
const 메신저 = dynamic(() => import('./기능부품/메신저'), { ssr: false });
```

---

## 4. 코드 품질

### 4-1. 메신저 silent-fail catch 🟠
**파일**: `app/main/기능부품/메신저.tsx`

여러 async 블록에서 에러를 catch 후 아무 처리 없이 무시:

```typescript
// ❌ 에러가 삼켜짐 — 메시지 전송 실패 여부 사용자 모름
try {
  await supabase.from('messages').insert(payload);
} catch {
  // 아무것도 없음
}
```

```typescript
// ✅ 최소 처리
} catch (error) {
  console.error('[메신저] 메시지 전송 실패:', error);
  // 필요 시 사용자 toast 알림
}
```

### 4-2. OP체크 빈 catch 4개 🟠
**파일**: `app/main/기능부품/OP체크.tsx`

수술 아이템 저장/수정 관련 async 블록 4곳에서 catch 내부 비어 있음.  
실패해도 사용자에게 피드백 없음.

### 4-3. 구성원현황 입력 유효성 없음 🟠
**파일**: `app/main/기능부품/인사관리서브/구성원현황.tsx`

직원 등록 폼에서 서버 제출 전 클라이언트 유효성 검사 없음:
- 이름 필드: 빈 문자열 허용
- 주민등록번호: 형식 검증 없음
- 연락처: 숫자 형식 검증 없음

```typescript
// ✅ 제출 핸들러에 추가
if (!form.name?.trim()) {
  alert('이름을 입력해 주세요.');
  return;
}
if (!/^\d{6}-\d{7}$/.test(form.ssn ?? '')) {
  alert('주민등록번호 형식이 올바르지 않습니다.');
  return;
}
```

### 4-4. toLocaleString() 로케일 미지정 🟡
**파일**: 다수 (grep: `toLocaleString()`)

```typescript
// ❌ 브라우저 로케일에 따라 다른 결과 (영문 OS에서 다른 숫자 형식)
amount.toLocaleString()

// ✅ 항상 한국 형식 보장
amount.toLocaleString('ko-KR')
```

패턴이 전체에 퍼져 있어 일괄 처리 필요. `grep -rn "\.toLocaleString()" app/` 로 전수 확인.

---

## 5. UX · 에러 처리

### 5-1. error.tsx 전무 🟠

```
app/
  error.tsx          ← ❌ 없음
  main/
    error.tsx        ← ❌ 없음
    loading.tsx      ✅ 1개만 존재
```

데이터 페칭 실패 시 전체 페이지 crash. 에러 경계 없음.

```tsx
// app/error.tsx (신규)
'use client';
export default function GlobalError({ error, reset }: { error: Error; reset: () => void }) {
  return (
    <div className="flex flex-col items-center justify-center min-h-screen gap-4 p-8">
      <h2 className="text-xl font-bold text-[var(--foreground)]">오류가 발생했습니다</h2>
      <p className="text-sm text-[var(--toss-gray-3)]">{error.message}</p>
      <button type="button" onClick={reset}
        className="px-4 py-2 bg-[var(--accent)] text-white rounded-[var(--radius-md)] text-sm font-semibold">
        다시 시도
      </button>
    </div>
  );
}
```

### 5-2. Suspense 경계 1개뿐 🟡

전체 387 TSX 중 Suspense 사용처 1개 (app/main/page.tsx).  
async 컴포넌트 로딩 중 UI 없음, 폭포식 데이터 요청 구조.

---

## 6. Next.js 특화 문제

### 6-1. 'use client' 77.5% 과남용 🟡

| 범위 | 파일수 | 'use client' | 비율 |
|------|--------|-------------|------|
| 전체 TSX | 387 | 300 | 77.5% |
| app/components | 16 | 13 | 81.3% |
| 기능부품 | 308 | 301 | 97.7% |

서버 캐싱 불가, 초기 번들 비대화. 데이터 표시 전용 컴포넌트(근태관리메인, 알림시스템 등)부터 분리 가능.

### 6-2. `<img>` 태그 22개 🟡

next/Image 미사용 파일 일부:
```
게시판.tsx
관리자전용서브/급여명세서서식관리.tsx  (2건)
관리자전용서브/전자결재양식관리.tsx   (2건)
마이페이지/index.tsx
... 외 17개
```

**비용**: WebP 자동변환 없음, CLS(레이아웃 시프트) 발생, 반응형 srcset 미지원.

### 6-3. 페이지별 메타데이터 없음 🟡

`app/layout.tsx`에만 전역 metadata 설정. 모든 페이지가 동일한 title/description 사용.  
OG 이미지, twitter:card, canonical, robots 미설정.

---

## 7. 환경설정

### 7-1. tsconfig.json 엄격 옵션 부족 🟡

누락 또는 false로 설정된 옵션:

```json
{
  "compilerOptions": {
    "noImplicitReturns": true,           // 현재: false/없음
    "noFallthroughCasesInSwitch": true,  // 현재: false/없음
    "noUncheckedIndexedAccess": true     // 현재: false/없음 (파급 효과 큼, 별도 수정 필요)
  }
}
```

실용 순서: `noImplicitReturns` → `noFallthroughCasesInSwitch` → (별도 PR) `noUncheckedIndexedAccess`

---

## 8. 테스트 · PWA · 인쇄

### 8-1. 인쇄 스타일 미검증

인쇄 전용 컴포넌트(급여명세서, 계약서, 퇴원심사서)에서 `@media print` 블록 외부 요소가 인쇄 시 노출될 수 있음.  
사이드바, 상단 네비, 버튼 등에 `print:hidden` 적용 여부 전수 확인 필요.

```bash
grep -rn "print:" app/ --include="*.tsx"
```

현재 적용 파일 수 극히 적음 → 인쇄 시 레이아웃 깨짐 가능.

### 8-2. 테스트 커버리지

```
app/__tests__/     → 0개
app/api/__tests__/ → 0개
```

유닛 테스트, API 라우트 테스트 전무. 최소 아래 대상부터 권장:
- `app/utils/date-utils.ts` (타임존 버그 방지)
- `app/api/discharge-review/route.ts` (AI + 보안 로직)
- `app/api/chat/og-preview/route.ts` (SSRF 방어 로직)

---

## 조치 우선순위

### 즉시 (1-2일)
1. `date-utils.ts` 타임존 이중 계산 수정 → **근태/퇴원심사 타임스탬프 오류 방지**
2. `게시판.tsx` + `업무가이드.tsx` Realtime cleanup → **WebSocket 연결 누수 차단**
3. `next.config.ts` 보안 헤더 추가 → **Clickjacking·MIME 스니핑 방어**

### 1주 내
4. `app/error.tsx`, `app/main/error.tsx` 신규 생성
5. `게시판.tsx` 페이지네이션 (500건 → 20건)
6. `OP체크.tsx`, `메신저.tsx` catch 블록 처리
7. `구성원현황.tsx` 폼 유효성 검사 추가

### 2주 내
8. `tsconfig.json` `noImplicitReturns`, `noFallthroughCasesInSwitch` 활성화
9. 대형 컴포넌트 dynamic import (OP체크, 메신저, 게시판)
10. `toLocaleString()` → `toLocaleString('ko-KR')` 일괄 치환

### 장기 (1개월+)
11. `<img>` → `next/Image` 22개 전환
12. 'use client' 분리 리팩터링 (서버/클라이언트 컴포넌트 구조 개선)
13. Suspense 경계 주요 세그먼트 추가
14. 단위 테스트 작성 (date-utils, AI 라우트)

---

## 기존 보고서와의 관계

| 보고서 | 내용 |
|--------|------|
| `보안진단보고서_2026-05-10.md` | Prompt Injection, SSRF, Rate Limit, 인증 |
| `코드진단보고서_2026-05-10.md` | any 타입, @ts-nocheck, 파일 크기, useMemo 버그 |
| `디자인진단보고서_2026-05-10.md` | slate 색상, bg-white, aria-label, 반응형 |
| **이 보고서** | 타임존 버그, Realtime 누수, 보안 헤더, 페이지네이션, error.tsx, 'use client' 과남용 등 |

---

*정적 분석 + 6개 전문 에이전트 병렬 전수조사 기반. 실제 침투 테스트 별도 권장.*
