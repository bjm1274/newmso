-- =====================================================================
-- Migration 0004: 비밀번호 초기화 플래그 컬럼 추가
--
-- 배경: 관리자가 직원 비밀번호를 초기화한 뒤, 해당 직원이 다음 로그인 시
-- 입력하는 비밀번호를 새 비밀번호로 설정하는 워크플로를 지원한다.
--
-- 보안: 비밀번호 미설정 계정을 제3자가 선점하는 취약점 방지를 위해
-- password_reset_required = 1 인 계정만 first-login(자동 설정)을 허용.
-- 우연히 비밀번호가 없는 계정(0 또는 NULL)은 여전히 차단된다.
--
-- password_reset_required : 0 = 일반(기본), 1 = 관리자가 초기화함
-- =====================================================================

ALTER TABLE staff_members ADD COLUMN password_reset_required integer DEFAULT 0;
