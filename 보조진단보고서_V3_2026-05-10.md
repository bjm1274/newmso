# MSO 코드베이스 심층 진단 보고서 V3
> 작성일: 2026-05-10 | V1·V2 미포함 영역 전수조사
> 조사 범위: Next.js 특화 / 데이터 페칭 / Supabase 패턴 / 날짜·i18n / 환경설정 / 테스트·PWA

---

## 핵심 요약

| 항목 | 심각도 | 즉시 조치 |
|------|--------|----------|
| Supabase Realtime 구독 누수 | 🔴 높음 | ✅ 즉시 |
| 날짜 타임존 이중 계산 버그 | 🔴 높음 | ✅ 즉시 |
| next.config.ts 보안 헤더 없음 | 🔴 높음 | ✅ 즉시 |
| 게시판 500건 무제한 로딩 | 🟠 중간 | 1주 내 |
| OP체크 빈 catch 블록 4개 | 🟠 중간 | 1주 내 |
| error.tsx 전무 | 🟠 중간 | 1주 내 |
| dynamic import 대형 파일 미적용 | 🟠 중간 | 2주 내 |
| `<img>` → next/Image 미전환 | 🟡 낮음 | 2주 내 |
| 'use client' 과남용 (77.5%) | 🟡 낮음 | 장기 |
| tsconfig 엄격 모드 미흡 | 🟡 낮음 | 장기 |

---

## 1. Supabase Realtime 구독 누수 🔴

### 1-1. 게시판.tsx
**파일**: `app/main/기능부품/게시판.tsx` 줄 615, 631

```typescript
// ❌ 현재 — cleanup 없음
const channel = supabase.channel('board-changes').on(...).subscribe();
const channel2 = supabase.channel('notifications').on(...).subscribe();
// useEffect return 없음 → 컴포넌트 언마운트 시 구독 유지
```

```typescript
// ✅ 수정
useEffect(() => {
  const ch1 = supabase.channel('board-changes').on(...).subscribe();
  const ch2 = supabase.channel('notifications').on(...).subscribe();
  return () => {
    supabase.removeChannel(ch1);
    supabase.removeChannel(ch2);
  };
}, []);
```

### 1-2. 업무가이드.tsx
**파일**: `app/main/기능부품/게시판서브/업무가이드.tsx` 줄 716

```typescript
// ❌ 현재 — 3테이블 구독 cleanup 없음
const channel = supabase.channel('guide-room')
  .on('postgres_changes', { table: 'guides' }, handler)
  .on('postgres_changes', { table: 'guide_comments' }, handler)
  .on('postgres_changes', { table: 'guide_views' }, handler)
  .subscribe();
// cleanup 없음
```

```typescript
// ✅ 수정
useEffect(() => {
  const channel = supabase.channel('guide-room')
    .on(...)
    .subscribe();
  return () => { supabase.removeChannel(channel); };
}, []);
```

**영향도**: Realtime WebSocket 연결이 컴포넌트 재마운트마다 누적 → 연결 수 초과 시 Supabase 에러, 메모리 누수

---

## 2. 날짜 타임존 이중 계산 버그 🔴

**파일**: `app/utils/date-utils.ts` 줄 33-36

```typescript
// ❌ 현재 — new Date()는 이미 로컬 시간, 거기에 9시간 추가하면 이중 적용
const now = new Date();
const koreaTime = new Date(now.getTime() + 9 * 60 * 60 * 1000);
return koreaTime.toISOString().slice(0, 19);
```

```typescript
// ✅ 수정 — 로컬 시간을 ISO 형식으로 직접 변환
const now = new Date();
const pad = (n: number) => String(n).padStart(2, '0');
return `${now.getFullYear()}-${pad(now.getMonth()+1)}-${pad(now.getDate())}T${pad(now.getHours())}:${pad(now.getMinutes())}:${pad(now.getSeconds())}`;
```

**영향도**: 한국 시간(KST)이 UTC+9인 환경에서 18시간 오차 발생 가능. 근태 기록, 퇴원심사 타임스탬프 전체에 영향

---

## 3. next.config.ts 보안 헤더 없음 🔴

**파일**: `next.config.ts`

현재 `headers()` 설정이 없어 아래 보안 헤더가 전혀 전송되지 않음:

| 헤더 | 기능 |
|------|------|
| `X-Frame-Options: DENY` | Clickjacking 방지 |
| `X-Content-Type-Options: nosniff` | MIME 스니핑 방지 |
| `X-XSS-Protection: 1; mode=block` | XSS 기본 방어 |
| `Referrer-Policy: strict-origin-when-cross-origin` | 레퍼러 정보 제한 |
| `Permissions-Policy` | 카메라/마이크 등 권한 제한 |
| `Strict-Transport-Security` | HTTPS 강제 |

```typescript
// ✅ 추가할 headers 설정
async headers() {
  return [
    {
      source: '/(.*)',
      headers: [
        { key: 'X-Frame-Options', value: 'DENY' },
        { key: 'X-Content-Type-Options', value: 'nosniff' },
        { key: 'X-XSS-Protection', value: '1; mode=block' },
        { key: 'Referrer-Policy', value: 'strict-origin-when-cross-origin' },
        { key: 'Permissions-Policy', value: 'camera=(), microphone=(), geolocation=()' },
        {
          key: 'Strict-Transport-Security',
          value: 'max-age=63072000; includeSubDomains; preload',
        },
      ],
    },
  ];
},
```

---

## 4. 게시판 500건 무제한 로딩 🟠

**파일**: `app/main/기능부품/게시판.tsx`

```typescript
// ❌ 현재 — limit(500) 또는 limit 없이 전체 로딩
const { data } = await supabase.from('board_posts').select('*').order('created_at', { ascending: false });
```

```typescript
// ✅ 페이지네이션 적용
const PAGE_SIZE = 20;
const { data, count } = await supabase
  .from('board_posts')
  .select('id, title, author_name, created_at, view_count', { count: 'exact' })
  .order('created_at', { ascending: false })
  .range(page * PAGE_SIZE, (page + 1) * PAGE_SIZE - 1);
```

**영향도**: 게시글 누적 시 초기 로딩 수백KB 이상 → 모바일 성능 저하

---

## 5. OP체크 빈 catch 블록 4개 🟠

**파일**: `app/main/기능부품/OP체크.tsx`

```typescript
// ❌ 현재 (4개 위치) — 에러 무시
try {
  await supabase.from('op_checklist_items').insert(items);
} catch {
  // 빈 블록
}
```

```typescript
// ✅ 수정 — 최소 console.error 또는 toast 처리
} catch (error) {
  console.error('[OP체크] 저장 실패:', error);
  // 필요 시 사용자 알림 추가
}
```

---

## 6. error.tsx 전무 🟠

**전체 앱**에 `error.tsx` 파일이 0개입니다. 데이터 페칭 실패 시 전체 페이지가 crash됩니다.

```
app/
  error.tsx          ← 없음 (루트)
  main/
    error.tsx        ← 없음
    loading.tsx      ✅ (유일하게 존재)
```

**최소 추가 대상**:
```
app/error.tsx        — 전체 앱 에러 경계
app/main/error.tsx   — 메인 대시보드 에러 경계
```

---

## 7. dynamic import 대형 파일 미적용 🟠

초기 번들에 포함된 대형 컴포넌트:

| 파일 | 줄수 | 동적 임포트 현황 |
|------|------|----------------|
| OP체크.tsx | 3,768 | ❌ 정적 로드 |
| 메신저.tsx | 3,395 | ❌ 정적 로드 |
| 게시판.tsx | 2,975 | ❌ 정적 로드 |
| 근태관리메인.tsx | 2,507 | ❌ 정적 로드 |
| 알림시스템.tsx | 2,353 | ❌ 정적 로드 |

**적용 파일**: `app/main/메인.tsx` (또는 라우팅 컴포넌트)

```typescript
import dynamic from 'next/dynamic';

const OP체크 = dynamic(() => import('./기능부품/OP체크'), { ssr: false });
const 메신저 = dynamic(() => import('./기능부품/메신저'), { ssr: false });
const 게시판 = dynamic(() => import('./기능부품/게시판'), { ssr: false });
```

---

## 8. `<img>` → next/Image 미전환 🟡

**22개 파일**에서 HTML `<img>` 태그 사용 중. 주요 파일:

```
app/main/기능부품/게시판.tsx
app/main/기능부품/관리자전용서브/급여명세서서식관리.tsx  (2건)
app/main/기능부품/관리자전용서브/전자결재양식관리.tsx   (2건)
app/main/기능부품/마이페이지/index.tsx
... 외 17개
```

**영향도**: WebP 자동변환, srcset, lazy-load 미적용 → 불필요한 고해상도 다운로드

---

## 9. 'use client' 과남용 🟡

| 범위 | 파일수 | 'use client' | 비율 |
|------|--------|-------------|------|
| 전체 TSX | 387 | 300 | 77.5% |
| app/components | 16 | 13 | 81.3% |
| 기능부품 | 308 | 301 | 97.7% |

서버 컴포넌트로 전환 가능한 대표 파일: `게시판.tsx`, `근태관리메인.tsx`, `알림시스템.tsx` (데이터 표시 위주)

**장기 개선 방향**: 서버 컴포넌트(데이터 페칭)와 클라이언트 컴포넌트(인터랙션) 분리

---

## 10. tsconfig.json 엄격 모드 미흡 🟡

```json
// 현재 누락 또는 비활성화된 옵션들
{
  "compilerOptions": {
    "noUncheckedIndexedAccess": false,   // 배열/객체 인덱스 접근 undefined 가능성 무시
    "exactOptionalPropertyTypes": false, // optional 타입 정확도 낮음
    "noImplicitReturns": false,          // 함수 반환값 누락 허용
    "noFallthroughCasesInSwitch": false, // switch fall-through 허용
  }
}
```

**현실적 추가 순서**: `noImplicitReturns` → `noFallthroughCasesInSwitch` → `noUncheckedIndexedAccess` (파급 효과 가장 큼, 별도 수정 필요)

---

## 조치 우선순위

### 1단계 (즉시, 1-2일)
1. `게시판.tsx`, `업무가이드.tsx` Realtime cleanup 추가
2. `date-utils.ts` 타임존 이중 계산 수정
3. `next.config.ts` 보안 헤더 추가

### 2단계 (1주 내)
4. `app/error.tsx`, `app/main/error.tsx` 신규 생성
5. `게시판.tsx` 무제한 로딩 → 페이지네이션
6. `OP체크.tsx` 빈 catch 블록 처리

### 3단계 (2주~1개월)
7. 대형 컴포넌트 dynamic import 적용
8. `<img>` → next/Image 전환 (22개)
9. `tsconfig.json` 엄격 옵션 단계적 활성화
10. 'use client' 분리 (장기 리팩터링)

---

*이 보고서는 V1(보안진단), V2(CODEX_TASKS_V2) 대상 외 새로운 항목만 수록합니다.*
