# MSO 코드 진단 보고서 (심층 2차)
> 작성일: 2026-05-10 | 대상: `app/` 전체 520개 파일 | 1차 대비 신규 발견 포함

---

## 핵심 요약

| 항목 | 심각도 | 파일수 | 건수 | 상태 |
|------|--------|--------|------|------|
| `as any` 타입 단언 | 🔴 높음 | 31 | 109+ | 미해결 |
| `@ts-nocheck` (Roster 전체) | 🔴 높음 | 7 | — | 미해결 |
| 파일 크기 500줄 초과 | 🔴 높음 | 34 | — | 미해결 |
| 중복 훅 파일 | 🔴 높음 | 1쌍 | 2 | 미해결 |
| `useMemo` 의존성 버그 | 🔴 높음 | 1 | 1 | **신규** |
| 미사용 코드 (메신저 레거시) | 🟡 중간 | 1 | 1 | **신규** |
| `@ts-ignore` | ✅ 없음 | 0 | 0 | 양호 |
| `catch (err: any)` | ✅ 해결 | 0 | 0 | **개선됨** |
| `console.log` 잔존 | 🟢 낮음 | 1 | 1 | 대부분 개선됨 |

---

## 1. TypeScript 타입 안전성 🔴

### 1-1. `as any` 단언 (109건, 31개 파일)

1차 조사 55건 → 심층 분석으로 **109건 확인**.

**영역별 분포**:
| 영역 | 건수 | 우선순위 |
|------|------|---------|
| 인사관리서브 | ~25건 | 🔴 1순위 |
| Roster 근무표 | ~12건 | 🔴 1순위 |
| 관리자전용 | ~15건 | 🟠 2순위 |
| API 라우트 | ~5건 | 🟠 2순위 |
| UI 컴포넌트 | ~10건 | 🟡 3순위 |

**핵심 패턴 및 수정 방향**:

```typescript
// ❌ 세션 접근 (roster-recommendation/route.ts:1725)
const userId = String((session.user as any)?.id || 'unknown');

// ✅ 수정
type SessionUser = { id: string; name?: string };
const userId = String((session.user as SessionUser)?.id || '');

// ❌ 배열 캐스팅 (데이터백업.tsx)
.upsert(batch as any[]);

// ✅ 수정 - Zod 검증 후 타입 확보
const validBatch = BackupSchema.array().parse(batch);
await supabase.from('...').upsert(validBatch);

// ❌ 브라우저 API (ESL관리.tsx)
const nav = navigator as any;
nav.bluetooth.requestDevice(...);

// ✅ 수정
const nav = navigator as Navigator & {
  bluetooth: { requestDevice(opts: unknown): Promise<BluetoothDevice> }
};
```

### 1-2. `@ts-nocheck` — Roster 영역 7개 파일 전체

**영향 파일**:
1. `roster/generateRosterPatternDraft.ts`
2. `roster/RosterPatternManager.tsx`
3. `roster/RosterWizardModal.tsx` ← Props가 `Record<string, any>` 전체
4. `roster/runPartialRosterRegeneration.ts`
5. `roster/useRosterPlannerPanels.tsx`
6. `roster/useRosterPolicyEditors.ts`
7. `roster/useRosterWizardState.ts`

**원인**: `RosterWizardModal`의 props가 30개 이상인데 전부 미타입화.
```typescript
// 현재
type RosterWizardModalProps = Record<string, any>;

// 수정 방향
interface RosterWizardModalProps {
  OFF_SHIFT_TOKEN: string;
  activeStaffs: StaffMember[];
  selectedCompany: string;
  selectedMonth: string;
  // ... 30개 명시
}
```

**조치**: 다음 마일스톤에서 Roster 모듈 타입 체계화 필수.

### 1-3. Window/Navigator 인터페이스 확장 미흡

| 파일 | 현재 코드 | 올바른 수정 |
|------|----------|-----------|
| `ErrorBoundary.tsx` | `(window as any).__errorLog` | `declare global { interface Window { __errorLog?: Error[] } }` |
| `알림시스템.tsx` | `(navigator as any).setAppBadge()` | `'setAppBadge' in navigator && navigator.setAppBadge?.(n)` |
| `ESL관리.tsx` | `const nav = navigator as any` | 구체적 BLE 인터페이스 선언 |

---

## 2. 파일 크기 초과 (JM 규칙 위반) 🔴

1차 조사 10개 → 심층 분석으로 **34개 확인**.

### 상위 10개 (분해 시급)

| 파일 | 줄수 | 분해 가능 컴포넌트 |
|------|------|-----------------|
| `OP체크.tsx` | **3,768** | OpCheckHeader, Checklist, MetaPanel, ProgressPanel, ScheduleList |
| `메신저.tsx` | **3,395** | MessageRenderer, Timeline, SidebarManager |
| `게시판.tsx` | **2,975** | BoardSearch, TemplateManager, CommentSection |
| `근태관리메인.tsx` | **2,507** | DateFilter, TableRenderer, ApprovalSection |
| `알림시스템.tsx` | **2,353** | NotificationGroup, FilterPanel, ListRenderer |
| `구성원현황.tsx` | **2,081** | ChartPanel, StatFilter, MemberTable |
| `급여정산.tsx` | **1,931** | PayTable, CalcLogic, EditForm |
| `인계노트.tsx` | **1,881** | Editor, SignaturePanel, Preview |
| `roster-recommendation/route.ts` | **1,778** | 프롬프트 구성, AI 응답 처리 분리 |
| `출퇴근기록.tsx` | **1,667** | MapView, TimeFilter, Statistics |

**OP체크.tsx 분해 전략**:
```
OP체크.tsx (3,768줄) →
├── OpCheckWorkspaceHeader.tsx (~200줄)
├── OpCheckChecklistSection.tsx (~400줄)
├── OpCheckMetaPanel.tsx (~300줄)
├── OpCheckProgressPanel.tsx (~200줄)
├── OpCheckScheduleList.tsx (~300줄)
└── op-check-hooks.ts (~300줄)
```

---

## 3. 중복 훅 파일 🔴 (신규 발견)

### 3-1. 메신저 워크플로우 훅 2개 중복

| 파일 | 줄수 | 차이점 |
|------|------|--------|
| `메신저메시지워크플로훅.ts` | 104줄 | `buildForwardedMessageContent()` 없음 |
| `메신저메시지액션워크플로훅.ts` | 104줄 | `buildForwardedMessageContent()` 있음 (줄 32-42) |

두 파일이 거의 동일한 로직 — 번들 2배 증가, 유지보수 버그 위험.

**조치**: `메신저메시지워크플로훅.ts` 삭제 후 `메신저메시지액션워크플로훅.ts`로 일원화. import 경로 일괄 변경.

---

## 4. `useMemo` 의존성 버그 🔴 (신규 발견)

**파일**: `app/main/hooks/useInventoryFilters.ts`

```typescript
// ❌ 현재 (줄 52 부근)
const expiryThreshold = useMemo(() => Date.now() + EXPIRY_SOON_MS, []);
// 의존성 []이므로 마운트 시점에 고정됨 → 시간이 지나면 만료 체크 오작동

// ✅ 수정
const expiryThreshold = Date.now() + EXPIRY_SOON_MS;
// useMemo 제거 — 상수에 가까운 값, 마운트마다 재계산이 맞음
```

**영향**: 재고 유효기간 알림 기능 오작동 가능성 (중요 의료 기능).

---

## 5. 중복 Supabase 쿼리 성능 문제 🟠

**파일**: `app/components/GlobalSearch.tsx`

```typescript
// ❌ 현재 - board_posts 2번 쿼리
const [byTitle, byContent] = await Promise.all([
  supabase.from('board_posts').select(...).ilike('title', term).limit(5),
  supabase.from('board_posts').select(...).ilike('content', term).limit(5),
]);
// approvals도 동일 패턴 반복

// ✅ 수정 - OR 조건 1번 쿼리
const { data } = await supabase
  .from('board_posts')
  .select(...)
  .or(`title.ilike.${term},content.ilike.${term}`)
  .limit(10);
```

API 호출 횟수가 현재보다 50% 감소.

---

## 6. 메신저 레거시 버튼 미사용 (신규 발견)

**파일**: `app/main/기능부품/메신저사이드바.tsx:138`

```tsx
<button className="hidden">
  {/* 레거시 그룹 생성 모달 - 현재 미사용 */}
</button>
```

→ 완전 제거 또는 기능 구현 필요.

---

## 7. 에러 처리 현황 ✅

1차에서 발견된 `catch (err: any)` 21개 → **현재 0개** (개선 완료).
`catch (error: unknown)` 패턴 27건 정상 사용 중.

---

## 8. 프로덕션 코드 오염 🟢

`@/lib/logger` 일괄 전환으로 `console.log` 직접 사용 **1건** 잔존:
```typescript
// 출퇴근기록.tsx (logger 내부에 any 캐스팅 잔존)
logger.warn('위치 확인 실패:', error && (error as any).message ? (error as any).message : error);
```

`error instanceof Error ? error.message : String(error)` 로 수정 필요.

---

## 즉시 조치 우선순위 (업데이트)

| 순위 | 항목 | 파일 | 난이도 | 영향도 |
|------|------|------|--------|--------|
| 1 | `useInventoryFilters.ts` useMemo 버그 | 1개 | 낮음 | 🔴 재고 기능 오작동 |
| 2 | 메신저 중복 훅 제거 | 2개 | 낮음 | 🔴 번들 최적화 |
| 3 | Roster `@ts-nocheck` 제거 | 7개 | 높음 | 🔴 타입 안전성 |
| 4 | GlobalSearch 중복 쿼리 최적화 | 1개 | 낮음 | 🟠 성능 |
| 5 | OP체크.tsx 분해 | 1개 | 매우 높음 | 🟠 유지보수 |
| 6 | `as any` → 타입 정의 | 31개 | 높음 | 🟠 타입 안전성 |
| 7 | 메신저 레거시 버튼 제거 | 1개 | 낮음 | 🟢 코드 정리 |

---

*1차 보고서 대비 추가: @ts-nocheck 7개, as any 109건(+54), useMemo 버그 1건, 중복 훅 1쌍, 500줄 초과 34개(+24), GlobalSearch 쿼리 중복.*
