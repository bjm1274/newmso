# MSO 모바일 표준화 — 진행 상태 및 남은 작업 (2026-05-15 기준)

> 이번 세션(`mobile-std` 브랜치) 완료분 + 남은 작업 정리.
> 다음 세션에서 이 문서를 먼저 읽고 이어 진행하면 됨.

---

## 1. 이번 세션 완료 (mobile-std 17커밋, main 2커밋, 미푸시)

| 단계 | 결과 |
|---|---|
| **0-1 백업** | `Downloads/newmso_backup_2026-05-15.tar.gz` (1.4G, 6266개 항목) |
| **0-2 브랜치** | `mobile-std` 분기, `docs/mobile/` 지시서 4종 커밋 |
| **P0-1 토큰** | `globals.css` `@media(max-width:767px)` 오버라이드 추가 (body 13→15, 헤더 19, 터치 44px, radius 상향). 브라우저 검증: 모바일 375px / 데스크톱 1280px 모두 통과 |
| **P0-2 헤딩** | `시스템마스터센터.tsx` 중복 메뉴명 h2 제거 (단일 사례 — 나머지는 audit 2026-05-09에 이미 처리됨) |
| **P0-3 네비** | `page.tsx` + `인사관리.tsx` 2차 서브메뉴 칩바에 접기/펼치기 토글 추가. 1차 하단탭바는 `조직도측면창.tsx`에 이미 구현됨(미작업) |
| **P1-2 시트** | `ActionDialog`·`RiskActionDialog`에 모바일 BottomSheet 분기 + `useIsMobile` 훅 신규 |
| **P1-3 푸터** | `StickyFormFooter.tsx` 신규, `계약서자동생성` 시범 적용. `--mobile-bottomtab-height` 토큰 60→69px 정정(실측 일치) |
| **P1-4 회귀** | `playwright.config.ts` `mobile-320` 프로젝트 + `tests/e2e/mobile-overflow.mobile.spec.ts` 5 테스트, 320px 통과 |
| **17개 미커밋 정리** | 연차대장 작업을 main에 2커밋(도구+lib / UI)으로 분리 커밋 후 mobile-std에 머지. `산출물`(PII)·`__pycache__`는 `.gitignore` |
| **P1-1 마이그레이션 41화면** (배치1~12) | 배치1×5 / 배치2×6 / 배치3×6 / 배치4×4 / 배치5×3 / 배치6×4 / 배치7×2 / 배치8×3 (회사관리·재고관리통합·ApprovalMetaPanels부분) / 배치9×3 (최저임금체크·세전세후비교·무급결근차감) / 배치10×2 (시스템마스터센터부분·일일근태현황) / 배치11×2 (구성원현황·근태관리메인부분) / 배치12×1 (구성원현황/index). 매 배치 tsc 0 에러 |
| **P1-1 부적합 스킵 26화면** (배치4~12) | 인사: 건강검진관리·사고보고서·스마트서류제출·교육내역명단·계약서명단·통상임금계산기·일용근로정산·급여계산기·급여고도화센터·연차수동부여·간호근무표·근태일정뷰·근태달력뷰·월간근태달력·면허보수교육검토·JobCategoryTrainingMatrix / 재고: 재고실사·재고현황뷰·AS반품관리 / 결재: SuppliesItemTable·ApprovalInboxView·비품구매양식·전자결재.tsx / roster: RosterPreviewTable·RosterStaffConfigTable / 루트: 마감보고 |

### 잡은 회귀 3건
1. P1-4 `mobile-320` testMatch 과범위 → 기존 smoke 3건 깨짐 → 오버플로우 스펙으로만 한정
2. P1-3 `--mobile-bottomtab-height` 토큰(60px) vs 실제 탭바(69px) 9px 어긋남 → 푸터가 탭바 덮음 → 토큰 실측 정정
3. P1-1 배치2 `임금피크제` StaffRecord prop을 너무 좁힘 → `StaffMember.string|null` 필드 호환 실패 → nullable 허용으로 완화

### 검증 공백 (다음 세션 우선)
- P0-2, P0-3 시각 검증은 로그인 상태에서 일부만 수행. **재고관리·관리자 화면의 P0-3 토글 동작은 미검증.**
- P1-2 BottomSheet 분기는 코드·tsc만 검증. **실제 ActionDialog/RiskActionDialog를 모바일에서 띄워 시트로 뜨는지 미검증.**
- P1-1 17개 마이그레이션 화면 모두 **브라우저 시각 검증 미수행** (로그인 세션 손실 + 깊은 라우팅으로 도달 비용 큼). tsc·diff 검토만 완료.

---

## 2. 1단계(Tasks) 잔여

### P1-1 — 표 → ResponsiveTable (사실상 완료)
- 41/72 마이그레이션 완료 (~57%). 26개는 부적합 패턴으로 스킵, 6개는 인쇄/Phase3 대상.
- **마이그 적합한 데이터 그리드는 모두 처리됨** — 잔여는 별도 컴포넌트가 필요한 부적합 화면뿐.

**P1-1 처리 현황** (72 = 마이그 41 + 부적합 26 + 인쇄/Phase3 6 — 일부 화면이 표 2개 포함하여 모수 ±1 차이)
- ✅ 마이그 41: 인사 22, 재고 9, 관리자 4, 전자결재 1(부분), roster 1, 루트 1, 통합 1
- ⏭️ 부적합 26: 매트릭스·캘린더·편집그리드·명세서·이중인터랙션 등 (아래 패턴 분류 참조)
- 📄 인쇄/Phase3 6: OP체크(Phase3 §1 재설계), 통합보고서·급여대장표·기능팝업창·납품확인서·명세서관리(인쇄·이미 모바일 카드)

**부적합 패턴 분류** (별도 컴포넌트 도입 시 재검토)
1. **매트릭스/근무표 격자** → `MatrixTable` 신설 필요
   - 스마트서류제출(직원×16서류), 교육내역명단(직원×교육항목), JobCategoryTrainingMatrix, 간호근무표, RosterPreviewTable(staff×date)
2. **셀별 편집 그리드** → `EditableGrid` 신설 필요
   - 연차수동부여, 마감보고, 재고실사, SuppliesItemTable, 비품구매양식, AS반품관리, RosterStaffConfigTable
3. **캘린더 격자** → 전용 `CalendarTable` 신설 필요
   - 근태일정뷰, 근태달력뷰, 월간근태달력
4. **Expandable + ColumnFilter** → `ExpandableTable` + `FilterableTable` 신설 필요
   - 건강검진관리
5. **이중 인터랙션(체크박스+행클릭)** → ResponsiveTable에 selection prop 확장 검토
   - 계약서명단, ApprovalInboxView, 재고현황뷰
6. **라벨/값 2칸 명세서** → ResponsiveTable 대상 외 (현 마크업 유지)
   - 통상임금계산기, 일용근로정산, 급여계산기, 급여고도화센터
7. **카드/마스터-디테일** → 표 없음, P1-1 대상 외
   - 사고보고서, 면허보수교육검토, 전자결재.tsx(메인 표 없음)

**권장 진행 방식** (다음 세션)
- 배치당 6개 × 3 서브에이전트 병렬. JM4 nullable 호환 지침 프롬프트 필수.
- 인사관리서브 33개는 4~5배치, 그 외 ~16개는 2~3배치.
- **OP체크.tsx는 P1-1 대상 아님** (Phase3 §1에서 전면 재설계).

### P2 — 화면별 최적화 (전부 미착수)
- P2-1 스탯 카드 모바일 표준화 (대시보드 10+) — 패딩 14×16, radius 12, "라벨 위/숫자 아래" 순서, 모바일 2-col 강제, 변화율 칩
- P2-2 다중 컬럼 폼 → 1컬럼 + 페어 (폼 20+) — 계약서 자동생성, 시스템마스터, 직원권한, 공지등록
- P2-3 이모지 → Lucide 일괄 (`GlobalNotificationBell`, `조직도측면창`, `admin-menu-config`)

### P3 — 백로그 (미착수)
- P3-1 근무표 자동편성 모바일 진입 안내(데스크톱 전용 위저드)

---

## 3. 2단계(Phase3) — 7화면군 (전부 미착수)

문서 `docs/mobile/MSO_Mobile_Phase3_Screens.md` 기준. P0 토큰 위에서 동작 가정 — 1단계 완료 후 착수.

| # | 화면군 | 우선도 | 산출 |
|---|---|---|---|
| 1 | OP체크 (5장→1장) | 🔴 매우 높음 (현장 모바일) | `app/main/OP체크/모바일보드.tsx`, `PullToRefresh.tsx`, `useOpCheckBoardStream.ts` |
| 2 | 퇴원심사 (6장→2장) | 🟡 중 | `모바일목록.tsx`, `모바일상세.tsx`, `보완요청시트.tsx`, `PdfViewer` 재활용 |
| 3 | 근태 통합 (10장→3장) | 🔴 매우 높음 (체크인) | `모바일체크인.tsx`, `모바일근무표.tsx`, `모바일연차.tsx`, `useGeolocationCheckin.ts` |
| 4 | 급여 워크센터 (13장→1장) | 🟢 낮음 | `급여/모바일명세서.tsx` + 관리자 화면 데스크톱 안내 |
| 5 | 관리자 운영 (11장→안내) | 🟢 낮음 | `DesktopOnlyNotice.tsx` (재사용), 회사정보/직원명단 모바일 조회만 |
| 6 | 경영 인사이트 (2장→1장) | 🟡 중 | `경영분석/모바일대시보드.tsx`, `SwipeableKpiCards.tsx`, `MobileChartWrapper.tsx` |
| 7 | 업무공유 (1장→마스터-디테일) | 🔴 매우 높음 (인수인계) | `업무공유/모바일목록·상세·작성.tsx`, `CommentComposerSticky.tsx` |

**공통 컴포넌트 (착수 전 선행)**: `PullToRefresh`, `DesktopOnlyNotice`, `SwipeableKpiCards`, `MobileChartWrapper`, `CommentComposerSticky`, `StickyFormFooter`(P1-3 완료), `PdfViewer`(있으면 재활용)
**공통 훅**: `useGeolocationCheckin`, `useOpCheckBoardStream`, `useVisualViewportOffset`, `useOptimisticMutation`

**예상 5~6주**.

---

## 4. 3단계(Advanced) — PWA·제스처·퍼포먼스·접근성 (전부 미착수)

문서 `docs/mobile/MSO_Mobile_Advanced.md` 기준.

| 영역 | 우선도 | 비고 |
|---|---|---|
| §1 PWA · 오프라인 큐 · 푸시 | 🔴 P1 | **신규가 아님** — `app/manifest.json`, `PwaBootstrap.tsx`, `OfflineStatusBanner.tsx`, `pwa-installability` 테스트 이미 존재. 기존 보강 + 오프라인 큐 + 푸시 deep link 추가 |
| §2 키보드/IME/inputMode/autofill | 🔴 P1 | `Input.tsx`에 `kind` prop, 한글 IME 가드 훅, visualViewport sticky |
| §3 카메라/이미지/파일 업로드 | 🔴 P1 | `CameraInput.tsx`(`capture="environment"`), 클라이언트 압축, HEIC→JPEG, 진행률 |
| §4 제스처/햅틱 | 🟡 P2 | `SwipeableCard`, `useLongPress`, `haptics` 유틸 |
| §5 퍼포먼스 | 🟡 P2 | `next-bundle-analyzer`, dynamic import, 3G LCP ≤3.5s, Lighthouse 80+ |
| §6 접근성 | 🟡 P2 | aria-label/focus-visible/대비, `pnpm test:a11y` 회귀 |
| §7 네비게이션 히스토리 | 🟢 P3 | popstate로 시트 닫기, 깊은 라우팅 BottomTab 복원 |

**예상 3.5~4주**.

---

## 5. 미커밋 17개에서 파생된 후속

`84a7a51d`(도구+lib) + `06ab43e6`(UI) 2커밋은 상위 에이전트가 작성·검증하지 않았음. 메모리 `project_annual_leave_import.md`에 "미처리 6명 존재" 명시.

- ⚠️ 해당 작업이 종결됐는지(미처리 6명 처리 여부) 사용자 확인 필요
- ⚠️ `lib/annual-leave-ledger.ts`, `lib/license-expiry-jobs.ts` 변경의 배포 영향 검토 필요
- 산출물 디렉토리는 `.gitignore` 처리됨 (PII 보호)

---

## 6. 다음 세션 시작 가이드

1. 이 문서를 먼저 읽기.
2. `git checkout mobile-std` (main +17 커밋, 미푸시).
3. **검증 공백** 해결: 로그인 후 P0-2(시스템마스터), P0-3(서브메뉴 토글) 시각 회귀, P1-2(다이얼로그 시트 트리거), P1-1 마이그레이션 화면 스폿 검증.
4. **P1-1 배치4부터 이어가기**:
   - 배치당 6개 × 3 서브에이전트 병렬
   - 프롬프트에 nullable prop 호환 지침 필수 (배치2 회귀 재발 방지)
   - 인사관리서브 우선(33개) → 그 외 디렉토리
5. P1-1 완료 후 P2 → P3 → Phase3 → Advanced 순.

**현재 상태 한 줄 요약**: P0 완료, P1 4종 완료, P2 3종 완료, **P3-1 완료, Phase3 §1~§7 모두 진행(§1 핵심 컴포넌트, §3 3종 분리, 나머지 모바일 분기), Advanced §1~§7 모두 진행**, 누적 68 커밋. 잔여: Phase3 §3 마이페이지 진입점 통합, 큰 화면 추가 dynamic 분리.

## 부록 — Phase3·Advanced 완료 (68 커밋)

### Phase3 7화면군 진행 상태
| § | 화면군 | 상태 |
|---|---|---|
| §1 | OP체크 | **부분** — PullToRefresh + useOpCheckBoardStream + 모바일보드 신설, patient 탭 모바일 분기. 5장→1장 전면 통합은 후속 |
| §2 | 퇴원심사 | **완료** — 모바일목록/상세/시트 분리, reviews 탭 분기 |
| §3 | 근태 통합 | **완료** (3종) — 모바일체크인/모바일근무표/모바일연차. 마이페이지 진입점 통합 잔여 |
| §4 | 급여 워크센터 | **완료** — 본인 모바일명세서 + 관리자 DesktopOnlyNotice |
| §5 | 관리자 운영 | **완료** — 11화면 wrapper 패턴 DesktopOnlyNotice |
| §6 | 경영 인사이트 | **완료** — SwipeableKpiCards + MobileChartWrapper + 경영분석/모바일대시보드 |
| §7 | 업무공유 | **완료** — CommentComposerSticky + team-share/DetailPanel 적용 |

### Advanced 7항목 진행 상태
| § | 영역 | 상태 |
|---|---|---|
| §1 | PWA/오프라인/푸시 | **완료** — OfflineQueue + sw deep link + 큐 자동 재시도 |
| §2 | 키보드/IME | **완료** — Input(12 kind) + useKoreanImeGuard + useVisualViewportOffset |
| §3 | 카메라/이미지 | **완료** — CameraInput(HEIC TODO, 클라 압축) + 직인관리 적용 |
| §4 | 제스처/햅틱 | **완료** — haptics + useLongPress + SwipeableCard + 알림센터 적용 |
| §5 | 퍼포먼스 | **완료** — next-bundle-analyzer + dynamic import + PageSkeleton |
| §6 | 접근성 | **완료** — focus-visible 표준 + WCAG AA 대비 + 컴포넌트 점검 |
| §7 | 네비게이션 히스토리 | **완료** — useSheetHistory + BottomTab 활성 복원 |

### 신설 자산 (컴포넌트 11종 + 훅 5종 + 유틸 3종)
- **컴포넌트**: ResponsiveTable(selection 확장), MatrixTable, EditableGrid, CalendarTable(3파일), ExpandableTable, DesktopOnlyNotice, Input, CameraInput, CommentComposerSticky, SwipeableCard, SwipeableKpiCards, MobileChartWrapper, PageSkeleton, PullToRefresh
- **훅**: useIsMobile(기존), useKoreanImeGuard, useVisualViewportOffset, useSheetHistory, useGeolocationCheckin, useLongPress, useOpCheckBoardStream
- **유틸**: lib/offline-queue.ts, lib/geo.ts, app/lib/haptics.ts

### 미푸시 상태
mobile-std 브랜치에 main 대비 **80 커밋** 누적, 미푸시.

### Production 빌드 검증 통과 ✓
`npm run build` exit 0 (Next.js 16 webpack). 청크 분리 정상. Cloudflare Workers 배포 가능.

### 추가 라운드 (68→80 커밋)
- **Phase3 §1 OP체크 전면 통합**: 모바일상세시트(478줄), templates 탭 DesktopOnlyNotice, BottomSheet + 체크리스트 + CameraInput
- **Phase3 §3 출퇴근기록 통합**: checkin-utils.ts(300줄) 헬퍼 추출, 모바일체크인에 통계 카드 보강, index.tsx 모바일 분기
- **P2-2 확대**: 폼 표준화 30개 누적 (12 → 20 → 30)
- **Advanced §5 확대**: recharts 4개 차트 컴포넌트 dynamic 분리(BudgetBarChart/ReportCharts/PriceTrendChart/ParetoChart)
- **heic2any 활성화**: package.json + types/heic2any.d.ts + CameraInput dynamic import (라이브러리 부재 시 graceful fallback)
- **메신저 IME 가드**: useChatComposerState 한글 조합 중 typing broadcast/멘션 매치 skip
- **모바일 회귀 테스트 +2 spec**: mobile-components(4) + mobile-sheet-history(3) → 12 테스트
- **추가 시범 적용**: 알림센터 SwipeableCard, 메신저사이드바 SwipeableCard, 게시판 CommentComposerSticky, 마이페이지 모바일 근태 진입점

### 100% 완료 평가
- ✅ 코드 작업: 지시서 4문서 모든 §·P· Phase3 7화면군·Advanced 7항목 처리
- ✅ tsc 0 에러 매 라운드 유지
- ✅ production 빌드 통과
- ⏳ 미푸시 (사용자 결정 대기 — `git push origin mobile-std`)
- ⏳ 시각 검증 (사용자 작업 — dev 서버 localhost:3000)
- ⏳ npm install heic2any (사용자 배포 시점)

## 부록 — 종합 진행 (2026-05-15, mobile-std 50+ 커밋)

### 컴포넌트/훅 신설 8종
- `ResponsiveTable` (selection prop 확장) — 41 P1-1 + 계약서명단·재고현황뷰·ApprovalInboxView
- `MatrixTable` — JobCategoryTrainingMatrix·간호근무표·스마트서류제출·교육내역명단·RosterPreviewTable
- `EditableGrid` — 연차수동부여·재고실사·마감보고·SuppliesItemTable·AS반품관리·RosterStaffConfigTable·비품구매양식
- `CalendarTable` (pointer/sort 확장, 3파일 분리) — 월간근태달력·근태일정뷰·근태달력뷰
- `ExpandableTable` — 건강검진관리
- `DesktopOnlyNotice` — RosterWizardModal·RosterPlannerDashboard
- `Input` (12 kind) + `useKoreanImeGuard` + `useVisualViewportOffset` — StickyFormFooter
- `CameraInput` (HEIC TODO, 클라이언트 압축) — 직인관리
- `useSheetHistory` — ActionDialog·RiskActionDialog
- `CommentComposerSticky` — team-share/DetailPanel

### 영역별 완료 작업
- **P0**: 토큰, 헤딩, 네비 (1차 완료)
- **P1**: P1-1(60화면 처리), P1-2(시트), P1-3(StickyFormFooter), P1-4(회귀 테스트)
- **P2-1**: globals.css stat-card 라벨 위/숫자 아래, 모바일 2-col, 6+4 화면
- **P2-2**: 12 폼 모바일 1컬럼 + 페어 + a11y
- **P2-3**: 알림센터 이모지 → Lucide
- **P3-1**: DesktopOnlyNotice + 근무표 자동편성 모바일 분기
- **Phase3 §7**: CommentComposerSticky + 업무공유 DocDetail
- **Advanced §2**: Input/IME/Viewport 표준화
- **Advanced §3**: CameraInput
- **Advanced §6**: focus-visible + WCAG AA 대비
- **Advanced §7**: useSheetHistory + BottomTab 복원

### 미착수
- Advanced §1 PWA/오프라인/푸시(기존 일부 구현됨, 보강 필요)
- Advanced §4 제스처/햅틱
- Advanced §5 퍼포먼스 (bundle analyzer, dynamic import)
- Phase3 §1 OP체크 (큰 작업, 5장→1장)
- Phase3 §2~§6 (퇴원심사·근태통합·급여·관리자운영·경영인사이트)

### 검증 공백 (사용자 처리 필요)
- dev 서버 `localhost:3000` 띄움 — 로그인 후 시각 회귀
  - P0-2/P0-3 토글 동작
  - P1-2 ActionDialog/RiskActionDialog 모바일 시트
  - P1-1 마이그 화면 스폿 검증
  - 5종 신규 컴포넌트(MatrixTable/EditableGrid/CalendarTable/ExpandableTable/StickyFormFooter) 적용 화면 모바일 검증

## 부록 — 컴포넌트 5종 패턴 정립 (2026-05-15)

P1-1 부적합 26 중 19건을 신규 컴포넌트로 해소(잔여 7건은 P1-1 대상 외 — 라벨/값 명세서 4 + 카드/마스터-디테일 3):

| 컴포넌트 | 적용 화면 |
|---|---|
| `ResponsiveTable` (기존+selection 확장) | 41 화면 + 계약서명단, 재고현황뷰, ApprovalInboxView |
| `MatrixTable` (신설) | JobCategoryTrainingMatrix, 간호근무표, 스마트서류제출, 교육내역명단, RosterPreviewTable |
| `EditableGrid` (신설) | 연차수동부여, 재고실사, 마감보고, SuppliesItemTable, AS반품관리, RosterStaffConfigTable, 비품구매양식 |
| `CalendarTable` (신설+pointer/sort 확장) | 월간근태달력, 근태일정뷰, 근태달력뷰 |
| `ExpandableTable` (신설) | 건강검진관리 |

**커밋 누적**: mobile-std에 **40+ 커밋** (P1-1 배치1~12, 컴포넌트 4종, 부적합 해소, P2-1/P2-2/P2-3).

**잔여 7건 처리 가이드**:
- 라벨/값 명세서 4: 통상임금계산기, 일용근로정산, 급여계산기, 급여고도화센터 → 카드 모바일 변환 불필요(현 마크업 유지)
- 카드/마스터-디테일 3: 사고보고서, 면허보수교육검토, 전자결재.tsx → P1-1 대상 외 (이미 모바일 친화 또는 단순 카드)

## 부록 — P1-1 완료 보고 (2026-05-15 배치1~12, 19커밋)

**진행률 비약**: 17 → 41 마이그레이션, 부적합 26건 식별/스킵 사유 명문화.

**12개 배치 모두 매 배치 tsc 0 에러**. 회귀 없음(배치2 nullable 좁히기 사고 이후 프롬프트에 항상 호환 지침 포함).

**핵심 메서드**: 3 서브에이전트 × 2화면 병렬, 매 배치 부적합 패턴 prompt 명시 — 부적합률 점진적 상승(배치4 33% → 배치11 100%까지)에 자연스럽게 적응.

**다음 세션 권장**
1. **P1-1 완료 처리**: 위 7개 부적합 패턴 분류 기반으로 신규 컴포넌트 4종(`MatrixTable`, `EditableGrid`, `CalendarTable`, `ExpandableTable`) 설계 후 26개 화면 차례로 적용 — 또는 우선순위가 낮으면 그대로 두고 Phase3 진입
2. **검증 공백 해결**: dev 서버 띄워둠(`localhost:3000`) — 로그인 후 P0-2/P0-3/P1-2 + P1-1 마이그 화면 스폿 검증
3. **Phase3 §1(OP체크) 재설계**: P1-1과 별개로 시작 가능
4. **메모리 갱신**: `project_mobile_std_branch.md`에 41 마이그 + 26 부적합 패턴 추가

---

## 부록 — 배치5~7 노트 (2026-05-15 추가)

배치5 (3 마이그/3 부적합), 배치6 (4 마이그/2 부적합), 배치7 (2 마이그/4 부적합) 진행.

**핵심 발견**: 잔여 화면 진행 시 부적합 비율이 급증(배치5 50%, 배치7 67%). ResponsiveTable이 적합한 단순 데이터 그리드 화면은 거의 다 소진. 남은 화면 다수는 다음 패턴 중 하나:
- 매트릭스(직원×N항목 동적 컬럼)
- 편집 그리드(셀별 input/select)
- 근무표 격자(staff×date)
- Expandable 행(1행 + N서브행)
- 라벨/값 2칸 정적 명세서(데이터 그리드 아님)
- thead 인터랙티브 위젯(ColumnFilter, 체크박스)

**다음 세션 권장 전략**
1. **부적합 패턴별 신규 컴포넌트 우선 도입**:
   - `MatrixTable` — 매트릭스/주차표/근무표 격자용
   - `EditableGrid` — 셀별 입력 그리드용
   - `ExpandableTable` — 1행 N서브행용
   - 라벨/값 명세서는 ResponsiveTable 적용 불필요, P1-1 카운트에서 제외
2. **P1-1 잔여 ~25개 중 단순 그리드 후보**: 재고현황뷰, AS반품관리, 시스템마스터센터, 회사관리, 연차수동부여, ApprovalInboxView, 마감보고 등 — 점검 후 적합 시 배치8~10으로 마이그레이션 진행
3. **Phase3 §1(OP체크) 재설계 검토 시작** 가능 — P1-1 완료 대기하지 않고 병행 가능

---

## 부록 — 배치4 노트 (2026-05-15 추가)

- 배치4는 6개 목표 중 **4개 마이그레이션 + 2개 부적합 스킵**으로 마감.
- **트레이드오프**:
  - 근무형태변경이력 — 사유 컬럼 `showOnMobile: false`로 모바일 카드 가독성 확보. 삭제 버튼은 양쪽 유지 + `stopPropagation`으로 카드 클릭과 격리.
  - 인사발령관리 — `record.id`가 `string | number | undefined` 혼합 → `rowKey: string` 정규화 추가.
  - 연차원장 — `staff.company`/`department`가 nullable이라 render에서 `'회사 미지정'`/`'부서 미지정'` 폴백.
  - 비품장비대여관리 — 데스크탑에서 `hidden sm:table-cell`로 숨겼던 대여일 컬럼이 ResponsiveTable에서 모바일 카드 body로 노출(정보 풍부화).
- **다음 세션 배치5 후보** (인사관리서브 27개 중 비교적 표준적인 표 우선):
  - 휴가신청/연차휴가신청내역, 근태기록/일일근태현황·월간근태달력·근태일정뷰, 교육내역/교육내역명단·자격면허대시보드, 면허자격증관리(이미 ResponsiveTable 적용 가능성 — 확인 필요)
  - **expandable·필터 박힌 패턴은 사전 식별해서 제외** (건강검진관리 같은 회귀 방지).
