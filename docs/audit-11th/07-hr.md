# 11차 · 07 인사관리

> 조사일: 2026-08-31 · 1차 조사(미검증).  
> 워크센터: member / attend / leave / payroll / welfare / docs (+ abnormal, 모바일 offboarding/archive).

## 조사 발견

### P0 후보
- **HR-01** 근태 KPI는 “평일 무기록=결근”. 결근 크론은 근무표 있는 사람만. KPI≠리스트≠급여.
- **HR-02** 연속결근 감지가 로스터/주말/연차를 무시해 28일 스트릭.
- **HR-03** 모바일 근태가 영문 status만 매칭. 실제 `attendance`는 한글(정상/지각) → 대시 0, 달력 점 없음.
- **HR-04** 의료기기 테이블에 회사 컬럼 없음 + AUTHENTICATED. 복지 KPI가 selectedCo를 무시.
- **HR-05** PUBLIC `staff_members` + HR 언마스크가 회사 스코프 없음 → A병원 HR이 B병원 주민번호/계좌/급여.
- **HR-06** 계약 서명이 클라 `contractText`를 바인딩 없이 저장. 키 없으면 평문.

### P1 후보
- **HR-07** 오프보딩 start는 세션을 안 끊음(`퇴사예정`). finalize/`status=퇴사` 로그인 차단은 살아 있음. 구성원 삭제는 `role=inactive`를 안 씀 → 재직 복구 시 옛 롤 복원.
- **HR-08** 연차 화면마다 잔여/소멸 기준이 다름(입사주기 vs 12/31 vs staff 미러).
- **HR-09** LeaveWorkcenter가 ledger 에러를 잔여 0으로 침묵.
- **HR-10** 모바일 “근무표” 탭이 로스터가 아니라 출퇴근 기록.
- **HR-11** 급여 잠금 UI fail-open, 잠금 후 reload 없음, 이체파일은 잠금월에도 허용.
- **HR-12** 건강검진 PC/모바일 컬럼이 달라 서로 안 보임. `result` PHI가 select('*').
- **HR-13** 모바일 복지 mutate가 `menu_인사관리`면 전 도메인 CRUD.

### P2
early_leave_records 무작성자, 정상처리가 없는 컬럼 UPDATE, 잠금 unique 없음, 세금표 이중, 배치 계약 발송이 한 연봉을 전원에게.

10차 AUTH-01(퇴사자 재로그인)은 **신규 로그인은 status=퇴사로 막힘**이 이번 조사에서 관측됨. 잔여는 기존 세션·퇴사예정·status 되돌리기.
