# 채팅(메신저) — 방(room) 데이터/목록/사이드바 그룹 전수조사 (G2)

조사일: 2026-05-29 / 대상 디렉터리: `app/main/기능부품/`
D1 정본: `lib/db/schema.ts` (chat_rooms, messages, room_read_cursors, pinned_messages, polls, poll_votes, message_reactions, message_bookmarks, chat_room_prefs 모두 존재 확인)

## 파일별 1줄 역할
- **메신저방데이터훅.ts** (1797줄): `useChatRoomDataSync` — 방 메시지 전체 로드(`fetchData`), 읽음커서/반응/북마크/고정/투표 메타동기화, 메시지 점프 로드, 방요약 빌드·영속화. 실제 `메신저.tsx`가 사용하는 정본.
- **메신저방관리훅.ts** (466): `useChatRoomManagement` — 방 나가기/멤버 추가·제외/이름변경/그룹·DM 생성. 시스템 메시지 삽입 + 푸시.
- **메신저방목록훅.ts** (157): `useChatRoomListSync` — 방목록 정렬·요약빌드·영속화. **오직 죽은 `메신저메시지조회훅.ts`만 import.**
- **메신저방전환훅.ts** (216): `useChatRoomNavigation` — `setRoom`(방 전환, 드래프트 저장/복원, 읽음커서 기록, unread 0 처리), 스크롤 제어.
- **메신저방환경설정훅.ts** (134): `useChatRoomPreferences` — 핀/숨김/핀순서 localStorage + chat_room_prefs upsert.
- **메신저사이드바.tsx** (559): `MessengerSidebar` — 방 목록/조직도 뷰 프레젠테이션 컴포넌트.
- **메신저사이드바훅.ts** (245): `useChatSidebarState` — visibleRooms/sidebarRoomItems 파생, dedup, 핀순서 동기화.
- **메신저상태훅.ts** (416): `useChatMessageEditing`/`useReadStatusModal`/`useChatMobileBackLayer` — 메시지 편집·읽음현황 모달·모바일 뒤로가기.
- **메신저파생훅.ts** (643): 스레드 요약/오버뷰/타임라인/미디어/멘션 등 순수 파생 `useMemo` 모음.

---

## 분류별 발견

### D 중복

**[D-1] 높음** — `메신저방데이터훅.ts` vs `메신저방목록훅.ts` 6개 함수 거의 완전 중복
- `메신저방데이터훅.ts:303-425` (`applyChatRoomsState`, `syncChatRoomsState`, `buildRoomSummaryFromMessages`, `applyRoomSummaryToState`, `persistRoomSummary`, `syncRoomSummaryFromMessages`)
  vs `메신저방목록훅.ts:26-148` — `buildRoomSummaryFromMessages`/`applyRoomSummaryToState`/`persistRoomSummary`/`syncRoomSummaryFromMessages`는 **바이트 단위 동일**, `applyChatRoomsState`만 `updateUnreadForRooms`(데이터훅) vs `scheduleUnreadRefresh`(목록훅) 차이.
- 근거: 두 파일 동일 로직. `메신저방목록훅.ts`는 `메신저메시지조회훅.ts:16`에서만 import되며, 그 파일은 어디서도 import되지 않음(아래 X-1). 즉 죽은 중복.
- 조치: `메신저방목록훅.ts` + `메신저메시지조회훅.ts` 동시 삭제 검토(X-1 참조). 살릴 경우 요약빌드 로직을 `메신저유틸`로 추출해 단일화.

**[D-2] 중간** — reactions 집계 로직 3회 중복
- `메신저방데이터훅.ts:543-589`(`refreshVisibleMessageReactions`), `:981-1029`(`syncVisibleMessageMetadata` 내부), `:1647-1693`(`fetchData` 내부) — emoji 카운트 + reactionUsersMap 빌드 + compareStaffMembers 정렬이 세 군데 동일 복붙.
- 근거: 동일 블록. 한 곳은 'Unknown'(영문), 다른 곳은 '알 수 없음'(한글) — K-1 참조.
- 조치: `buildReactionMaps(rows, resolveStaffProfile)` 헬퍼로 추출.

**[D-3] 중간** — pinned messages 조회/조립 로직 중복
- `메신저방데이터훅.ts:618-689`(`refreshRoomPinnedMessages`), `:1032-1093`(syncVisibleMessageMetadata), `:1584-1640`(fetchData) — `pinned_messages` 조회 → `.slice(-1)` → 누락분 messages 재조회 → setPersistedPinnedMessages 3중 중복.
- 조치: `loadPinnedMessages(roomId, messages)` 헬퍼로 통합.

**[D-4] 중간** — polls/votes 조회 로직 중복
- `메신저방데이터훅.ts:691-729`(`refreshRoomPolls`), `:1096-1133`(syncVisibleMessageMetadata), `:1695-1728`(fetchData) — POLL_SELECT 조회 + poll_votes 집계 voteMap 동일 3중복.

### Q 유사기능

**[Q-1] 높음** — 5개 방 훅의 책임 경계 부적절: 데이터훅이 목록·요약·메타·점프를 모두 떠안음
- `메신저방데이터훅.ts` (1797줄) 안에 (a)방목록 상태관리(`syncChatRoomsState`/`applyChatRoomsState`), (b)방요약 빌드·영속화, (c)unread 계산, (d)읽음커서, (e)반응/북마크/고정/투표 메타, (f)메시지 페이지네이션·점프(`fetchMessagePage`/`loadMessagesAroundMessage`), (g)전체 `fetchData`가 한 훅에 응집.
- `메신저방목록훅.ts`(=목록 책임)는 분리 시도였으나 사용처 없이 사장 → 분리 미완.
- 조치: (a)(b)(c)를 목록훅으로, (d)(e)를 메타훅으로, (f)를 페이지네이션훅으로 실제 분리하고 데이터훅은 조립만. jm 스킬(500줄/단일책임) 위배.

**[Q-2] 중간** — `메신저파생훅`(순수 파생) vs `메신저상태훅`(부수효과) 분리는 적절
- `메신저파생훅.ts`: 전부 `useMemo` 순수 파생(스레드/타임라인/미디어/멘션) — 부수효과 없음. 적절.
- `메신저상태훅.ts`: `useState`+DB쓰기(편집 저장, audit 로그) + 모달 상태. 부수효과 담당. 적절.
- 단, `메신저상태훅.ts`에 성격이 다른 3개 훅(편집/읽음모달/모바일백레이어)이 한 파일에 공존 — 파일명 "상태"가 모호. 조치: 큰 문제 아니나 파일 분리 권장(낮음).

### X 죽은코드

**[X-1] 높음** — `메신저메시지조회훅.ts` 전체가 죽은 코드
- 근거: `useChatRoomDataSync`/`메신저메시지조회훅` Grep 결과, `메신저메시지조회훅`을 import하는 `.ts/.tsx` 소스 **없음**(docs/주석에서만 언급). 실제 컴포넌트 `메신저.tsx:37`은 `메신저방데이터훅`의 동명 `useChatRoomDataSync`를 사용.
- 파급: 이 파일이 죽으면서 그 유일한 의존인 `메신저방목록훅.ts`(D-1)도 사실상 죽음.
- 조치: `메신저메시지조회훅.ts` + `메신저방목록훅.ts` 삭제 검토. (단 `반응.ts`, `chatQueryService.ts`의 "주석 언급"은 코드 의존 아님 — 삭제 무방)

**[X-2] 중간** — `메신저파생훅.ts` 미사용 지역변수 `userParticipated`
- `메신저파생훅.ts:257-259`(`useThreadSummaries`), `:292-294`(`useThreadOverviews`) — `userParticipated` 계산만 하고 사용 안 함. 같은 위치 `needsAttention`은 항상 `false` 상수(:260, :295)로 하드코딩.
- 근거: 두 변수 모두 객체 리턴에서 `needsAttention`만 들어가고 항상 false. `userParticipated`는 완전 미사용.
- 조치: `userParticipated` 제거, `needsAttention: false` 의도 확인(스레드 주목 기능 미구현 잔재로 의심).

**[X-3] 중간** — `메신저사이드바.tsx` 7개 props 수신 후 즉시 폐기
- `메신저사이드바.tsx:136-138`: `void [onMovePinnedRoom, attentionThreadItems, mentionInboxItems, threadInboxItems, onOpenAttentionThreadItem, onOpenMentionItem, onOpenThreadItem];` — 받지만 안 씀.
- 근거: 본문에서 위 7개 미사용(멘션/스레드 인박스 UI 제거됨). 타입 정의(`:62-74`)도 같이 유지됨.
- 조치: 멘션/스레드 인박스가 폐기된 기능이면 props·타입 제거. 호출처(`메신저.tsx`)에서 전달 코드도 함께 정리.

### U 불필요

**[U-1] 중간** — `메신저방전환훅.ts` 미사용 파라미터 다수
- `메신저방전환훅.ts:47,53,56` — `optimisticUnreadFloorRef`→`_optimisticUnreadFloorRef`, `setLoadingRoomId`→`_setLoadingRoomId`, `loadingRoomId`→`_loadingRoomId` 언더스코어로 받기만 하고 미사용.
- 조치: 타입·전달 인자에서 제거.

### T 타입(any·강제캐스팅)

**[T-1] 중간** — `polls` 전반 `any[]`
- `메신저방데이터훅.ts:153`(`setPolls: Dispatch<SetStateAction<any[]>>`), `:699`,`:1100`,`:1511`(`{ data: any[] | null }`), `메신저파생훅.ts:550`(`polls: Array<Record<string, unknown>>`) — poll 타입 미정의. jm4 위배.
- 조치: `Poll` 타입 정의(schema polls 기준: id/room_id/creator_id/question/options/created_at) 후 교체.

**[T-2] 중간** — 강제 캐스팅 `as unknown as ChatRoom`
- `메신저방관리훅.ts:296`(`result.room as unknown as ChatRoom`), `:389` 동일. `createOrUpsertChatRoom` 반환 타입과 ChatRoom 불일치를 이중 캐스팅으로 우회.
- 조치: `createOrUpsertChatRoom` 반환 타입을 ChatRoom 호환으로 정렬.

**[T-3] 낮음** — `message.staff as { name?: string }` 반복 캐스팅
- `메신저상태훅.ts:112,213,223`, `메신저파생훅.ts:570` — ChatMessage.staff 타입이 약해 인라인 캐스팅 반복.

### E 에러처리

**[E-1] 중간** — SQL 에러를 console.warn/error로만 삼키는 무음 실패 (D1 정본 주의 해당)
- `메신저방데이터훅.ts:588`(`console.warn('message reactions query failed')`), `:1692`(동일), `:296`(unread 동기화 실패), `:411`(요약저장 실패) 등 — catch 후 토스트 없이 콘솔만. 잘못된 컬럼/RLS면 사용자 무인지.
- 조치: 사용자 영향 큰 경로(요약 영속화 실패 등)는 최소 1회 토스트 또는 재시도. jm3 위배.

**[E-2] 낮음** — `메신저방환경설정훅.ts:60-64` chat_room_prefs upsert 실패를 `console.debug`로 강등
- 핀/숨김 DB 동기화 실패가 debug 레벨이라 운영 로그에서 사라짐. localStorage는 반영되나 기기 간 불일치 가능.

### P 성능(중복API·useEffect의존성·폴링)

**[P-1] 높음** — `fetchData`가 방 전체 메시지를 limit 없이 매번 로드
- `메신저방데이터훅.ts:1430-1440`: `.from('messages').in('room_id', roomIdsToLoad).order('created_at', ascending:true)` — **`.limit()` 없음**. 방에 메시지가 수천 건이면 매 폴링/방진입마다 전부 가져옴.
- 대조: `loadMessagesAroundMessage`/`fetchMessagePage`는 페이지네이션(`MESSAGE_PAGE_SIZE=50`, +1) 사용 — 같은 훅 안에서 일관성 없음.
- 조치: `fetchData`도 최근 N건만 로드 후 위로 페이지네이션하도록 통일.

**[P-2] 높음** — `fetchData`가 매 호출마다 `fetchAllChatRooms({ force: true })`로 전체 방 재조회
- `메신저방데이터훅.ts:1377`: `force: true`로 캐시 무시. 방 전환·폴링마다 전체 방목록 + 멤버 repair + unread 배치까지 연쇄. (audit 문서 supabase-edge-log에서 per-room HEAD 폭발로 이미 지적된 경로)
- 조치: 방 목록은 별도 주기/캐시로 분리, 방 진입 시엔 해당 방 메시지만.

**[P-3] 중간** — `메신저방관리훅` 멤버 변경 후 전체 `fetchData()` 재실행
- `메신저방관리훅.ts:123`(`applyRoomMemberChange`에서 `await fetchData()`) — 초대/제외 1건마다 방 전체 메시지+메타 재로드. 로컬 상태는 이미 `updateRoomMembersLocally`로 갱신됨.
- 조치: fetchData 대신 멤버 상태만 갱신하거나 가벼운 부분 갱신.

**[P-4] 중간** — `sortChatRoomsWithNoticeFirst` + `setChatRooms` 빈번 재정렬
- `applyRoomSummaryToState`(데이터훅:374-396)가 메시지 수신/메타동기화마다 전체 방 배열 map+정렬. 방 많을수록 O(n log n) 반복.

**[P-5] 낮음(의심)** — `메신저사이드바훅.ts:150-163` useEffect가 핀순서를 effect 안에서 localStorage 기록
- `effectivePinnedRoomOrder` 변경마다 setState + localStorage. `arraysMatch` 가드는 있으나, 파생값을 effect로 다시 상태화하는 패턴은 렌더 추가 유발. 조치: 파생만으로 충분한지 검토.

### SEC 보안

**[SEC-1] 중간** — 멤버 제외/방 관리 권한이 클라이언트 `created_by` 비교에만 의존
- `메신저방관리훅.ts:202`(`if (selectedRoom.created_by !== (effectiveChatUserId || user?.id)) return;`) — 방장 권한 체크가 프런트에만. `patchChatRoom`(`@/lib/chat-rooms-client`) 서버측 권한 검증 없으면 임의 사용자가 직접 API로 members 변경 가능.
- 근거: 체크가 UI 가드뿐, members는 `patchChatRoom`으로 그대로 PATCH(`:73-76`).
- 조치: 서버 엔드포인트에서 요청자=created_by(또는 멤버) 검증 필요. (서버측 별도 확인 권장 — 의심)

**[SEC-2] 낮음** — 메시지 편집 권한도 클라이언트 가드만
- `메신저상태훅.ts:88`(`if (sender_id !== currentUser && !isMso) return;`) — `saveEditedMessage`(:199) UPDATE 자체엔 sender 조건 없이 `.eq('id', ...)`만. 서버/RLS 미보장 시 타인 메시지 수정 가능.

### K 한글깨짐

**[K-1] 낮음** — reaction unknown 라벨 한/영 혼재
- `메신저방데이터훅.ts:559,564`('Unknown' 영문), `:996,999`('Unknown'), vs `:1664,1675`('알 수 없음') — 같은 의미인데 표기 불일치(깨짐 아닌 일관성). 조치: '알 수 없음'으로 통일.
- (실제 모지바케/인코딩 깨짐은 발견되지 않음)

### S 문법/런타임 · DBG 디버그
- S: 명백한 문법/런타임 버그 미발견.
- DBG: `메신저방환경설정훅.ts:62` `console.debug` 외 임시 디버그 로그 없음(나머지는 정상 error 로깅).

---

## 집계표

| 분류 | 높음 | 중간 | 낮음 | 계 |
|---|---|---|---|---|
| D 중복 | 1 | 3 | 0 | 4 |
| Q 유사기능 | 1 | 2 | 0 | 3 |
| X 죽은코드 | 1 | 2 | 0 | 3 |
| U 불필요 | 0 | 1 | 0 | 1 |
| T 타입 | 0 | 2 | 1 | 3 |
| E 에러처리 | 0 | 1 | 1 | 2 |
| P 성능 | 2 | 2 | 1 | 5 |
| SEC 보안 | 0 | 1 | 1 | 2 |
| K 한글 | 0 | 0 | 1 | 1 |
| S 문법 | 0 | 0 | 0 | 0 |
| DBG | 0 | 0 | 0 | 0 |
| **계** | **6** | **14** | **6** | **24** |

## 특별임무 결론
- 5개 방 훅 책임 중복: **있음(높음).** `메신저방목록훅`이 분리 의도였으나 죽은 `메신저메시지조회훅`만 참조해 사장 → 데이터훅(1797줄)이 목록·요약·메타·페이지네이션·점프를 전부 떠안음(D-1, Q-1, X-1). 관리/전환/환경설정 훅은 책임 분리 양호.
- 파생훅 vs 상태훅: **분리 적절.** 파생훅=순수 useMemo, 상태훅=useState+DB부수효과. 다만 상태훅 내 3훅 공존·파생훅 죽은 변수(X-2)는 정리 대상.
