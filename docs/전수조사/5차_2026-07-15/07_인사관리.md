# ⑦ 인사관리 (HR) — 5차 전수조사

| 항목 | 내용 |
|------|------|
| **조사일** | 2026-07-15 |
| **범위** | PC `app/main/기능부품/인사관리.tsx` · `인사관리워크센터/**` · `인사관리서브/**` · 모바일 `app/main/모바일/인사관리/**` · `lib/annual-leave-*` · `lib/payroll-*` · `lib/hr-checklists.ts` · `lib/db/auth/policies.ts`(employment_contracts·onboarding) · `lib/feature-permissions.ts` / `lib/access-control.ts`(`hr_*`) |
| **방법** | 정적 코드 정독(READ-ONLY) + 3차 `07_인사관리.md` · 4차 fable5 · 버그수정_2026-07-15 · 모바일_PC패리티_2026-07-13 교차검증 |
| **수정** | 본 문서는 **감사 전용** (생산 코드 변경 없음) |

---

## 0. 요약 카운트

| 등급 | 건수 | 설명 |
|------|------|------|
| **C (Critical)** | **4** | 모바일 허브 권한 미게이트, attendance 이중 테이블, 연차 부여 시 staff_members 직접 갱신, 모바일 관리자 게이트 과대허용 |
| **M (Major)** | **14** | 워크센터↔레거시 병행, 근태 dual-path UI, 휴가계획서 무동작, 복지 IncidentReport 고아, 경조사 무음 실패, 급여 레거시 폴백 잔존 등 |
| **m (Minor)** | **12** | SY INC. 하드코딩, toast/placeholder, dead import, KPI 세션 로컬 등 |
| **이미 해결 (회귀 OK)** | **12+** | 계약서 SELF_OR_SAME_COMPANY, 연차 SSOT/15폴백, employ_type→employment_type, base_salary, certificate_issuances, nurse_schedules 스키마, 급여 지각분 attendances 연동 등 |

**한 줄 요약:** PC는 **B(4그룹) 6워크센터**로 사이드바가 정리됐고, 7-15 계약서·연차 SSOT 수정은 코드상 유지된다. 남은 구조 리스크는 **모바일 허브의 세부 `hr_*` 미적용**, **`attendance` vs `attendances` 이중 경로**, **연차 승인 시 `staff_members` 직접 쓰기(SSOT 우회 잔존)**, **급여·근태의 워크센터 셸 + 레거시 실기능 2경로**다.

---

## 1. 아키텍처 맵

### 1.1 PC 진입 · 사이드바

| 항목 | 위치 | 내용 |
|------|------|------|
| 메인 | `인사관리.tsx:42–49` | `HR_TABS` 6개: member / attend / leave / payroll / welfare / docs |
| 그룹 | `:51` | 인력 · 근태·휴가 · 보상 · 복지·문서 |
| 라우터 | `인사관리워크센터/index.tsx` | `HrWorkcenterRouter` → 워크센터 컴포넌트 |
| 레거시 별칭 | `인사관리.tsx:54–118` | 옛 한글 메뉴 → 영문 id (`REMOVED_MENU_FALLBACKS`) |
| 접근 거부 | `:355–365` | `menu_인사관리` 또는 세부 탭 0개 시 잠금 UI |
| 회사 필터 | `:232–240` | 직원 company + 항상 `SY INC.` 포함 |
| 직원 등록 | `:225` | `isAdminUser` 또는 `hr_직원등록` |

```
[사이드바 4그룹]
  인력        → member
  근태·휴가   → attend, leave
  보상        → payroll ★
  복지·문서   → welfare, docs
        ↓
  HrWorkcenterRouter(workcenterId)
```

> **abnormal**은 사이드바 독립 메뉴가 아님. `attend` 내부 탭 + 라우터 id `abnormal` 호환 유지 (`index.tsx:161–168`, `REMOVED_MENU_FALLBACKS`).

### 1.2 권한 키 (`hr_*`)

**등록 목록** — `lib/feature-permissions.ts:126–155`

| 그룹 | 키 |
|------|-----|
| 구성원 | `hr_직원등록`, `hr_구성원_열람`, `hr_구성원_관리`, `hr_인사발령`, `hr_포상징계`, `hr_교육`, `hr_오프보딩` |
| 근태 | `hr_근태_열람`, `hr_근태_수정`, `hr_근무표생성` |
| 연차 | `hr_연차휴가` |
| 급여 | `hr_급여`, `hr_급여_승인`(critical) |
| 복지 | `hr_경조사`, `hr_건강검진`, `hr_면허자격증`, `hr_의료기기점검`, `hr_사고보고서` |
| 문서 | `hr_계약`, `hr_문서보관함`, `hr_증명서`, `hr_서류제출` |

**합집합 진입** — `lib/access-control.ts:574–581`  
워크센터 id(`member`/`attend`/…)당 세부 키 OR. LEGACY 별칭으로 `hr_구성원` ↔ `hr` / `hr_구성원_열람` 등 호환 (`:244–262`).

**PC 탭 게이트** — `인사관리.tsx:151–182`  
`canAccessHrTab`: attend=근태|연차|근무표, leave=연차|근태, welfare=5종 복지 OR, docs=4종 문서 OR, member=구성원|발령, payroll=`hr_급여`.

### 1.3 모바일 진입

| 항목 | 위치 | 내용 |
|------|------|------|
| 라우터 | `모바일/인사관리/index.tsx` | hub + 9 메뉴 + form-member / form-leave |
| 허브 메뉴 | `:87–98` | member, attend, leave, **abnormal**(별도), payroll, welfare, docs, offboarding, archive |
| 권한 | `:277–279` | **`perm` 지정 메뉴만** `canAccessHrSection` — **offboarding·archive만 perm 있음** |
| 관리 모드 | `canMutateTeamAbnormal` | 근태/연차/복지/계약 전사 관리 토글 (`data-hooks.ts:665–672`) |

---

## 2. PC 워크센터 전수 맵 (탭 · 액션 · 파일)

### 2.1 member — 구성원

**파일:** `MemberWorkcenter.tsx` · `MemberWorkcenter/*` · 레거시 `구성원현황.tsx` / `오프보딩.tsx` / `사고보고서.tsx`

| 탭 id | 라벨 | 본문 | 주요 액션 | C/M/m 메모 |
|-------|------|------|-----------|------------|
| `list` | 구성원 | `StaffTable` + `StaffDrawer` | 선택/더블클릭 편집, 신규 등록, 다중 선택, 문서함 점프, 키보드 ↑↓/Enter/Esc | 편집·등록 → `구성원현황` 임베드 (`:244–278`) |
| `appointment` | 인사발령 | `AppointmentBoard` | 발령 목록·상세 (레거시 위임) | `hr_인사발령` 별도 탭 게이트 없음(워크센터 진입 시 전부 노출) |
| `disciplinary` | 징계 | `DisciplinaryBoard` (dynamic) | 징계 위원회/이력 | |
| `education` | 교육·자격 | `EducationBoard` | 의무교육·자격 만료 | |
| `accident` | 사고보고서 | `사고보고서` | 사고 등록·목록 | **복지 권한(`hr_사고보고서`)과 탭 위치 불일치** — 사이드바는 member |
| `offboarding` | 오프보딩 | `오프보딩` | 퇴사 체크리스트, 권한 회수 흐름 | `onboarding_checklists` SELF_OR_SAME_COMPANY |

**KPI:** `computeMemberKpis` (`MemberWorkcenter/data.ts`) — 전체 인원 / 신규 / 퇴사예정 / 평균 근속.  
**고용형태:** `employment_type` 사용 (`data.ts:142`, `StaffTable.tsx`) — 3차 S-4 `employ_type` **해소 확인**.

**온보딩:** 직원 등록 시 `onboarding_checklists` 입사 기본 항목 (`구성원현황.tsx` + `lib/hr-checklists.ts` ENTRY_DEFAULTS).

---

### 2.2 attend — 근태

**파일:** `AttendWorkcenter.tsx` · `AttendWorkcenter/*` · 탭 내 `AbnormalWorkcenter`

| 탭 id | 라벨 | 본문 | 주요 액션 | 데이터 소스 |
|-------|------|------|-----------|-------------|
| `dashboard` | 대시보드 | `AttendDashboard` | 당일 현황 KPI 연동 | 워크센터 KPI: **`attendances`** (`:79–83`) |
| `schedule` | 근무표 편성 | `RosterWorkspace` (+ `RosterGrid`) | 월 편성, 자동/AI, 승인 요청 | `shift_assignments` + `/api/ai/roster-recommendation`, `/api/roster/approval-request` |
| `calendar` | 달력 | `AttendCalendar` | 월간 근태 그리드 | |
| `abnormal` | 근태이상 감지 | `AbnormalWorkcenter` | 6종 패턴 카드, 규칙, 조치 이력 | `attendances` + `early_leave_records` |

**initialTab 해석:** `index.tsx:resolveAttendInitialTab` — 간호근무표/근무표* → schedule, 근태이상/지각 → abnormal.

**레거시 병행 (Major 구조):**  
`인사관리서브/근태기록/*` (`근태관리메인`, 달력/근무표/대시보드 뷰) + `간호근무표.tsx`(`nurse_schedules` — **스키마 존재 확인** `lib/db/schema.ts:2526`) 가 워크센터와 별도 경로로 잔존.  
근무표 시스템: RosterWorkspace(`shift_assignments`) ↔ 레거시 근태근무표뷰 ↔ 간호근무표(`nurse_schedules`) — **3중 구조 유지 (3차 D-2)**.

---

### 2.3 leave — 연차·휴가

**파일:** `LeaveWorkcenter.tsx` · `LeaveWorkcenter/*`

| 구역(mainTab) | 라벨 | 본문 | 주요 액션 |
|---------------|------|------|-----------|
| `balance` | 잔여 현황 | `LeaveBalanceTable` + `LeaveQuickForm` | 행 클릭 prefill, 더블클릭 상세(팝업), 승인/반려/삭제 |
| `calendar` | 캘린더 | `LeaveCalendar` | leave_requests 일자 펼침 |
| `expiry` | 소멸·촉진 | `LeaveExpiryBoard` | 사용 권유 → 폼 prefill |
| `diagnose` | 정합 진단 | API 결과 표 | `GET/POST /api/admin/annual-leave/diagnose` 재계산 |

**KPI:** 전체 잔여 / 사용률 / 소멸예정(30일) / 신청 대기.  
**데이터:** `leave_balances` 우선, 없으면 staff `annual_leave_*` (`LeaveWorkcenter/data.ts:172–214`).  
**승인 시 sync:** `POST /api/admin/annual-leave/sync` (`LeaveWorkcenter.tsx:236–245`).  
**발생 원장:** `leave_accruals` (상세 팝업, 최신 annual:N 1건 규칙 `:425–438` — 서버 `resolveGrantedDaysFromAccruals`와 정합).

**주의 (C3):** 연차(부여) 승인 시 여전히 `staff_members.annual_leave_total` **직접 update** (`:228–231`) — 화면 안내(“직원 명단 DB 미수정”)와 충돌. 이후 sync가 leave_balances를 맞추나 **이중 쓰기**.

**레거시:** `휴가신청/*` (휴가관리메인, 연차원장, 연차촉진, 연차소멸알림 등) — 워크센터와 도메인 중복.

---

### 2.4 payroll — 급여 워크센터

**파일:** `payroll/PayrollWorkcenter.tsx` + 13 modules + `payroll-context` / `payroll-fetch` / `payroll-domain`

| 모듈 id | 이름 | 실기능 연결 | 비고 |
|---------|------|-------------|------|
| `settlement` | 급여 정산 | `ModSettlement` → 레거시 `급여정산.tsx` (showLegacy) | 5단계 셸 + 실정산 임베드 |
| `ledger` | 급여 대장 | `ModLedger` + 레거시 `급여상세` / 인건비 분석 | |
| `simulator` | 시뮬레이터 | `ModSimulator` | context 데이터 |
| `retirement` | 퇴직 정산 | `ModRetirement` → `중간정산` + 명세서발송 | |
| `pension` | 퇴직연금 | `ModPension` → `퇴직연금관리` | `s.base` 폴백 잔존 가능 |
| `insurance` | 4대보험 | `ModInsurance` → `4대보험EDI` | |
| `withholding` | 원천징수 | `ModWithholding` → `원천징수파일생성` | initialModule `원천징수파일` 딥링크 |
| `wagePeak` | 임금피크제 | `ModWagePeak` → `임금피크제` | `base_salary \|\| base \|\| 3000000` |
| `minWage` | 최저임금 | `ModMinWage` — domain 계산 | **실데이터 동작** |
| `taxFree` | 비과세 | `ModTaxFree` | |
| `ordinary` | 통상임금 | `ModOrdinary` | |
| `unpaid` | 미지급 수당 | `ModUnpaid` → `미지급수당알림` | |
| `absence` | 무급결근 | `ModAbsence` | base_salary 전월 대비 감지 |

**회귀:** 급여정산이 `attendances`에서 `late_minutes` 조회·맵 적용 (`급여정산.tsx:568+`, `:912–926`) — 3차 S-1 빈 Map **해소 방향 확인**.  
**구조:** 3차 D-3 그대로 — 워크센터=셸/KPI, 핵심 쓰기는 레거시 컴포넌트. `payroll-data.ts` 상단 주석은 여전히 “더미 골격” 표현(모듈 메타는 실사용).

---

### 2.5 welfare — 복지

**파일:** `WelfareWorkcenter.tsx` · `WelfareWorkcenter/*` · 레거시 경조사/건강검진/면허/의료기기

| 탭 id | 라벨 | 본문(dynamic) | KPI 소스 |
|-------|------|---------------|----------|
| `calendar` | 달력 | `WelfareCalendar` | 생일·경조·점검 통합 |
| `family` | 경조사 | `경조사관리` | `congratulations_condolences` |
| `checkup` | 건강검진 | `건강검진관리` | `health_checkups` |
| `license` | 면허·자격 | `면허자격증관리` | `staff_licenses` |
| `device` | 의료기기 점검 | `의료기기점검` | `medical_devices` |

**고아 import:** `IncidentReport` dynamic import (`WelfareWorkcenter.tsx:77–80`) — **WELFARE_TABS에 없음**, 미사용(사고는 member 탭).  
**3차 이슈 잔존 가능:** 경조사 INSERT 실패 시 로컬 폴백(4차 hr-02) — 재확인 시 해당 파일 저장 핸들러 점검 필요.  
**device_name:** 점검 이력 insert는 `device_name: selectedDevice.name` (`의료기기점검.tsx:93`) — 삭제 UI의 잘못된 컬럼 이슈는 부분 개선.

---

### 2.6 docs — 계약·문서

**파일:** `DocsWorkcenter.tsx` · `DocsWorkcenter/*`

| 탭 id | 라벨 | 본문 | 비고 |
|-------|------|------|------|
| `contract` | 계약 현황 | `계약관리` + `DocsContractSummary` | `employment_contracts` |
| `autogen` | 계약서 자동생성 | `계약서자동생성` + Wizard 배너 | `base_salary` 사용 확인 (`계약서자동생성.tsx:68`) |
| `repository` | 문서보관함 | `문서보관함` | `certificate_issuances` 정합 |
| `certificate` | 증명서 발급 | `증명서발급` | |
| `submission` | 서류 제출 | `스마트서류제출` | REQUIRED_DOCS_KPI 16종 |

**정책 (7-15 FIXED):**  
`lib/db/auth/policies.ts:222–228`

| 테이블 | select | insert | update | delete |
|--------|--------|--------|--------|--------|
| `employment_contracts` | SELF_OR_SAME_COMPANY | ADMIN_OR_MANAGER | SELF_OR_SAME_COMPANY | ADMIN_ONLY |
| `onboarding_checklists` | SELF_OR_SAME_COMPANY | SELF_OR_SAME_COMPANY | SELF_OR_SAME_COMPANY | (registry) |

→ 직원이 서명대기 계약 **조회·서명 가능**. 알림 type `인사` 정합은 버그수정 문서 기준.

**계약 하위:** `인사관리서브/계약문서/*` (본문·미리보기·전자서명·비밀유지 등).

---

### 2.7 abnormal (사이드바 비노출 · attend 내장)

| 영역 | 파일 | 액션 |
|------|------|------|
| 감지 카드 | `AbnormalDetectionCard` | 종류별 패턴 선택 |
| 규칙 | `AbnormalRuleConfig` | 활성 규칙 (admin/system master) |
| 조치 로그 | `AbnormalActionLog` | 처리 이력 |
| 데이터 | `AbnormalWorkcenter/data.ts` | 집계 |
| 조기퇴근 | `early_leave_records` 전체 조회 | 회사 필터 없음 — **MSO 전 회사 운영 의도**(4차 재분류) |

---

## 3. 모바일 전수 맵

### 3.1 허브 (`index.tsx`)

| 메뉴 id | 화면 | PC 대응 | 권한 |
|---------|------|---------|------|
| member | `구성원.tsx` | member | **미게이트** (항상 노출) |
| attend | `근태.tsx` | attend | **미게이트** |
| leave | `연차.tsx` | leave | **미게이트** |
| abnormal | `근태이상.tsx` | attend/abnormal | **미게이트** (PC는 사이드바 없음) |
| payroll | `급여워크센터.tsx` | payroll | **미게이트** |
| welfare | `복지.tsx` | welfare | **미게이트** |
| docs | `계약문서.tsx` | docs | **미게이트** |
| offboarding | `오프보딩.tsx` | member/offboarding | `hr_오프보딩` |
| archive | `문서보관함.tsx` | docs/repository | `hr_문서보관함` |
| form-member | `구성원등록.tsx` | StaffForm | `hr_구성원` 검사 (`:140`) |
| form-leave | `연차신청.tsx` | leave apply | |

### 3.2 서브 탭 · 액션

| 화면 | 탭/모드 | 액션 | 데이터 |
|------|---------|------|--------|
| **구성원** | 명단 / 인사발령 / 징계 / 교육·자격 / 사고 | 검색, 등록(+), 수정 | `useStaffList`, `발령탭`, `징계탭`, `교육자격탭`, `사고보고서탭` |
| **근태** | 내 근태: 대시·근무표·달력 / 전사 관리 | 조정 신청 | **본인: `attendance`** (`data-hooks.ts:158`) / 관리: `attendances` |
| **연차** | 내 연차 / 전사 관리 | 신청, 휴가계획서 버튼 | `useMyLeaveBalance` → **`useAnnualLeaveSummary`** (SSOT) |
| **근태이상** | 카드·조치 | 사유 요청 알림 | `canMutateTeamAbnormal` |
| **급여** | PC 13모듈 전체 | dynamic `PayrollWorkcenter` | PC 동일 context |
| **복지** | 경조/검진/면허/기기 | 등록·수정(관리자) | `useWelfareBundle` |
| **계약·문서** | 내문서/증명서/계약/서류 / 전사 | 발급·업로드·서명 흐름 | `employment_contracts`, repository |
| **오프보딩** | 체크리스트 | 퇴사 처리 | `onboarding_checklists` |
| **문서보관함** | 규정·양식 등 | 열람 | `hr_문서보관함` |

### 3.3 연차 모바일 (7-15 회귀)

| 항목 | 상태 |
|------|------|
| `useMyLeaveBalance` | `useAnnualLeaveSummary` 위임 (`data-hooks.ts:104–121`) — **패리티 문서의 staff-only 경로 수정됨** |
| 15일 폴백 | 서버·summary 경로에서 제거 방향 (내정보 5차와 동일 SSOT) |
| 잔액 재계산 | 관리자 진단 API / rebalance 스크립트 (배포 후 DB 재계산 권장) |
| 휴가계획서 버튼 | `연차.tsx:112` `<MBtn block>휴가계획서</MBtn>` — **onClick 없음 (무동작)** |

---

## 4. 라이브러리 · API · 정책 SSOT

### 4.1 연차

| 모듈 | 역할 |
|------|------|
| `lib/annual-leave-balance.ts` | `resolveGrantedDaysFromAccruals`(최신 annual:N), `recalculateLeaveBalance` → `leave_balances` UPSERT |
| `lib/annual-leave-ledger.ts` | 당해 사용 동기화 `syncAnnualLeaveUsedForStaff({ year })` |
| `lib/annual-leave-summary.ts` | UI SSOT 훅 (`remaining_days` 정합) |
| `lib/annual-leave-accrual.ts` | 월/연 발생 |
| `lib/annual-leave-promotion*.ts` | 촉진 |
| `lib/annual-leave-expiry.ts` | 소멸 |
| `lib/leave-policy-settings.ts` / `leave-notice*.ts` | 정책·공지 |

**Admin API:**  
`/api/admin/annual-leave/{sync,diagnose,manual-grant,accrual-run,announce-run,promotion-run}`  
**Cron:** `annual-leave-accrual` / `expiry` / `promotion` / `leave-notice-announcements`

### 4.2 급여

| 모듈 | 역할 |
|------|------|
| `lib/payroll-records.ts` / `payroll-record-upsert.ts` | 대장 저장 |
| `lib/payroll-deductions.ts` / `insurance-*` / `allowance-hours` / `working-hours` | 계산 |
| `lib/payroll-governance.ts` / `notice-dispatch` | 거버넌스·발송 |
| `lib/severance-pay.ts` / `tax-free-limits.ts` / `withholding-tax-file.ts` | 퇴직·비과세·원천 |
| `lib/data/payroll.ts` | 데이터 헬퍼 |
| Cron | `payroll-notice` |

### 4.3 계약 · 온보딩 · 근태 기타

| 모듈 | 역할 |
|------|------|
| `lib/contract-*.ts` | 템플릿·암호화·만료 잡·교대 연동 |
| `lib/hr-checklists.ts` | 입사/퇴사 체크리스트 기본값·정규화 |
| `lib/hr-history-ledger.ts` | 인사이력 |
| `lib/attendance-anomalies.ts` / `attendance-deduction.ts` | 이상·차감 |
| `lib/document-submission-shared.ts` | 필수 서류 카테고리 공유 |
| `lib/certificate-types.ts` | 증명서 타입 |

### 4.4 근태 이중 테이블 (전 제품 공통 Critical 축)

| 테이블 | 주 사용처 |
|--------|-----------|
| **`attendance`** | 모바일 체크인, 내정보 출퇴근, 모바일 HR 본인 월간, 마이페이지 쓰기 |
| **`attendances`** | PC AttendWorkcenter KPI, 급여정산 지각분, 모바일 근태관리자, 이상 감지 |

→ `syncToAttendances` 등 동기화 경로가 있으나 **화면마다 읽기 소스가 갈림** → 관리자 대시보드 vs 직원 화면 불일치 재발 가능. (내정보 5차 M-C3 / 패리티 M2 와 동일 축)

---

## 5. Critical

### C1. 모바일 인사 허브 — 6+ 메뉴 세부 `hr_*` 미적용

| 항목 | 내용 |
|------|------|
| **파일:라인** | `모바일/인사관리/index.tsx:87–98`, `:277–279` |
| **증상** | `menu_인사관리`만 있으면 member·attend·leave·abnormal·**payroll**·welfare·docs 카드가 전부 노출. PC는 탭별 `canAccessHrTab`으로 분리. |
| **영향** | `hr_급여=false` 직원도 모바일에서 급여 13모듈 진입 가능(데이터는 RLS/정책 의존). 계약·구성원 PII 관리 UI 노출. |
| **수정** | PC `HR_TABS.perm` / `HR_WORKCENTER_UNIONS`와 동일 키를 각 메뉴에 부여 후 filter. payroll은 `hr_급여` 필수. |

### C2. `attendance` vs `attendances` 이중 경로 (인사 영역)

| 항목 | 내용 |
|------|------|
| **파일:라인** | 모바일 `data-hooks.ts:158` (`attendance`) · `근태관리자.tsx:52` (`attendances`) · PC `AttendWorkcenter.tsx:79–83` (`attendances`) · 마이페이지/체크인 `attendance` |
| **증상** | 직원 본인 월간 KPI는 `attendance`, 관리자·급여·워크센터는 `attendances`. 동기화 누락 시 “직원은 출근인데 관리자 대시보드 미반영”. |
| **수정** | 읽기 SSOT를 하나로 통일하거나, 모든 쓰기 후 동기화 강제 + 관리자 화면 dual-read 폴백 문서화. |

### C3. 연차(부여) 승인 시 `staff_members.annual_leave_total` 직접 갱신

| 항목 | 내용 |
|------|------|
| **파일:라인** | `LeaveWorkcenter.tsx:210–231`, `:280–290` (삭제 시 역산) |
| **증상** | UI 카피/버그수정 문서는 “leave_balances만·staff 명단 미수정”인데, 부여 승인 시 staff 컬럼을 씀. 다른 화면이 staff 컬럼을 읽으면 SSOT 붕괴 재발. |
| **수정** | 부여는 accruals + `recalculateLeaveBalance`만. staff 컬럼 쓰기는 제거 또는 읽기 전용 미러로 강등. |

### C4. 모바일 `canMutateTeamAbnormal` 과대허용

| 항목 | 내용 |
|------|------|
| **파일:라인** | `data-hooks.ts:665–672` |
| **증상** | `permissions.menu_인사관리 === true` 만으로 전사 근태·연차·복지·계약 **관리 모드** 토글. PC의 `hr_근태_수정` / `hr_연차휴가` / `hr_급여` 세분과 불일치. |
| **수정** | 관리 모드를 액션별 `canAccessHrSection`으로 교체 (최소: 근태 수정 / 연차 / 계약 / 복지 각각). |

---

## 6. Major

| ID | 파일:라인 | 내용 |
|----|-----------|------|
| **M1** | 워크센터 전반 | **워크센터 셸 + 레거시 실기능 2경로** (payroll 13모듈·member 편집·docs 탭). 유지보수·권한·버그 수정 이중 부담. |
| **M2** | 근태 근무표 | **shift_assignments / 레거시 근무표뷰 / nurse_schedules** 3중. 승인 워크플로 일관성 위험. |
| **M3** | `연차.tsx:112` | 모바일 **휴가계획서 버튼 무동작** (onClick 없음). 결재 `연차계획폼` 딥링크 미연결. |
| **M4** | `WelfareWorkcenter.tsx:77` | **IncidentReport dynamic import 고아** — 사고는 member 탭으로 이전됐으나 import 잔존. |
| **M5** | `인사관리.tsx:107` vs member 탭 | **사고보고서** 레거시 fallback은 `member`이나, 권한 키는 `hr_사고보고서`(복지 계열). 권한 관리 UI 라벨과 탭 위치 불일치. |
| **M6** | `경조사관리` (4차 hr-02) | INSERT 실패 시 로컬 가짜 id 폴백 가능성 — **무음 데이터 손실** 재검증·수정 권장. |
| **M7** | `임금피크제.tsx:87`, `퇴직연금관리.tsx:41` | `base_salary \|\| base \|\| 3000000` — 미입력 시 **300만 폴백**으로 금액 왜곡. |
| **M8** | 모바일 급여 | PC `PayrollWorkcenter` 그대로 임베드 — 터치 UX·권한(C1) 취약. `hr_급여_승인` 별도 UI 게이트 불명확. |
| **M9** | PC leave 승인 | 승인/반려가 워크센터에서 직접 `leave_requests` update — 전자결재 연동 여부와 이중 채널 가능. |
| **M10** | `DocsWorkcenter` KPI | 회사 필터 없이 전사 집계 가능(MSO 의도일 수 있으나 일반 회사 매니저 세션과 혼동). |
| **M11** | 모바일 구성원 `+` | 등록 버튼 항상 표시 — 본 화면 권한은 form에서만 `hr_구성원` 검사. |
| **M12** | 탭 내부 세분 권한 | PC member에서 발령/징계/교육/오프보딩 탭을 **세부 hr_* 로 숨기지 않음** (워크센터 진입=전 탭). |
| **M13** | `LeaveWorkcenter/data.ts:300` | 알림 `sender_company` 기본값 **`SY INC.`** 하드코딩. |
| **M14** | 근태유틸 중복 (3차 D-1) | `근태유틸.ts` ↔ `근태관리메인-내부유틸.ts` 대량 복제 — Roster 경로와 레거시 병행 시 드리프트. |

---

## 7. Minor

| ID | 파일:라인 | 내용 |
|----|-----------|------|
| m1 | `인사관리.tsx:238` | 사업체 목록에 항상 `SY INC.` 삽입 |
| m2 | 계약서 placeholder | `박철홍정형외과` / `박철홍` (`계약서자동생성.tsx:272–273`) |
| m3 | `AbnormalWorkcenter` `nowIso` | 날짜만 반환 — “이번 달 누적” KPI 세션성 (4차 hr-05) |
| m4 | `payroll-data.ts` 주석 | “더미 골격” 문구 잔존 (모듈 메타는 사용 중) |
| m5 | Welfare `IncidentReport` | dead code 정리 |
| m6 | index 라우터 주석 | “급여 라우터 제외” 옛 주석 vs 실제 payroll 포함 (`index.tsx:10–11`) |
| m7 | 모바일 허브 검색 버튼 | aria만 있고 동작 없음 (`index.tsx:296–298`) |
| m8 | `DocsContractSummary` 주석 | `employ_type` 폴백 언급 — 코드는 employment_type 위주, 주석 정리 |
| m9 | 필수서류 REQUIRED_DOCS | Docs KPI / 스마트서류 / 마이페이지 등 **N곳 정의** 동기화 부담 |
| m10 | toast tone | 3차 U-2 다수 — 급여·교육 등 재점검 권장 |
| m11 | `ModSettlement` any | `user?: any` 등 JM4 위반 잔존 |
| m12 | 모바일 abnormal 메뉴 | PC 사이드바 통합 정책과 달리 모바일 허브에 **독립 카드** — 패리티 혼란 |

---

## 8. 3차·4차·7-15 교차검증 (회귀표)

| 이슈 | 출처 | 5차 판정 |
|------|------|----------|
| 연차촉진 upsert NOT NULL | 3차 해소 | 유지(별도 재발 징후 없음) |
| 계약 notifications body | 3차 해소 | 유지 |
| `employ_type` → 전원 정규 | 3차 S-4 | **FIXED** — `employment_type` |
| `s.base` 계약 자동생성 | 3차 S-2 | **FIXED** — `base_salary` (`계약서자동생성.tsx:68`) |
| `certificate_issues` | 3차 S-6 | **FIXED** — `certificate_issuances` |
| `staff_certifications` | 3차 S-5 | 교육 패널 경로 재구성(EducationBoard) — 구 패널 잔존 여부 낮음 |
| 급여 지각 차감 빈 Map | 3차 S-1 | **개선** — attendances + late_minutes 맵 |
| nurse_schedules 없음 | 4차 hr-01 | **스키마 존재** (`schema.ts:2526`) — 런타임 배포 여부만 확인 |
| 경조사 무음 실패 | 4차 hr-02 | **잔존 가능 (M6)** |
| 계약서 ADMIN_ONLY | 7-15 | **FIXED** SELF_OR_SAME_COMPANY |
| 연차 15폴백·이중 공식 | 7-15 / 패리티 | **코드 SSOT 정렬** — DB 재계산·일부 UI 무동작(휴가계획서) 남음 |
| 근태 dual table | 패리티 M2 | **잔존 (C2)** |
| 워크센터↔레거시 병행 | 3차 D/Q | **잔존 (M1)** |

---

## 9. 권한 매트릭스 (요약)

| 기능 | PC 게이트 | 모바일 게이트 | 서버/정책 |
|------|-----------|---------------|-----------|
| 인사 메뉴 자체 | `menu_인사관리` + 세부 OR | 동일(셸) | — |
| 구성원 탭 | `hr_구성원` 계열 | **허브 미게이트** / 등록폼만 `hr_구성원` | staff RLS |
| 근태 | `hr_근태*` OR | 허브 미게이트 / 관리=`canMutateTeamAbnormal` | attendance* |
| 연차 | `hr_연차휴가` OR 근태 | 허브 미게이트 | leave_* + admin API |
| 급여 | `hr_급여` | **허브 미게이트** | payroll records |
| 복지 | 5키 OR | 허브 미게이트 | 도메인 테이블 |
| 계약 | 4키 OR | 허브 미게이트 | employment_contracts 정책 |
| 오프보딩 | member 합집합 | **`hr_오프보딩`** | checklists |
| 문서보관함 | docs 합집합 | **`hr_문서보관함`** | repository |

---

## 10. 파일 인벤토리 (조사 범위)

### PC 워크센터
```
app/main/기능부품/인사관리.tsx
app/main/기능부품/인사관리워크센터/
  index.tsx, workcenter-common.tsx
  MemberWorkcenter.tsx + MemberWorkcenter/*
  AttendWorkcenter.tsx + AttendWorkcenter/*
  LeaveWorkcenter.tsx + LeaveWorkcenter/*
  AbnormalWorkcenter.tsx + AbnormalWorkcenter/*
  payroll/* (PayrollWorkcenter + 13 Mod* + context/fetch/domain)
  WelfareWorkcenter.tsx + WelfareWorkcenter/*
  DocsWorkcenter.tsx + DocsWorkcenter/*
```

### PC 레거시 서브 (워크센터가 위임)
```
app/main/기능부품/인사관리서브/
  구성원현황*, 오프보딩, 사고보고서, 인사발령관리, …
  근태기록/*, 간호근무표, 근무형태관리, …
  휴가신청/*, 연차소멸알림, 공휴일달력, …
  급여명세/* (정산·대장·보험·피크 등), 원천징수파일생성
  경조사·건강검진·면허·의료기기, …
  계약관리, 계약서자동생성, 계약문서/*, 문서보관함, 증명서발급, 스마트서류제출
```

### 모바일
```
app/main/모바일/인사관리/
  index.tsx, data-hooks.ts, form-helpers.tsx
  구성원*, 근태*, 연차*, 근태이상*, 급여워크센터.tsx
  복지*, 계약문서*, 오프보딩, 문서보관함
  발령·징계·교육·사고 탭 컴포넌트
```

### Lib / API
```
lib/annual-leave-*.ts, leave-*.ts, payroll-*.ts, hr-checklists.ts,
lib/contract-*.ts, attendance-*.ts, feature-permissions.ts, access-control.ts,
lib/db/auth/policies.ts
app/api/admin/annual-leave/*, app/api/cron/annual-leave-*, payroll-notice, …
```

---

## 11. 권장 스프린트

### Sprint H1 — 권한·보안 (1–2일)
1. 모바일 허브 메뉴에 PC와 동일 `hr_*` 필터 (**C1**)  
2. `canMutateTeamAbnormal` → 세부 권한 (**C4**)  
3. 급여 모듈 마운트 시 `hr_급여` / 승인 액션 시 `hr_급여_승인`  

### Sprint H2 — 연차 SSOT 마감 (1일)
4. LeaveWorkcenter 부여 승인 시 `staff_members` 쓰기 제거 (**C3**)  
5. 운영 DB `diagnose?rebalance` / `scripts/rebalance-leave-balances.mjs`  
6. 모바일 휴가계획서 → 결재 compose 연결 (**M3**)  

### Sprint H3 — 근태 단일화 (2–3일)
7. `attendance`/`attendances` 읽기 SSOT + 동기화 강제 (**C2**)  
8. 근무표 write 경로 하나로 수렴 (Roster vs 레거시) (**M2**)  

### Sprint H4 — 위생 (여유)
9. Welfare IncidentReport 고아 제거, 경조사 에러 토스트, 300만 폴백 제거  
10. SY INC./박철홍 placeholder 정리, 주석·더미 문구 정리  

---

## 12. 결론

1. **PC 정보 구조**는 B안 6워크센터로 안정. 사이드바·권한 합집합·레거시 별칭이 잘 정리됨.  
2. **7-15 핵심 수정**(계약서 직원 조회·서명, 연차 accrual/remaining SSOT, 모바일 summary 훅)은 **코드상 유지**.  
3. **최대 잔존 리스크**는 (a) 모바일 세부 권한 공백, (b) 근태 이중 테이블, (c) 연차 staff 컬럼 직접 쓰기, (d) 급여·근태 워크센터/레거시 2경로.  
4. 3차의 잘못된 컬럼/테이블 Critical 다수는 **해소**됐으나, **구조 중복(D/Q)** 과 **하드코딩·무동작 UX**는 여전히 Major/Minor로 남음.  
5. 배포 후 운영 액션: 연차 잔액 일괄 재계산, Electron 알림은 인사 범위 외이나 계약 서명 알림 수신과 연계 확인.

---

## 13. 문서 인덱스

```
docs/전수조사/5차_2026-07-15/
  00_요약.md
  01_알림.md
  02_내정보.md
  03_추가기능.md
  07_인사관리.md   ← 본 문서

교차:
  3차_2026-06-01/07_인사관리.md
  4차_fable5_2026-06-10/03_인사관리.md
  버그수정_2026-07-15_계약서_알림_연차.md
  모바일_PC패리티_2026-07-13/00_*.md
```
