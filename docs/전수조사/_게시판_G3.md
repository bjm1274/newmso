# 모바일 게시판 그룹 전수조사 (G3)

작업 디렉터리: `D:\newmso`
대상: `app/main/모바일/게시판/`
조사일: 2026-05-29
D1 정본 스키마 확인: `lib/db/schema.ts` (board_posts L277, board_post_comments L236, board_post_likes L255, board_post_reads L265)

---

## 1. 파일별 역할 (1줄)

| 파일 | 역할 |
|---|---|
| `data-hooks.ts` (501) | 데이터 계층 — 목록/상세/댓글 조회 훅, createBoardPost, 카테고리 상수, 날짜·아바타·첨부 유틸 |
| `index.tsx` (157) | 라우터 — list↔detail↔write view 전환, 좋아요/별표 상태 부모 보관 |
| `게시판목록.tsx` (267) | 목록 UI — 카테고리 칩바, 글 카드, 별표 토글, PTR |
| `게시판상세.tsx` (534) | 상세 UI — 본문 마크다운, 첨부, 좋아요/별표/공유, 조회수, 댓글 입력 |
| `글작성.tsx` (459) | 작성 폼 — 카테고리/제목/본문/첨부/옵션, 온·오프라인 분기 insert |
| `글작성옵션.tsx` (141) | 작성 옵션 섹션 — 고정/중요도/익명/예약발행 |
| `댓글트리.tsx` (177) | 댓글 1단계 트리 — root/replies 분리 렌더 |
| `마크다운.tsx` (270) | 자체 안전 마크다운 렌더러 (dangerouslySetInnerHTML 미사용) |
| `별표훅.ts` (188) | 즐겨찾기 — board_post_stars 서버 + LS 폴백 |
| `상세훅.ts` (102) | 조회수 +1 RPC, 댓글/좋아요 폴링 realtime |
| `좋아요훅.ts` (131) | board_post_likes 토글 + likes_count 동기화 |
| `첨부업로드.ts` (90) | 작성 폼 첨부 업로드 wrapper + MIME/크기/확장자 검증 |
| `첨부카드.tsx` (132) | 상세 첨부 — 이미지 인라인 / 파일 카드 |

---

## 2. 분류별 발견

### D — 데스크톱/모바일 중복
- **D-1 [중]** `data-hooks.ts:5` 주석 "PC 게시판.tsx의 supabase 쿼리 패턴을 발췌해 모바일 전용으로 압축". 목록·상세·작성 쿼리(board_posts select/insert, board_post_comments, board_post_likes) 전체가 PC `app/main/기능부품/게시판.tsx`와 동일 테이블·동일 컬럼 셋·동일 fallback(`withMissingColumnsFallback`)을 재구현. 공통 select 상수·normalizeBoardPost·buildAttachmentMetaContent 등은 `게시판공통.ts`에서 재사용 중이라 일부 공유는 되어 있으나, **목록 조회/댓글 수 집계/insert 로직 자체는 PC와 별도 복제**. 근거: data-hooks.ts:86-135(목록·댓글수), 254-266(댓글insert) vs 게시판.tsx 동일 패턴.
  - 조치: 데이터 레이어를 게시판공통.ts 또는 별도 board-queries 모듈로 추출해 PC/모바일 공유 권장(현 구조 동작에는 문제 없음 — 유지보수 부채).

### S — 문법/런타임 오류
- 발견 없음. (모든 파일 import 경로·타입 정합 확인)

### K — 한글 깨짐
- 발견 없음. (소스·주석 모두 정상 UTF-8 한글)

### X — 죽은 코드
- **X-1 [낮]** `댓글트리.tsx:177` `export { buildTree as buildCommentTree }` — 외부 참조 없음(Grep: `buildCommentTree` 매치 0건, 파일 내부에서만 사용). 죽은 export.
  - 조치: export 제거(내부 함수로 유지).
- **X-2 [낮·의심]** `data-hooks.ts:291-293` re-export(`toggleStarServer`/`loadStarSet`/`useStarSet`, alias `toggleStar`). `toggleStar` alias는 "게시판목록.tsx가 옛 이름으로 import" 주석이 근거지만, 현재 게시판목록.tsx:23은 `toggleStarServer`를 직접 import(별표훅에서). Grep 결과 `toggleStar`(별칭) 실사용처 없음 → 회귀 보호용 죽은 alias 의심.
  - 조치: `toggleStar` alias 및 미사용 re-export 정리 검토.

### U — 불필요
- **U-1 [낮]** `좋아요훅.ts:99-100` likes_count 동기화 시 매 토글마다 (1) delete/insert (2) count(exact,head) (3) board_posts.update 3쿼리 발생. count 쿼리는 best-effort update 직전 매번 풀스캔성 집계 → P 항목과 중복. (성능 P-2 참조)

### Q — 유사기능
- **Q-1 [중]** 별표(`별표훅.ts`)와 좋아요(`좋아요훅.ts`)가 거의 동일한 구조(내 Set 로드, 낙관적 토글, missing-table 폴백, useXxx 훅). 코드 중복도 높음. 다만 별표는 LS 폴백 보유, 좋아요는 없음 — 동작 차이 존재.
- **Q-2 [낮]** `data-hooks.ts:491 inferKind` 와 `게시판공통.ts:119 inferAttachmentType` 가 유사(확장자→image/video/file 판정). inferKind는 svg/avif/heif 포함, inferAttachmentType은 avif 미포함 — 미묘한 불일치(첨부 표시엔 영향 적음).

### T — 타입(any·강제 캐스팅)
- **T-1 [중]** `data-hooks.ts:100,213` `result as unknown as { data: ...; error: unknown }` 이중 강제 캐스팅. supabase 빌더 반환 타입을 무리하게 좁힘. (compat 레이어 한계상 불가피한 면 있으나 unknown 경유 캐스팅 다수)
- **T-2 [낮]** 전반적으로 `(post as { is_anonymous?: ... })`, `post.board_type as string | null`, `post.views as number` 등 인라인 캐스팅 빈번(게시판상세.tsx:221,236,353 / 게시판목록.tsx:50,66,95). BoardListPost 타입에 해당 필드가 충분히 정의되지 않아 캐스팅으로 메움.
  - 조치: BoardListPost/BoardPost 타입에 is_anonymous·is_pinned·status·tags·scheduled_publish_at 명시.

### E — 에러처리
- **E-1 [중]** `별표훅.ts:53-66 isMissingOrDeniedError` 와 `좋아요훅.ts:18-23 isMissingTable` 가 **Postgres 에러코드(42P01/42501/PGRST116/23505)** 기준으로 분기. 현재 런타임은 D1(`lib/supabase.ts`: d1Client). D1 경로는 PG 코드를 반환하지 않음 → 코드 기반 분기는 사실상 message 문자열 매치에만 의존. (board_post_stars 부재와 결합되면 SEC/DBG 무음 폴백으로 이어짐 — SEC-2 참조)
- **E-2 [낮]** `data-hooks.ts:147,219,235,273` catch에서 toast만, console 로그 없음 → 디버깅 시 원인 추적 어려움(JM3 로그/사용자메시지 분리 미흡). 단, 사용자 알림은 됨.

### P — 성능(폴링·중복 API)
- **P-1 [중]** `data-hooks.ts:117-135` 목록 댓글 수 집계: 최대 100개 글의 댓글을 `board_post_comments.select('post_id').in('post_id', ids)`로 **전 댓글 행을 클라이언트로 받아** JS reduce 카운트. 댓글 많은 글이 누적되면 페이로드 폭증. count 집계 쿼리(group by) 또는 서버 RPC로 대체 권장.
- **P-2 [중]** `좋아요훅.ts:93-100` 좋아요 토글 1회당 3 라운드트립(mutate + count + update). 또한 `board_posts.likes_count`는 fire-and-forget(`void`)로 갱신돼 실패 무음.
- **P-3 [낮]** `상세훅.ts:84-100` 댓글/좋아요 8초 폴링(`pollIntervalMs:8000`). 상세 화면 상주 시 지속 폴링 — realtime-bus 공통 정책이나 의도 확인 필요(P 분류 기록용).
- **P-4 [낮]** `index.tsx:48 overridePosts` 패턴: 별표/좋아요 변경 시 전체 posts 배열 map 복제. 100개 기준 경미하나, refetch와 override 상태 이원화로 재조회 시 override가 stale될 수 있음(의심).

### SEC — 보안(XSS·업로드 검증)
- **SEC-1 [정보/양호]** `마크다운.tsx` XSS 안전: dangerouslySetInnerHTML 미사용, 모든 출력 React 노드, link href는 `SAFE_HREF=/^(https?:|mailto:|tel:|#|\/)/i`로 javascript:/data: 차단(L23-30), `rel="noopener noreferrer" target="_blank"`(L127-128). 본문 메타(`[[ATTACHMENTS_META]]`)는 렌더 전 제거. 이미지 src는 `buildStorageInlineUrl`(R2) 경유. → XSS 취약점 미발견.
- **SEC-2 [높]** `별표훅.ts:21 TABLE_NAME='board_post_stars'` — **D1 정본 스키마에 board_post_stars 테이블이 존재하지 않음**. 근거: `lib/db/schema.ts` Grep 결과 board_post_likes/board_post_reads만 존재, board_post_stars 0건. `scripts/migrate-d1/output/d1_schema.sql`에도 0건(존재하는 곳은 모바일 게시판 파일과 build-report·extracted_policies뿐). → 별표 서버 저장(loadStarSet/toggleStarServer)은 **항상 실패 → isMissingOrDeniedError/catch로 무음 폴백 → localStorage만 사용**. 결과: 즐겨찾기가 기기·브라우저 로컬에만 저장돼 기기 간/재설치 시 유실. 사용자에겐 toast조차 안 뜸(무음 기능 반쪽 동작). 태스크에서 경고한 "옛 스키마 미존재 테이블 쿼리" 패턴에 정확히 해당.
  - 조치: (a) D1에 board_post_stars 테이블 추가(권장 — likes와 동형: post_id/user_id/created_at, unique(post_id,user_id)) 또는 (b) 별표를 LS 전용 기능으로 명시하고 서버 쿼리 제거.
- **SEC-3 [정보/양호]** `첨부업로드.ts:30-46 validateFile` — 확장자 블랙리스트(exe/bat/cmd/com/msi/scr/cpl/app/sh/ps1, L20-22) + 크기 한도(이미지 25MB/동영상 200MB/일반 50MB) 검증. 오프라인 경로(글작성.tsx:162)도 validateFile 호출. → 기본 검증 존재.
  - 보완 [낮]: 검증이 클라이언트 전용. 서버(R2 업로드 라우트) 측 MIME/크기 재검증 여부는 본 범위 밖이나 권장. 또한 블랙리스트 방식이라 svg(스크립트 내장 가능)·html·htm 미차단 — svg는 inferKind에서 image로 분류되나 첨부 다운로드/inline 열람 시 위험 가능(의심, R2 Content-Disposition 정책에 의존).

### DBG — 디버그
- 발견 없음. (console.log/debugger 잔존 없음 — Grep 무매치)

---

## 3. 추가 정합성 노트 (D1 스키마 대조)

- board_posts insert payload(`data-hooks.ts:383`, `글작성.tsx:106`) 컬럼은 정본 스키마와 일치(board_type/title/content/author_id/author_name/company/company_id/is_anonymous/attachments/is_pinned/status/scheduled_publish_at 모두 L277-309에 존재). ✔
- `is_anonymous: boolean`로 insert(L392) — D1 컬럼은 `integer().default(0)`. compat/D1 드라이버가 boolean→0/1 변환 여부에 의존(타 메뉴 동일 패턴, 본 그룹 한정 문제 아님 — 의심 표기).
- board_post_comments insert(parent_comment_id 포함) 정본 L243과 일치. ✔
- 태스크가 언급한 `board_post_status_reads`는 코드·스키마 어디에도 없음. 모바일 게시판은 읽음 추적(board_post_reads) 미사용(PC만 사용). → 모바일엔 해당 없음.

---

## 4. 집계표

| 분류 | 건수 | 높음 | 중간 | 낮음/정보 |
|---|---|---|---|---|
| D 중복 | 1 | 0 | 1 | 0 |
| S 문법/런타임 | 0 | - | - | - |
| K 한글깨짐 | 0 | - | - | - |
| X 죽은코드 | 2 | 0 | 0 | 2 |
| U 불필요 | 1 | 0 | 0 | 1 |
| Q 유사기능 | 2 | 0 | 1 | 1 |
| T 타입 | 2 | 0 | 1 | 1 |
| E 에러처리 | 2 | 0 | 1 | 1 |
| P 성능 | 4 | 0 | 2 | 2 |
| SEC 보안 | 3 | 1 | 0 | 2 |
| DBG 디버그 | 0 | - | - | - |
| **합계** | **17** | **1** | **6** | **10** |

### 높음(High) 항목
- **SEC-2**: `별표훅.ts:21` board_post_stars 테이블이 D1 정본에 부재 → 별표 서버 저장 무음 실패, localStorage 폴백만 동작(기기 간 유실).
