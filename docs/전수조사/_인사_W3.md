# 인사관리 [조직도/근무현황] 그룹 전수조사 — W3

조사일: 2026-05-29 / 작업 디렉터리: D:\newmso
대상: 조직도서브 7개 파일 + 근무현황.tsx
정본 스키마 확인: lib/db/schema.ts (staff_members, attendance↔attendances, shift_assignments, work_shifts, org_teams)

---

## 0. 특별임무 결과

### (a) 조직도본문.tsx 가 메신저를 import 하는가 → ★확인됨 (채팅 진입점)
- `app/main/기능부품/조직도서브/조직도본문.tsx:14` `const loadChatView = () => import('../메신저');`
- `:59` `const ChatView = dynamic(loadChatView, { ssr:false, ... })`
- `:287~303` `mainMenu === '채팅'` 일 때 `<ChatView .../>` 렌더 — **조직도본문.tsx(=MainContent 셸)가 채팅 메뉴의 실제 진입점**.
- 주의: 파일명이 "조직도본문"이지만 내용은 조직도 전용이 아니라 전체 메인콘텐츠 라우터(MainContent). 이름-내용 불일치(가독성 혼란).

### (b) 근무현황.tsx 가 추가기능 메뉴에서도 쓰임 → ★확인됨 (중복 진입)
- 데스크톱: `app/main/기능부품/추가기능공통.tsx:128` `const loadWorkStatusView = () => import('./근무현황');`
- 모바일: `app/main/모바일/추가기능/index.tsx:29` `import 근무현황 from './근무현황';` (별도 모바일 파일)
- 즉 데스크톱 `기능부품/근무현황.tsx`는 추가기능 메뉴에서 동적 로드되어 들어옴. "인사관리"가 아니라 추가기능 그룹 소속 컴포넌트. 인사관리 워크센터에서의 직접 진입은 grep상 없음(추가기능 경유).

### (c) 조직도측면창 이 알림센터를 포함 → ★확인됨 (알림 메뉴 교차)
- `app/main/기능부품/조직도서브/조직도측면창.tsx:22` `import NotificationCenter from '../알림센터';`
- `:971~973`(데스크톱), `:1074~1077`(모바일) `<NotificationCenter user={effectiveUser} onOpenMenu={onMenuChange} />`
- 조직도측면창.tsx 내용은 **Sidebar(좌측 메인 네비 + 채팅 안읽음 뱃지 + 알림센터)** 임. 파일명("조직도측면창")과 실제 책임(글로벌 사이드바) 불일치.

---

## 1. 분류별 발견

### 【X 죽은코드】 — 높음

#### X-1. 조직도그림.tsx 전체가 죽은코드 (1048줄)  ★높음
- 파일: `조직도서브/조직도그림.tsx` 전체.
- 근거: 전체 코드베이스 grep 결과 `조직도그림` 또는 이 파일의 `OrgChart` default export를 import하는 곳이 **없음**. 활성 조직도는 `OrgChart.tsx`(조직도본문.tsx:11 `import('./OrgChart')`가 유일 진입).
- `조직도그림.tsx`는 하드코딩된 회사명(`:94` `['전체','박철홍정형외과','수연의원','SY INC.']`)·구버전 캔버스/피라미드 로직을 담은 이전 버전. 디자인 문서(docs/디자인_통일_현황.md:45, docs/대화_이어가기_정리.md)에도 구파일로 언급.
- 권장: 삭제(또는 `_deprecated`로 격리). 1048줄·supabase 직접쓰기 다수 포함된 채 방치 → 유지보수 혼란·audit 누락 위험(docs/audit_coverage.csv:99에 "insert/update 8회 audit 미호출"로 기록).

#### X-2. 조직도직원카드.tsx (StaffCardRow) 죽은코드 (93줄)  ★높음
- 파일: `조직도서브/조직도직원카드.tsx`.
- 근거: `StaffCardRow`를 import하는 파일이 없음. 사용처인 조직도그림.tsx는 **자체 내부 정의**(조직도그림.tsx:975)의 동명 함수를 쓰므로 이 모듈을 참조하지 않음. org-chart-types.ts의 `toSafeText`만 재export 경유로 쓰일 뿐, 본 컴포넌트는 미사용.
- 권장: X-1과 함께 삭제.

#### X-3. org-chart-types.ts 대부분 죽은코드 (의심)
- 파일: `조직도서브/org-chart-types.ts`.
- `toSafeText`/`getStaffExtensionText`는 죽은코드 조직도직원카드.tsx에서만 import. 활성 OrgChart.tsx는 자체 `normalizeText`를 정의해 사용(OrgChart.tsx:114). 타입(OrgTeam, OrgDepartment 등)도 조직도그림.tsx가 자체 재정의(조직도그림.tsx:11~52)하여 이 파일을 import하지 않음.
- 즉 X-1·X-2 제거 시 org-chart-types.ts도 고아가 됨. 동반 정리 대상.

#### X-4. 조직도그림.tsx 내 DIVISION_HEAD_POSITIONS/MSO_DIVISION_HEAD_POSITIONS·캔버스 레이아웃 로직
- X-1에 포함(파일 통째 죽은코드). 캔버스 저장(`saveLayout` companies.memo JSON, 조직도그림.tsx:326~349)·드래그 이동(`handleMoveStaff`)도 전부 미사용 경로.

### 【D 중복】

#### D-1. StaffCardRow 정의 2벌  ★중간
- 조직도직원카드.tsx:20 `export function StaffCardRow` 와 조직도그림.tsx:975 `function StaffCardRow` 가 거의 동일(사진+이름+직책, employee_no swap drag). 둘 다 죽은코드지만 중복 자체가 X-1/X-2의 원인.

#### D-2. 조직도 내부 타입 중복 정의  ★중간
- `OrgTeam/OrgDepartment/OrgPyramidData/OrgListData/CanvasLayout/CompanyInfo/OrgChartProps` 가 org-chart-types.ts(4~45)와 조직도그림.tsx(11~52)에 **완전 중복** 정의. (DRY 위반)

#### D-3. 근무중 판정 로직 중복 (OrgChart vs 근무현황)  ★중간
- OrgChart.tsx의 `isWorkingAttendance`(156)·`getPresenceMeta`(160) 와 근무현황.tsx의 `selectedDateRows` activeStaffIds 계산(627~633)이 "check_in 있고 check_out 없음" 동일 규칙을 각자 구현. 공통 util 부재.

#### D-4. formatClockLabel / toDateKey 중복  ★낮음
- `formatClockLabel`·`toDateKey`가 OrgChart.tsx(122,129)와 근무현황.tsx(87,114)에 별도 구현(로직 미묘하게 다름 → 표시 불일치 가능). 공통화 권장.

#### D-5. 출근/퇴근 라벨 중복 계산  ★낮음
- OrgChart.tsx:1181 에서 `formatClockLabel(getAttendanceCheckIn(attendance))` 를 한 줄에서 2회 호출(같은 값). 사소한 중복 연산.

### 【SEC / S 문법·런타임】

#### S-1. work_shifts `.eq('is_active', true)` — boolean vs integer  ★높음(의심)
- 근무현황.tsx:261 `.eq('is_active', true)`.
- 정본 schema.ts:2399 `is_active: integer().default(1)` → D1은 정수(1/0) 저장. `@/lib/supabase`는 D1 프록시. boolean `true`가 SQL 바인딩 시 `1`로 강제되지 않으면 **항상 빈 결과 → 근무유형 전체 누락(무음실패)**.
- OrgChart.tsx 등 다른 곳은 `.eq('date', todayKey)`처럼 문자열만 사용. is_active를 boolean으로 넘기는 유일 지점.
- 확인 필요: 프록시(lib/supabase)의 boolean→1 변환 여부. 변환 안 하면 근무현황 "현재 근무중" 시프트 카드가 비어 보임.

#### S-2. window.location.reload() 전면 리로드  ★중간 (죽은코드 내부)
- 조직도그림.tsx:408,646,797,869,1005 / 조직도직원카드.tsx:50 직원 이동·정렬 후 `window.location.reload()`. SPA 전체 새로고침은 UX/성능 악화. 단 X-1/X-2 죽은코드라 실제 영향 없음 — 제거로 동시 해소.

### 【T 타입(any·강제캐스팅)】

#### T-1. WorkStatus user:any, appData any 캐스팅  ★중간
- 근무현황.tsx:218 `function WorkStatus({ user }: { user?: any })`.
- :306 `shift_id: (s as any).shift_id ?? null` — appData.staffs에 shift_id가 타입에 없어 any 캐스팅. StaffMember에 shift_id 미정의 시 폴백 동작은 하나 타입 안전성 상실.

#### T-2. OrgChart fetchOrgTeams row:any[]  ★낮음
- OrgChart.tsx:339 `for (const row of (data || []) as any[])`. org_teams 행 타입 미지정. schema 기반 타입 부여 가능.

#### T-3. 조직도본문.tsx 다수 강제 캐스팅  ★낮음
- 조직도본문.tsx:345 `data.depts as unknown as Record<string,unknown>[]`, :334 `initialApprovalIntent as Record<string,unknown>...`, :356 `user as never`. props 타입 불일치를 캐스팅으로 우회 — 타입 설계 정합성 부족.

#### T-4. 조직도그림.tsx `(viewData as any).type` / 다중 `as OrgPyramidData`  ★낮음 (죽은코드)
- 조직도그림.tsx:727 `(viewData as any).type === 'pyramid'` 등. X-1로 동반 제거.

### 【P 성능(중복 API·useEffect 의존성)】

#### P-1. OrgChart 전 직원 디렉터리 + org_teams 매 마운트 fetch  ★중간
- OrgChart.tsx:300 `fetchOrgChartDirectory` `select('*')` 전 직원 전체 컬럼 로드. 모듈 레벨 캐시(`orgChartDirectoryCache`)로 1회 방어하나 `select('*')`는 과다 컬럼.
- :793 useEffect 의존성 `[staffs]` — 부모 staffs 배열 참조가 매번 바뀌면 재실행. 캐시가 막아주지만 setAllStaffs 재계산은 발생.

#### P-2. 근무현황 load effect 의존성에 appData.staffs  ★중간
- 근무현황.tsx:377 의존성 `[..., appData.staffs]`. appData.staffs 참조가 갱신될 때마다 work_shifts/shift_assignments/attendance/attendances 4개 쿼리 + getStaffShiftsBatch 전체 재호출. AppDataContext가 staffs를 새 배열로 자주 교체하면 과다 재fetch.

#### P-3. 근무현황 갱신 트리거 3중  ★중간
- realtime 구독(379~403, 6개 테이블 `*`) + 60초 setInterval(443~448) + focus/visibility(realtime 활성 시 noop). realtime이 살아있으면 interval 60초는 사실상 보조이나, refreshNonce 증가가 4쿼리+batch 전체 재실행. interval은 30초 throttle(383) 미적용 → realtime throttle 우회 가능.

#### P-4. todayAttendance.find 반복 (O(n²) 의심)  ★낮음
- 근무현황.tsx:1007 모달에서 staff별 `todayAttendance.find((r)=>r.staff_id===staff.id)` 매 렌더 선형탐색. staffMap처럼 Map 사전구성 권장.

### 【E 에러처리】

#### E-1. 조직도그림 memo JSON.parse 미세이프 (죽은코드)  ★낮음
- 조직도그림.tsx:165,330 `JSON.parse(coData.memo)` try/catch는 있으나(169) X-1 죽은코드.

#### E-2. 근무현황 load catch가 모든 상태 초기화  ★낮음
- 근무현황.tsx:362~367 catch에서 staffs 등 전부 `[]`로 리셋. Promise.allSettled로 개별 처리하므로 catch 진입은 드물지만, 진입 시 화면 전체 공백. 부분 실패와 전체 실패 구분 없음.

#### E-3. getStaffShiftsBatch 실패 silent  ★낮음
- 근무현황.tsx:321~323 `.catch(()=>{})` 완전 무음. 테이블 미존재 폴백 의도지만 다른 에러도 삼킴(디버깅 곤란).

### 【K 한글깨짐】
- 대상 파일 본문에서 깨진 한글 미발견. (UTF-8 정상)

### 【U 불필요】

#### U-1. OrgChart depts prop 미사용  ★낮음
- OrgChart.tsx:14 `depts?: string[]|Record<string,unknown>[]` props 선언되나 본문에서 사용처 없음(조직도본문.tsx:281에서 전달만). 불필요 prop.
- 또한 OrgChart.tsx:766 구조분해에 `depts` 자체가 빠져 있어 받지도 않음 → 전달 무의미.

#### U-2. 조직도그림.tsx 주석처리 상수  ★낮음
- 조직도그림.tsx:79~80 `// const CAN_SEE_PERSONAL_CONTACT_POSITIONS = [...]` 주석 잔재 (죽은코드 내부).

#### U-3. compact prop 부분활용  ★낮음
- OrgChart.tsx:771 `compact` 받지만 :1103 padding 1곳에만 사용. 의도 대비 활용 빈약(경미).

### 【Q 유사기능】

#### Q-1. 두 개의 조직도 구현 병존  ★높음
- 활성 `OrgChart.tsx`(피라미드+근무현황 통합, org_teams division 인덱스) vs 죽은 `조직도그림.tsx`(피라미드/리스트/캔버스, 하드코딩 회사). 동일 목적 2구현 → X-1 해결 시 정리.

#### Q-2. "오늘 근무중" 뷰 2곳  ★중간
- OrgChart.tsx "오늘 근무중" 섹션(1105~)과 근무현황.tsx "현재 근무중"(796~) 이 거의 동일 정보(출근·미퇴근 직원)를 다른 UI/다른 쿼리로 각각 표시. 진입 경로 다름(조직도 vs 추가기능)이나 로직·데이터 중복.

### 【DBG 디버그】

#### DBG-1. console.error 잔존  ★낮음
- OrgChart.tsx:807,860 / 조직도본문.tsx (없음) / 조직도그림.tsx:170,345 / 조직도측면창.tsx:828. 운영 로깅 정책상 점검(대부분 catch 내 error 로깅이라 허용 범위이나 사용자 식별 정보 노출 여부 확인).

---

## 2. D1 정본 정합성 메모
- staff_members 정본 컬럼 확인: employee_no(2000), resign_date(2051), resigned_at(2016), shift_id(2023), auth_user_id(2036), annual_leave_total(2021)/used(2022), extension(2060) — 코드 사용 컬럼 모두 실재.
- attendance(135, status default '정상') ↔ attendances(180, status default 'present') 별개 테이블. OrgChart.tsx:823~831·근무현황.tsx:277~293 둘 다 두 테이블 병합 조회 → 정합. current_status는 attendances에만 존재(191), 근무현황.tsx:281 withMissingColumnFallback로 방어 — 양호.
- org_teams(1400): company_name/division/team_name 실재. OrgChart.tsx:332·조직도그림.tsx:157 조회 컬럼 정합.
- 조직도본문.tsx:233~237 staff_members `.select('annual_leave_total, annual_leave_used').eq('id', user.id).single()` — 컬럼 정본 일치. 단 `.single()`은 0행이면 에러(아래 P/E 무관, 정상 직원이면 OK).
- ★S-1 (is_active boolean) 만 정본 타입(integer)과 바인딩 불일치 위험.

---

## 3. 분류별 건수표

| 분류 | 건수 | 높음 |
|---|---|---|
| D 중복 | 5 | 0 |
| S 문법/런타임 | 2 | 1 (S-1) |
| K 한글깨짐 | 0 | 0 |
| X 죽은코드 | 4 | 2 (X-1, X-2) |
| U 불필요 | 3 | 0 |
| Q 유사기능 | 2 | 1 (Q-1) |
| T 타입 | 4 | 0 |
| E 에러처리 | 3 | 0 |
| P 성능 | 4 | 0 |
| SEC 보안 | 0 | 0 |
| DBG 디버그 | 1 | 0 |
| **합계** | **28** | **4** |

(S-1을 S와 별개 SEC로 보지 않고 S로 분류. 무음실패 성격상 SEC 인접.)
