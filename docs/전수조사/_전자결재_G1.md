# 전자결재 [메인/인박스/워크플로/액션훅] 전수조사 (G1)

조사일 2026-05-29 / 작업 디렉터리 D:\newmso
정본 스키마 확인: lib/db/schema.ts (approvals/approval_history/approval_templates/approval_delegation/approval_form_types 존재, approval_documents 없음, notifications=user_id/read_at/body/metadata)

## 파일별 역할 (1줄)

- `전자결재.tsx` (1427): 전자결재 루트 컨테이너 — 상태/데이터fetch/권한분기/하위뷰(작성·인박스·상세) 조립, 다수 훅 통합.
- `전자결재-types.ts`: 타입·상수 re-export 허브 (ApprovalView/ApproverTemplate/ApprovalViewProps 등).
- `전자결재-utils.ts` (358): 결재선/참조자 정규화·색상·날짜·폼타입·출결정정 폴백 순수 유틸.
- `ApprovalInboxView.tsx` (519): 기안함/결재함/참조함 공통 인박스 UI(필터·일괄·KPI·보드/테이블 분기).
- `ApprovalInboxColumns.tsx`: ResponsiveTable 컬럼 정의 빌더(제목/기안자/유형/일자/상태/관리).
- `WorkflowBoard.tsx` (302): 결재함 칸반 보드(기안작성/대기/검토중/완료/반려 5단계 분류).
- `ApprovalWorkflowKpi.tsx`: 결재함 상단 KPI 4카드(내차례/이번달승인/반려/평균소요).
- `ApprovalFlowCard.tsx` (424): 작성하기 결재선·참조자 선택 카드(StaffPicker, 템플릿 저장/불러오기).
- `useApprovalActions.ts` (343): 단건 승인/반려/회수 액션 훅.
- `useApprovalBulkActions.ts`: 일괄 승인/반려 + 서버 transition/process-final fetch 래퍼.
- `useApprovalRouting.ts` (290): 결재선 ID 해석·현재결재자·지연알림·라우팅 동기화.
- `useApprovalDelegation.ts`: 위임자 해석·위임 라우팅·위임 지연알림.

---

## 분류별 발견

### SEC 보안 (승인권한·결재선검증)

1. **[SEC-중] 반려 클라이언트 폴백이 서버 권한검증을 우회** — `useApprovalActions.ts:215-254`
   ```ts
   } catch (serverTransitionError) {
     console.error('반려 서버 처리 실패, 클라이언트 fallback 실행:', serverTransitionError);
   }
   ...
   const rejectResult = await supabase.from('approvals')
     .update({ status: '반려', meta_data: {...} }).eq('id', item.id);
   ```
   근거: 정상 경로(`/api/approvals/transition`)는 server-approval-transition.ts:414에서 `!actor.isAdmin && effectiveCurrentApproverId !== actor.id` 서버 검증. 그러나 서버 호출이 예외(네트워크/500)면 catch 후 클라이언트가 직접 `supabase.from('approvals').update`로 반려. 이 경로는 클라 가드(line 181 `currentApproverId !== user?.id`)만 통과하면 RLS 없는 D1 프록시로 직접 쓰기 → 서버 검증 부재. 심각도 중(상위 가드 존재하나 신뢰경계가 클라이언트).
   조치: 폴백 제거하고 서버 실패 시 사용자에게 재시도 안내만. 또는 폴백 전 서버 재검증.

2. **[SEC-중] 회수가 전적으로 클라이언트 검증·직접쓰기** — `useApprovalActions.ts:267-321`
   ```ts
   const handleRecallAction = useCallback(async (item) => {
     if (!canUserRecallItem(item)) { toast(...); return; }
     ...
     const { error } = await supabase.from('approvals')
       .update({ status: '회수', current_approver_id: null, meta_data: ... }).eq('id', item.id);
   ```
   근거: 승인/반려와 달리 회수는 서버 라우트가 없음(app/api/approvals = process-final/transition/upload만). `canUserRecallItem`은 `sender_id === user.id` 클라 검증뿐. D1 프록시 직접 update이므로 변조 시 타인 기안 회수 가능. 심각도 중.
   조치: 회수도 서버 라우트로 이전하거나 sender_id 서버측 재확인.

3. **[SEC-의심-낮] 지연알림 meta_data 직접 update** — `useApprovalRouting.ts:231`, `useApprovalDelegation.ts:144`
   `supabase.from('approvals').update({ meta_data: nextMetaData })` — 권한검증 없이 클라에서 임의 결재 문서의 meta_data 갱신. 지연알림 트래커 한정이나 페이로드 변조 시 임의 meta 주입 가능. 심각도 낮(영향 제한적). 조치: 서버측 처리 권장(의심).

### X 죽은코드

4. **[X-중] processFinalApprovalOnServer 전달되나 미사용** — `useApprovalActions.ts:26,814` / `전자결재.tsx:768,814`
   ```ts
   // useApprovalActions params 타입(26)·전자결재.tsx에서 전달(814)
   processFinalApprovalOnServer: ProcessFinalApprovalOnServer;
   ```
   근거: useApprovalActions 본문에서 `processFinalApprovalOnServer` 참조 0회(40-56 destructure에도 없음, return에도 없음). 최종승인 후처리는 서버 transition이 자체 수행. Grep 결과 useApprovalActions 내부 사용처 없음.
   조치: useApprovalActions 파라미터 타입·전달 인자 제거.

5. **[X-중] ApprovalInboxView에 전달되나 destructure 안 되는 props 다수** — `ApprovalInboxView.tsx:77-120` vs `전자결재.tsx:1302-1326`
   타입에 선언되고 부모가 값 전달하지만 컴포넌트 파라미터 destructure(77-120)에 없어 사용 불가:
   `defaultApprovalMonth`(38), `defaultApprovalWeekDate`(39), `effectiveApprovalDateRange`(40), `hasApprovalFilterOverrides`(41), `resolveApprovalTemplateDesign`(59), `resolveApprovalDelegateSnapshot`(60), `resolveApprovalLockSnapshot`(67), `isApprovalEditLockedItem`(68).
   근거: 73줄 destructure 목록에 위 8개 미포함. 부모는 전부 전달(1302-1326). 죽은 prop 인터페이스.
   조치: 미사용 prop은 타입·전달 양쪽에서 제거.

6. **[X-낮] hasApproverSelection prop 미사용** — `ApprovalFlowCard.tsx:165` (실제 사용 226에서 사용됨 → 오탐 아님). 제외.

### U 불필요

7. **[U-낮] inboxHeaderStats가 KPI와 중복 계산** — `ApprovalInboxView.tsx:144-165` + `ApprovalWorkflowKpi.tsx:83-109`
   근거: 결재함 헤더 서브라인(내차례/이번달처리)과 바로 아래 ApprovalWorkflowKpi가 동일 myTurn/approvedThisMonth를 각각 listForView 순회로 재계산. 같은 화면 2회 동일 루프.
   조치: KPI 컴포넌트 결과를 헤더와 공유하거나 헤더 수치 제거.

8. **[U-낮] currentIndex 삼항이 동일 분기** — `WorkflowBoard.tsx:172-177`
   ```ts
   const currentIndex = approved ? totalSteps
     : rejected ? Math.max(summary.currentStep - 1, 0)
     : Math.max(summary.currentStep - 1, 0);
   ```
   근거: rejected/else 분기 결과 동일. `approved ? totalSteps : Math.max(summary.currentStep-1,0)` 로 단순화 가능. 조치: 삼항 축약.

### P 성능 (중복 API·useEffect 의존성)

9. **[P-중] effect 의존성에 BUILTIN_FORM_TYPES(매 렌더 새 배열)** — `전자결재.tsx:165,606-614`
   ```ts
   const BUILTIN_FORM_TYPES: string[] = Array.from(BUILTIN_FORM_TYPE_NAMES); // 165: 매 렌더 새 배열
   useEffect(() => { setCustomFormTypes((prev)=>...sanitize...) }, [BUILTIN_FORM_TYPES]); // 614
   ```
   근거: 165는 useMemo 없이 매 렌더 새 참조 생성. 614 effect와 188 composeFormTabs useMemo 의존성에 들어가 매 렌더 effect/메모 재실행. sanitize는 내부 비교로 무한루프는 막지만 불필요 재실행.
   조치: `BUILTIN_FORM_TYPES`를 useMemo([])로 안정화.

10. **[P-낮] getLeaveRequestSummary를 useCallback 의존성에 포함** — `전자결재.tsx:903-928`
    근거: `buildApprovalSearchText` deps에 `getLeaveRequestSummary`(import 함수, 참조 안정). 무해하나 불필요. 조치: deps에서 제거.

11. **[P-의심-낮] fetchApprovals 후 대기건마다 syncDelegatedApprovalRouting 개별 update** — `전자결재.tsx:695-700`
    근거: 목록 로드마다 status==='대기' 전건에 대해 Promise.allSettled로 라우팅 동기화 update(다건 PATCH). 문서 많으면 매 fetch마다 N건 쓰기. 이미 동기화된 건은 useApprovalRouting:250 `Object.keys(updates).length===0` 이면 null 반환으로 스킵하나 매번 평가. 심각도 낮(의심).

### T 타입 (any·강제캐스팅)

12. **[T-중] openConfirm/openPrompt options:any** — `useApprovalActions.ts:21-22`, `useApprovalBulkActions.ts:51-52`
    ```ts
    openConfirm: (options: any) => Promise<boolean>;
    openPrompt: (options: any) => Promise<string | null>;
    ```
    근거: useActionDialog 옵션 타입 존재할 텐데 any로 받음. 조치: useActionDialog의 옵션 타입 import.

13. **[T-낮] TemplateDesign = Record<string, any>** — `ApprovalInboxView.tsx:15`
    `type TemplateDesign = Record<string, any>;` any 맵. 조치: unknown 또는 구체 타입.

14. **[T-낮] resolveStoredTemplateDesign store:Record<string,any>** — `전자결재.tsx:79`
    any 파라미터. 조치: unknown + 내부 좁히기.

15. **[T-낮] 광범위 ApprovalRecord = Record<string, unknown> 캐스팅 반복** — 전 파일
    근거: 결재 항목을 unknown 맵으로 다루며 `item.meta_data as ApprovalRecord` 등 캐스팅 산재. 정본 타입(approvals 스키마 기반 interface) 부재. 조치: approvals 행 인터페이스 정의 권장(의심·대규모).

### E 에러처리

16. **[E-중] 반려 폴백 update 결과 error를 사용자에게 끝까지 안 알리는 분기 존재** — `useApprovalActions.ts:232-254`
    근거: 1차 폴백 update(232) error면 2차 legacy update(244)로 재시도, 둘 다 실패해야 toast 에러(253). 단 `syncDelegatedApprovalRouting`이 routingError 반환 시(219-223) 이른 return 처리는 됨. 중첩 폴백으로 흐름 추적 난해 + 서버성공/폴백성공 모두 "반려 처리했습니다" 동일 토스트 → 사용자가 폴백 발생 인지 불가. 심각도 중.
    조치: 폴백 자체 재검토(SEC-1과 연계).

17. **[E-낮] 승인 핸들러 끝 도달 불가 토스트** — `useApprovalActions.ts:166`
    근거: try 내 result.ok/result.error 모두 return, catch도 return → 166 `toast('승인 처리 결과를 확인하지 못했습니다')`는 result가 ok도 error도 아닐 때만(이론상 도달). 방어용이나 데드에 가까움. 조치: 의도면 주석, 아니면 정리.

18. **[E-낮] 다수 catch가 console만 + 무음** — `전자결재.tsx:569,627,672` (localStorage JSON.parse 등 `catch {}`)
    근거: 672 `} catch { }` 완전 무음(템플릿/즐겨찾기 로드 실패). 사용자 영향 적으나 디버깅 곤란. 조치: console.warn 최소 기록.

### Q 유사기능

19. **[Q-중] syncApprovalRouting vs syncDelegatedApprovalRouting 거의 동일** — `useApprovalRouting.ts:238-270` vs `useApprovalDelegation.ts:151-203`
    근거: 두 함수 모두 approver_line/current_approver_id 계산 후 missing-column 폴백 while 루프로 update. Delegation판은 위임 분기만 추가. syncApprovalRouting은 routing 훅 return에 포함되나 실제 fetchApprovals(698)는 syncDelegatedApprovalRouting만 사용 → syncApprovalRouting 사용처 확인 필요(아래 X-20).
    조치: 공통 update 루프 헬퍼 추출.

20. **[X-중] syncApprovalRouting 완전 미사용(죽은코드 확정)** — `useApprovalRouting.ts:238,286`
    근거: 전역 Grep 결과 호출처 0. 훅 return(286)에 노출되나 부모 전자결재.tsx destructure(524-539)에서도 안 받음. fetchApprovals(698)는 syncDelegatedApprovalRouting만 사용. 확정 죽은코드.
    조치: syncApprovalRouting 함수·return 제거(Q-19 공통화와 함께 정리).

21. **[Q-낮] resolveApprovalDelaySnapshot vs 두 sync*DelayNotifications 지연계산 중복** — `useApprovalRouting.ts:120-144,166-236` + `useApprovalDelegation.ts:77-149`
    근거: 지연 임계/경과 계산 로직이 스냅샷용·라우팅 알림용·위임 알림용 3곳 분산. 조치: 공통화.

### D 중복

22. **[D-중] markApprovalNotificationsAsRead 알림 dedupe 블록 두 훅 복붙** — `useApprovalRouting.ts:211-234` vs `useApprovalDelegation.ts:123-147`
    근거: upsertNotificationWithDedupe 호출 + dedupeKey 포맷 + 직후 approvals.update(meta_data) 블록이 사실상 동일(주석까지 유사). 조치: 공통 함수로 추출.

23. **[D-낮] buildApprovalHistoryEntry 중복 정의** — `useApprovalActions.ts:72-77` 와 `useApprovalRouting.ts:95-100`
    근거: 동일 시그니처·동일 본문(action/actor_id/actor_name/note). 조치: 유틸 공유.

### K 한글깨짐

- 발견 없음. 한글 라벨/주석 정상(UTF-8). 

### S 문법/런타임

24. **[S-낮] webdriver 분기로 결재함 UI가 갈림** — `ApprovalInboxView.tsx:482`, `ApprovalInboxColumns.tsx:79`
    근거: `window.navigator.webdriver` true(E2E)면 보드 대신 테이블+인라인 승인/반려 버튼 노출. 런타임 버그 아님(테스트 편의)이나 프로덕션 코드에 테스트 분기 잔존. 심각도 낮. 조치: 의도 확인(테스트 전용 분기 격리 권장).

### DBG 디버그

25. **[DBG-낮] console.error/warn 다수 상존** — `useApprovalActions.ts:161,216`, `useApprovalRouting.ts:233`, `useApprovalDelegation.ts:146`, `전자결재.tsx:273,593,596,641,656,688`
    근거: 운영 로깅이나 일부는 무음 catch 보조용. 심각도 낮. 조치: 로깅 정책 통일(중복 아님).

---

## 집계표

| 분류 | 건수 | 항목 |
|------|------|------|
| D 중복 | 2 | 22,23 |
| S 문법/런타임 | 1 | 24 |
| K 한글깨짐 | 0 | - |
| X 죽은코드 | 3 | 4,5,20 |
| U 불필요 | 2 | 7,8 |
| Q 유사기능 | 3 | 19,21,(20) |
| T 타입 | 4 | 12,13,14,15 |
| E 에러처리 | 3 | 16,17,18 |
| P 성능 | 3 | 9,10,11 |
| SEC 보안 | 3 | 1,2,3 |
| DBG 디버그 | 1 | 25 |
| **합계(고유 발견)** | **25** | 1~25 |

### 심각도 높음/중 핵심
- SEC-1 반려 클라이언트 폴백 서버검증 우회 (중)
- SEC-2 회수 클라이언트 단독 검증·직접쓰기 (중)
- X-4 processFinalApprovalOnServer 죽은 전달 (중)
- X-5 ApprovalInboxView 미destructure prop 8개 (중)
- X-20 syncApprovalRouting 완전 미사용 죽은코드 확정 (중)
- P-9 BUILTIN_FORM_TYPES 매 렌더 새 배열 effect 재실행 (중)
- E-16 반려 중첩 폴백 흐름·동일토스트 (중)
- Q-19 routing/delegation update 루프 중복 (중)
- D-22 지연알림 dedupe 블록 복붙 (중)
- T-12 openConfirm/openPrompt any (중)
