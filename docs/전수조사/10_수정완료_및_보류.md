# 전수조사 → 수정 작업 결과 (두 보고서 통합 적용)

> 작업일: 2026-05-29 · 두 보고서(Opus `docs/전수조사/*` + Gemini `code_audit_report.md`) 항목을 병렬로 실제 코드 수정
> 원칙: 각 항목을 **현재 코드로 재검증 → 일치(미수정)하는 것만 수정**, Gemini가 이미 고친 것은 스킵
> ✅ 전체 `npx tsc --noEmit` 통과(EXIT 0) — **133파일 삭제 + 81파일 수정** 후 타입 에러 0
> ⚠️ **아직 커밋 안 함** — `git status`/`git diff`로 검토 후 커밋 필요

## 🟢 적용 완료 (안전 수정)

### 보안
- **[Critical] 권한 상승 차단**: `lib/db/auth/policies.ts` — `staff_members`에 `staffPrivilegeGuard` 추가. `role`/`permissions`/`password` 컬럼 변경은 admin claims일 때만 허용(프로필 자기수정·구성원 등록 등 비권한 쓰기는 유지). `canAccess`에 guard 실행 로직 추가.
- **웹훅 시크릿 제거**: `입금실시간조회.tsx` 평문 토큰 2곳 → 플레이스홀더 문구.
- **stored XSS 차단**: `서류제출.tsx`·`납품확인서.tsx`의 `document.write` 보간값 escape 적용.
- **데이터초기화 서버 재검증**: `데이터초기화.tsx` 파괴적 삭제 직전 `/api/admin/verify-unlock` 서버 재검증 강제(force_logout 비번 공백 폐쇄).
- **워크센터 탭 권한 게이팅**: `관리자전용.tsx` 영문 워크센터 경로에 `canAccessAdminSection` 검증 추가.

### 스키마/컬럼 정합 (무음 기능불능 해소)
- 알림: GlobalNotificationBell·모바일 알림.tsx `is_read`/`read`→`read_at`.
- 내정보: 모바일 data-hooks `approval_documents`→`approvals`·`staff_id`→`user_id`·`read`→`read_at`, index.tsx 알림 `message`→`body`, 출퇴근 토글 테이블 일치(attendance).
- 추가기능: 모바일 data-hooks(`attendances.date`→`work_date`, OP `last_updated_*`→`updated_*`, daily_checks→daily_closures 재작성), 마감보고 `daily_reports`→`daily_closures`, 직원평가/인계노트 정본 컬럼, OP체크상세 `checklist`→`prep_items`.
- 채팅: 모바일 업로드 `chat_messages`→`messages`, audit_logs details `JSON.stringify`.
- 전자결재: **recall API D1 스택 전면 재작성**, 모바일 첨부 `approval_attachments`→`meta_data.attachments`, 참조함 정본 컬럼.
- 인사관리: 모바일 `licenses`→`staff_licenses`·`staff_appointments`→`personnel_appointments`·`attendance_adjustments`→`attendance_corrections`·`notifications.staff_id`→`user_id`·`employment_contracts.title` 제거, **연차 계산 공식 정본(`calculateApprovedAnnualLeaveUsage`/`calculateLeaveDays`)으로 통일**.
- 재고관리: `inventory_logs.amount`→`quantity`, 모바일 `inventory_transactions`→`inventory_logs`, `inventory.unit` 제거/fallback, `suppliers.reg_num/ceo`→`business_number/contact_name`, 발주 `supplier_id` FK 연결, StatusWorkcenter myCount 버그.
- 관리자: 법인손익 `gross_pay`/`record_type` 추가+interim 필터(이중집계 해소), 모바일 audit_logs 정본 컬럼.

### 한글 깨짐 / 무효 클래스
- 알림인박스 `쨌`→`·`, 경영대시보드 `'WOM'`→`'집계 준비중'`, 관리행정양식·퇴원심사·메신저타임라인 무효 Tailwind(`bg-X/10/50`, `border-X/20/50`) 정리, 게시판 CSS 충돌.

### escapeHtml 통합
- `lib/escape-html.ts` 신설 → 전자결재-utils/document-print-utils/certificate-print-utils 3곳 통합.

### 죽은 코드 삭제 (~33,000줄+, import 0 재확인)
- **인사관리 ~26,800줄/105파일**: roster/(63)+근무표엔진(7), 구성원현황 그림자(2414+섹션, 3229줄), 조직도그림·직원카드·편집기(1444), 근태뷰 11파일(2146), 급여 스텁 16파일(1928).
- **채팅 ~4,600줄/11파일**: 메신저메시지조회훅(1575, 동명중복)·메신저패널·오버레이·모달·DrawerSections·AttachmentPanel·Layout·ConversationPanel·ModalHost·검색이동훅·방목록훅.
- **추가기능 ~1,260줄**: op-check-view-utils(551)·op-check-helpers(397), OP체크 `{false&&}` 312줄.
- **내정보**: 명세서디자인(SalarySlipUI)·역할별대시보드(+자식3+types).
- **전자결재 ~500줄**: AttendanceCorrectionSummary·DateSection.
- **재고관리**: inventory-workflow-utils(287)·item-utils(153)·charts 2개.
- **게시판**: Guide 컴포넌트 3종.
- 각 메뉴 다수 미사용 import/state/prop/죽은 JSX 제거.

### 검증 부산물
- 인사 재검증 중 **감사 보고서 오탐 정정**: `비과세한도체크`·`최저임금체크`·`주52시간모니터링`은 import 0이라 보고됐으나 **라이브 importer 존재 확인 → 삭제 보류·보존**. (verify-first 원칙이 잘못된 삭제를 방지)

---

## ⏸️ 보류 (설계/구조 변경 필요 — 별도 작업 권장)

### 스키마 신설 필요 (테이블 자체 부재)
- `op_consultations`(모바일 수술상담), `congratulations_condolences`(경조사), `early_leave_records`(조기퇴근), `generated_contracts`(계약서자동생성), `board_post_stars`(게시판 별표), `as_repair_records`/`return_records`(AS반품), `message_templates`/`external_integrations`(관리자 운영설정) — 테이블 생성 후 연동 또는 기능 제거 결정 필요.

### 보안 (서버 경계 재설계)
- `messages` PUBLIC delete 제한(작성자 async 판정 필요), 결재 상신/반려/회수 클라 폴백 권한 서버화, 출퇴근 위치검증 서버화, 급여명세 비밀번호 게이트 서버 권한화, 증명서 발급 권한, 계약 PII 평문 저장 암호화, 모바일 구성원 등록 권한 가드, API rate-limiting 도입, push 방해금지(야간) 체크.

### 데이터 무결성
- 재고이관 원자적 처리(현 API는 목적지 기존품목 필수 — 신규생성 흐름과 충돌), payroll_records upsert 정본 헬퍼 통일(덮어쓰기 위험), 4대보험/소득세율 SSOT(lib) 일원화.

### 구조/중복 (lib·God 컴포넌트)
- escapeHtml 외 cross-lib 중복(normalizeApprovalLineIds 4중·toDateKey 4곳·notification lib 중복·toStringRecord 등), God 컴포넌트 분리(근태관리메인 2196·급여정산 1931·메신저 3839·메신저방데이터훅 1797·시스템마스터센터 1509·게시판 3304·OP체크 등), 데스크톱↔모바일 도메인/2벌 통합(OP체크·퇴원심사·서류제출), 게시판공통↔view-utils 분기 통합.

### 성능 (동작 변경)
- 폴링 중복/간격(알림센터 10초·타이핑 2중·메시지 2초·게시판 10초 이중 등) → 이벤트 구독/디바운스, `select('*')` 32곳+ 상수화, 1초 타이머 전체 리렌더 분리, N+1 쿼리.

### 기타
- `/api/admin/audit/*` 5종 라우트 신설, 55KB 메가 라우트 분할, LIKE 와일드카드 이스케이프(공유 D1 라우트), 워크센터 `user={null}` 전달, 이중 라우팅(영문/한글) 통합.

---

---

## ✅ 보류 항목 후속 작업 (2차) — 완료분

> tsc 0 + `npm run build` 성공 재확인 완료.

### 웨이브 1 (정의 명확·고가치)
- **미존재 테이블 9종 신설**: `op_consultations`·`congratulations_condolences`·`early_leave_records`·`generated_contracts`·`board_post_stars`·`as_repair_records`·`return_records`·`message_templates`·`external_integrations` → `lib/db/schema.ts` + `lib/db/migrations/0007_missing_feature_tables.sql` + `policies.ts` 등록. 소비처는 이미 올바른 테이블명 사용 중이라 코드 수정 불필요. **⚠️ 실제 D1 반영 필요**: `npx wrangler d1 execute <DB> --remote --file=lib/db/migrations/0007_missing_feature_tables.sql`
- **결재 cross-lib 중복 통합**: `lib/approval-shared.ts` 신설(normalizeApprovalLineIds/resolveStoredCurrentApproverId/buildApprovalHistoryEntry/resolveEffectiveApproverId 코어). `lib/date-utils.ts`에 `toDateKey` 통합(4곳). (process-final의 trim 차이 1건 보류)
- **관리자 감사 API 5종 구현**: `/api/admin/audit/{summary,anomalies,payroll-outliers,backups,restore}` — 권한 게이트(401/403) + D1 집계. restore는 시스템마스터+확인토큰 후에도 501(파괴 위험) 보수 처리.
- **알림 lib 정리**: `lib/notification-shared.ts`(toStringRecord/FCM/WebPush dedup), push 방해금지(야간) 적용, rate-limit 5개 라우트(chat-push·push-self-test·repush-unread·mark-read·chat/upload).

### 웨이브 2 (데이터 무결성·보안 경계)
- **급여 SSOT**: payroll_records 정본 upsert 헬퍼 통일(급여정산·중간정산·연말정산, record_type 분리로 덮어쓰기 방지), 중간정산 국민연금 상·하한 누락 수정. (소득세표는 이미 정본 사용 중이었음 — 감사 추정 정정. 비과세 childcare/night/overseas는 정본 미커버라 보류)
- **messages 삭제 권한**: 정책 프레임워크에 `asyncGuards` 추가 → 작성자 본인/admin만 메시지 soft-delete 허용.
- **LIKE ESCAPE**: `app/api/d1/query` LIKE 절에 `ESCAPE '\'` 추가(입력 이스케이프 본체는 호출부 작업이라 보류).
- **권한 가드**: 발급 증명서(인사권한자만 전체조회, 일반은 본인분), 모바일 구성원등록(인사권한+회사 가드), 워크센터 user 전달.
- **재고이관 원자성**: `stock-transfer` API를 D1 `batch()`로 확장(목적지 신규생성 포함) + 재고이관.tsx가 항상 API 경유 → 재고 증발 방지.

---

## ✅ 대형 구조 리팩토링 (3차) — 완료분
> 매 배치 tsc 0 + `npm run build` 성공. 모두 **동작 보존 순수 추출**(로직/상태/effect/렌더 순서 불변).

### 웨이브 3 (God 비의존 항목)
- **계약 PII 암호화**: `lib/contract-crypto.ts`(AES-GCM, `enc:v1:` 버전 프리픽스, 평문 하위호환, env `CONTRACT_ENCRYPTION_KEY`). 저장(마이페이지 서명)/읽기(문서보관함) 적용.
- **system-master 메가 라우트 분할**: `route.ts` 1383→116줄 디스패처 + `_shared.ts` + handlers 7개. 외부 계약 불변.
- **알림센터 폴링 → 이벤트 구독**(`NOTIFICATION_LIST_UPDATED_EVENT`), **LIKE 입력 이스케이프 호출부**(메신저검색/GlobalSearch/감사로그뷰어), **process-final trim 통일**.

### 웨이브 4 (God 분리 + 모바일 통합)
- **God 컴포넌트 분리** (줄수 감소):
  - 메신저.tsx 3834→3428, 게시판.tsx 3280→2848(+폴링 단일채널), 근태관리메인 2201→1104, 급여정산 1931→1032, 시스템마스터센터 1509→756, OP체크 3439→2561.
  - 모두 인라인 JSX→co-located 서브컴포넌트(props), 순수 헬퍼/타입→공통 모듈. 상태/effect/금액계산 로직은 본체 유지.
- **데스크톱↔모바일 공통 코어 통합**(UI 병합 아님, 양쪽 동작 보존):
  - 퇴원심사 `퇴원심사/공통.ts`(stayDays·status 정규화 SSOT), 서류제출 `lib/document-submission-shared.ts`(업로드 정책 MIME 화이트리스트+30MB 통일, status '발급완료'→'제출' 정정).

## ⏸️ 최종 잔여 (의도적 미적용 — 회귀 위험 대비 가치 낮음)
- **`select('*')` 32곳+ 명시 컬럼화**: 컬럼 누락 시 silent 깨짐 위험이 크고(런타임), 가치는 미미해 **일괄 미적용**. God 분리 중 안전한 파일은 정리됨. 필요 시 파일별로 개별 검증하며 진행 권장.
- 각 God 컴포넌트의 **핵심 상태/effect 로직 자체의 훅 분해**: 동작 동일성 위험으로 보류(프레젠테이션 추출까지만). 추가 분해는 e2e 동반 권장.
- OP체크/메신저 등 잔여 미세 분해, useChatRealtimeBridge orphan 정리 등.

## 다음 단계
1. `git status` / `git diff`로 검토 (대량 삭제·수정·신규 lib/API).
2. **마이그레이션 0007 적용**(wrangler) — 신설 테이블이 실제 동작하려면 필수.
3. 커밋(메뉴별 또는 일괄, 새 브랜치 권장).
4. Gemini 병렬 편집분과 충돌 확인.
