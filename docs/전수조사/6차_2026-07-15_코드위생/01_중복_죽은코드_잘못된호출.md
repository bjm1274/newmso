# 6차 전수조사 — 코드 위생: 중복 · 죽은 코드 · 잘못된 호출

| 항목 | 내용 |
|------|------|
| **조사일** | 2026-07-15 |
| **범위** | `app/main/**` (기능부품·모바일), `lib/**`, `app/api/**` |
| **제외** | `node_modules`, `.next`, `handoff`, `scratch`, `backups`, 일반 `docs` (본 보고서 제외) |
| **방법** | grep + read_file 정적 전수 (READ-ONLY). 런타임 미실행 |
| **성격** | 코드 위생 조사 문서. 수정 없이 이슈 목록·통합 권장만 기록 |
| **이슈 수** | **42건** (C 9 / M 22 / m 11). 동일 패턴 묶음 포함 |

---

## 0. 한 줄 결론

| 축 | 판단 |
|----|------|
| **이중 데이터 경로** | `attendance`↔`attendances`, `messages`↔스키마 `chat_messages` 잔존이 **운영 불일치의 핵심** |
| **중복 모듈** | `chat-write-service` = message-write+push-enqueue+rooms-client 합본 잔재. inventory 4분할 중 3개가 사실상 죽은 복제 |
| **staffId 혼용** | 모바일 결재·알림탭은 `useResolvedStaffId` 정착, **알림설정·수술상담·게시판 쓰기·복지** 등은 raw `user.id` 잔존 |
| **권한** | `canAccess*` 계열은 **UI/셸에만 존재**. `app/api/**` 에서 import 0 → 서버 경로가 access-control 우회 구조 |
| **알림 읽음** | 인앱 UI는 `PUT /api/notifications`, SW/큐는 `/api/notifications/mark-read` — **이중 엔드포인트** |

---

## 1. Critical (C)

### C-01. attendance vs attendances 이중 테이블 (스키마·쓰기·읽기 전 계층)

| | |
|--|--|
| **심각도** | **C** |
| **위치** | `lib/db/schema.ts` L128–187 (`attendance` / `attendances`); 쓰기·읽기 다수 |
| **증상** | 단수 `attendance`(date, check_in/out, 한글 status)와 복수 `attendances`(work_date, check_in_time/out, 영문 status)가 공존. 화면마다 한쪽만 보면 KPI·근태이상·급여·체크인이 어긋남 |
| **핫스팟** | · 체크인 정본: `기능부품/마이페이지/출퇴근기록/index.tsx` (~L375+) → `attendance` + `checkin-utils.syncToAttendances` (~L281) 로 복수 동기화<br>· 모바일 체크인: `모바일/내정보/출퇴근체크인.tsx` (~L97, L309) → **단수만** 쓰는 경로 존재<br>· 출결정정 이중 upsert: `전자결재서브/출결정정양식.tsx` L423–431<br>· 근태이상 이중 update: `모바일/인사관리/data-hooks.ts` L870–886<br>· 근무현황 폴백: `기능부품/근무현황.tsx` ~L269–280<br>· 관리 화면 다수: AttendWorkcenter*, 지각조퇴, 급여정산 → `attendances` |
| **통합 권장** | ① `attendances`를 SSOT로 확정 ② 체크인·정정·이상탐지 전부 dual-write 헬퍼 1곳 ③ `attendance` 읽기 제거 후 마이그레이션·뷰 또는 삭제 |

### C-02. 출결정정/근태 조회 시 raw `user.id` + 이중 테이블

| | |
|--|--|
| **심각도** | **C** |
| **위치** | `기능부품/전자결재서브/출결정정양식.tsx` L129–136, L378, L434 |
| **증상** | 조회·upsert 전부 `user.id` 직접 사용. auth id ≠ staff_members.id 인 계정은 타인/빈 근태·정정 기록 가능. 모바일 대응폼(`모바일/결재/출결정정폼.tsx`)은 `useResolvedStaffId` 사용 → **PC/모바일 불일치** |
| **통합 권장** | PC 폼도 `resolveStaffLike`/`useResolvedStaffId`로 통일. staffId 해석 후 dual-write 헬퍼 호출 |

### C-03. `chat_messages` 스키마 잔존 vs 앱 전역 `messages` 사용

| | |
|--|--|
| **심각도** | **C** (스키마·정책 혼선 / 잘못된 테이블 재도입 위험) |
| **위치** | `lib/db/schema.ts` L310–316 `chat_messages`; L1172+ `messages`; `lib/db/auth/policies.ts` 에 `chat_messages` 포함 |
| **증상** | 앱 코드·쓰기 헬퍼는 전부 `messages`. 주석으로 모바일 구경로 `chat_messages` 제거 이력 명시 (`모바일/채팅/메시지액션.ts` 헤더, `채팅방.tsx` ~L759). 스키마·RLS 목록에 구테이블이 남아 신규 개발이 잘못된 테이블을 다시 붙일 위험 |
| **통합 권장** | 데이터 잔존 여부 확인 후 `chat_messages` 드롭 또는 archive. policies/schema에서 제거. 문서에 SSOT=`messages` 고정 |

### C-04. `app/api/**` 에서 `canAccess*` / `hasPermission` (access-control) 미사용

| | |
|--|--|
| **심각도** | **C** |
| **위치** | `lib/access-control.ts` (canAccessMainMenu/Hr/Inventory/Admin/Board/Approval 등); `app/api/**` import **0건** |
| **증상** | 메뉴·섹션 세분 권한은 클라이언트 UI 게이트 중심. API는 `isAdminSession`/`readSessionFromRequest` 등 조악 체크 또는 로그인-only. 모바일 세부 권한 미배선(5차 보고)과 결합 시 **서버 우회 mutate** 가능 |
| **참고 핫스팟** | `api/work-shifts/route.ts` L40 — 로컬 `hasPermission` 재정의; `api/inventory/stock-*` 로그인 세션 위주(5차 C); `api/d1/mutate` 범용 쓰기 |
| **통합 권장** | 민감 mutate 라우트에 `canAccessHrSection` 등 서버 포팅 또는 claims 기반 동일 키 검사. 클라 게이트는 UX용으로만 취급 |

### C-05. 알림 읽음 이중 경로 (인앱 PUT vs SW mark-read)

| | |
|--|--|
| **심각도** | **C** (읽음 상태·뱃지·재푸시 불일치) |
| **위치** | · 인앱: `알림시스템/notification-api.ts` L74–96 → `PUT /api/notifications`<br>· SW/큐: `public/push-notification-shared.js` ~L262–274 → `POST /api/notifications/mark-read`<br>· 라우트: `api/notifications/mark-read/route.ts`, `api/notifications/route.ts` PUT |
| **증상** | 같은 의미를 두 API가 처리. 한쪽만 성공하면 인앱 목록·푸시 큐·재발송(cron) 상태가 갈라짐. 알림센터/인박스/모바일 알림탭은 모두 notification-api 경로 |
| **통합 권장** | mark-read 라우트를 PUT 핸들러로 위임(또는 반대). 클라이언트·SW 단일 엔드포인트. e2e(`push-notification-shared.desktop.spec.ts`)도 동시 수정 |

### C-06. inventory 모듈 4분할 중 3개 사실상 죽은 복제

| | |
|--|--|
| **심각도** | **C** (유지보수 시 잘못된 파일 수정 위험) |
| **위치** | LIVE: `app/main/inventory-utils.ts` (훅·결재·서버 import)<br>DEAD: `inventory-db-services.ts`, `inventory-workflow.ts`, `inventory-validation.ts` — **외부 import 0** (서로만 참조) |
| **증상** | `processInventoryIssue`/`fetchSupportInventoryRows`/`summarizeSupplyRequestWorkflow` 등이 utils에 실사용 구현, db-services·workflow에 **동일 시그니처 복제본**. 수정 시 한쪽만 고치면 배포 반영 안 됨 |
| **소비처(정본)** | `hooks/useSupplyWorkflow.ts`, `lib/server-approval-processing.ts`, 재고·결재 서브 |
| **통합 권장** | db-services/workflow/validation 삭제 또는 utils re-export 래퍼로 축소. 신규 코드는 `@/app/main/inventory-utils` + `@/lib/inventory-stock-client` 만 |

### C-07. PC 마이페이지 체크인 → attendances `early_leave_minutes` 스키마 불일치

| | |
|--|--|
| **심각도** | **C** (조퇴 분 유실 / 차감 계산 오류) |
| **위치** | `마이페이지/출퇴근기록/checkin-utils.ts` L305–316; `lib/db/schema.ts` attendances L169–182 (**컬럼 없음**); `lib/attendance-deduction.ts` L18, L115; `attendance-anomalies.ts` L40, L162 |
| **증상** | upsert 시 `early_leave_minutes` 시도 후 missing-column fallback. 스키마에 컬럼이 없으면 **항상 폴백** → 조퇴 분 미저장. 차감·이상탐지는 해당 필드를 읽음 |
| **통합 권장** | 마이그레이션으로 컬럼 추가 **또는** 차감 로직을 status/`notes`/별도 테이블로 이전. 스키마·코드·테스트 3자 정합 |

### C-08. `lib/auto-report-generator.ts` company 필터 컬럼 오용 가능

| | |
|--|--|
| **심각도** | **C** |
| **위치** | `lib/auto-report-generator.ts` L62–71: `eq(staffMembersTable.company, companyId)` |
| **증상** | 파라미터명은 `companyId`인데 비교 대상이 `staff_members.company`(회사 **이름** 필드). UUID company_id를 넘기면 직원 0명 집계 → 월간 HR 리포트 공허 |
| **통합 권장** | `company_id` 컬럼 비교 또는 이름 정규화 후 비교. 호출부 인자 의미 문서화 |

### C-09. 모바일 다수 쓰기 경로 raw `user.id` (staff 불일치 핫스팟)

| | |
|--|--|
| **심각도** | **C** (데이터 소유자 오류) |
| **위치(대표)** | · `모바일/내정보/알림설정.tsx` L43 — push 설정 키에 raw id<br>· `모바일/추가기능/수술상담.tsx` L171 — `staff_id: user.id`<br>· `모바일/게시판/data-hooks.ts` L407, L605; `글작성.tsx` L181 — author_id<br>· `모바일/인사관리/복지.tsx` L328<br>· `모바일/추가기능/인계노트.tsx` L285; `직원평가.tsx` L264; `OP체크상세.tsx` L139+<br>· `모바일/채팅/새대화.tsx` L33; `채팅/index.tsx` L56–57 폴백 |
| **증상** | 결재·내정보 홈 등은 `useResolvedStaffId` 적용 완료. 위 경로는 미적용 → 알림·작성자·상담 이력이 staff 키와 불일치 |
| **통합 권장** | 모바일 공통 `useResolvedStaffId` 필수 훅. raw `user.id` 쓰기 grep 금지를 린트/CI에 추가 |

---

## 2. Major (M) — 중복 구현

### M-01. `lib/chat-write-service.ts` = 3모듈 합본 잔재

| | |
|--|--|
| **심각도** | **M** |
| **위치** | `lib/chat-write-service.ts` 전체 (~160줄) vs `chat-message-write.ts`, `chat-push-enqueue.ts`, `chat-rooms-client.ts`, `chat-upload-constants.ts` |
| **증상** | 동일 `insertChatMessageWithFallback` / `enqueueChatPushJob` / `createOrUpsertChatRoom` / `patchChatRoom` / 용량 상수 복제. **실 import는 오프보딩의 `patchChatRoom` 1곳만** (`인사관리서브/오프보딩.tsx`). 나머지는 분리 모듈 사용 |
| **통합 권장** | 오프보딩을 `chat-rooms-client`로 전환 후 `chat-write-service.ts` 삭제 |

### M-02. 날짜 포맷 유틸 다중화 (lib + 화면 로컬)

| | |
|--|--|
| **심각도** | **M** |
| **위치** | · 정본 후보: `lib/seoul-time.ts` (`formatKoreanDateKey`, `getKoreanTodayString`)<br>· `lib/date-utils.ts` (`formatDateDisplay` **import 0 = 죽은 export**, `toDateKey`, `getMonthBoundaries`)<br>· `lib/date-formatter.ts` (`formatDateLabel` ≈ formatDateDisplay, `formatWon`)<br>· `lib/annual-leave-promotion.ts` L211 로컬 `formatDateKey` (KST 아님)<br>· 화면: `게시판서브/guide-utils.ts` formatDate; `OP체크/schedule-helpers.ts` formatDateLabel; `모바일/결재/format-utils` formatTs; `모바일/채팅/data-hooks` formatChat*; `마이페이지/연차휴가내역` 로컬; `certificate-print-utils` 로컬; `시스템마스터서브` formatDateTime; `모바일/인사관리/data-hooks` formatDateShort 등 **10+** |
| **증상** | UTC/로컬/KST 혼용 → “어제/오늘” 경계 버그 (채팅은 KST 주석으로 이미 사고 이력) |
| **통합 권장** | 표시: `date-formatter` + seoul-time. 키: 오직 `formatKoreanDateKey`/`getKoreanTodayString`. 로컬 복제 삭제 |

### M-03. 모바일 결재 `formatTs` 4중 복제

| | |
|--|--|
| **심각도** | **M** |
| **위치** | `모바일/결재/format-utils.ts` L17–39 (정본); `결재함-cards.tsx` L26; `기안함.tsx` L45; `참조함.tsx` L20 — **동일 로직** |
| **통합 권장** | cards/기안/참조는 `format-utils` import. 로컬 함수 삭제 |

### M-04. 채팅 타임스탬프 PC·모바일 규칙 불일치

| | |
|--|--|
| **심각도** | **M** |
| **위치** | 모바일: `모바일/채팅/data-hooks.ts` L900–939 (KST 명시); PC: `메신저사이드바.tsx` L95–108 `formatRoomTime` (sameDay를 **로컬 Date 필드**로 비교) |
| **증상** | PC 사이드바 “오늘” 판정이 디바이스 TZ 의존 → UTC 서버 시각과 어긋날 수 있음 |
| **통합 권장** | 공통 `formatChatTimestamp`를 lib 또는 메신저유틸로 승격, PC/모바일 공유 |

### M-05. Pull-to-refresh 이중 구현

| | |
|--|--|
| **심각도** | **M** |
| **위치** | · `app/components/PullToRefresh.tsx` — **import 0 (죽은 컴포넌트)**<br>· LIVE: `모바일/공통/usePullToRefresh.ts` + `PullRefreshIndicator.tsx` (게시판목록·채팅목록·근태이상) |
| **통합 권장** | `app/components/PullToRefresh.tsx` 삭제 또는 모바일 훅 래퍼로 통합 |

### M-06. EmojiPicker PC/모바일 UI 이중 (데이터는 공유됨)

| | |
|--|--|
| **심각도** | **M** (개선 여지) |
| **위치** | 데이터 공유 OK: `메신저액션서브/emoji-data.ts`, `emoticon-engine.ts`<br>UI 이중: `EmojiPicker.tsx` vs `모바일/채팅/이모지피커.tsx` |
| **증상** | 4차 jscpd ~249줄 중복. 데이터 추출은 완료, UI 동작/접근성 드리프트 위험 |
| **통합 권장** | 공유 프레젠테이션 훅 + 셸만 PC/모바일 분리 |

### M-07. formatPermissionValue 이중

| | |
|--|--|
| **심각도** | **M** |
| **위치** | `관리자전용서브/직원권한서브.tsx` L88; `직원권한통합/permission-review.ts` L4 — 동일 export |
| **통합 권장** | permission-review만 유지, 서브는 re-export |

### M-08. 금액 포맷 분산

| | |
|--|--|
| **심각도** | **M** |
| **위치** | `lib/date-formatter.formatWon`; `모바일/인사관리/data-hooks.formatMoney` L592; 재고 data-hooks/use-analyze-extra 로컬; 급여 모바일명세서 로컬; 결재 format-utils readAmount |
| **통합 권장** | `formatWon`/`formatMoney` 단일 모듈 (`lib/money-format.ts` 권장 — 날짜 파일에 금액이 있는 것도 정리) |

### M-09. 알림 센터 · 인박스 · 알림시스템 읽음/목록 로직 중복

| | |
|--|--|
| **심각도** | **M** |
| **위치** | `알림센터.tsx` markAsRead L216; `알림인박스.tsx` L946+; `알림시스템.tsx` 폴링·구독; 모바일 `알림탭.tsx` L139–151 — 모두 notification-api 호출하지만 **상태 관리·이벤트 emit 패턴 3벌** |
| **증상** | `emitNotificationReadEvent` vs 인박스 로컬 `emitNotificationReadSync`; `NOTIFICATION_LIST_UPDATED_EVENT` 상수 이중 정의 (아래 m) |
| **통합 권장** | `useNotificationsInbox` 단일 훅으로 목록/읽음/삭제/이벤트 흡수 |

### M-10. 채팅 푸시 트리거 PC/모바일 복제

| | |
|--|--|
| **심각도** | **M** |
| **위치** | `기능부품/메신저.tsx` ~L1128–1151; `모바일/채팅/푸시트리거.ts` L18–37 — 동일 chat-push + flush |
| **통합 권장** | `lib` 또는 메신저 공용 `triggerChatPushFlush(messageId)` |

### M-11. 결재 업로드 이중 API

| | |
|--|--|
| **심각도** | **M** |
| **위치** | `api/approval/upload` (presigned); `api/approvals/upload` (직접 R2); 오프라인 큐는 **단수** 경로 (`offline-upload-queue.ts` L161); UI 다수는 **복수** (`approvals/upload`) |
| **증상** | 두 계약(presigned vs multipart) 혼재. 오프라인 재시도가 온라인 경로와 불일치할 수 있음 |
| **통합 권장** | 단일 업로드 계약 확정 후 라우트 1개 deprecate |

### M-12. `company` vs `company_id` 필터 혼용

| | |
|--|--|
| **심각도** | **M** |
| **위치** | `lib/company.ts` getCompanyFilterIds; 게시판 `eq('company', company)`; 인사 모바일 data-hooks L364; inventory `eq('company', …)`; staff select에 `company` 이름 필드 다수 |
| **증상** | 회사 전환(MSO) 시 이름/ID 혼동으로 과다·과소 조회 |
| **통합 권장** | 쿼리 헬퍼 `applyCompanyScope(q, scope)` 단일화. 컬럼 SSOT 문서화 |

### M-13. `lib/dashboard-widgets.ts` vs `lib/data/dashboard-widgets.ts` 이름 충돌

| | |
|--|--|
| **심각도** | **M** (혼동) |
| **위치** | 타입/레이아웃: `lib/dashboard-widgets.ts`; 데이터 fetch: `lib/data/dashboard-widgets.ts` |
| **증상** | 둘 다 “dashboard-widgets”. import 경로 실수 유도 |
| **통합 권장** | `dashboard-widget-types.ts` / `dashboard-widget-queries.ts` 로 리네임 |

### M-14. PC 근무현황·메신저 등 attendance 폴백 패턴 복제

| | |
|--|--|
| **심각도** | **M** |
| **위치** | `근무현황.tsx`, `메신저.tsx` ~L3029, `조직도서브/OrgChart.tsx` ~L805, `staff-shift-resolver.ts` L217–230, `모바일/추가기능/data-hooks.ts` L311–316 |
| **증상** | “단수 실패 시 복수” 또는 병행 조회 복붙 |
| **통합 권장** | `lib/attendance-read.ts` (`fetchAttendanceRange(staffIds, from, to)`) 한 곳에서 dual-read 흡수 |

### M-15. leave days 계산은 ledger 정본이나 폴백 산식 잔존

| | |
|--|--|
| **심각도** | **M** |
| **위치** | 정본: `lib/annual-leave-ledger.calculateLeaveDays`; 소비: LeaveWorkcenter, 연차원장, 모바일 연차관리자<br>폴백: `lib/server-approval-processing.ts` ~L336 `Math.ceil((end-start)/day)+1` |
| **증상** | 반차·주말 정책이 ledger와 서버 final 처리에서 달라질 수 있음 |
| **통합 권장** | 서버도 `calculateLeaveDays`/`getLeaveDaysForRow`만 사용 |

---

## 3. Major (M) — 죽은 코드 · 미사용 경로

### M-16. `lib/date-utils.formatDateDisplay` export 사용 0

| | |
|--|--|
| **심각도** | **M** (죽은 export) |
| **위치** | `lib/date-utils.ts` L22–27 |
| **통합 권장** | 삭제 또는 `formatDateLabel` re-export로 통합 후 호출부 이전 |

### M-17. `lib/use-local-date-key.ts` 의 `useLocalDateKey` 훅 미사용

| | |
|--|--|
| **심각도** | **M** |
| **위치** | `lib/use-local-date-key.ts` L12; 사용은 `formatLocalDateKey`만 (`출퇴근기록/index.tsx`) |
| **통합 권장** | 훅 삭제 또는 체크인 화면에 적용 |

### M-18. `lib/offline-queue-db.ts` import 0

| | |
|--|--|
| **심각도** | **M** |
| **위치** | `lib/offline-queue-db.ts` — 파일 헤더만 자기 참조. 실제 엔트리는 `offline-queue.ts` + storage |
| **통합 권장** | 파일 삭제 또는 공식 re-export 문서로 명시 |

### M-19. `app/api/check-notices/` 빈 라우트 디렉터리

| | |
|--|--|
| **심각도** | **M** |
| **위치** | `app/api/check-notices/` — route.ts 없음; 코드베이스 fetch 0 |
| **통합 권장** | 디렉터리 삭제 |

### M-20. 빈 폴더: `기능부품/근태기록/`, `기능부품/알림센터/`

| | |
|--|--|
| **심각도** | **M** (구조 혼동) |
| **위치** | `app/main/기능부품/근태기록/` 비어 있음; `알림센터/` 비어 있음 (실제 구현은 `알림센터.tsx` 파일) |
| **통합 권장** | 빈 폴더 제거 |

### M-21. chat-write-service 내 중복 insert/enqueue (M-01 상세)

| | |
|--|--|
| **심각도** | **M** |
| **위치** | 상기 M-01. `insertChatMessageWithFallback` 호출 전부 `chat-message-write` 쪽 |
| **통합 권장** | M-01과 동일 |

---

## 4. Major (M) — 잘못된 호출 · 시그니처 · 우회

### M-22. API 로컬 `hasPermission` vs access-control 불일치

| | |
|--|--|
| **심각도** | **M** |
| **위치** | `api/work-shifts/route.ts` L40–; `bulk-deactivate/route.ts` L27 — 로컬 함수, `role === 'admin'` 위주 |
| **증상** | UI의 `canAccessHrSection('근무형태')` 등과 불일치 가능 |
| **통합 권장** | access-control 또는 feature-permissions 서버 공유 |

### M-23. 모바일 관리자/추가기능 세부 canAccess 미배선 (패턴)

| | |
|--|--|
| **심각도** | **M** (5차 교차, 위생 관점 재확인) |
| **위치** | 모바일 허브 일부만 `canAccessMainMenu` (`추가기능/허브.tsx`, 공유캘린더). 인사 hr_*, admin_* 세부는 서버·UI 모두 약함 |
| **통합 권장** | 모바일 라우트 가드 테이블 + API 동일 키 |

### M-24. 모바일 알림설정 push staffId = session.user.id 서버 경로

| | |
|--|--|
| **심각도** | **M** |
| **위치** | `api/notifications/push-subscription/route.ts` L75; `push-self-test/route.ts` L35 — `String(session.user.id)` |
| **증상** | 구독 테이블 staff_id가 해석된 staff id와 다르면 푸시 미수신 |
| **통합 권장** | 서버에서도 staff resolve (claims / staff_members lookup) |

### M-25. `formatDateKey` in annual-leave-promotion (로컬 날짜)

| | |
|--|--|
| **심각도** | **M** |
| **위치** | `lib/annual-leave-promotion.ts` L211–213; 호출 L181+ |
| **증상** | `formatKoreanDateKey` 대신 로컬 Y-M-D → KST 자정 경계에서 촉진 일자 오판 가능. promotion-dispatch는 seoul-time 사용 |
| **통합 권장** | `formatKoreanDateKey`로 교체 |

### M-26. 메신저·GlobalSearch 등 messages 직접 from 난립

| | |
|--|--|
| **심각도** | **M** |
| **위치** | 메신저방데이터훅 다수, GlobalSearch, OP체크, useTodoWorkflow — `db.from('messages')` 직접 |
| **증상** | write는 insertChatMessageWithFallback로 통일됐으나 **read/update/delete 컬럼 선택·soft-delete 규칙** 분산 |
| **통합 권장** | chat-query-service 읽기/삭제 API 단일화 |

### M-27. attendance status 어휘 이중 (한글 vs 영문)

| | |
|--|--|
| **심각도** | **M** |
| **위치** | `attendance.status` 기본 `"정상"`; `attendances.status` 기본 `"present"`; 출결정정 statusMap L416–419 |
| **증상** | 한쪽만 갱신 시 필터·이상탐지 miss. 모바일 abnormal은 양쪽 맞춤 시도 |
| **통합 권장** | SSOT 테이블 하나로 합칠 때 어휘 단일 enum |

### M-28. `staffMembersTable.company` (이름) 기반 서버 잡 다수

| | |
|--|--|
| **심각도** | **M** |
| **위치** | birthday, leave-notice-cron, contract-expiry, inapp check-inventory, server-approval inventory 지원부서 등 |
| **증상** | 회사 표기 변경·공백 차이면 잡 누락. C-08과 동일 계열 |
| **통합 권장** | company_id 기준 조인 + 표시명 분리 |

---

## 5. Minor (m)

### m-01. `NOTIFICATION_LIST_UPDATED_EVENT` 상수 이중 정의

| | |
|--|--|
| **심각도** | **m** |
| **위치** | `알림시스템.tsx` L68; `알림시스템/notification-api.ts` L3 — 문자열 동일 `'erp-notification-list-updated'` |
| **통합 권장** | notification-api만 export, 알림시스템은 re-export |

### m-02. `app/components` 일부 죽은 심볼 (tmp_orphan 교차)

| | |
|--|--|
| **심각도** | **m** (재확인 필요 — 동적 import 가능) |
| **위치 후보** | `tmp_orphan/dead_exports_v2.tsv` 기준: AppLogo, BottomTab, DataTable, GlobalNotificationBell, SignaturePad 컴포넌트 파일, MatrixTable, SwipeableCard 등. **현 트리에 파일이 없는 항목도 포함**되어 스냅샷 시차 있음 |
| **통합 권장** | 최신 기준 `ts-prune`/`knip` 재실행 후 삭제 배치 |

### m-03. 모바일 결재 `pickAvatarTone` 이중

| | |
|--|--|
| **심각도** | **m** |
| **위치** | `format-utils.ts` L10; `결재함-cards.tsx` 동일 패턴 |
| **통합 권장** | format-utils로 통합 |

### m-04. `readAmount` 결재 카드 복제

| | |
|--|--|
| **심각도** | **m** |
| **위치** | format-utils L49; 결재함-cards / 기안함 로컬 유사 |
| **통합 권장** | format-utils 단일 |

### m-05. lucide-shim 사용처 1곳

| | |
|--|--|
| **심각도** | **m** |
| **위치** | `기능부품/lucide-shim.tsx`; 소비 `메신저첨부.tsx` only |
| **통합 권장** | lucide-react 직접 import로 교체 후 shim 삭제 검토 |

### m-06. 증명서 인쇄 유틸 경로 공유는 양호, 문서 인쇄 이중 코멘트

| | |
|--|--|
| **심각도** | **m** |
| **위치** | `certificate-print-utils` 공유 중; `ArchivedDocumentView`에 “동일 패턴” 주석 — 완전 공유 여부 재점검 |
| **통합 권장** | print-utils 완전 단일 모듈 |

### m-07. `useCompany` 이원화

| | |
|--|--|
| **심각도** | **m** |
| **위치** | `lib/useCompany.ts` (persist selected id); `contexts/CompanyContext.tsx` (React state) |
| **증상** | 이름 유사, 역할 다름 — 온보딩 시 혼동 |
| **통합 권장** | `selectedCompanyStorage` / `CompanyContext` 네이밍 정리 |

### m-08. OP체크 formatDateLabel 로컬

| | |
|--|--|
| **심각도** | **m** |
| **위치** | `OP체크/schedule-helpers.ts` L152 vs lib date-formatter |
| **통합 권장** | lib 사용 또는 명확한 도메인 포맷 이름 |

### m-09. 게시판 formatShortDate PC/모바일 이중

| | |
|--|--|
| **심각도** | **m** |
| **위치** | `게시판서브/post-table-helpers.ts` L69; `모바일/게시판/data-hooks.ts` L698 |
| **통합 권장** | 게시판공통 모듈로 승격 |

### m-10. 메신저포매팅 markdown 파서 PC 전용

| | |
|--|--|
| **심각도** | **m** |
| **위치** | `메신저포매팅.ts` → PC 메시지렌더만. 모바일 버블 마크다운 지원 여부 불일치 가능 |
| **통합 권장** | 모바일 버블에도 동일 파서 적용 또는 의도적 차이 문서화 |

### m-11. chat-upload-constants vs chat-write-service 상수 복제

| | |
|--|--|
| **심각도** | **m** |
| **위치** | `chat-upload-constants.ts` LIVE; write-service 상단 복제 (M-01) |
| **통합 권장** | M-01 삭제로 해소 |

---

## 6. 이중 데이터 경로 요약표

| 경로 A | 경로 B | 심각도 | 상태 |
|--------|--------|--------|------|
| `attendance` | `attendances` | **C** | 동시 운영, 컬럼·status 어휘 상이 |
| `messages` | `chat_messages` (schema only) | **C** | 앱은 A만 사용, B 스키마 잔존 |
| `PUT /api/notifications` | `POST .../mark-read` | **C** | 인앱 vs SW |
| `/api/approval/upload` | `/api/approvals/upload` | **M** | presigned vs 직접 업로드 |
| `inventory-utils` | inventory-db-services/workflow | **C** | B 죽은 복제 |
| `chat-message-write` 등 분리 모듈 | `chat-write-service` | **M** | B 거의 미사용 |
| `notification-api` 이벤트 상수 | `알림시스템.tsx` 상수 | **m** | 문자열 동일 이중 정의 |
| `lib/date-utils` | `lib/date-formatter` + seoul-time | **M** | 역할 겹침 |
| PullToRefresh 컴포넌트 | usePullToRefresh 훅 | **M** | A 미사용 |

---

## 7. staffId / useResolvedStaffId 핫스팟 매트릭스

| 영역 | resolved 사용 | raw user.id 잔존 | 우선순위 |
|------|---------------|------------------|----------|
| 모바일 결재 폼·index | ✅ | format-utils history 일부 | 중 |
| 모바일 내정보 홈·연차·증명·알림탭 | ✅ | 알림설정 ❌ | **고** |
| 모바일 채팅방 | ✅ | 목록/새대화 폴백 raw | 중 |
| 모바일 게시판 쓰기 | ❌ | author_id raw | **고** |
| 모바일 추가기능(상담·인계·평가·OP) | ❌ | staff/author raw | **고** |
| PC 전자결재 출결정정 | ❌ | 전면 raw | **고** |
| PC 마이페이지 일부 | ✅ resolved | 연차휴가내역 userIdKey raw | 중 |
| API push-subscription | session.user.id | staff resolve 없음 | **고** |

---

## 8. 권장 통합 로드맵 (우선순위)

| 단계 | 작업 | 효과 |
|------|------|------|
| **P0** | attendance SSOT + dual-write 헬퍼 1곳; 출결정정 PC staff resolve | 근태·급여·체크인 정합 |
| **P0** | 알림 mark-read 엔드포인트 단일화 | 뱃지·재푸시 정합 |
| **P0** | 모바일 쓰기 raw user.id → useResolvedStaffId | 소유자 오류 방지 |
| **P1** | chat-write-service·inventory 죽은 3파일·PullToRefresh·check-notices 삭제 | 위생·오수정 방지 |
| **P1** | formatTs/formatChatTimestamp/seoul-time 통합 | 날짜 경계 버그 |
| **P1** | API에 canAccess* 서버 포팅 (재고·근무표·d1 mutate 민감 테이블) | 권한 우회 차단 |
| **P2** | chat_messages 스키마 제거; company_id 필터 통일; 날짜/금액 포맷 모듈화 | 장기 유지보수 |

---

## 9. 조사 한계

- 동적 `import()` / 문자열 테이블명 조합은 일부 누락 가능.
- `tmp_orphan/dead_exports_v2.tsv` 는 과거 스냅샷(main-v2 등 현 트리 부재 경로 포함) — **재실행 전 삭제 금지**.
- RLS/D1 런타임 권한은 정적 조사 범위 밖 (5차 채팅·재고 C 이슈 교차 참고).
- e2e 테스트 코드의 테이블 mock 경로는 “죽은 API” 판정에서 제외하지 않음(테스트만이 참조하는 경로도 표기).

---

## 10. 이슈 인덱스 (42)

| ID | 심각도 | 유형 | 제목 |
|----|--------|------|------|
| C-01 | C | 이중경로 | attendance vs attendances |
| C-02 | C | 잘못된호출 | 출결정정 PC raw user.id + dual table |
| C-03 | C | 이중경로 | chat_messages 스키마 잔존 |
| C-04 | C | 권한우회 | API canAccess* 미사용 |
| C-05 | C | 이중경로 | 알림 읽음 PUT vs mark-read |
| C-06 | C | 죽은/중복 | inventory 3파일 죽은 복제 |
| C-07 | C | 스키마 | early_leave_minutes 부재 |
| C-08 | C | 잘못된호출 | auto-report company vs companyId |
| C-09 | C | staffId | 모바일 쓰기 raw user.id 핫스팟 |
| M-01 | M | 중복 | chat-write-service 합본 잔재 |
| M-02 | M | 중복 | 날짜 포맷 다중화 |
| M-03 | M | 중복 | formatTs 4중 |
| M-04 | M | 중복 | 채팅 타임스탬프 PC/모바일 |
| M-05 | M | 죽은 | PullToRefresh 컴포넌트 |
| M-06 | M | 중복 | EmojiPicker UI 이중 |
| M-07 | M | 중복 | formatPermissionValue |
| M-08 | M | 중복 | 금액 포맷 |
| M-09 | M | 중복 | 알림 읽음 상태관리 3벌 |
| M-10 | M | 중복 | chat-push 트리거 |
| M-11 | M | 이중경로 | approval upload 2 API |
| M-12 | M | 이중경로 | company vs company_id |
| M-13 | M | 중복 | dashboard-widgets 이름 충돌 |
| M-14 | M | 중복 | attendance 폴백 패턴 |
| M-15 | M | 중복 | leave days 폴백 산식 |
| M-16 | M | 죽은 | formatDateDisplay |
| M-17 | M | 죽은 | useLocalDateKey |
| M-18 | M | 죽은 | offline-queue-db |
| M-19 | M | 죽은 | api/check-notices 빈 디렉터리 |
| M-20 | M | 죽은 | 빈 폴더 근태기록·알림센터 |
| M-21 | M | 중복 | write-write insert 복제 (M-01) |
| M-22 | M | 권한 | work-shifts 로컬 hasPermission |
| M-23 | M | 권한 | 모바일 세부 canAccess 미배선 |
| M-24 | M | staffId | push API session.user.id |
| M-25 | M | 잘못된호출 | promotion formatDateKey 로컬 |
| M-26 | M | 잘못된호출 | messages 직접 접근 난립 |
| M-27 | M | 이중경로 | status 한글/영문 |
| M-28 | M | 잘못된호출 | 서버 잡 company 이름 필터 |
| m-01 | m | 중복 | NOTIFICATION 이벤트 상수 |
| m-02 | m | 죽은 | components dead exports 후보 |
| m-03 | m | 중복 | pickAvatarTone |
| m-04 | m | 중복 | readAmount |
| m-05 | m | 죽은 | lucide-shim 단독 |
| m-06 | m | 중복 | 증명서/문서 인쇄 |
| m-07 | m | 혼동 | useCompany 이원 |
| m-08 | m | 중복 | OP formatDateLabel |
| m-09 | m | 중복 | 게시판 formatShortDate |
| m-10 | m | 패리티 | 메신저 마크다운 PC only |
| m-11 | m | 중복 | chat size constants |

> 표 행 수 47(인덱스) / 본문 상세 이슈 **42+** (M-21은 M-01 하위 표기). 중복 묶음 허용 기준 충족.

---

*생성: 2026-07-15 코드 위생 6차 전수조사 (정적 grep/read 전용).*
