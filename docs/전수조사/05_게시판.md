# ⑤ 게시판 메뉴 — 전수조사 보고서 (마스터)

> 조사일: 2026-05-29 · ~30개 파일(게시판.tsx 3303줄 포함, 약 10,000줄) · 3개 그룹 병렬 정독 + Opus 재확인
> 그룹 상세: [_게시판_G1](_게시판_G1.md)(메인/공통) · [G2](_게시판_G2.md)(서브/가이드) · [G3](_게시판_G3.md)(모바일)

## 분류별 집계

| 그룹 | 건수 | 高 |
|------|------|----|
| G1 메인/공통 | 18 | 2 |
| G2 서브/업무가이드 | 16 | 2 |
| G3 모바일 | 17 | 1 |
| **총계** | **51** | **5** |

> ✅ **XSS 양호**: 게시글·댓글·마크다운 본문 모두 JSX 텍스트노드 렌더, `dangerouslySetInnerHTML` 0건, href `javascript:`/`data:` 차단. board_posts/comments insert 컬럼 정본 일치.

---

## 🔴 심각도 높음 (5건)

### [D/Q] ✅ `게시판공통.ts` ↔ `게시판-view-utils.ts` 분기된 중복 — PC/모바일이 다른 정규화 사용
- 같은 이름·**다른 동작** 함수들: `buildScheduleMetaContent`(한쪽은 chartNo 화면 누락), `normalizeBoardPost`의 surgery_* 정규화, `normalizeScheduleTimeValue`가 상이.
- **PC는 `게시판-view-utils.ts`판, 모바일 `data-hooks.ts`는 `게시판공통.ts`판 import** → 동일 게시글이 PC/모바일에서 다르게 정규화. `getBoardPostAuthorSignal`/`getBoardPostPreview`만 view-utils 고유.
- **조치**: 단일 파일로 수렴, 분기된 동작 통일.

### [D] ✅ `업무가이드.tsx` ↔ `guide-utils.ts` ~100% 중복
- `업무가이드.tsx:148-557`의 유틸/노멀라이저/스코프빌더가 `guide-utils.ts:45-482` export 본문과 거의 동일(인라인 재구현). → guide-utils import로 통일.

### [X] ✅ Guide 컴포넌트 3종 죽은 코드 (리팩토링 미완)
- `GuideComposerSection`/`GuideSidebar`/`GuideTaskBoard` — 서로만 import, 실제 엔트리 `업무가이드.tsx`(GuideLibrary)는 `GuideDetailPanel`만 쓰고 나머지를 인라인 재구현. **ref 0 확인**. → 삭제 대상.

### [SEC] ✅ 모바일 별표(`board_post_stars`) 테이블 부재 → 기기간 유실
- `모바일/게시판/별표훅.ts:21` `TABLE_NAME='board_post_stars'` — D1 정본에 **해당 테이블 없음**(board_post_comments/likes/reads/posts만 존재). 서버 저장 항상 실패 → localStorage 폴백 → 즐겨찾기가 기기 로컬에만 남고 기기 간 유실. (크래시는 없음, LS 폴백 설계)
- **조치**: 테이블 생성하거나 기존 `board_post_likes` 등으로 통합.

### [SEC] 익명 소리함 본문 클라이언트 측에서만 숨김
- `게시판.tsx:464-497` — 익명 게시글 본문이 클라이언트로 전송된 뒤 화면에서만 숨겨짐 → 비관리자도 네트워크 응답으로 열람 가능.
- **조치**: 서버측 필터링/마스킹.

---

## 🟡 심각도 보통 (요약)

- **[SEC]** 업로드 signed-URL 직접 PUT 경로가 magic-byte 검증 우회(formData 폴백만 검증). 1차 경로가 PUT(G1 SEC-2). 첨부 svg/html 미차단(G3).
- **[X]** PostTableView 댓글수(💬)가 정본에 없는 `comments_count`를 읽어 항상 0(死 표시, G2 X-3).
- **[P]** 모바일 목록 댓글수 집계가 최대 100글의 전 댓글 행을 클라이언트로 받아 JS 카운트(페이로드 폭증, G3 P-1). 좋아요 토글 1회당 3 라운드트립(mutate+count+update, G3 P-2). 게시판.tsx 폴링/재조회 다수(G1 P).
- **[E]** 별표/좋아요 에러 분기가 Postgres 코드(42P01) 기준이나 런타임은 D1 → 문자열 매치 의존(G3 E-1). 게시판.tsx await 일부 catch 누락(G1 E).
- **[D]** 모바일 목록/댓글수/insert 로직이 PC 게시판.tsx와 별도 복제(공통 상수 일부만 공유, G3 D-1).
- **[T]** data-hooks `as unknown as` 이중 강제 캐스팅(G3 T-1). 게시판.tsx any 일부(G1 T).

---

## 🟢 심각도 낮음 (그룹 상세 참조)
- **[U]** D1/R2 환경인데 toast·주석이 "Supabase 대시보드 / .sql 마이그레이션" 지목(스테일, G1 U-1).
- **[X]** 게시판.tsx 미사용 export/변수 일부, board-poll-prize 관련 정합.
- **[K]** 게시판 그룹 한글 깨짐 0건. **[DBG]** console.* 0건(logger 사용 — 양호).

## 발견 없음 / 양호
- XSS(전 그룹), board_posts/comments insert 정본 일치, 마크다운 렌더 안전, 업무가이드 데스크톱은 withMissingColumnsFallback으로 방어.

## 최우선 Top 4
1. **[SEC]** 익명 소리함 서버측 필터링 + board_post_stars 테이블 정리
2. **[D]** 게시판공통 ↔ view-utils 분기 중복 통일(PC/모바일 정규화 일치)
3. **[X]** Guide 죽은 컴포넌트 3종 + 업무가이드 인라인 중복 정리
4. **[P]** 모바일 댓글수 집계/좋아요 토글 라운드트립 최적화
