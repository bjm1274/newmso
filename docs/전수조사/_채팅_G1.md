# 채팅(메신저) — 메인 컨테이너/레이아웃/패널 그룹 전수조사 (G1)

작업 디렉터리: D:\newmso · 조사일: 2026-05-29 · 정본: lib/db/schema.ts

## 파일별 1줄 역할

| 파일 | 줄 | 역할 | 상태 |
|---|---|---|---|
| `app/main/기능부품/메신저.tsx` | 3839 | **실사용** 데스크톱 채팅 메인 컨테이너. `조직도본문.tsx`가 `dynamic(() => import('../메신저'))`로 로드. 모든 state/effect/훅 조합 + 인라인 JSX 렌더. | 사용중 |
| `app/main/기능부품/메신저패널.tsx` | 554 | `MediaArchivePanel`, `GlobalSearchModal` 정의. | **죽은코드(0 import)** |
| `app/main/기능부품/메신저공통.tsx` | 85 | `MessengerAvatar`, `buildMessengerImageAlt`, `MessengerStatusUserRow` 공용 UI. | 사용중(13+ 파일) |
| `app/main/기능부품/메신저타입.ts` | 14 | `PresenceInfo`, `DeliveryState` 타입. | 사용중(7 파일) |
| `app/main/기능부품/MessengerLayout.tsx` | 42 | `MessengerSidebar`+`ConversationPanel`+`ModalHost` 조립 레이아웃. | **죽은코드(0 import)** |
| `app/main/기능부품/MessengerConversationPanel.tsx` | 262 | controller 기반 대화 패널(헤더/타임라인/컴포저/드로어). | **죽은코드(MessengerLayout만 import → 그 Layout도 죽음)** |
| `app/main/기능부품/useChatViewController.ts` | 10 | `ChatView` 재export + `ChatViewController = Record<string, any>` + `useChatViewController(){ return {} }` 스텁. | 부분 죽음 |
| `app/main/기능부품/메신저오버레이.tsx` | 605 | 5개 모달/패널(MessageEdit, EditHistory, PollComposer, SlashCommand, Thread, ReadStatus) 정의. | **죽은코드 확정(0 code import, 아래 X-5)** |

---

## 분류별 발견

### D 중복

**D-1 [심각도: 높음] 메신저패널.tsx 전체가 다른 파일의 중복**
- `메신저패널.tsx:32` `export function MediaArchivePanel` ↔ `메신저미디어아카이브.tsx:18` 동일 컴포넌트
- `메신저패널.tsx:157` `export function GlobalSearchModal` ↔ `메신저전역검색.tsx:60` 동일 컴포넌트
- 근거: 실사용처(`메신저.tsx:64-65`, `MessengerModalHost.tsx:5,7`)는 모두 `메신저미디어아카이브`/`메신저전역검색`에서 import. `grep "from '...메신저패널'"` → 0건.
- 조치: `메신저패널.tsx` 삭제(554줄).

**D-2 [심각도: 높음] 레이아웃 추출 컴포넌트 클러스터가 인라인 JSX와 중복**
- `메신저.tsx:3051-3676`(인라인 `<div data-testid="chat-view">` + Sidebar + `<main>` 헤더/타임라인/컴포저/드로어/모달) 가 실제 렌더링됨.
- `MessengerLayout.tsx:16` / `MessengerConversationPanel.tsx:21` 가 동일 구조(같은 `data-testid="chat-view"`, `chat-room-header-avatar`, `chat-open-drawer` 등)를 controller 기반으로 재구현.
- 근거: 외부에서 `MessengerLayout` import 0건(`MessengerLayout`은 `ConversationPanel`/`ModalHost`만 import). 진입점 `조직도서브/조직도본문.tsx:14` `loadChatView = () => import('../메신저')`.
- 조치: 추출 마이그레이션을 완료(메신저.tsx를 Layout/Panel/ModalHost/controller로 분리)하거나, 미완 추출 3파일 삭제. 현재는 양쪽 다 유지되어 표류(divergence) — 인라인 헤더에는 투표/결재초안/아카이브 버튼(3130-3172)이 있으나 죽은 Panel에는 없음(반대로 죽은 Panel의 뒤로가기는 `<ChevronLeft>` 아이콘, 인라인은 텍스트 "뒤로").

### S 문법/런타임 — 발견 없음 (빌드/런타임 치명 이슈 미발견)

### K 한글깨짐 — 발견 없음
- 근거: `grep "Ã|â€|ï¿½|ê°|ë°|ì„"` → 8파일 전부 0건. 주석·UI 문자열 모두 정상.

### X 죽은코드

**X-1 [심각도: 높음] MessengerLayout.tsx / MessengerConversationPanel.tsx / MessengerModalHost.tsx 클러스터**
- 3파일 모두 `useChatViewController`의 `ChatViewController` 타입에만 의존하며 서로만 import. 진입점 어디서도 `MessengerLayout`을 렌더하지 않음.
- 근거: `grep import.*MessengerLayout` → `MessengerLayout` 자신 외 0건. 데스크톱 진입은 `메신저.tsx` 인라인 JSX.
- 조치: 추출 완료 안 할 거면 3파일(MessengerLayout 42 + ConversationPanel 262 + MessengerModalHost 157 = 461줄) 삭제.

**X-2 [심각도: 중간] useChatViewController() 스텁 훅**
- `useChatViewController.ts:8-10` `export function useChatViewController(): ChatViewController { return {}; }` — 빈 객체 반환, 호출처 0건.
- 근거: `grep useChatViewController(` 호출 0건. 이 파일에서 실사용되는 건 `default ChatView` 재export(`조직도본문` import 경로는 `../메신저` 직접이라 이 재export도 미사용)와 `ChatViewProps` 타입뿐.
- 조치: `useChatViewController()` 함수 + `ChatViewController` 타입 제거(레이아웃 클러스터 삭제와 함께).

**X-5 [심각도: 높음] 메신저오버레이.tsx 전체 죽은코드(중복)**
- `메신저오버레이.tsx`는 MessageEdit/EditHistory/PollComposer/SlashCommand/Thread/ReadStatus 모달을 정의하나, `메신저.tsx`는 동일 기능을 `메신저수정모달`(`:70`), `메신저스레드패널`(`:71`), `메신저투표모달`(`:72`), `메신저읽음모달`(`:73`)에서 import.
- 근거: 전체 코드베이스 `grep 메신저오버레이` → **code import 0건**(docs 문서 2건만 참조). 정의된 6개 export 모두 다른 파일에 중복 존재.
- 조치: 605줄 삭제.

### U 불필요 — 발견 없음(중대)
- `MessengerLayout.tsx:13`, `MessengerConversationPanel.tsx:18`, `MessengerModalHost.tsx:19` 의 `const c = controller as any;` 패턴은 T-2 참조.

### Q 유사기능

**Q-1 [심각도: 중간] 헤더 UI 2종 표류**
- 실사용 인라인 헤더(`메신저.tsx:3081-3185`)와 죽은 `MessengerConversationPanel.tsx:23-76` 헤더가 같은 목적이나 마크업 상이(뒤로가기 텍스트 vs 아이콘, 헤더 액션 버튼 유무). D-2와 동일 뿌리.

### T 타입(any·강제캐스팅)

**T-2 [심각도: 중간] controller as any 패턴**
- `MessengerLayout.tsx:13`, `MessengerConversationPanel.tsx:18`, `MessengerModalHost.tsx:19` 모두 `const c = controller as any;` → `ChatViewController = Record<string, any>`(`useChatViewController.ts:6`)로 인해 채팅 컨트롤러 전역이 타입 미검증. 단 모두 죽은코드라 영향은 삭제로 해소.
- `메신저.tsx` 본문은 `any`/`as any` 거의 없음(전수 grep상 `: any`/`as any` 0건, 매우 양호).

### E 에러처리 — 양호
- `메신저.tsx` 비동기 블록 대부분 try/catch + `logger.error` + 사용자 toast(예: `:2787-2789`, `:1918-1919`). read-cursor 쓰기는 서버 라우트 위임(`:684-705`)으로 D1 직접접근 실패 회피(주석 근거 명시).

### P 성능(중복API·useEffect 의존성·리렌더)

**P-1 [심각도: 중간/의심] 방 목록 로드 effect가 staff 디렉터리 로드 후 재실행**
- `메신저.tsx:1887-1930` `loadRooms`(공지방 upsert + `fetchAllChatRooms({force:true})`)의 dep에 `noticeRoomMemberIds` 포함(`:1930`). `noticeRoomMemberIds`는 `allKnownStaffs`(`:446` 비동기 디렉터리 로드) 변동 시 갱신 → 마운트 직후 staff 도착 시 force refetch 1회 추가 발생.
- 조치: 공지방 멤버 동기화(`syncNoticeRoomMembers`, `:1932`)와 초기 force 로드를 분리하거나, force를 최초 1회로 제한. (의심)

**P-2 [심각도: 낮음] resolveRoomMemberProfile 의존성 누락(스테일 클로저)**
- `메신저.tsx:407-431` `resolveRoomMemberProfile`가 본문에서 `effectiveChatUserId`(`:411`) 참조하나 dep 배열은 `[resolveStaffProfile, user?.id]`(`:430`)뿐 — `effectiveChatUserId` 누락. 또한 `effectiveChatUserId`는 `:498`에서 선언되어 정의 위치상 이후이나 클로저 호출 시점엔 안전.
- 조치: dep에 `effectiveChatUserId` 추가, 또는 정의 순서 정리.

**P-3 [심각도: 낮음] resolveStaffProfile 의존성 불일치**
- `메신저.tsx:375-406` dep에 `user?.avatar_url`(`:405`) 포함하나 본문은 `getProfilePhotoUrl(user)` 사용 — avatar_url 직접 참조 없음(불필요 dep 또는 의도 불명).

### SEC 보안 — 발견 없음
- 근거: `dangerouslySetInnerHTML`/`innerHTML`/`eval` 0건. notifications insert(`:2784`)·update는 `user_id`/`read_at`/`type`/`body`/`metadata` 정본 컬럼만 사용, 사용자 입력은 toast/렌더에서 텍스트로만 처리.

### DBG 디버그(console·TODO)

**DBG-1 [심각도: 낮음] console.error 잔존(로거 미사용)**
- `메신저.tsx:3037` `console.error('Failed to load attendance for staff profile:', e);` — 파일 전반은 `logger.error` 사용하는데 이 1곳만 `console.error`. (TODO/FIXME/debugger 0건)
- 조치: `logger.error`로 통일.

---

## D1 정본 컬럼 검증 (확인)

- `notifications` 사용처 전부 정본 일치: `update({read_at})` `.eq('user_id',…)` `.in('type',[...])` `.is('read_at',null)`(`:934-939`, `:959-964`, `:2528-2533`, `:2554-2559`), insert payload `user_id/type/title/body/read_at/metadata`(`:2765-2782`). staff_id/read/is_read/message 미사용 ✓.
- `messages`: `.select('room_id, sender_id, created_at')`(`:785`) — content 컬럼 기반(`:984` body는 notifications) ✓.
- `attendance`(`:3009` staff_id/date/check_in/check_out/status) / `attendances`(`:3015` staff_id/work_date/check_in_time/check_out_time/status) / `chat_rooms`(`:809,1595,1892`) / `chat_room_prefs`(`:1193`) 모두 정본 일치 ✓.

---

## 집계표

| 분류 | 건수 | 높음 | 중간 | 낮음/의심 |
|---|---|---|---|---|
| D 중복 | 2 | 2 | 0 | 0 |
| S 문법/런타임 | 0 | - | - | - |
| K 한글깨짐 | 0 | - | - | - |
| X 죽은코드 | 4 | 2 | 2 | 0 |
| U 불필요 | 0 | - | - | - |
| Q 유사기능 | 1 | 0 | 1 | 0 |
| T 타입 | 1 | 0 | 1 | 0 |
| E 에러처리 | 0 | - | - | - |
| P 성능 | 3 | 0 | 1 | 2 |
| SEC 보안 | 0 | - | - | - |
| DBG 디버그 | 1 | 0 | 0 | 1 |
| **합계** | **12** | **4** | **5** | **3** |

### 핵심 권고
1. (높음) `메신저패널.tsx`(554줄) + `메신저오버레이.tsx`(605줄) 삭제 — 둘 다 code import 0건, 전부 다른 파일에 중복 존재.
2. (높음) 레이아웃 추출 클러스터(`MessengerLayout`+`ConversationPanel`+`MessengerModalHost`+`useChatViewController` 스텁 = 약 470줄) 삭제 또는 마이그레이션 완료 결정. 현재 인라인 JSX와 표류 중.
3. `메신저.tsx`(3839줄) 단일책임 위반 — 위 추출이 완료되면 자연 해소. 본문 자체의 타입/에러처리/보안 품질은 양호.
4. 죽은코드 합계 약 **1630줄**(메신저패널 554 + 메신저오버레이 605 + Layout클러스터 461) 제거 가능.
