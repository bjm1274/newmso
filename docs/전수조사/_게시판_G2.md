# 게시판 메뉴 — [게시판서브 / 업무가이드] 그룹 전수조사 (G2)

조사일: 2026-05-29 / 작업트리: D:\newmso / 정본 스키마: lib/db/schema.ts (board_posts L277-316, board_post_comments L236-253)

## 파일별 역할 (1줄)

| 파일 | 역할 |
|---|---|
| 업무가이드.tsx | 데스크톱 업무공유(GuideLibrary) 엔트리. board_posts(board_type='업무가이드'/'업무가이드_팀할일') CRUD + 팀별 자료/할일. **유틸·타입을 전부 인라인 재구현** |
| GuideDetailPanel.tsx | 선택 자료 상세 패널(폰트/집중모드, 첨부 카드/이미지 갤러리). 업무가이드.tsx가 유일하게 import하는 서브컴포넌트 |
| GuideComposerSection.tsx | 자료 작성/수정 폼 (프레젠테이션). **어디서도 import 안 됨 → 死** |
| GuideSidebar.tsx | 회사/팀 선택 + 검색/필터 + 자료목록 사이드바. **死** |
| GuideTaskBoard.tsx | 팀별 할일 보드 (프레젠테이션). **死** |
| guide-types.ts | Guide* 타입 정의(확장본: mobileSummary, linkedTaskTemplates 등). GuideDetailPanel/Sidebar/Composer/TaskBoard/utils만 참조 |
| guide-utils.ts | Guide 순수 유틸/노멀라이저/스코프빌더(483줄). 동일 5개 死파일군에서만 참조 |
| board-poll-prize.ts | 투표 상품 추첨(Fisher-Yates) + board_posts.poll 업데이트 + 결과 댓글. 게시판.tsx가 사용 (정상) |
| PostTableView.tsx | 게시판 표 컴팩트 목록 뷰. 게시판.tsx가 사용 (정상) |
| PostMoreMenu.tsx | 게시물 ⋯ 드롭다운(별표/북마크/수정/공유/삭제). PostTableView가 사용 (정상) |
| post-table-helpers.ts | PostTableView 헬퍼(즐겨찾기 localStorage, 태그/첨부/댓글 카운트, 날짜포맷) |

---

## 분류별 발견

### D — 중복 (가장 심각)

**[D-1] 業務가이드.tsx ↔ guide-utils.ts: 유틸/노멀라이저/스코프빌더 거의 100% 중복** — 심각도: 높음
- 근거: 두 파일에 동일 구현이 병존.
  - 업무가이드.tsx L148-557: `normalizeText, buildTeamKey, buildSelectColumns, inferAttachmentType, extractMetaMarker, extractAttachmentMetaFromContent, buildAttachmentMetaContent, extractGuideMetaFromContent, buildGuideContent, extractGuideTaskMetaFromContent, buildGuideTaskContent, normalizeGuideKind/Audience/TaskPriority, parseKeywords, normalizeGuideResource, normalizeGuideTask, formatDate, formatDateOnly, getGuideKindLabel, getGuideAudienceLabel, getTaskPriorityMeta, sortGuideTasks, runGuideMutation, buildCompanyScopes, matchesCompanyScope, matchesTeamScope`
  - guide-utils.ts L45-482: 동일 함수 전부 `export`로 재정의 (本문 동일).
- 인용(업무가이드.tsx L148): `function normalizeText(value: unknown) { return String(value || '').trim(); }`
- 인용(guide-utils.ts L45): `export function normalizeText(value: unknown) { return String(value || '').trim(); }`
- 조치: 업무가이드.tsx의 인라인 정의를 제거하고 guide-utils.ts에서 import. (단, 死코드 군 정리 방향과 함께 결정 — 아래 X-1 참조)

**[D-2] 상수/컬럼목록 중복 정의** — 심각도: 중간
- 업무가이드.tsx L22-44 (`GUIDE_BOARD_TYPE, GUIDE_DISPLAY_NAME, GUIDE_TASK_BOARD_TYPE, ATTACHMENTS_META_*, GUIDE_META_*, GUIDE_TASK_META_*, GUIDE_POST_REQUIRED_SELECT_COLUMNS, GUIDE_POST_OPTIONAL_COLUMNS`) vs guide-utils.ts L19-41 동일 export.
- 조치: 단일 출처로 통합.

**[D-3] 타입 중복** — 심각도: 중간
- 업무가이드.tsx L51-146 (`GuideKind, GuideAudience, GuideTaskPriority, GuideMetaPayload, GuideTaskMetaPayload, GuideRow, GuideResource, GuideTask, OrgTeamRow, OrgStaffRow, TeamScope, CompanyScope`) 가 인라인 정의됨. guide-types.ts에 동일(확장)본 존재.
- 조치: guide-types.ts로 일원화.

**[D-4] GuideDetailPanel JSX 중복** — 심각도: 중간
- GuideDetailPanel은 실제 사용되나, 업무가이드.tsx L1322-1517(작성폼)·L1520-1674(사이드바)·L1689-1865(할일보드)이 GuideComposerSection/GuideSidebar/GuideTaskBoard와 **동일 JSX를 인라인으로 재구현**. 분리 컴포넌트는 死(X-1).

### X — 죽은 코드

**[X-1] GuideComposerSection / GuideSidebar / GuideTaskBoard / guide-utils.ts / guide-types.ts — 미사용** — 심각도: 높음
- 참조확인(Grep 전역): 이 5개 파일은 **서로만** import. 실제 화면 엔트리 업무가이드.tsx(=GuideLibrary, 게시판.tsx L22·L1512에서 사용)는 GuideDetailPanel만 import(L20)하고 나머지는 import하지 않음.
- 즉 GuideComposerSection/GuideSidebar/GuideTaskBoard는 어떤 렌더 경로에도 도달 불가. guide-utils.ts/guide-types.ts는 그 死컴포넌트(+ 死컴포넌트가 쓰는 GuideDetailPanel 일부)만을 위한 정의.
  - 단 GuideDetailPanel.tsx는 guide-types.ts(L10)·guide-utils.ts(L11) import → guide-types/guide-utils는 **부분적으로 살아있음**(GuideDetailPanel 경유). GuideComposerSection/GuideSidebar/GuideTaskBoard 3개는 완전 死.
- 조치: 3개 컴포넌트 삭제하거나, 반대로 업무가이드.tsx를 이들로 리팩터링(권장: D-1~4 해소 겸). 현 상태는 "리팩터링 미완"으로 추정.

**[X-2] mobileSummary / linkedTaskTemplates 기능 死(데스크톱)** — 심각도: 중간
- GuideDetailPanel.tsx L161-166은 `selectedResource.mobileSummary`를 렌더하지만, 업무가이드.tsx의 인라인 `normalizeGuideResource`(L308-330)는 `mobileSummary`를 **세팅하지 않음**(guide-utils.ts L209-231도 동일). 따라서 데스크톱에서 항상 `undefined` → 요약 박스 미표시. workflowDependencies/linkedTaskTemplates도 작성/저장 경로 없음.
- 조치: 사용 안 할 거면 guide-types.ts의 해당 필드+GuideDetailPanel 블록 제거, 쓸 거면 작성 UI/노멀라이저 보강.

**[X-3] 댓글 수(💬) 항상 0** — 심각도: 중간
- post-table-helpers.ts L66-70 `getCommentCount`는 `comments_count ?? comment_count`를 읽음. 정본 board_posts(schema L277-316)에 두 컬럼 없음, 전역 Grep상 어디서도 채워지지 않음(해당 키는 이 한 곳만 등장).
- 인용: `const value = record.comments_count ?? record.comment_count;`
- 결과: PostTableView L401-405 💬 카운트·`comments` 정렬(L170-171)이 모두 무효(항상 0).
- 조치: 댓글수를 board_post_comments 집계로 주입하거나, 컬럼/정렬옵션 제거.

### Q — 유사기능 / 설계

**[Q-1] 데스크톱 업무가이드.tsx vs 모바일 추가기능/업무가이드.tsx** — 심각도: 낮음
- 동일 board_type('업무가이드')을 데스크톱(전체 CRUD)·모바일(read-only, data-hooks.ts L700 `in('board_type',['업무가이드','guide'])`)이 각각 구현. 의도된 분리로 보이나 board_type 키 표기('업무가이드' vs 'guide') 혼재는 주의.

### T — 타입(any · 강제캐스팅)

**[T-1] 업무가이드.tsx — supabase 결과 강제 캐스팅 다발** — 심각도: 중간
- L697 `return result as unknown as QueryResult<GuideRow[]>;` (L708 동일), L425 `mutation: (...) => PromiseLike<any>`, L588·590 `(s as any).company_id`, L715-717 `as GuideRow[] / as OrgTeamRow[]`.
- 근거: D1 프록시 응답이 타입화 안 되어 이중 캐스팅. guide-utils.ts L348은 동일 mutation을 `PromiseLike<{data;error}>`로 더 정확히 타입화(중복본이 더 안전한 역설).
- 조치: QueryResult 제네릭으로 단일화, `any` 제거.

**[T-2] board-poll-prize.ts L121 `(staffRows as any)`** — 심각도: 낮음
- 인용: `const found = ((staffRows as any) ?? []).find(...)`. select 결과를 `{id;name}[]`로 타입화 가능.

**[T-3] PostTableView 셀 값 강제 캐스팅** — 심각도: 낮음
- L167-169 `(b.views as number)`, `(b.likes_count as number)` 등. BoardPost 필드가 number로 정의되어 있으면 불필요한 캐스팅(또는 타입 정의 보강 필요).

### SEC — 보안 (XSS / 권한)

**[SEC-1] 첨부 url 직접 href/src(image)** — 심각도: 낮음(의심)
- GuideDetailPanel.tsx L201-205 `href={attachment.type==='image' ? attachment.url : buildManagedDownloadUrl(...)}`, L253 `<img src={attachment.url} />`, 업무가이드.tsx L1685 `window.open(clicked.url,'_blank')`.
- url은 R2 업로드 결과(서버측 키)로 보이나, content 임베드 메타(extractAttachmentMetaFromContent)에서 복원되는 경로도 있어 사용자 제어 문자열이 url로 들어올 수 있음 → `javascript:` 스킴 차단 검증 없음. `window.open` 시 `noopener` 미지정.
- 조치: url 스킴 화이트리스트(https/r2) 검증, target=_blank에 noopener. (확신 낮음=의심)

**[SEC-2] 업무가이드 권한이 자유게시판에 매핑** — 심각도: 낮음(설계 확인 필요)
- lib/access-control.ts L69 `업무가이드: { read:'board_자유게시판_read', write:'board_자유게시판_write' }`. 업무가이드 전용 권한키가 없어 자유게시판 권한을 그대로 차용. 의도일 수 있으나 별도 게시판인데 권한 분리 불가.

### E — 에러 처리

**[E-1] toggleTask 낙관적 업데이트 롤백 부재** — 심각도: 중간
- 업무가이드.tsx L1234-1272: 상태를 먼저 setTeamTasks(L1260)한 뒤 DB update 실패 시 `void loadGuideWorkspace()`(L1270)로 전체 재요청하나, 사용자에게 실패 toast 없음. 네트워크 실패 시 무음 롤백.
- 조치: catch에서 toast 경고 추가.

**[E-2] board-poll-prize 댓글 삽입 실패 무음(설계상 허용)** — 심각도: 낮음
- L154-157 댓글 실패는 `logger.warn`만. 추첨 성공 보장 의도로 적절. 단 호출부(게시판.tsx L2915)에서 사용자 안내 없음 — 정보 제공 고려.

### P — 성능

**[P-1] board_posts realtime 전체 재로드** — 심각도: 낮음
- 업무가이드.tsx L730-742: board_posts/org_teams 변경 시 `loadGuideWorkspace()` 전체 재실행(batchWindowMs 1000으로 완화). board_posts는 모든 게시판 공용 테이블이라 무관한 게시글 변경에도 업무가이드 워크스페이스가 재요청됨(board_type 필터 불가, 정본 realtime-bus 한계).
- 조치: 가능하면 변경 row의 board_type로 디바운스/스킵.

### S / K / U / DBG

- S(문법/런타임): 발견 없음.
- K(한글깨짐): 발견 없음(모든 한글 정상).
- U(불필요): X-1과 중복 — 死파일군이 곧 불필요 코드.
- DBG(디버그): 잔존 `console.*`는 모두 의도적 에러 로깅(업무가이드.tsx L649,719,1050,1105,1213,1269,1296; post-table-helpers L38,49). 별도 조치 불요.

---

## 집계표

| 분류 | 건수 | 높음 | 중간 | 낮음 |
|---|---|---|---|---|
| D 중복 | 4 | 1 | 3 | 0 |
| S 문법/런타임 | 0 | - | - | - |
| K 한글깨짐 | 0 | - | - | - |
| X 죽은코드 | 3 | 1 | 2 | 0 |
| U 불필요 | (X-1 포함) | - | - | - |
| Q 유사기능 | 1 | 0 | 0 | 1 |
| T 타입 | 3 | 0 | 1 | 2 |
| E 에러처리 | 2 | 0 | 1 | 1 |
| P 성능 | 1 | 0 | 0 | 1 |
| SEC 보안 | 2 | 0 | 0 | 2 |
| DBG 디버그 | 0 | - | - | - |
| **합계** | **16** | **2** | **8** | **6** |

## 특별임무 결론
- **스키마 일치**: 업무가이드.tsx의 board_posts 사용(컬럼 id/board_type/title/content/author_id/author_name/company/created_at/updated_at/company_id/attachments)은 정본 board_posts(schema L277-316)와 일치. withMissingColumnsFallback/runGuideMutation으로 optional 컬럼(updated_at/company_id/attachments) 누락도 방어 → **무음실패 위험 낮음**. board-poll-prize의 poll/poll_votes·board_post_comments insert 컬럼도 정본 일치.
- **Guide* 중복(D)**: 심각. 업무가이드.tsx가 유틸/타입/JSX를 전부 인라인 재구현 → guide-utils/guide-types/GuideComposerSection/GuideSidebar/GuideTaskBoard와 이중화. 분리 컴포넌트 3종은 死(X-1).
- **PostTableView/PostMoreMenu**: 게시판.tsx에서 정상 사용, 게시판.tsx와 중복 아님(목록 뷰 분리). 단 댓글수 기능은 死(X-3).
