# 01. 내정보 · 알림 탭

경로: `app/main/모바일/내정보/**`, `셸/MobileShell.tsx` mypage/notif

---

## Critical

### M-C1. 생체인증 우회
- **파일:** `홈.tsx:227–268`
- **증상:** 급여·증명서 진입 시 “생체 인증 완료” 후 비밀번호 없이 통과. 모달 오픈 ~400ms 후 자동 실행.
- **원인:** `PublicKeyCredential.isUserVerifyingPlatformAuthenticatorAvailable()` 만 확인. `navigator.credentials.get()` 없음.
- **수정:** WebAuthn assertion 또는 자동 호출 제거 후 비밀번호 경로만.

---

## Major

| ID | 파일 | 증상 | 원인 |
|----|------|------|------|
| M1 | `data-hooks.ts:90–94` | 미완료 할 일 카운트 항상 0 | `completed` 컬럼 없음 → 스키마는 `is_complete` |
| M2 | `연차.tsx:69–91` | 연차 신청 버튼 무반응 | onClick 없음 |
| M3 | `useLeaveBalance` vs `useMyLeave` | 홈 잔여연차 ≠ 연차 화면 | 데이터 소스·폴백(기본 15일) 불일치 |
| M4 | `data-hooks.ts` history | 반차 미표시 또는 1일로 표기 | `isAnnualLeaveType`만 필터, 반차 0.5 미반영 |
| M5 | `나의할일.tsx:37–39` | 할일→채팅이 목록만 열림 | `onSwitchTab('chat')`만, roomId 무시 |
| M6 | `알림탭.tsx:152–157` | 채팅 알림→방 미진입 | `setMainMenu('채팅')`만 |
| M7 | `index.tsx` vs `증명서.tsx` | 셀프 발급 UI 미연결 | records → PayrollAndCertificatesHub만 |
| M8 | `정보수정.tsx:265–282` | 프로필 사진 변경 무반응 | onClick/input 없음 |
| M9 | `useMonthlyAttendance` | 실패 시 「집계 중」 영구 | error 시 data null 유지 |
| M10 | `출퇴근체크인` vs 월집계 | 자정/TZ 경계 어긋남 | 로컬 date vs KST month |
| M11 | `알림탭` load fail | 「알림 없음」으로 위장 | catch 없음 |

---

## Minor

- recordsView 재진입 시 탭 유지 (`index.tsx`)
- silent catch 전반
- hire_date 없으면 연차수 칩 미표시
- 출퇴근 openLog = today \|\| stale 혼동 가능

---

## 라우팅 메모

- mypage 탭은 전역 `subView` 동기화 **제외** (`MobileShell.tsx:272–277`) — 출퇴근 튕김 방지용, 정상.
- sub: attend / leave / records / edit / todo / notifSettings
