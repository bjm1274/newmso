# Fable5 재실행 — ⑥ 모바일 (검토 22파일)

> ✅ = Opus 코드 직접 재확인. [검증] = Fable5 독립 검증자 판정.
> 정책: 모든 기능이 모바일에서도 동작해야 하며 "PC에서 하세요" 우회 스텁 금지.

## ⚠️ 의도된 MSO 설계로 재분류 (결함 아님)
- ~~**mobile-1** 조직도 `company='전체'`~~, ~~**mobile-2** 근무현황 `company='전체'`~~ — **정상 동작**. MSO(병원경영지원회사)가 여러 회사를 운영 대행하므로 회사 간 직원 열람은 의도된 설계(2026-06-10 사용자 확인). Fable5가 critical로 올린 것은 **오판**(도메인 맥락 부족). 메모리 `[[mso-cross-company-visibility]]`.
  - 남는 minor: `조직도.tsx` 헤더 8행 주석 "JM5: company 필터로 본인 회사만 / 회사 격리"가 실제 의도와 **모순되는 stale 주석** → 문서 정리 권장(보안 버그 아님).

## MAJOR
- **mobile-3 [기능격차]** `모바일/채팅/채팅방.tsx:708-725` — "채팅방 나가기"가 `onClick={onBack}`만. chat_rooms.members 제거/나가기 API 없음 → 재입장 시 멤버 유지·unread·알림 누적. 가짜 액션. [검증 confirmed major]
- **mobile-4 [의미없는]** `모바일/채팅/채팅방.tsx:616` — 알림수신 토글 `defaultChecked`만(onChange/state/DB 없음). 항상 켜짐, 무음 설정 저장 안 됨. [검증 confirmed major]
- **mobile-6 [기능격차]** `모바일/인사관리/계약문서.tsx:156,401` — 증명서 '발급'·서류 '제출'이 toast("PC에서 진행")만. PC 우회 스텁(정책 위반). [검증 confirmed major]
- **mobile-7 [기능격차]** `모바일/인사관리/연차신청.tsx:239` — 결재자 '변경'이 toast("PC에서 지원")만. 자매폼 `결재/연차신청폼.tsx`는 SApprovalApproverPicker로 정상 동작 → 동일 업무 두 화면 기능 불일치 + PC 스텁. [검증 confirmed major]

## MINOR
| ID | 파일:라인 | 분류 | 내용 |
|---|---|---|---|
| mobile-5 | 채팅/채팅방.tsx:343 | 의미없는 | "실시간 연결됨" 정적 라벨 — 실제는 폴링, 오프라인에도 동일 표시(오도) |
| mobile-8 | 인사관리/구성원등록.tsx:261 | 기능격차 | '사진 추가' toast("PC에서 지원") 스텁(등록 insert 자체는 동작) |
| mobile-9 | 인사관리/구성원등록.tsx:11 | 죽은코드 | 헤더 주석 "PC에서만 insert" stale — 실제는 enqueueSupabaseMutation로 insert |
| mobile-10 | 재고/물품자산.tsx:298 | 의미없는 | 'UDI 스캔' 버튼 onClick 없음(no-op), 하단은 "데스크톱에서" 안내 |
| mobile-11 | 재고/재고현황.tsx:228-231 | 의미없는 | 입고/출고 푸터 버튼 영구 disabled '준비 중'(실기능은 입출고.tsx에 별도) |
| mobile-12 | 추가기능/퇴원심사상세.tsx:273 | 기능격차 | 코멘트 탭 "데스크톱에서 작성" 안내만(승인/반려는 동작) |
| mobile-13 | 결재/결재상세.tsx:233-234 | 코드오류 | approve 시 낙관적 status '대기'→'대기'(무변화 no-op), 직후 재조회로 정상화 |
| mobile-14 | 결재/결재상세.tsx:51 | 중복 | `pickAvatarTone`이 채팅/data-hooks.ts:641과 별도 알고리즘으로 중복(시드별 색 불일치) |

## Sonnet 4차 대비
- 회사 간 가시성(mobile-1/2)은 두 모델 다 오판(의도된 설계). 그 외 no-op UI(mobile-3/4)·PC 스텁(mobile-6/7)은 유효. Sonnet은 132파일 전수로 PC 스텁 14건+를 더 광범위 열거 — **합집합 권장** — [06(Sonnet)](../4차_2026-06-10/06_모바일.md)의 추가 스텁(증명서 4종·마감보고·수술상담 녹음·재고 예측/AS 등) 유효.
