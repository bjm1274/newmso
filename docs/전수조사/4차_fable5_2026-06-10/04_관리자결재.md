# Fable5 재실행 — ④ 관리자·전자결재·마이페이지 (검토 28파일)

> [검증] = Fable5 독립 검증자 판정.
> ※ resume에서 재실행되어 1차와 다른(둘 다 유효한) 발견 집합. 본 표가 정본.

## MAJOR (전건 검증 confirmed)
- **admin-01 [기능격차]** `관리자전용서브/알림자동화설정.tsx:72-96,296-356` — 급여/연차 자동알림 토글이 `localStorage.setItem(mso_auto_*)` 전용. 이 키를 읽는 서버 cron/API가 저장소 전체에 없음(급여 cron 부재; leave-notice/todo-reminder/backup-cron만 존재). "매월 N일 자동 발송" 미동작, 동작은 '즉시 실행' 버튼뿐. [검증 confirmed]
- **admin-02 [기능격차]** `관리자전용서브/예산관리.tsx:86-130` — 예산 설정·집행 전체가 read/writeLocalStorage 전용(파일 내 supabase/fetch 0건). 브라우저별 데이터 → 타 관리자 비공유·캐시 삭제 시 소실. [검증 confirmed]
- **admin-03 [의미없는]** `관리자워크센터/ExecDashboard.tsx:64-215` — 개요 KPI(5월 매출 182.4M·방문환자 1,284명·현금잔고 412M)·CHART_DATA·BUDGET_DATA·PNL_BY_CORP(+38M) 전부 정적 상수. data.staffs/user는 받지만 개요 수치에 미사용. 가짜 경영지표가 임원 대시보드 노출. [검증 confirmed]
- **admin-04 [의미없는]** `관리자워크센터/FormsWorkcenter.tsx:36-58` — 결재양식 개요 KPI(전체 14종·이번 달 142회)·version·usageCount·status 전부 정적 상수. 실양식관리(ApprovalFormTypesManager)와 별개로 가짜 통계 표시. [검증 confirmed]
- **admin-05 [코드오류]** `마이페이지/index.tsx:322-331` — 계약서명 완료 알림을 `user_id='system_admin'`으로 insert. notifications.user_id는 staff_members(id) FK이고 'system_admin' 시드 없음→FK 위반 가능, HR 통지 무음 실패. [검증 confirmed, 메커니즘 보정: catch 삼킴이 아니라 d1-compat insert가 throw 안 하고 반환 error를 미검사. FK는 프로덕션 D1에서만 강제]

## MINOR
| ID | 파일:라인 | 분류 | 내용 |
|---|---|---|---|
| admin-06 | 관리자전용서브/시스템마스터센터.tsx:463-473 | 의미없는 | `showFlaggedOnly` 분기가 양쪽 모두 chatRooms 반환 — 필터 미구현 |
| admin-07 | 관리자전용서브/팝업창관리자.tsx:31-61 | 중복 | `isAllowedFile`(export·미사용) ↔ `validatePopupFileSelection` 중복 + 영상 한도 안내 '25MB' vs 실제 200MB 모순 |
| admin-08 | 관리자전용서브/직원권한통합.tsx:130-185 | 죽은코드 | `fetchStaffs` 빈 본문 no-op인데 resetPassword에서 await 호출 |
| ~~admin-09~~ | lib/db/auth/policies.ts:725-732 | — | ~~APPROVAL_SCOPE가 회사 일치만 검사~~ → **의도된 MSO 설계로 재분류**(회사 간 결재 처리 허용, 2026-06-10 사용자 확인). 결함 아님. `[[mso-cross-company-visibility]]` |
| admin-10 | 전자결재서브/ApprovalInboxView.tsx:501 | 의미없는 | 운영 렌더가 `window.navigator.webdriver`(테스트 감지)로 WorkflowBoard/ResponsiveTable 분기 — 실사용/테스트 경로 상이 |

## 1차 패스에서만 나온 유효 발견(흡수)
- 반려 서버 실패 시 클라이언트 직접 `supabase.from('approvals').update` 폴백이 서버 전이(알림·잠금·라우팅 정리) 우회(useApprovalActions.ts:211) — Sonnet 4차의 "클라 폴백" 보류 항목과 동일. 무조건 onClose(ApprovalDetailModal) 포함. → [04(Sonnet)](../4차_2026-06-10/04_PC_관리자결재.md).
