# Fable5 재실행 — ⑧ 저장소 위생·중복 (검토 14, jscpd 분석)

> 전 발견 minor(검증자 단계 없음). git ls-files / grep 근거 동봉. Sonnet 4차 ⑧과 동일 결론 — 두 모델 일치.

## git 추적 정크 (git rm --cached 대상)
| ID | 대상 | 근거 |
|---|---|---|
| hygiene-01 | 루트 `*.txt` 16개(test_output·submissions_output*·single_test_output*·walkthrough_output·test-insert-out·__skel·_tmp_*) | git 추적·참조 0. .gitignore의 `tmp*`는 디렉터리 한정이라 미커버 |
| hygiene-02 | 루트 `test_db.js`(27B)·`test_emoticon.js`·`test-insert.mjs`·`query_poll.py`·`query.sql` | package.json·scripts·tests 참조 0(grep NONE) |
| hygiene-03 | `scratch_zip/` 40파일 | 디자인 벤더 사본, import 0. public/emoticon·globals.css와 중복(emoticon-face.js 236L, chat-ui.css↔globals.css 198L). .gitignore 미등록 |
| hygiene-04 | `handoff/newmso15/` 38파일 | docs/mobile/ 문서와 통째 중복(MSO_Mobile_Improvement.html 1916L 등), import 0 |
| hygiene-05 | `scripts/migrate-d1/output/*.sqlite` 2개(2.16MB+2.15MB) | build-sqlite.mjs 생성 산출물 바이너리 추적 — diff 불가·리포 비대 |
| hygiene-06 | `scripts/__pycache__/*.pyc` 2개 | .gitignore 106-107이 무시 규칙 보유하나 기추적분 잔존 — git rm --cached 필요 |

## 중복 코드 (jscpd)
| ID | 대상 | 규모 |
|---|---|---|
| hygiene-07 | `d1_schema_final.sql` ↔ `d1_schema_missing.sql` | 2595L 중복 — SQL 포맷 전체 중복률 27.8%(7246/26038)의 주원인(산출물 .gitignore 처리 시 해소) |
| hygiene-08 | `메신저액션서브/EmojiPicker.tsx` ↔ `모바일/채팅/이모지피커.tsx`(149L), `근태관리메인-내부유틸.ts` ↔ `근태유틸.ts`(140L) | PC/모바일 이중트리 구조 중복. 후자는 헤더 주석 "통합 금지(approvals.type 상이)" — 순수 유틸만 추출 여지. TS 전체 중복 7.1% |

## .gitignore
- hygiene-09: `scratch_zip/`·`handoff/`·`*.sqlite`·루트 `_tmp_*.txt`/`*_output*.txt` 패턴 누락 → 신규 규칙 추가 + git rm --cached 동시 필요.

## Sonnet 4차 대비
- 두 모델이 **완전 일치**(정크 추적·sqlite/pyc·scratch_zip/handoff·SQL 중복률·PC/모바일 쌍둥이). 결정적 산출물(git/jscpd) 기반이라 모델 비의존. → [08(Sonnet)](../4차_2026-06-10/08_죽은코드_중복_위생.md).
