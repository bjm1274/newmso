# 4차 전수조사 (Fable5 재실행) — 요약 (2026-06-10)

> 사용자 요청으로 **스킬(Opus→Sonnet 위임 advisor 전략)을 끄고, 조사·검증 전 단계를 Fable5로 재실행**한 결과.
> 구조: 8개 영역 병렬 정독(Fable5 감사자) → 영역별 **독립 Fable5 검증자**가 critical/major를 인용 코드와 대조해 적대적 재검증(감사자≠검증자 분리).
> 객관 검사(tsc)는 모델 무관·결정적이라 재실행만 함(본체 0건, e2e 2건 — `@supabase/supabase-js` 미설치).
> ⚠️ 기록 전용. 코드 수정은 별도 지시 시.

## 실행 메모 (재현성)
- 워크플로가 1차에 세션 한도로 `hr·lib 감사`와 `admin·sql 검증` 4건이 끊겨, 모델을 `claude-fable-5[1m]`로 전환 후 **resume**(캐시 재사용)으로 완주.
- resume 과정에서 `admin·sql 감사`가 재실행되며 1차와 **다른(둘 다 유효한) 발견 집합**을 산출 — Fable5의 비결정성을 보여줌. 본 보고서는 완주본(정합)을 정본으로 삼고, 1차에서만 나온 유효 발견(미존재 테이블 다수·PUBLIC_ALL)은 별도 표기해 흡수.
- Opus(본인)가 critical/신규 고가치 건을 **코드로 직접 재검증**(아래 ✅).

## 검증 결과 총괄

| 구분 | 건수 |
|---|---|
| Fable5 감사 발견(8영역 합, 원시) | 81 |
| 교차영역 중복 제거 후 고유 | ~75 |
| **검증자 confirmed critical** | **3** (정정 전 5 — mobile-1/2는 의도된 설계로 제외) |
| **검증자 confirmed major** | **20** (정정 전 21 — hr-03 제외) |
| 심각도 강등 (major→minor) | 1 (lib-04) |
| **검증자 기각(오탐)** | **1 (lib-06)** |
| **의도된 MSO 설계로 재분류(결함 아님)** | **4+ (mobile-1·mobile-2·hr-03·PUBLIC_ALL·admin-09)** |
| minor(미검증, 그대로 보고) | 약 47 |

영역별 검토 파일 수: api 84 · lib 56 · hr 31 · admin 28 · common 38 · mobile 22 · sql 28 · hygiene 14.

## ⚠️ 정정 (2026-06-10 사용자 피드백) — 회사 간 가시성은 의도된 MSO 설계

이 시스템은 **MSO(병원경영지원회사)가 여러 병원/회사를 운영 대행**하는 구조라, 한 회사 사용자가 **다른 회사 직원을 열람·수정하는 것은 의도된 설계이며 보안 결함이 아님**(읽기·쓰기 모두 의도, 사용자 확인). 따라서 아래 항목은 **결함에서 제외**한다:

- ~~mobile-1 조직도 `company='전체'`~~, ~~mobile-2 근무현황 `company='전체'`~~ → **정상 동작**(전 회사 열람이 목적). 단 `조직도.tsx` 헤더 주석 "JM5: 본인 회사만 / 회사 격리"는 실제 의도와 모순되는 **stale 주석**이라 정리 권장(minor, 문서).
- ~~hr-03 조기퇴근 회사필터 없음~~ → **정상**(전 회사 조회 의도).
- ~~PUBLIC_ALL 테넌트 격리 부재~~ → **의도된 정책**(MSO가 전 회사 CRUD). 결함 아님.
- ~~admin-09 APPROVAL_SCOPE 회사 일치만 검사~~ → **의도 범위**(회사 간 결재 처리 허용)로 재분류.

**단, 이 설계가 면죄부는 아닌 별개 사안**: 인증 자체가 없어 **비로그인 외부에서도 호출 가능한** 엔드포인트는 고쳐야 함(사용자 확인). → `api-1`(아래 #1)은 "회사 간 IDOR"이 아니라 **"미인증 호출 가능"**으로 프레이밍을 바꿔 **유효 지적으로 유지**.

이 도메인 사실은 메모리 `[[mso-cross-company-visibility]]`에 저장됨.

## 🔴 confirmed CRITICAL 3건 (정정 후, 검증자 + Opus 코드 재확인)

1. **[보안/미인증] 인증 없는 연차 재계산 엔드포인트** — [app/api/admin/annual-leave/sync/route.ts:5](../../../app/api/admin/annual-leave/sync/route.ts) 세션 검사 0. middleware는 `/api`를 보호 안 함 → **비로그인 외부에서도** 임의 staffId로 `syncAnnualLeaveUsedForStaff`/`recalculateLeaveBalance` 트리거 가능. (회사 간 접근이 아니라 **익명 접근**이 문제 — 로그인 필수로 수정 필요.) (api-1) ✅
2. **[기능 불능] D1 미지원 `db.transaction()` — 부서 재고 소모 프로덕션 실패** — [lib/db/functions/inventory.ts:205](../../../lib/db/functions/inventory.ts) `atomicStockConsumeWithLog`가 drizzle `db.transaction()` 사용. D1은 명시적 BEGIN/COMMIT를 거부해 런타임 throw. 실라우트 [app/api/inventory/stock-consume/route.ts:58](../../../app/api/inventory/stock-consume/route.ts)이 `getD1Drizzle`로 호출(주석은 "배치 트랜잭션"이라 적혀 있으나 실제는 transaction). 로컬 better-sqlite3 mock은 BEGIN 지원→dev 무회귀 착시. 형제 `stock-transfer`는 이미 `db.batch()`로 재작성됨이 방증. (lib-01=sql-03) ✅
3. **[무음 데이터 손실] 미존재 테이블 `nurse_schedules`** — [간호근무표.tsx:226](../../../app/main/기능부품/인사관리서브/근태기록/간호근무표.tsx) select/delete/insert하지만 schema.ts·d1_schema_final.sql·마이그레이션에 테이블 없음([policies.ts:635]에 이름만; supabase 마이그 주석 "by product decision 제외"). `근태관리메인.tsx:18,934`에서 렌더되는 현역. 읽기는 에러 미처리(빈값), 저장은 무음 실패. 간호 근무표 자동편성 전면 불능. (hr-01) ✅

## confirmed MAJOR 20건 (정정 후, 요약)

| ID | 파일 | 내용 |
|---|---|---|
| api-2 | ai/roster-recommendation/route.ts:9 | 모듈스코프 Map 레이트리밋 — Workers isolate 분산으로 무효, Gemini Pro 호출 과소차단 |
| api-3=sql-01=lib-04* | d1/query·mutate/route.ts | 매 요청 런타임 DDL(CREATE TABLE/INDEX) — read 핫패스에 마이그레이션 (*검증자가 lib-04는 minor로 강등) |
| lib-02 | d1/query/route.ts:54 | `contains` op이 WhereSchema enum 누락→`.contains()` 쿼리 전부 400, GlobalSearch·OP체크 "내 채팅방 찾기" 무음 실패 |
| lib-03=sql-06 | d1/query/route.ts:353 | count 경로가 limit/range 무시·전 행 메모리 로드 — 대형 테이블 잠재 DoS |
| lib-05 | lib/rate-limit.ts:114 | recordFailedAttempt read-then-write TOCTOU — 동시 브루트포스 과소집계(보안 통제 약화) |
| hr-02 | 경조사관리.tsx:219 | INSERT 실패를 삼키고 가짜 UUID로 로컬 위장 저장 — 경조금 기록 무음 손실·집계 누락 |
| ~~hr-03~~ | 조기퇴근감지.tsx:57 | ~~회사 필터 없음~~ → **의도된 MSO 전 회사 조회로 재분류(결함 아님)** |
| admin-01 | 알림자동화설정.tsx:72 | 급여/연차 자동알림 토글이 localStorage 전용 — 읽는 서버 cron 부재, "자동 발송" 미동작 |
| admin-02 | 예산관리.tsx:86 | 예산 설정·집행 전체가 localStorage 전용 — 타 관리자 비공유·캐시 삭제 시 소실 |
| admin-03 | ExecDashboard.tsx:64 | 경영 개요 KPI/PnL/법인손익이 하드코딩 더미("5월 매출 182.4M")를 임원 화면에 실측처럼 노출 |
| admin-04 | FormsWorkcenter.tsx:36 | 결재양식 개요 KPI·사용횟수 하드코딩 더미 |
| admin-05 | 마이페이지/index.tsx:322 | 계약서명 완료 알림 user_id='system_admin' insert — FK 위반 가능, HR 통지 무음 실패(검증자: 메커니즘은 catch가 아니라 반환 error 미검사) |
| mobile-3 | 모바일/채팅/채팅방.tsx:708 | "채팅방 나가기"가 onBack만 — 멤버십 제거/나가기 API 없음, 재입장 시 멤버 유지·unread 누적 |
| mobile-4 | 모바일/채팅/채팅방.tsx:616 | 알림수신 토글 onChange/state/DB 없음 — 항상 켜짐, 무음 설정 저장 안 됨 |
| mobile-6 | 모바일/인사관리/계약문서.tsx:156,401 | 증명서 발급·서류 제출이 "PC에서 진행" toast 스텁(모바일 전기능 정책 위반) |
| mobile-7 | 모바일/인사관리/연차신청.tsx:239 | 결재자 변경 PC 스텁 — 자매폼 결재/연차신청폼은 정상 동작(기능 불일치) |
| sql-02=lib-12 | lib/db/auth/policies.ts:829 | filterByPolicy가 STAFF_IN_SCOPE 등에서 행당 staff_members 조회 N+1(최대 1000행 순차) |
| sql-05 | d1/query/route.ts:288 | limit+range 동시 지정 시 `LIMIT n LIMIT m OFFSET k` 잘못된 SQL → 500(잠복) |

## 검증자가 기각/강등한 항목 (Fable5 자가 교정)

- **기각** `lib-06`(오프라인 큐 네트워크 분류 오류, 감사자 major) — 검증자: 전제 오류. 실제 브라우저 fetch 실패는 `Failed to fetch`/`NetworkError`/`Load failed`로 reject되어 `isNetworkError`가 정상 매칭. 리터럴 `'Network error'`는 비-Error throw 폴백일 뿐. 큐잉 정상 동작. → 감사자≠검증자 분리가 자기 오탐을 잡은 사례.
- **강등** `lib-04`(매 요청 DDL, major→minor) — `IF NOT EXISTS`는 존재 시 저렴한 no-op이라 round-trip 1회 수준. 단 동일 사안을 sql-01/api-3은 major 유지(아키텍처상 잘못된 위치).

## Sonnet(4차) 대비 차이 — Fable5가 더 잡거나 바로잡은 것

**회사 간 가시성 — 두 모델 다 빗나감(정정):**
- 모바일 조직도/근무현황 `company='전체'`를 Sonnet은 minor, Fable5는 critical로 봤으나 **둘 다 오판**. 실제로는 의도된 MSO 설계(전 회사 열람·수정). 어느 모델도 "MSO가 전 회사 운영 대행"이라는 도메인 맥락을 코드만으로 알 수 없었음(헤더 주석이 오히려 "회사 격리"라고 적어 오도). → 코드 주석/문서가 도메인 의도와 어긋날 때 감사 모델이 오판하는 전형. 메모리 `[[mso-cross-company-visibility]]`로 향후 차단.

**Fable5 신규(Sonnet 4차 미검출):**
- `stock-consume` db.transaction 프로덕션 실패(lib-01) — Sonnet은 inventory transaction을 major lib로만 언급, 실라우트 연결·critical 격상 못 함
- `contains` op 400으로 .contains() 쿼리 전부 실패(lib-02)
- count 전 행 로드 잠재 DoS(lib-03/sql-06), rate-limit TOCTOU(lib-05), limit+range 중복 LIMIT(sql-05), filterByPolicy N+1(sql-02)
- 경조사 위장 저장(hr-02), 조기퇴근 회사필터 없음(hr-03)
- 알림자동화 localStorage 미동작(admin-01), 감사 심야판정 UTC/KST 오류(sql-10), APPROVAL_SCOPE 프록시 검사(admin-09), system master UUID claim 비활성(lib-11)

**Sonnet에 있었고 이번 완주본 Fable5가 직접 재현 안 함(여전히 유효):**
- GlobalSearch 모지바케 `'?쒕ぉ ?놁쓬'`(Sonnet C-1) — 이번 common 패스는 다른 파일에 집중해 미재현. 별도 보고서 [05_PC_공통기타.md](../4차_2026-06-10/05_PC_공통기타.md) 유효.
- 미존재 테이블 5종(education_completions·email_queue·leave_policies·nurse_schedules·work_type_change_history) — 완주본은 nurse_schedules(hr-01)만 명시. 나머지는 1차 Fable5 sql 패스 + Sonnet + Opus 직접 검증으로 확정. **테이블 신설/기능제거 결정 필요(미해결)**.
- 한글 컴포넌트명 lint 사각지대(222건 오탐) — tsc/eslint는 모델 무관이라 재실행 안 함. [05](../4차_2026-06-10/05_PC_공통기타.md) 유효.

**결론**: 두 모델이 헤드라인 critical(연차 sync 미인증·nurse_schedules·매요청 DDL)에 일치. 그 위에 **Fable5는 D1 트랜잭션 실패, 쿼리 라우트 잠복버그(contains/count/limit+range), rate-limit TOCTOU**를 더 깊게 잡았다. 반면 회사 간 가시성은 **두 모델 다 도메인 맥락 부족으로 오판**(의도된 MSO 설계) — 코드 주석이 의도와 어긋날 때의 한계. Sonnet 보고서의 UI/표면 항목(모지바케·lint)과 상호보완.

## 영역별 상세
[01_API.md](01_API.md) · [02_라이브러리.md](02_라이브러리.md) · [03_인사관리.md](03_인사관리.md) · [04_관리자결재.md](04_관리자결재.md) · [05_공통.md](05_공통.md) · [06_모바일.md](06_모바일.md) · [07_SQL.md](07_SQL.md) · [08_위생.md](08_위생.md)

## 권장 조치 순서
1. **즉시(보안)**: api-1 **인증 추가(로그인 필수)** — 비로그인 외부 호출 차단. ※ 회사 간 가시성은 의도된 설계이므로 테넌트 필터 도입하지 말 것(`조직도.tsx` 등 stale 주석만 정리)
2. **이번 주(기능 불능)**: lib-01 stock-consume `db.batch()` 전환 · hr-01 등 미존재 테이블 신설/제거 결정 · lib-02 contains enum 추가 · hr-02 경조사 저장 오류 표면화
3. **이번 달(라우트/인프라)**: 매요청 DDL→마이그레이션 1회 · count/limit+range 라우트 수정 · rate-limit 원자적 upsert · localStorage 전용 관리데이터(admin-01/02) 서버화 · sql-08 마이그레이션 journal 정합
4. **점진**: 더미 대시보드(admin-03/04) 실연동/표기 · N+1(sql-02) 메모이즈 · 중복(야간 비과세 240k·pickAvatarTone·LIKE ESCAPE) · git 위생([08](08_위생.md))
