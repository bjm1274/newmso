# Fable5 재실행 — ⑤ 공통·기타(메신저·알림·게시판 등) (검토 38파일)

> 전 발견이 minor라 검증자 단계 없음(critical/major 0건). 그래도 폴링/주석 불일치가 다수.

| ID | 파일:라인 | 분류 | 내용 |
|---|---|---|---|
| common-01 | 알림시스템.tsx:1342 | 의미없는 | `const useServerSideChatNotifications = true` 선언 후 전 파일 미참조(Grep 유일 매치) — 죽은 선언 |
| common-02 | 알림시스템/settings.ts:29 | 중복 | `normalizeKeywordList`가 filter-helpers.ts:9(export)와 본문까지 동일 — import로 통합 가능 |
| common-03 | useChatTypingD1.ts:9,153 | 코드오류 | 주석 "2.5초 폴링" vs 실제 `POLL_INTERVAL_MS=5000`(5초) |
| common-04 | 메신저구독훅.ts:335-339 | 코드오류 | 주석 "1500ms로 환원" vs 실제 `pollIntervalMs:1000` |
| common-05 | 알림시스템.tsx:1625-1640 | 코드오류 | 주석 "8000→30000ms 비용절감" vs 실제 `pollIntervalMs:3_000`(3초, 10배 차이) — 의도 확인 후 한쪽 정정 |
| common-06 | app/components/PwaBootstrap.tsx:145-151 | 죽은코드 | `resolveQueueEndpoint`가 switch에 case 없어 항상 null → 제네릭 오프라인 큐 플러시 영구 무동작(실 mutation은 offline-queue-supabase 경로). 매 마운트 등록만 |
| common-07 | 메신저데이터유틸.ts:75-84 | SQL오류 | unread 카운트 `.or()` 필터를 cursor 값 문자열 보간으로 생성 — 현재 cursor는 DB ISO라 위험 낮으나 입력 출처 변경 시 필터 파싱/인젝션 위험 |

## 비고
- Sonnet 4차가 잡은 **GlobalSearch 모지바케**(`'?쒕ぉ ?놁쓬'`, C-1)·**메신저공통 양분기 동일 삼항**·**근태기록 빈 폴더** 등은 이번 Fable5 공통 패스가 다른 파일에 집중해 미재현. 해당 항목은 [05(Sonnet)](../4차_2026-06-10/05_PC_공통기타.md)에서 유효. 두 패스 합집합으로 보는 것을 권장.
- 폴링 주석-코드 불일치(common-03/04/05)는 동일 패턴 다수 — 실제 폴링 부하/유지보수 혼선 유발. subscribeRealtime 호출부 일제 점검 권장.
