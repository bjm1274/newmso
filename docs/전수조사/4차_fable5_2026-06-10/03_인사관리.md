# Fable5 재실행 — ③ 인사관리(HR) (검토 31파일)

> [검증] = Fable5 독립 검증자 판정.

## CRITICAL
- **hr-01 [SQL오류]** `인사관리서브/근태기록/간호근무표.tsx:226,249-250` — 미존재 테이블 `nurse_schedules` select/delete/insert. schema.ts·d1_schema_final.sql·마이그레이션에 없음(policies.ts:635에 이름만, supabase 마이그 주석 "by product decision 제외"). `근태관리메인.tsx:18,934`에서 렌더되는 현역. 읽기는 `.then(({data}))`로 에러 미처리(빈값), 저장은 일반 '저장 실패' 토스트만. **간호 근무표 자동편성 전면 불능.** [검증 confirmed critical]

## MAJOR
- **hr-02 [에러처리]** `인사관리서브/경조사관리.tsx:219-228` — `congratulations_condolences` INSERT 실패 시 에러 토스트 없이 `crypto.randomUUID()`로 가짜 id 붙여 records 상태에만 추가. 사용자는 정상 저장으로 오인, DB 미영속 → 새로고침 시 소실·총지급액 집계 누락. **무음 데이터 손실.** [검증 confirmed major]
- **hr-03 [보안]** `인사관리서브/조기퇴근감지.tsx:57-75,91-102` — `early_leave_records`를 회사 조건 없이 조회, displayRecords·통계도 selectedCo 미적용(부서 필터만). 타 회사 직원 조기퇴근/이름/부서 노출. company 컬럼은 존재(schema.ts:2533)하고 타 HR 화면은 selectedCo 필터를 일관 적용하는 것과 대조. [검증 confirmed major]

## MINOR
| ID | 파일:라인 | 분류 | 내용 |
|---|---|---|---|
| hr-04 | 근태기록/근태관리메인.tsx:755-774 | 죽은코드 | `if (false && request.requested_by)` 봉인된 반려 알림 INSERT(실제 반려알림은 handleReject:817에서 정상 — 기능 누락 아님, 도달불가 코드) |
| hr-05 | 인사관리워크센터/AbnormalWorkcenter.tsx:67,181 | 의미없는 | `nowIso()`가 시각 없는 날짜만 반환 + '이번 달 누적' KPI가 세션 로컬값(새로고침 시 0) |
| hr-06 | 휴가신청/휴가관리메인.tsx:398-424 | 성능 | 전 직원 연차 재계산이 직원당 update+sync fetch 순차 루프(N+1) |
| hr-07 | 급여명세/중간정산.tsx:473 | 중복 | 야간 비과세 한도 240,000원이 급여정산(976)·중간정산·비과세한도체크(46) 3중 하드코딩(타 한도는 설정테이블 로드) |
| hr-08 | 급여명세/미지급수당알림.tsx:121 | 코드오류 | 기본급 미설정 시 `\|\| 2000000` 폴백으로 미지급수당 추정액 왜곡 — 0/'미입력'이 안전 |

## 비고
- nurse_schedules 외 미존재 테이블(education_completions·email_queue·leave_policies·work_type_change_history)은 1차 Fable5 sql 패스 + Sonnet + Opus 직접 검증으로 확정. 본 완주본 HR 패스는 nurse_schedules만 명시 — 동일 "무음 기능불능" 패턴. → [07_SQL.md](07_SQL.md), [00_요약.md](00_요약.md).
