# ⑧ 재고관리 메뉴 — 전수조사 보고서 (마스터)

> 조사일: 2026-05-29 · ~70개 파일(inventory-utils 1057, 재고현황뷰 834 등, 약 18,000줄) · 4개 그룹 병렬 정독 + Opus 재확인
> 그룹 상세: [_재고_W1](_재고_W1.md)(통합/유틸/워크센터) · [W2](_재고_W2.md)(현황/실사/이관/마감) · [W3](_재고_W3.md)(발주/거래처/물품/AS) · [W4](_재고_W4.md)(UDI/QR/스캔/모바일)

## 분류별 집계

| 그룹 | 건수 | 高 |
|------|------|----|
| W1 통합/유틸/워크센터 | 29 | ~3 |
| W2 현황/실사/이관/마감 | 20 | 1 |
| W3 발주/거래처/물품/AS | 23 | 5 |
| W4 UDI/QR/스캔/모바일 | 22 | 3 |
| **총계** | **94** | **12** |

---

## 🔴 테마 1: D1 스키마 드리프트 (✅ 확인) — 무음 기능불능

| 코드 위치 | 잘못된 참조 | 정본 | 결과 |
|-----------|-------------|------|------|
| `use-status-data.ts:85` | `inventory_logs.amount` (명시 select) | `quantity` ✅ | SQL 500 → 부서별 사용량 TOP5 영구 빈값 |
| `모바일 입출고-form.tsx:69` | `inventory_transactions` 테이블 | 부재 ✅ (정본 `inventory_logs`) | mutate 403 → **모바일 입출고 기록 전면 불능** |
| `모바일 물품등록.tsx:143` | `inventory.unit` 컬럼 (fallback 없음) | 부재 | "no column named unit" 500 |
| `AS반품관리.tsx` | `as_repair_records`/`return_records` 테이블 | 부재 ✅ | Supabase 경로 전부 실패, localStorage 전용(서버 미동기) |
| `명세서관리.tsx:75,112` | `suppliers.reg_num`/`ceo` | 부재 (정본 `business_number`/`contact_name`) | 사업자번호·대표자 저장/표시 안 됨 |
| 워크센터 hooks(IO/Item/Analyze) | `select('*')` 후 `amount`/`item_name`/`from_location`/`unit`/`code`/`sku`/`qr_code`/`udi` 등 매핑 | 다수 부재 | 전 화면 '-'/0 폴백 |
| `policies.ts:338` | `purchase_orders.requester_company/requester_department` | 부재 | 스코프 컬럼 참조 오류 |

→ 데스크톱 핵심 CRUD(inventory_logs/purchase_orders insert)는 대체로 정본 일치. 문제는 **워크센터 대시보드 hooks + 모바일 + AS/명세서**에 집중.

## 🔴 테마 2: 죽은 코드 (✅ import 0 확인)

| 파일 | 규모 | 비고 |
|------|------|------|
| `inventory-workflow-utils.ts` | 287줄 | import 0, inventory-utils와 글자단위 중복본 |
| `inventory-item-utils.ts` | 153줄 | workflow-utils만 import(그 자체가 死) → 사실상 死. inventory-utils 중복 |
| `재고현황뷰.tsx` hidden 블록 6개 + 종속 prop/상태 | ~350줄 | 영구 `hidden`(컨트롤바·알림배너·공급워크플로우·완료히스토리·헤더·유효기간센터) |
| `charts/ParetoChart.tsx`, `PriceTrendChart.tsx` | - | ✅ import 0. `InventoryInfoBanner`(return null)도 미사용 |
| 워크센터 placeholder 버튼/탭 다수 | - | onClick 미연결, AnalyzeWorkcenter 월마감/실사 placeholder |

## 🔴 테마 3: 데이터 무결성·보안

- **[E] 재고이관 비원자적 쓰기 (재고 증발 위험)**: `재고이관.tsx:262-408 handleTransfer` — 출발지 차감→목적지 증가→이력 기록을 독립 호출로 처리, 중간 실패 시 출발지만 차감. (소모는 `atomicStockConsumeWithLog` batch로 보호되는 것과 대조) → 트랜잭션/배치 필요.
- **[SEC] 납품확인서 stored XSS**: `납품확인서.tsx:68-100 printDelivery`가 supplier_name·notes·item.name을 이스케이프 없이 `document.write`.
- **[S] myCount 버그**: `StatusWorkcenter.tsx:47 useStatusData()` 인자 누락 → myCount 항상 0(칩 카운트와 결과 불일치).
- **[기타] FK 미연결**: 발주관리/PurchaseOrderModal 둘 다 `supplier_id` 미저장.

---

## 🟡 심각도 보통 (요약)

- **[D] 워크센터 4종 hook 보일러플레이트 ~720줄 반복**(EMPTY+useState+useEffect(Promise.all+catch)). `useWorkcenterFetch` 추출 가능.
- **[Q] 두 진실원(Two Sources of Truth)**: 영문 워크센터(읽기전용 더미 대시보드) vs 한글 뷰(실 CRUD)가 동일 데이터를 별도 표현 → 재고관리통합이 둘 다 보유.
- **[D] 발주 insert 로직 중복**: 발주관리 ↔ PurchaseOrderModal(공통 헬퍼 추출 권장, Modal은 부서별현황·모바일에서 쓰여 死 아님).
- **[Q] 거래처 CRUD 이중 구현**(거래처관리 ↔ 명세서관리). qty/minQty 헬퍼 3곳 중복(inventory-utils 있음에도).
- **[Q] 라벨/도착 불일치**: 재고현황뷰 "부서별 재고" 버튼이 소모기록(DepartmentConsumption)으로 이동.
- **[T] inventory.unit write 무fallback**(수정모달, 무음실패 위험). 다수 any/캐스팅.
- **[U] inventory-utils.ts(1057)** 3책임 혼재(헬퍼/fetch/305줄 트랜잭션), 500줄 2배 초과. console.error 3건.
- **[E] 워크센터 hook error 미확인 catch**(무음). 발주/AS 다수 catch 무피드백.

## 🟢 심각도 낮음 — 각 `_재고_W*.md` 참조

## 양호
- 데스크톱 inventory_logs/purchase_orders/자산등록 insert 컬럼 정본 일치. quantity/min_quantity 쌍 동시 갱신 정합성 유지.
- 외부 장비 키 하드코딩 없음(스캐너=키보드웨지, UDI/QR=mock). K 한글깨짐 0건(전 그룹).

## 최우선 Top 5
1. **[S]** 스키마 드리프트 수정: `inventory_transactions`→`inventory_logs`, `amount`→`quantity`, `inventory.unit`/`suppliers.reg_num` 처리 (모바일 입출고·워크센터 무음 불능 해소)
2. **[E]** 재고이관 원자적 배치 처리(재고 증발 방지)
3. **[SEC]** 납품확인서 XSS 이스케이프
4. **[X]** 죽은 코드 정리(workflow-utils 287·item-utils 153·재고현황뷰 hidden 350·charts)
5. **[S]** AS반품/명세서 테이블·컬럼 정합(서버 동기 복구) + 발주 supplier_id FK 연결
