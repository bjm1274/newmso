# ③ 추가기능 메뉴 — 전수조사 보고서

> 조사일: 2026-05-29 · 허브형 메뉴, ~45개 파일(약 20,000줄) · 5개 도메인 그룹 병렬 정독 + Opus 코드 재확인
> ✅ = Opus 코드 재확인 완료 · 조직도/근무현황/부서별재고는 인사·재고 메뉴에서 감사(중복 회피)

## 대상 그룹
- **A 허브/공통** (추가기능.tsx, 추가기능공통.tsx, 모바일 index/허브/data-hooks/외부웹팩스·주차/업무가이드/업무공유/MRI)
- **B OP체크** (OP체크.tsx 3787줄 + op-check-* 5 + OP체크/ 6 + 모바일 OP체크 4)
- **C 수술상담·퇴원심사**
- **D 인계노트·마감보고·직원평가**
- **E 입금실시간조회·ESL관리**

## 분류별 집계 (5그룹 합산, 약 102건)

| 분류 | 高 | 中 | 低 | 계 |
|------|----|----|----|----|
| D 중복 | 3 | 5 | 6 | 14 |
| S 문법/런타임(스키마) | 8 | 5 | 3 | 16 |
| K 한글깨짐 | 0 | 0 | 0 | 0 |
| X 죽은코드 | 4 | 2 | 8 | 14 |
| U 불필요 | 0 | 2 | 6 | 8 |
| Q 유사기능 | 2 | 5 | 3 | 10 |
| T 타입 | 0 | 4 | 9 | 13 |
| E 에러처리 | 2 | 3 | 6 | 11 |
| P 성능 | 0 | 3 | 5 | 8 |
| SEC 보안 | 4 | 1 | 3 | 8 |
| DBG 디버그 | 0 | 0 | 4 | 4 |
| **총계** | **23** | **30** | **53** | **106** |

> ⚠️ **이 메뉴의 지배적 패턴**: 모바일 화면(`모바일/추가기능/*` + `data-hooks.ts`)이 **D1 컷오버 전 옛 Supabase 스키마 기준**으로 작성되어, 미존재 테이블/컬럼 쿼리 → SQL 에러 → `catch`가 삼킴 → **"크래시 없는 무음 기능불능"**. 데스크톱 화면은 대부분 정본 스키마에 정합(폴백/조인 사용).

---

## 🔴 심각도 높음 (23건 → 핵심 12건 상세)

### [SEC] ✅ 가상계좌 웹훅 시크릿 토큰 평문 하드코딩 (클라이언트 번들 노출)
- **위치**: `app/main/기능부품/입금실시간조회.tsx:661`
- **확인**: `a382ddced410...e1b02` 64자 토큰이 `'use client'` 컴포넌트에 평문. 웹훅 라우트는 `process.env.VIRTUAL_ACCOUNT_WEBHOOK_TOKEN`으로 검증([virtual-account-webhook/route.ts:16](../../app/api/payments/virtual-account-webhook/route.ts:16))하는데, UI가 운영자에게 "이 토큰과 동일하게 설정"을 안내 → 실제 시크릿이 브라우저 번들에 공개. 토큰 보유자가 가짜 `DEPOSIT_CALLBACK` 입금 콜백 위조 가능.
- **조치**: UI에서 실제 토큰 문자열 제거(플레이스홀더로), 토큰 회전(rotate), 환경변수로만 관리.

### [SEC] ✅ 퇴원심사 환자 PII 테넌트 격리 불가
- **위치**: `퇴원심사.tsx:196-208 loadData`, `모바일/추가기능/data-hooks.ts:511 useDischargeReviews`
- **확인**: `discharge_reviews` 스키마에 `company`/`company_id` 컬럼 **없음**(patient_name·diagnosis·birth_date·gender·surgery_name 등 PII 보유). 회사 필터 없이 전체 100건 로드 → **타 법인 환자 차트 교차 노출**.
- **조치**: 테이블에 회사 스코프 컬럼 추가 + 필터, 또는 서버 API 경유로 RLS 적용.

### [SEC] EXTERNAL_LINKS 사내 IP·평문 HTTP 하드코딩
- **위치**: `추가기능공통.tsx:230-231` — `http://kmp0001103.iptime.org/...`(평문), `https://webfax.uplus.co.kr/m`. 데스크톱은 무검증 `<a href>`, 모바일은 `safeExternalUrl`+https 강제 — **정책 정반대**.
- **조치**: 환경변수+https 화이트리스트로 통일, IP 제거.

### [S] ✅ 미존재 테이블/컬럼으로 인한 모바일 기능 무음 실패 (다수)
| 기능 | 위치 | 문제 | 결과 |
|------|------|------|------|
| 모바일 수술상담 저장 | `모바일/추가기능/수술상담.tsx:202` | `op_consultations` 테이블 **부재**(✅) | 저장 무음 실패/큐 적체 |
| 모바일 마감보고 제출 | `모바일/추가기능/마감보고.tsx:42` | `daily_reports` 테이블 **부재**(✅, POLICY_REGISTRY 미등록→403) | 제출 버튼 상시 불능 |
| 모바일 마감보고 조회 | `data-hooks.ts:966 useClosingToday` | `daily_checks.eq('date')/('company')` 미존재 컬럼 | 일마감 탭 항상 빈 데이터 |
| 모바일 직원평가 작성 | `모바일/추가기능/직원평가.tsx:283` | 미존재 컬럼 6개 + 필수 `staff_id`/`category` 누락 | insert NOT NULL 위반/실패 |
| 모바일 인계노트 작성/조회 | `모바일/추가기능/인계노트.tsx:294`, `data-hooks.ts:371` | `note_scope`/`company` 미존재(데스크톱은 폴백, 모바일은 raw) | 저장 실패·빈 목록 |
| 모바일 근무현황 | `data-hooks.ts:277 useWorkNow` | `attendances.eq('date')`(정본 `work_date`) | 출근매칭 0건→전원 '근무중' |
| 모바일 OP 상태저장 | `OP체크상세.tsx:131`, `data-hooks.ts:848 setOpCardState` | `last_updated_at/by`·`checklist`·`company` 미존재(정본 `updated_at/by`·`prep_items`) | 체크/상태전환 영구 저장 안 됨 |

### [X] ✅ OP체크 대형 죽은 파일 2개 (~948줄)
- **위치**: `op-check-view-utils.ts`(551줄), `op-check-helpers.ts`(397줄) — 둘 다 **import 0건**(Grep 확인). `OP체크/checklist-helpers·patient-check-helpers·schedule-helpers`(실사용)와 **글자단위 동일한 3중 복제본**.
- **조치**: 두 파일 삭제. `op-check-types.ts`도 `ANESTHESIA_OPTIONS`만 남기고 정리.

### [D] OP체크 헬퍼 3중 복제 + 모바일 OP체크 화면 2벌
- **헬퍼 3중**: `op-check-helpers.ts`(죽음) ≡ `OP체크/*-helpers.ts`(실사용) ≡ `op-check-view-utils.ts`(죽음).
- **모바일 OP체크 2벌**: `기능부품/OP체크/모바일보드·상세시트`(정본 준수, 데스크톱이 위임) vs `모바일/추가기능/OP체크보드·상세·카드·메시지시트`(스키마 위반, 허브 진입) — **둘 다 라이브**. 상태모델도 불일치(정본 4개 vs 허브 '보류' 추가 5개).
- **조치**: 정본 준수 버전으로 일원화, 허브 버전 폐기/위임.

### [D/Q] ✅ 퇴원심사 모바일 화면 2벌 + status 체계 불일치
- **벌 A** `모바일/추가기능/퇴원심사목록·상세`(셸 진입, status `review_requested`/`rejected`/`approved`) vs **벌 B** `기능부품/퇴원심사/모바일목록·상세`(데스크톱 좁은뷰포트, status `approved`만). 데스크톱은 `pending`/`approved`만 인지 → 벌 A의 `rejected`/`review_requested` 미처리.
- **조치**: 한 벌로 통합, status 값 체계 통일.

### [E] 무음 데이터 손실 — 퇴원심사 createReview
- **위치**: `퇴원심사.tsx:287-297` — insert 실패 시 catch에서 `crypto.randomUUID()`로 **가짜 id 로컬 state에만 추가**, toast 없음. 사용자는 저장 성공으로 오인 → 새로고침 시 소실.

---

## 🟡 심각도 보통 (30건 — 요약)

### D/Q 중복·유사
- **[D]** 허브 카드 메타 PC/모바일 2벌(`FEATURE_CARDS` 10종 ↔ 허브 `modules` 12종, id 체계 불일치 `'OP체크'`↔`'opboard'`).
- **[D]** `withOptionalQueryFallback` 재구현(`OP체크.tsx:121`이 `op-check-utils.ts:237` import하고도 로컬 재정의).
- **[Q]** OP 메시지 발송 채널 이원화: 데스크톱 `chat_rooms`+`messages` vs 모바일 허브 `notifications` insert — 수신측 채팅 vs 알림 갈림.
- **[D/Q]** 입금조회 데이터 경로 이원화: 데스크톱 `/api` (인증·회사격리) vs 모바일 `supabase` 직접쿼리(S-1 버그 보유).
- **[Q]** `runAutoCompare`(퇴원심사) 누락/추가 비교를 3곳에서 각자 계산. `stayDays` 4곳 중복 정의.
- **[Q]** 외부주차/외부웹팩스 화면 쌍둥이(`ExternalSystemScreen`로 통합 가능). `safeExternalUrl`+`isAdmin` 두 파일 동일 정의.

### S/SEC/T
- **[S]** 모바일 `useDeposits` `eq('company')`(정본 `company_id`) → 회사격리 실패. status 무필터로 취소/대기건까지 합산 과대집계.
- **[SEC]** 수술상담 AI 프롬프트가 "환자 이름·주민번호 그대로 포함" 지시 → 결과를 localStorage 평문 저장(PII 브라우저 잔류).
- **[T]** 데스크톱 컴포넌트 `user: any`/`staffs: any[]` 다수(퇴원심사·마감보고·직원평가시스템·입금·ESL — 모바일은 ErpUser 타입화). `data-hooks` 미존재 컬럼 다중 폴백 매핑(스키마 드리프트 은폐).

### E/P
- **[E]** `data-hooks` 조회 실패를 `console.warn`로 삼켜 사용자 무피드백(useHandoverNotes/useStaffEvaluations/useClosingToday). 퇴원심사 템플릿 CRUD catch 무피드백.
- **[P]** 입금실시간조회 15초 폴링이 검색어 입력마다 인터벌 재생성+즉시 재조회(JM2 위반) → 디바운스 필요.
- **[P]** OP보드 5초 폴링 + 허브 15초 폴링 + `useOpCheckBoardStream` 5초 폴링 동시 존재(모바일 2구현 각자 폴링). hero는 카운트만 필요한데 board_posts(100)+op_patient_checks(200) 반복.

### U
- **[U]** `useClosingToday`의 daily_checks "체크리스트" 해석 분기 전체가 정본과 무관(동작 불가 죽은 분기).
- **[U]** 모바일 수술상담 전환율 72%·샘플 카드 전부 하드코딩(데모 잔재, 운영 오인).

---

## 🟢 심각도 낮음 (53건 — 핵심만)

- **[X]** `external:'iframe'` 분기 도달 불가(`추가기능.tsx:273`, FEATURE_CARDS에 iframe 없음). `accentClass` 필드 전체 미사용(주석도 자인). `op-check-types.ts` 컬럼상수 대부분 미사용. `OPCardIcon`/`KpiCard`/`TimeBox` export 미사용. 입금 `stats` useMemo 미사용.
- **[X]** 입금 **엑셀 업로드 파이프라인 전체 데드**(`parseExcelFile`·`fileImportRef`·드롭존 UI 미연결, "내보내기" 버튼 영구 비활성+핸들러 라벨 불일치).
- **[U]** 입금 결제수단 `<select>` 무동작(value/onChange 없음). 허브/업무가이드 검색 버튼·업무공유 북마크/공유 버튼 onClick 미연결(死버튼).
- **[S]** `퇴원심사.tsx:1251` 잘못된 Tailwind `bg-purple-500/10/50`(이중 opacity, 무효). `Gemini 3` 배지(실제 `gemini-2.5`).
- **[E]** 입금 삭제/일괄등록/ESL 로드 실패 무음. 수술상담 localStorage 파싱 catch 완전 무음.
- **[T]** `as unknown as QueryResult<T>`(OP체크 9회, compat 관례), ESL `Map<string,any>`·`catch(err:any)` 다수.
- **[DBG]** `console.warn/error` 운영 잔존 다수(data-hooks 전 훅, 입금, ESL, 퇴원심사). `get-binding.ts:27 eval("require(...)")`(서버 전용, 린트 경고).
- **[SEC]** ESL은 API 키 없음(Web Bluetooth 로컬 직결 — 양호). discharge-review가 API 키를 URL 쿼리스트링 전송(헤더 권장).

---

## 발견 없음 / 양호
- **K 한글깨짐**: 5그룹 전부 0건.
- 데스크톱 OP체크(`OP체크.tsx`)·마감보고·직원평가시스템·입금 데스크톱: 정본 스키마 정합, 에러처리·로깅 양호.
- ESL `handover_notes` 의존은 `content` 마커 파싱 방식이라 정상 동작.

## 최우선 Top 6
1. **[SEC]** 웹훅 토큰 평문 제거 + 회전 (`입금실시간조회.tsx:661`) — 즉시
2. **[SEC]** 퇴원심사 환자 PII 회사 격리 (`discharge_reviews` 회사 컬럼/API)
3. **[S]** 모바일 다수 기능 스키마 불일치 일괄 수정(수술상담·마감보고·직원평가·인계노트·근무현황·OP상태저장)
4. **[X]** OP체크 죽은 파일 2개(~948줄) + 입금 엑셀 데드 제거
5. **[D/Q]** OP체크·퇴원심사 모바일 2벌 일원화
6. **[E]** 퇴원심사 createReview 무음 데이터 손실 수정
