# 4차 전수조사 — ③ PC 인사관리 트리 (2026-06-10)

> 범위: `인사관리서브/`(90) + `인사관리워크센터/`(64) = 154파일 전수, 52파일 정독
> ✅표시 = Opus 코드 직접 재확인 완료

## CRITICAL

### [C-1] 근무표 반려 알림 영구 미발송 — `if (false && ...)` ✅
- 파일: `근태기록/근태관리메인.tsx:755`
- `if (false && request.requested_by) { await supabase.from('notifications').insert(...) }` — 반려 처리 시 신청자 알림이 **어떤 조건에서도 발송되지 않음**. 디버깅용 비활성화가 커밋된 것으로 추정. 즉시 복구 또는 의도라면 코드 삭제+사유 주석.

### [C-2] KPI 카드 그리드 영구 비렌더 — `{false && ...}` ✅
- 파일: `구성원현황.tsx:2019`
- 면허등록인원·계약직인원·부서수 KPI 블록이 `{false &&}`로 봉인. 죽은 JSX ~수십 줄.

### [C-3] handleApproveEss 죽은 함수
- 파일: `구성원현황.tsx:925`
- 정의만 있고 호출 0 (JSX는 `handleApproveEssSafe`만 사용). grep 전수 확인.

## MAJOR

| # | 파일 | 분류 | 내용 |
|---|------|------|------|
| M-1 | `구성원현황.tsx:1027` | 코드오류 | useEffect가 `편집모드`·`신규직원.연차총개수`를 읽으면서 deps에는 `working_hours_per_week`만 — stale closure로 연차 자동 덮어쓰기 위험 |
| M-2 | `인사관리워크센터/WelfareWorkcenter/WelfareCalendar.tsx:49` | 코드오류 | todayIso를 `new Date()` 로컬 계산 — KST 보정 없음(±1일). 타 파일은 `getKoreanTodayString()` 사용 |
| M-3 | `구성원현황.tsx:1682-1762` ↔ `MemberWorkcenter/StaffDrawer.tsx:104-178` | 중복 | 직원 소프트/하드 삭제 로직 2벌 — 대상 테이블·순서 동일, 한쪽 수정 시 불일치 위험 |
| M-4 | `급여명세/급여정산.tsx:122-146, 196-218` | 중복 | 퇴직월 판별(말일 여부 분기) 동일 로직 2곳 |
| M-5 | `급여정산.tsx:976`+`중간정산.tsx:473`+`비과세한도체크.tsx:46` | 중복 | 야간근로 비과세 한도 240,000원 3중 하드코딩 — 법령 개정 시 누락 위험. `lib/tax-free-limits.ts`로 통합 |
| M-6 | `근태기록/근태유틸.ts:9` ↔ `간호근무표.tsx:41` | 중복 | `isWardDept` 정규식 2벌 |
| M-7 | `근태기록/근태관리메인-내부유틸.ts:11` ↔ `근태유틸.ts:13` | 중복 | ROSTER_CREATOR/APPROVER_POSITIONS 상수 2벌 (jscpd: 두 유틸 파일 간 ~303줄 중복) |
| M-8 | `스마트서류제출.tsx:57-60` | 의미없는 코드 | `.limit(0)` 쿼리 실행 후 결과 미사용 — 잊힌 개발 잔재 |

## MINOR

| # | 파일 | 내용 |
|---|------|------|
| m-1 | `구성원현황.tsx:330-343` | 주석 처리된 useMemo 블록(닫는 괄호 2개 문법 깨진 채) 방치 |
| m-2 | `구성원현황.tsx:681,689,980,1944` | exhaustive-deps 무시 4회 — 사유 주석 없음 |
| m-3 | `급여정산.tsx:196` | filteredStaffs useMemo 미적용 — 매 렌더 재계산 + effect 재실행 유발 |
| m-4 | `급여정산.tsx:167` | 원천징수율 effect deps에 yearMonth 누락 — 연도 변경 시 미갱신 |
| m-5 | `중간정산.tsx:472,497` | 생산직 비과세 요건·퇴직소득 분류과세 TODO 미구현 상태로 운영 — 세액 오차 가능 |
| m-6 | `계약문서/계약서양식편집.tsx:40` | loadTemplate effect deps 누락 |
| m-7 | `급여명세/최저임금체크.tsx:47`, `총인건비예측.tsx:12` | user prop 선언만, 미사용 |
| m-8 | `급여명세/미지급수당알림.tsx:33-39` | 야간시간 1분 단위 루프(12h=720회) — 구간 교집합 O(1) 대체 가능 |
| m-9 | `교육내역/education-types.ts:31` | @deprecated EducationCategory 장기 방치 — 소비 코드는 전부 현역 |

## 연계 사항

- `교육내역/education-utils.ts:150,202`의 `education_completions`, `간호근무표.tsx:226`의 `nurse_schedules`, `hr-history-ledger.ts:151`의 `work_type_change_history` — **D1 미존재 테이블 참조(무음 기능불능)**. 상세는 [07_SQL_DB.md](07_SQL_DB.md).

## 오탐 기각

- ~~`근태관리메인.tsx:256` daysInMonth TDZ → 날짜 "undefined" 오염~~ ✅**기각** — `fetchAttendance`는 렌더 후 호출되는 일반 함수이고 JS 클로저는 바인딩을 캡처하므로 호출 시점엔 852행 `const daysInMonth` 초기화 완료. 정상 동작 확인. (선언 위치가 사용처보다 한참 뒤라는 가독성 문제만 존재)
