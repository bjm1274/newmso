# 4차 전수조사 — ⑧ 죽은코드·중복·저장소 위생 (2026-06-10)

> 근거: jscpd 리포트(2026-06-08, 3.1MB) 파싱 + git ls-files 대조 + grep 참조 검색
> ✅표시 = Opus가 git ls-files로 직접 재확인 완료

## 중복 코드 통계 (jscpd)

| 항목 | 값 |
|------|----|
| 전체 | 438,905줄 / 1,404파일 중 중복 41,292줄 — **9.41%** (클론 2,020쌍) |
| TypeScript | 7.06% (8,371줄) |
| TSX | 2.38% (3,830줄) |
| SQL | **27.83%** (7,246줄) — 대부분 migrate-d1 산출물·supabase 히스토리 |
| JSON | 20.67% — drizzle 스냅샷 |

소스 코드 주요 중복쌍 (산출물 제외):

| 중복 | 파일쌍 |
|------|--------|
| ~303줄 | `근태관리메인-내부유틸.ts` ↔ `근태유틸.ts` |
| ~249줄 | `메신저액션서브/EmojiPicker.tsx` ↔ `모바일/채팅/이모지피커.tsx` |
| ~235줄 | `모바일/결재/연차신청폼.tsx` ↔ `일반기안폼.tsx` |
| ~168줄 | `마이페이지/서류제출.tsx` ↔ `인사관리서브/스마트서류제출.tsx` |
| ~145줄 | `api/d1/mutate/route.ts` ↔ `api/d1/query/route.ts` |
| ~121줄 | `api/board/upload/route.ts` ↔ `api/chat/upload/route.ts` |
| ~118줄 | `구성원현황.tsx` ↔ `MemberWorkcenter/StaffDrawer.tsx` |

## CRITICAL — git 오염 (전부 ✅ git ls-files 재확인)

### [C-1] 루트 정크 파일 20개 git 추적 중
Playwright 로그 9종(`test_output.txt`, `submissions_output*.txt` 5종, `single_test_output*.txt` 2종, `walkthrough_output.txt`), 임시 파일 6종(`_tmp_ascii/grep/idx/status/tok.txt`, `__skel.txt`), 일회성 스크립트 4종(`test_db.js`(27바이트), `test_emoticon.js`, `test-insert.mjs`, `query_poll.py`), `query.sql`. **전부 소스 참조 0** (grep 확인). `.gitignore`의 `__tmp_*`/`tmp*/` 패턴이 이 파일들과 불일치해 누락. → `.gitignore` 패턴 보강 + `git rm --cached`.

### [C-2] 바이너리 산출물 4개 git 추적 중 (4.1MB+)
- `scripts/migrate-d1/output/d1_schema.sqlite` (2.06MB), `d1_schema_for_drizzle.sqlite` (2.05MB) — build-sqlite.mjs가 생성하는 산출물
- `scripts/__pycache__/*.pyc` 2개 — .gitignore에 패턴은 있으나 기추적 파일이라 무시 안 됨
→ `git rm --cached` 필요.

### [C-3] 완료된 프로토타입 78파일 git 추적 중
- `scratch_zip/` 40개 — 초기 모바일 프로토타입 HTML/JSX (handoff 이전 버전, 내용 중복)
- `handoff/` 38개 — 모바일 redesign handoff 문서+JSX (redesign 완료로 역할 종료)
→ 보존 가치 판단 후 `git rm -r` 또는 `docs/_archive` 이동.

## MAJOR

| # | 항목 | 내용 |
|---|------|------|
| M-1 | `scripts/migrate-d1/output/*.sql` | d1_schema_final ↔ d1_schema_missing ~2,595줄 중복 — 산출물이므로 git 제거 시 SQL 중복률 대폭 해소 |
| M-2 | PC↔모바일 쌍둥이 컴포넌트 | EmojiPicker 등 — 공통 훅 추출로 절반 이상 제거 가능 (정책상 이중 트리 유지하더라도 로직 공유) |
| M-3 | `lib/db/migrations/meta` 스냅샷 중복 | 0000↔0010 67클론 — 마이그레이션 체인 정리(07 문서 C-2)와 함께 재생성 |
| M-4 | upload 라우트 2벌·d1 라우트 2벌 | 공통 핸들러/헬퍼 추출 후보 |

## MINOR / 비추적(이미 무시됨 — 디스크 정리만 해당)

| 디렉토리 | 상태 |
|----------|------|
| `scratch/`(135), `tmp/`(135), `tmp_orphan/`(11), `backups/`(15), `analysis_artifacts/`(819 PNG) | git 비추적 ✅ — 로컬 디스크 정리만 선택적으로 |
| `data/2026_monthly_withholding_table.json` | 추적 중이나 **급여 계산에 실사용** — 유지 (정당) |
| `docs/` 214파일 | 정당한 문서 — 유지 |

## 죽은 소스 파일 메모

- 이름 패턴(old/backup/copy/v1/unused) 기반 죽은 파일은 **미발견** — 3차 수정에서 33,000줄 삭제가 유효했음을 시사.
- `lib/backup-config.ts`·`lib/backup-cron.ts`는 이름과 달리 현역(백업 기능 구현체) — 오인 삭제 금지.
- 빈 폴더: `app/main/기능부품/근태기록/` ([05_PC_공통기타.md](05_PC_공통기타.md) M-5).
- 개별 죽은 심볼(함수·state 단위)은 각 영역 문서 참조.
