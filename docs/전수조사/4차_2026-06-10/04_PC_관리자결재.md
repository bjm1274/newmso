# 4차 전수조사 — ④ PC 관리자·전자결재·마이페이지 (2026-06-10)

> 범위: `관리자워크센터/`(20) + `관리자전용서브/`(35) + `전자결재서브/`(37) + `마이페이지/`(26) = 135파일 전수 정독
> ✅표시 = Opus 코드 직접 재확인 완료

## CRITICAL

### [C-1] 운영 화면에 하드코딩 더미 수치 — ExecDashboard / FormsWorkcenter
- 파일: `관리자워크센터/ExecDashboard.tsx:64-80, 136-145`, `관리자워크센터/FormsWorkcenter.tsx:43-58`
- 경영 KPI("5월 매출 182.4M원", "전월 동기 +4.2%")·PnL 차트·결재양식 14종 카드가 **DB 연동 없는 정적 더미 데이터**로 운영 화면에 표시. 경영진이 실데이터로 오인할 위험이 있어 표시 차단 또는 실연동 필요. (⑤ 공통 트리 에이전트도 동일 건 중복 보고 — 단일 건으로 집계)

### [C-2] AuditWorkcenter PREVIEW_LOG_ROWS 영구 빈 배열
- 파일: `관리자워크센터/AuditWorkcenter.tsx:160-196`
- `const PREVIEW_LOG_ROWS = []` 고정 → "주요 로그 없음" 항상 표시, map 분기는 영구 죽은 코드. 실데이터 연동 누락.

### [C-3] 직원권한통합 fetchStaffs no-op
- 파일: `관리자전용서브/직원권한통합.tsx:130-132, 185`
- 본문이 주석뿐인 빈 async 함수를 `await fetchStaffs()`로 호출. 갱신을 기대하는 호출부 의도와 불일치(실제 갱신은 AppDataContext가 담당) — 제거 및 호출부 정리.

## MAJOR

| # | 파일 | 분류 | 내용 |
|---|------|------|------|
| M-1 | `전자결재서브/ApprovalDetailModal.tsx:~300` | 코드오류 | 승인/반려 실패·취소 시에도 `onClose()` 무조건 실행 — 실패 toast와 동시에 모달 닫힘 |
| M-2 | `전자결재서브/ApprovalMetaPanels.tsx:490-519, 657-694` | 중복 | resolveBand/getShortLabel/getStyleForBand 같은 파일 내 2중 정의(HTML용/컴포넌트용) |
| M-3 | `시스템마스터센터-modules/PermissionDiffPanel.tsx` ↔ `직원권한통합/PermissionDiffPanel.tsx` | 중복 | 동명 export 컴포넌트 2개(기능 상이) — import 혼동 유발, 개명 권장 |
| M-4 | `관리자전용서브/재무대시보드.tsx:7-35` | 의미없는 코드 | period 상태(Q1~Q4)가 fetch에 미반영 — 버튼 클릭=동일 쿼리 반복. budgets state는 setter 호출 0으로 영구 빈 배열 |
| M-5 | `마이페이지/index.tsx:326` | 코드오류 | notifications insert에 `user_id: 'system_admin'` 고정 리터럴 — FK 위반 가능(TODO 인정 주석 존재) |
| M-6 | `관리자전용서브/팝업창관리자.tsx:31-61` | 중복+오류 | isAllowedFile ↔ validatePopupFileSelection 거의 동일 2벌 + 한쪽 안내문 "25MB"가 실제 한도 200MB와 불일치 |
| M-7 | `전자결재서브/관리행정양식.tsx:21-28, 30` | 죽은 코드 | hospitalGuides(`void` 억제)·hospitalDepts 선언만, 사용 0 |
| M-8 | `마이페이지/index.tsx:66-67, 127-129` | 죽은 코드 | selectedCo/selectedCompanyId props 선언만, isMobile 훅 호출 후 미사용 |
| M-9 | `관리자전용서브/예산관리.tsx:86-88` | 코드오류 | 예산·집행 데이터 LocalStorage 전용 저장 — 사용자·기기 간 공유 불가(관리 기능인데 개인 브라우저 의존) |

## MINOR

| # | 파일 | 내용 |
|---|------|------|
| m-1 | `전자결재서브/useApprovalActions.ts:228-250` | 반려 클라 폴백에서 동일 update 즉시 재시도(1차 실패 시에만) — 무의미한 재시도. ✅검증: 이중 기록은 발생하지 않음(에이전트의 critical 판정 강등). 근본 문제는 서버 전환 실패 시 클라가 직접 DB를 쓰는 폴백 설계(3차 보류 항목 잔존) |
| m-2 | `전자결재서브/useApprovalBulkActions.ts:65-80` | processFinalApprovalOnServer 반환만 되고 호출처 0 |
| m-3 | `관리자전용서브/직원권한통합.tsx` | setPermissionReview 선행 후 저장 실패 시 롤백 없음 |
| m-4 | `관리자워크센터/AuditWorkcenter.tsx:208-209` | AccessAuditLog+AuditLogViewer 동시 렌더 — 대형 쿼리 2건 동시 발생 |
| m-5 | `전자결재서브/연차사용계획서양식.tsx:38-45` | props 함수가 deps에 직접 — 상위 리렌더마다 effect 재실행 |
| m-6 | `전자결재서브/수리요청서양식.tsx:6-7` | setExtraData 강제 캐스팅 + `(prev: any)` 패턴 |
| m-7 | `관리자워크센터/CompanyWorkcenter/CompanyDocsTab.tsx:23-43` | limit(100) 후 클라 필터 — DB 레벨 `.eq(company_name)` 누락 |
| m-8 | `관리자전용서브/팀관리.tsx:21-25` | toUIDivision 변환 방향 불명(총무부→운영본부?) — 사유 주석 필요 |
| m-9 | `재무대시보드.tsx:15` | budgets 죽은 state (M-4와 연계) |
| m-10 | `마이페이지/서류제출.tsx` ↔ `인사관리서브/스마트서류제출.tsx` | jscpd ~168줄 중복(12클론) — 공통 로직 추출 후보 |
| m-11 | `관리자전용서브/경영분석/모바일대시보드.tsx` | 한글명 아닌 영문 lint 오탐 1건 잔존 영역 — 컴포넌트명 정책 정리 시 함께 |
