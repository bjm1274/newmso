# 4차 전수조사 — 요약 (2026-06-10)

> 조사 방식: 8개 영역 병렬 정독(Sonnet 서브에이전트) + 객관 검사(tsc/ESLint/jscpd) → Opus가 critical/major 전건 코드 재검증(오탐 4건 기각·강등 포함)
> 범위: app(723파일) · lib(196) · api(98라우트) · 모바일(132) · D1/Supabase 마이그레이션 · 저장소 위생 — 소스 약 28만 라인
> ⚠️ 이 문서는 **기록 전용**. 코드 수정은 별도 지시 시 진행.

## 객관 검사 결과

| 검사 | 결과 |
|------|------|
| `npx tsc --noEmit` | 본체 0건. `tests/e2e/real-chat-flicker.mobile.spec.ts`, `real-db.desktop.spec.ts` 2개만 제거된 `@supabase/supabase-js` 참조로 컴파일 불가 |
| `npx eslint app lib` | 1,145건 (에러 237 / 경고 908) — 아래 분해 참조 |
| jscpd (2026-06-08) | 전체 중복률 9.41% (TS 7.1%, TSX 2.4%, SQL 27.8%) |

ESLint 분해:
- **871건** `no-restricted-syntax` — `supabase.from()` 직접 호출. lib/fetcher 마이그레이션 미완(점진 전환 중 명시됨)
- **222건** `react-hooks/rules-of-hooks` 에러 — **전부 한글 컴포넌트명(`게시판`, `채팅` 등) 오탐**. 단, 이 오탐 더미 때문에 진짜 훅 순서 위반이 생겨도 린트로 잡을 수 없는 **사각지대** 상태 (별도 조치 필요: 컴포넌트 영문명+한글 alias 또는 룰 설정)
- 기타 52건 (`no-unescaped-entities` 12, `prefer-const` 8, 미사용 eslint-disable 36 등)

## 발견 총계 (Opus 재검증 후)

| 영역 | critical | major | minor | 비고 |
|------|----------|-------|-------|------|
| ① API 라우트 (100파일) | 2 | 10 | 10 | 오탐 1 기각 — [01_API라우트.md](01_API라우트.md) |
| ② 라이브러리 (195파일) | 0 | 6 | 11 | critical 1→major 강등 — [02_라이브러리.md](02_라이브러리.md) |
| ③ PC 인사관리 (154파일) | 3 | 8 | 8 | 오탐 1 기각 — [03_PC_인사관리.md](03_PC_인사관리.md) |
| ④ PC 관리자·결재 (135파일) | 3 | 9 | 11 | critical 1→minor 강등 — [04_PC_관리자결재.md](04_PC_관리자결재.md) |
| ⑤ PC 공통·기타 (480파일) | 1 | 8 | 14 | [05_PC_공통기타.md](05_PC_공통기타.md) |
| ⑥ 모바일 (133파일) | 3 | 19 | 15 | PC스텁 14+ — [06_모바일.md](06_모바일.md) |
| ⑦ SQL·DB | 4 | 6 | 4 | 오탐 1 기각·1 축소 — [07_SQL_DB.md](07_SQL_DB.md) |
| ⑧ 죽은코드·중복·위생 | 3 | 4 | 3 | [08_죽은코드_중복_위생.md](08_죽은코드_중복_위생.md) |
| **합계** | **19** | **70** | **76** | **165건** |

## 🔴 최우선 조치 (Critical — 전건 Opus 코드 재검증 완료)

1. **[보안] `/api/*`는 middleware 보호 밖** — [middleware.ts](../../../middleware.ts)는 `/main/*`만 검사. 각 라우트가 자체 인증해야 하는 구조인데 **`/api/admin/annual-leave/sync`는 인증이 전혀 없음**(비로그인 외부 호출로 임의 직원 연차 재계산 가능). 전 라우트 인증 유무 일제 점검 필요. ✅검증
2. **[보안] 채팅방 PATCH 수평 권한 상승** — [app/api/chat-rooms/[id]/route.ts:52](../../../app/api/chat-rooms/[id]/route.ts) 헤더 주석에는 멤버십 검증 정책이 적혀 있으나 **구현이 없음**. 로그인한 누구나 임의 방의 이름·멤버·타입 변경 가능. ✅검증
3. **[무음 기능불능] D1 미존재 테이블 5종 런타임 참조** — `education_completions`(교육내역), `email_queue`(급여명세서 이메일발송), `leave_policies`(연차정책 탭), `nurse_schedules`(간호근무표), `work_type_change_history`(근무유형이력). schema.ts·마이그레이션·실적용 스키마(d1_schema_final.sql) 모두 부재 확인 → 해당 기능은 호출 즉시 "no such table". ✅검증 (에이전트가 9종으로 보고했으나 4종은 실존 — 오탐 정정)
4. **[인프라] D1 마이그레이션 체인 재현 불가** — `0000_lovely_alice.sql` 전체 주석처리(+빈 컬럼 인덱스 5개), `_journal.json`에 idx 0·9·10만 존재(0001~0008·0011~0012 누락), 0010 태그 `married_stardust` ↔ 실파일명 불일치. 새 환경 구축이 공식 체인으로 불가능하고, 부족분을 **`/api/d1/mutate`·`query`가 매 요청 런타임 DDL로 때우는 중**(C-2/C-3). 마이그레이션 정리 + 런타임 DDL 제거 필요.
5. **[기능 결함] 근무표 반려 알림 영구 미발송** — [근태관리메인.tsx:755](../../../app/main/기능부품/인사관리서브/근태기록/근태관리메인.tsx) `if (false && request.requested_by)` — 반려해도 신청자가 알림을 못 받음. ✅검증

## 메뉴 횡단 핵심 이슈

- **모바일 "PC 우회 스텁" 14건+** — 정책(모든 기능 모바일 동작) 직접 위반: 증명서 발급 4종·계약문서 PDF·조직도·직원평가·마감보고·결재자변경·퇴원심사 코멘트·수술상담 녹음·재고 예측/AS·UDI·구성원 사진첨부 등. 상세 목록은 [06_모바일.md](06_모바일.md).
- **가짜 UI(no-op)**: 모바일 채팅방 알림 토글(onChange 없음, 항상 켜짐 표시)·채팅방 나가기(DB 미처리)·"실시간 연결됨" 하드코딩 ✅검증. PC 쪽 ExecDashboard·FormsWorkcenter는 **하드코딩 더미 수치("5월 매출 182.4M원")가 운영 화면에 표시**.
- **푸시 파이프라인 우회 잔존**: `useSupplyWorkflow.ts` 3곳이 `notifications` 직접 insert → 앱 닫힘 상태 푸시 미발송 (3차 조사의 동일 패턴 잔존).
- **린트 사각지대**: 한글 컴포넌트명 때문에 `rules-of-hooks` 사실상 무력화(222건 오탐 속에 진짜 위반 식별 불가).
- **중복 코드 구조 패턴**: PC↔모바일 쌍둥이(EmojiPicker ~249줄 등), `pickAvatarTone` 5중, 직원삭제 로직 2중, 알림 TYPE_CFG 3중, 야간 비과세 한도 24만원 3중 하드코딩(법령 개정 리스크).
- **저장소 위생**: 루트 정크 20개 파일 git 추적 중, `.sqlite` 4.1MB·`.pyc` 바이너리 추적, `handoff/`+`scratch_zip/` 78개 프로토타입 추적. [08_죽은코드_중복_위생.md](08_죽은코드_중복_위생.md).
- **사용자 노출 인코딩 깨짐**: [GlobalSearch.tsx:162](../../../app/components/GlobalSearch.tsx) `'?쒕ぉ ?놁쓬'`(="제목 없음" 모지바케)이 검색 결과에 그대로 표시 ✅검증.

## 오탐 정정 기록 (이번 차수에서 기각/강등된 항목)

| 항목 | 에이전트 판정 | 재검증 결과 |
|------|--------------|-------------|
| transcribe 인증 조건 역전 (`auth.status`) | major | **기각** — 성공 시 `status: null`이라 동작 동일 (lib/server-extra-feature-access.ts 확인) |
| `leave_requests`·`license_continuing_education`·`medical_devices`·`message_bookmarks` 미존재 + realtime ALLOWED_TABLES 오류 | critical | **기각** — 4종 모두 schema.ts와 d1_schema_final.sql에 실존 |
| 근태관리메인 `daysInMonth` TDZ로 날짜 오염 | critical | **기각** — 클로저는 바인딩 캡처, 호출 시점(렌더 후)엔 초기화 완료. 동작 정상 (구조만 취약) |
| useApprovalActions 반려 2중 write | critical | **minor 강등** — 2차 update는 1차 실패 시에만 실행되는 동일 쿼리 재시도. 이중 기록 없음. 실제 문제는 클라 폴백 자체(기존 보류 항목) |
| mutation-builders `filter()` 연산자 붕괴 | critical | **major 강등** — 결함 실재하나 non-eq/in 호출처 현재 0곳(grep 확인). 무음 지뢰로 분류 |
| ai/chat 키 접두사 하드코딩 | major(시크릿 노출) | **재해석** — Firebase 웹 API 키는 NEXT_PUBLIC 공개값. 실제 문제는 실키 미설정 시 mock 응답으로 장애 은폐되는 sentinel 설계 |

## 1~3차 대비 변화

- 3차 지적 `gemini-3.5-flash` 오타 모델명 → **수정 확인됨** (현재 미검출).
- 3차 수정분(스키마 정합·죽은코드 33,000줄 삭제·God 분리)은 이번 조사에서 재검출되지 않음 — 수정 유효.
- 3차 "보류" 항목 중 잔존 확인: 결재 클라 폴백 권한 서버화, `select('*')` 다수, PC↔모바일 이중 구현.
- 신규 발견 다수는 그간 추가된 코드(워크센터·모바일 트리·D1 라우트)에 집중.

## 권장 조치 순서

1. **즉시(보안)**: ① `/api/admin/annual-leave/sync` 인증 추가 ② chat-rooms PATCH 멤버십 검증 ③ API 전 라우트 인증 점검 체크리스트
2. **이번 주(기능)**: ④ 미존재 테이블 5종 — 테이블 신설 또는 기능 비활성 결정 ⑤ `if(false&&)` 반려 알림 복구 ⑥ GlobalSearch 모지바케 수정 ⑦ useSupplyWorkflow 푸시 파이프라인 경유
3. **이번 달(인프라)**: ⑧ D1 마이그레이션 체인 정리 + 런타임 DDL 제거 ⑨ git 위생(정크 20+바이너리 4+프로토타입 78 `git rm --cached`) ⑩ 모바일 PC스텁 해소 로드맵
4. **점진**: 중복 통합(아바타톤·삭제로직·비과세상수), `supabase.from()` 871건 fetcher 전환, 한글 컴포넌트 린트 대책
