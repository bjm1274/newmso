# 4차 전수조사 — ⑤ PC 공통·기타 트리 (2026-06-10)

> 범위: `기능부품/` 루트 85파일 + 메신저·게시판·알림·재고·OP체크·조직도·퇴원심사 등 하위 + `app/main` 루트 + `app/components` + `app/login` + `app/share-target` = 480파일 훑기, ~95파일 정독
> ✅표시 = Opus 코드 직접 재확인 완료

## CRITICAL

### [C-1] GlobalSearch 인코딩 깨진 문자열 사용자 노출 ✅
- 파일: `app/components/GlobalSearch.tsx:162`
- `title: post.title || '?쒕ぉ ?놁쓬'` — "제목 없음"의 CP949 모지바케가 폴백 문자열로 그대로 커밋되어 제목 없는 게시물 검색 시 사용자에게 깨진 글자가 표시됨.

## MAJOR

| # | 파일 | 분류 | 내용 |
|---|------|------|------|
| M-1 | `알림시스템.tsx:1640` ✅ | 코드오류 | `{ pollIntervalMs: 3_000 }` 실값 3초 vs 인라인 주석 "30000ms" — 10배 불일치. 폴백 폴링 시 알림 테이블 3초 간격 조회(성능)·주석 오신뢰 위험. 의도 확인 후 한쪽 정정 필수 |
| M-2 | `알림시스템/settings.ts:29-38` ↔ `알림시스템/filter-helpers.ts:9-18` | 중복 | normalizeKeywordList 동일 구현 2벌 |
| M-3 | `메신저메시지서비스.ts:20-29` ↔ `메신저메시지액션워크플로훅.ts:32-42` | 중복 | buildForwardedMessageContent 2벌 — 전달 메시지 포맷 SSOT 위반 |
| M-4 | `알림시스템/ui-config.ts` ↔ `알림인박스.tsx:130-144` ↔ `알림센터.tsx:35-47` | 중복 | 알림 타입 아이콘·컬러·라벨 3중 정의 — 타입 추가 시 3곳 수정 |
| M-5 | `기능부품/근태기록/` (빈 폴더) | 죽은 코드 | 파일 0개 빈 디렉토리 — 실구현은 `인사관리서브/근태기록/`. 삭제 |
| M-6 | `메신저공통.tsx:60-63` ✅ | 의미없는 코드 | containerClass 삼항 양 분기 동일 문자열 — tone 무효 |
| M-7 | `app/main/inventory-utils.ts:244` ↔ `재고관리서브/물품등록.tsx:54-56` | 중복 | normalizeInventoryUnit 2벌 (물품등록은 같은 모듈서 딴 함수는 import 중) |
| M-8 | `마이페이지/출퇴근기록/checkin-utils.ts:81` ↔ `출퇴근기록/index.tsx:66-69` | 중복 | buildDateWithTime 2벌 (이미 같은 유틸 import 중인데 이 함수만 재구현) |

## MINOR

| # | 파일 | 내용 |
|---|------|------|
| m-1 | `useChatTypingD1.ts:8,15` | 주석 "2.5초" vs 실값 5000ms 불일치 |
| m-2 | `메신저방전환훅.ts:47,53,56` | 언더스코어 미사용 파라미터 3개 — 호출부 정리 기회 |
| m-3 | `메신저방전환훅.ts:73` | localStorage 키 직접 조립(STORAGE_KEYS 미사용) |
| m-4 | `알림센터.tsx:57,60,126` | user/notification/state 전반 any |
| m-5 | `알림시스템.tsx:1725` | deps에 `user?.permissions` 객체 참조 — 과도 재실행 위험 |
| m-6 | `알림시스템.tsx:~1840, ~1876` | visibilitychange 리스너 2중 등록 — 통합 가능 |
| m-7 | `메신저구독훅.ts:246` | SSE 폴백 시 chat-global-messages 2초 폴링 |
| m-8 | `lucide-shim.tsx` | import 1곳뿐인 간접 계층 |
| m-9 | `GlobalSearch.tsx:19-24` | 인터페이스 전체 any |
| m-10 | `GlobalSearch.tsx:62-108` | 함수 반환 useMemo — useCallback 의미 오용 |
| m-11 | `재고관리서브/{재고실사,발주관리,UDI관리}.tsx` | any 20개+ (types.ts 존재하나 미활용) |
| m-12 | `재고관리서브/발주관리.tsx:125-128` | effect 내 비안정 함수 호출, deps 누락 |
| m-13 | `app/components/ErrorBoundary.tsx:20-26` | `(window as any).__errorLog` 전역 any 조작 |
| m-14 | `app/main/admin-menu-config.ts:23-31` | 레거시 한글 id union 8개 — alias 처리 실코드 존재 여부 확인 필요 |

## 참고 (린트 사각지대)

- ESLint `react-hooks/rules-of-hooks` 에러 222건은 전수 확인 결과 **한글 컴포넌트명 오탐**(React는 한글 식별자 컴포넌트를 정상 인식하나 lint는 대문자 시작만 컴포넌트로 판정). 모바일 트리(216건)와 `전자결재서브/ApprovalMetaPanels.tsx`(3건) 등에 분포. **실제 훅 위반이 발생해도 이 노이즈에 묻혀 탐지 불가** — (a) 컴포넌트 영문명 전환, (b) 해당 룰 커스텀, (c) 파일별 명시 disable+사유 중 택일 필요.
