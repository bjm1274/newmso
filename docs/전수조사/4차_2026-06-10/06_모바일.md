# 4차 전수조사 — ⑥ 모바일 트리 (2026-06-10)

> 범위: `app/main/모바일/` 133파일(tsx 100, ts 33) 전수
> 정책 기준: **모든 기능이 모바일에서도 동작해야 함 — PC 우회 스텁 금지**
> ✅표시 = Opus 코드 직접 재확인 완료

## CRITICAL

### [C-1] 채팅방 알림 토글 — 완전한 가짜 UI ✅
- 파일: `채팅/채팅방.tsx:615-627`
- `<input type="checkbox" defaultChecked>` — onChange 없음, 상태 없음, DB 저장 없음. 토글 노브 위치까지 `translateX(20px)` 하드코딩으로 **항상 켜짐**으로 보임. 사용자가 알림을 끈 줄 알아도 실제로는 아무 일도 일어나지 않음.

### [C-2] 채팅방 나가기 — DB 미처리 ✅
- 파일: `채팅/채팅방.tsx:707-710`
- "방 나가기" 버튼이 `onClick={onBack}` — 화면 전환만 수행. 멤버십 제거·읽음 커서·구독 해제 없음. 나간 줄 알았던 방의 알림·메시지가 계속 수신됨.

### [C-3] 결재함 칩 항상 활성 표시
- 파일: `결재/결재함.tsx:251-252`
- "결재함" 칩 `className="on"` 하드코딩 — 기안함/참조 탭으로 이동해도 결재함이 선택된 것처럼 표시. (하단 세그는 올바르게 조건부)

## MAJOR — PC 우회 스텁 (정책 위반 14건+)

| # | 파일:라인 | 내용 |
|---|----------|------|
| M-1 | `인사관리/계약문서.tsx:156` | 증명서 발급 4종(재직·경력·원천징수·지급명세) 전부 "PC에서 진행해주세요" toast |
| M-2 | `인사관리/계약문서.tsx:114` | file_url 없는 문서 "PC에서만 열 수 있습니다" |
| M-3 | `인사관리/계약문서.tsx:~401` | 제출 버튼 → "마이페이지에서 진행" (자기참조 순환 안내) |
| M-4 | `추가기능/조직도.tsx:236` | 풀 조직도 데스크톱 유도 |
| M-5 | `추가기능/직원평가.tsx:240` | 상세 분석·HR 반영 데스크톱 유도 |
| M-6 | `추가기능/마감보고.tsx:175,195` | 주간·월간 마감 데스크톱 유도 |
| M-7 | `결재/결재상세.tsx:590` | 빈 본문 시 "PC에서 전체 양식 확인" — 구형 기안은 모바일에서 항상 이 fallback |
| M-8 | `인사관리/연차신청.tsx:239` | 결재자 변경 "PC에서 지원됩니다" |
| M-9 | `결재/연차신청폼.tsx:184` | 결재선 오류 시 "PC에서 재상신" 유도 |
| M-10 | `추가기능/퇴원심사상세.tsx:273` | 코멘트 작성 데스크톱 유도(입력폼 없음) |
| M-11 | `추가기능/외부주차.tsx:75,95-97` | 데스크톱 유도 + "모바일 알림만 받기" 버튼 onClick 없음(죽은 버튼) |
| M-12 | `재고/분석마감.tsx:256,509,542` | 예측 모델·부서통계·AS 등록 데스크톱 유도 3건 |
| M-13 | `재고/물품자산.tsx:348` | UDI 동기화 데스크톱 유도 |
| M-14 | `추가기능/수술상담.tsx:84` | 녹음·분석(핵심 기능) 데스크톱 유도 |
| M-15 | `인사관리/구성원등록.tsx:102,261` | 회사 미특정 시 PC 유도 + 사진 첨부 PC 전용 |

## MAJOR — 기타

| # | 파일 | 분류 | 내용 |
|---|------|------|------|
| M-16 | `채팅/채팅방.tsx:343` | 의미없는 코드 | "실시간 연결됨" 정적 문자열 — 연결 상태와 무관하게 항상 표시 ✅ |
| M-17 | `채팅/data-hooks.ts:80,258,657` | 타입 | as any 캐스트 3건 (staffs 정규화·커서 순회·표시명) |
| M-18 | `셸/MobileShell.tsx:66` | 타입 | pendingContract `useState<any>` — 계약 서명 70줄 로직이 무타입 |
| M-19 | `관리자/{결재양식,운영설정,회사관리}.tsx` | 의미없는 코드 | user 파라미터 선언 후 자식(PC 워크센터 재사용 컴포넌트)에 미전달 |

## MINOR

| # | 파일 | 내용 |
|---|------|------|
| m-1 | `결재/결재상세.tsx:51` 외 | pickAvatarTone 5중 정의 (결재함-cards:19 export, 결재상세:51, 채팅 data-hooks:641, 게시판 data-hooks:433, 인사관리 data-hooks:625) |
| m-2 | `결재/{기안함:47,참조함:20}` | formatTs 3중(결재함-cards:26 export 존재) — 미묘한 로직 차이로 동기화 위험 |
| m-3 | `결재/기안함.tsx:54` | readAmount 2중 |
| m-4 | `채팅/채팅목록.tsx:747` | OrgPlaceholder 정의만, 전 저장소 호출 0 (grep 확인) |
| m-5 | `추가기능/조직도.tsx:22` | `const company = '전체'` 하드코딩 — user.company 무시, 멀티테넌트 격리 약화 |
| m-6 | `추가기능/근무현황.tsx:29` | 동일 — company '전체' 고정 |
| m-7 | `셸/MobileShell.tsx:240,294` | route as any 캐스트 |
| m-8 | `셸/MobileShell.tsx:189` | effect deps에 getTabFromMenu 누락 (강등: 함수가 매 렌더 재생성되어 실질 위험 낮음) |
| m-9 | `채팅/이모지피커.tsx:56,286` | as any 캐스트 |
| m-10 | `관리자/index.tsx:127,134-140` | 영구 disabled 알림 버튼 + "데이터 준비 중" 정적 hero |
| m-11 | `관리자/경영대시.tsx` | company 추출 후 미전달 |
| m-12 | `채팅/data-hooks.ts:~227` | deps에 messages 배열 참조 — 폴링마다 커서 fetch 재트리거 |
| m-13 | `채팅/data-hooks.ts:~441` | messages.length dep + 내용 클로저 읽기 — stale 위험 |
| m-14 | `결재/useApproverLine.ts:43-45` | staff_members 전사 조회(회사 필터 없음) 후 클라 필터 |
| m-15 | `결재/useApprovalFormBase.ts` | useApproverLine과 결재선 자동 매핑 로직 중복 |

## 구조 메모

- PC↔모바일 쌍둥이: `메신저액션서브/EmojiPicker.tsx` ↔ `채팅/이모지피커.tsx` ~249줄 중복(jscpd), `결재/연차신청폼` ↔ `일반기안폼` ~235줄.
- ESLint rules-of-hooks 오탐 216건이 이 트리에 집중(한글 컴포넌트명) — [05_PC_공통기타.md](05_PC_공통기타.md) 참고.
