# 전수조사 재조사 — 전체 요약 (2026-06-01)

> 기준 커밋: 4bd799d2 · 9개 메뉴(알림·내정보·추가기능·채팅·게시판·전자결재·인사관리·재고관리·관리자) 전수조사
> 방식: 메뉴별 파일 정독(서브에이전트 병렬) + Grep import 카운트·D1 스키마 직접 검증. 코드 수정 없음(조사 전용).
> 분류: D 중복 · S 문법/런타임 · K 한글깨짐 · X 죽은코드 · U 불필요 · Q 유사기능

## 🔴 메뉴 공통 패턴 (구조적 문제 3가지)

### 1. 모바일 화면의 하드코딩 목업 / 미연동
실제 라우팅되어 사용자에게 보이지만 DB fetch 없이 더미를 표시:
- **알림**: `모바일/내정보/알림탭.tsx` 더미 6건(실 구현 SAlert는 죽은 코드)
- **내정보**: 모바일 연차·급여명세·증명서·정보수정 전부 하드코딩(데스크톱은 정상 DB)
- **추가기능**: 모바일 수술상담(읽기)·마감보고(주/월탭)·인계노트(환자별)
- **재고/인사/관리자**: Hub KPI 하드코딩 더미
→ 모바일 사용자는 가짜 데이터를 보거나 빈 화면. (인사관리·관리자 모바일은 데스크톱 컴포넌트 래핑이라 예외적으로 실데이터)

### 2. 워크센터 ↔ 레거시 서브 병행 재구현
새 "워크센터" UI가 기존 레거시 컴포넌트를 재사용하지 않고 처음부터 재구현 → 동일 기능 2벌 유지:
- **인사관리**: LeaveWorkcenter/AbnormalWorkcenter/payroll이 레거시 휴가관리메인/급여정산 등과 병행
- **재고관리**: Status/Item/IO/Analyze 워크센터가 재고현황뷰/발주관리 등 15+화면 재구현
- **관리자**: ExecDashboard ↔ 한글 레거시 6 대시보드 2경로 노출

### 3. God 컴포넌트의 인라인 재구현 → 추출 훅 고아화
- **채팅**: `메신저.tsx`(3428줄)가 추출 훅 로직을 인라인 재구현 → **고아 파일 18개 ~3,500줄**(useChatRealtimeBridge/읽음상태훅/ReadSync/NavigationState/NoticeAdmin 등 + 모바일 퀵스위치바/반응선택)

## 🔴 즉시 조치 권장 (런타임 버그 / 데이터 손상)

| # | 메뉴 | 항목 | 영향 |
|---|------|------|------|
| 1 | 추가기능 | 모바일 인계노트/직원평가 **Rules of Hooks 위반**(조기 return 뒤 useMemo) | 작성 폼 진입 시 React 크래시 |
| 2 | 재고관리 | use-analyze-data가 없는 `amount/qty` 컬럼 참조(정본 quantity) | ABC분석·수요예측 사용량 항상 1, 납기 '-' |
| 3 | 전자결재 | 출결정정양식이 없는 `approval_status` 컬럼 insert | 첫 insert 항상 실패 후 폴백, 이중기록 의심 |
| 4 | 인사관리 | 계약관리 notifications `message` 컬럼(정본 body) | 서명요청 알림 본문 누락 |
| 5 | 채팅 | 모바일 오프라인 업로드 `message_type`만 쓰고 file_kind 누락 | 오프라인 전송 이미지 미표시 |
| 6 | 알림 | 모바일 알림탭 더미 + 실구현 죽은 코드 | 모바일 알림 사실상 미작동 |
| 7 | 관리자 | 워크센터 대시보드 staffs=[]/user=null 전달 | 실사용 경로 KPI degrade |
| 8 | 인사관리 | 연차촉진시스템 upsert가 NOT NULL `target_year`/`step`/`id` 누락 | 연차촉진 기록 INSERT 제약위반 실패 |
| 9 | 인사관리 | 계약서자동생성 `s.base`(정본 base_salary)·의료기기점검 `device_name`(정본 name) | 급여 자동입력 빈값·장비명 미표시 |

> 참고: 인사관리는 2차 정독으로 인사관리서브 전 파일(서브 ~120개) 커버 완료. 위 8·9 외 경조사관리 저장실패 은폐(U), 인사통합설정 무동작(U), 급여명세서.tsx·components/RiskActionDialog 죽은 alias/중복(X) 등 추가.

## 🟠 대규모 죽은 코드 (삭제 후보)

| 메뉴 | 죽은 코드 | 규모 |
|------|----------|------|
| 채팅 | 고아 훅 16 + 모바일 2 | ~3,500줄 |
| 인사관리 | 급여계산기·근무형태변경이력·자동이체·포상징계·전자서명패드·노무준수패널·모바일급여명세서·DocsCertSummary + 죽은 분기 | ~2,900줄+ |
| 게시판 | 업무가이드.tsx.bak(untracked) + getCommentCount 死기능 | ~1,870줄 |
| 관리자 | 모바일 data-hooks(398)+공용UI(169) + barrel/fallback | ~600줄+ |
| 내정보 | 단축키관리.tsx + 모바일 서류제출/이력 + 프로필카드 showSecret | ~1,000줄+ |
| 추가기능 | op-check-types·org-chart-types(거의 통째) + 다수 prop | ~400줄+ |

## 🟠 중복 코드 핵심 (D)

- **게시판**: 게시판공통.ts ↔ view-utils.ts 함수 ~20쌍 중복 + **동작 분기**(PC/모바일 상태색·예약메타 불일치). 업무가이드.tsx가 guide-utils/types inline 재구현(~450줄).
- **채팅**: 메신저방데이터훅 메타데이터 처리 3중 복제. MIME매핑/escapeLikePattern 클라·서버 중복.
- **인사관리**: 근무표 3중 시스템(shift_assignments 2 + nurse_schedules), 연차 통계 3중 계산, 근태유틸 2벌(상수값 불일치).
- **전자결재**: ApprovalMetaPanels JSX 패널 5종(~250줄) 死, buildApprovalHistoryEntry 3중 래퍼.
- **전역**: `KpiCard` 동명 컴포넌트 7곳+ 각자 정의, `extractScheduleMetaFromContent` 3중, `toSafeText`/`normalizeKeywordList` 중복.

## 🔵 보안 (대체로 양호 — 5/30 수정 반영 확인)

- ✅ 데이터초기화 서버 재검증, 직원권한통합 staffPrivilegeGuard, 계약 PII 암호화(과거 '평문' 지적 해소), LIKE 이스케이프, escapeHtml 통합 — 모두 적용 확인.
- ⚠️ 출퇴근 GPS 위치검증이 100% 클라이언트(서버 재검증 없음, 좌표 미저장). 외부 주차링크 평문 HTTP+사내IP를 데스크톱이 무검증 렌더(모바일은 https 강제). 모바일 구성원등록 role 클라 기록(서버 RLS 의존).

## 🟢 한글 깨짐 (K)
- 거의 전무. `명세서자동추출.tsx:268` "이미 지나"(→이미지나) 오타 1건만. (5/29 지적된 쨌·WOM 등은 5/30 수정 확인)

## 메뉴별 보고서
- [01_알림](01_알림.md) · [02_내정보](02_내정보.md) · [03_추가기능](03_추가기능.md) · [04_채팅](04_채팅.md) · [05_게시판](05_게시판.md) · [06_전자결재](06_전자결재.md) · [07_인사관리](07_인사관리.md) · [08_재고관리](08_재고관리.md) · [09_관리자](09_관리자.md)

## 전 메뉴 도달성 스윕 (importer=0 고아 — 전수 확인)
9개 메뉴 전 디렉토리(서브/워크센터/모바일) + 기능부품 루트 + app/components + 모바일 공통/셸을 Grep import 카운트로 전수 스캔. **확정 고아 파일 목록(코드 전체 import 0)**:
- 채팅: useChatRealtimeBridge·useChatReadSync·메신저읽음상태훅·useChatComposerState·useChatRetryQueueState·useChatViewController·useChatArchiveMessages·useChatThreadActions·useChatNavigationState·useChatNoticeAdmin·useChatIdentityDirectory·useChatDrawerController·useChatPreferenceState·메신저워크플로훅·MessengerDateJumpModal·메신저스와이프 + 모바일 퀵스위치바·반응선택 (18개 ~3,500줄)
- 인사관리: 급여계산기·근무형태변경이력·급여자동이체·포상징계관리·전자서명패드·노무준수패널·급여명세서(alias)·모바일 급여명세서·DocsCertSummary (~2,900줄)
- 추가기능: op-check-types.ts(164줄 중 ANESTHESIA_OPTIONS 1개만 생존, 나머지 死)·org-chart-types(getStaffExtensionText 1개만 생존)
- 게시판: 업무가이드.tsx.bak(untracked, 1870줄)
- 관리자: 모바일 data-hooks(398)·공용UI(169)
- **공용/신규 발견 고아(importer=0 재검증 확정)**: `app/components/AppLogo.tsx`·`app/components/DataTable.tsx`·`app/components/useModalFocusTrap.ts` · `app/components/RiskActionDialog.tsx`(인사관리서브판이 정본, 죽은 중복) · `app/main/기능부품/useChatTimelineScroll.ts`(채팅 19번째 고아) · `app/main/모바일/셸/플레이스홀더.tsx`(MobileShell이 실 컴포넌트로 전환, stale)
- 그 외 서브 디렉토리(전자결재서브·게시판서브·재고관리서브·관리자전용서브·전 워크센터): **importer=0 고아 없음(클린)**.
- 비고: `오프라인실패배너.tsx`는 MobileShell이 import(고아 아님). `비품대여물품설정.tsx`는 관리자 폴더에 있으나 재고관리통합이 로드(재고 기능, 양 메뉴 클러스터 미정독 — 저위험 reachable).

> 즉 죽은 "파일"은 위 목록이 전부. 나머지 메뉴 파일은 모두 도달 가능하며, 각 메뉴 보고서의 X 항목은 파일 내부의 죽은 분기/미사용 export·prop을 가리킴.

## 권장 처리 순서
1. **런타임 버그 7건**(위 표) — 사용자 영향 직접적.
2. **죽은 코드 일괄 삭제**(채팅 고아 18 + 인사 6 + .bak 등 ~9,000줄) — tsc/build 확인하며 배치 단위.
3. **워크센터↔레거시 / 모바일 목업** — 설계 결정 필요(통합 vs 제거), 별도 작업.
4. **중복 통합**(게시판공통/view-utils, KpiCard, 메신저방데이터훅) — 동작 보존 리팩터.
