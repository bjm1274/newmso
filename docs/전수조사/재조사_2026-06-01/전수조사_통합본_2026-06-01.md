# MSO ERP 전수조사 통합 보고서 (2026-06-01)

> 알림·내정보·추가기능·채팅·게시판·전자결재·인사관리·재고관리·관리자 9개 메뉴 reachable 전 파일 정독
> 기준: D(중복)·S(문법/런타임)·K(한글깨짐)·X(죽은코드)·U(불필요)·Q(유사기능)
> 이 파일은 개별 보고서 9개+요약을 하나로 합친 통합본입니다.

---

# 전수조사 재조사 — 전체 요약 (2026-06-01)

> 기준 커밋: 4bd799d2 · 9개 메뉴(알림·내정보·추가기능·채팅·게시판·전자결재·인사관리·재고관리·관리자) 전수조사
> 방식: 메뉴별 파일 정독(서브에이전트 병렬) + Grep import 카운트·D1 스키마 직접 검증. 코드 수정 없음(조사 전용).
> 분류: D 중복 · S 문법/런타임 · K 한글깨짐 · X 죽은코드 · U 불필요 · Q 유사기능

## 🔴 메뉴 공통 패턴 (구조적 문제 3가지)

### 1. 모바일 화면의 하드코딩 목업 / 미연동
실제 라우팅되어 사용자에게 보이지만 DB fetch 없이 더미를 표시:
- **알림**: `모바일/내정보/알림탭.tsx` 더미 6건(실 구현 SAlert는 죽은 코드)
- **내정보**: 모바일 연차·급여명세·증명서·정보수정 전부 하드코딩(데스크톱은 정상 DB)
- **추가기능**: 모바일 수술상담(읽기)·마감보고(주/월탭)·인계노트(환자별)
- **재고/인사/관리자**: Hub KPI 하드코딩 더미
→ 모바일 사용자는 가짜 데이터를 보거나 빈 화면. (인사관리·관리자 모바일은 데스크톱 컴포넌트 래핑이라 예외적으로 실데이터)

### 2. 워크센터 ↔ 레거시 서브 병행 재구현
새 "워크센터" UI가 기존 레거시 컴포넌트를 재사용하지 않고 처음부터 재구현 → 동일 기능 2벌 유지:
- **인사관리**: LeaveWorkcenter/AbnormalWorkcenter/payroll이 레거시 휴가관리메인/급여정산 등과 병행
- **재고관리**: Status/Item/IO/Analyze 워크센터가 재고현황뷰/발주관리 등 15+화면 재구현
- **관리자**: ExecDashboard ↔ 한글 레거시 6 대시보드 2경로 노출

### 3. God 컴포넌트의 인라인 재구현 → 추출 훅 고아화
- **채팅**: `메신저.tsx`(3428줄)가 추출 훅 로직을 인라인 재구현 → **고아 파일 18개 ~3,500줄**(useChatRealtimeBridge/읽음상태훅/ReadSync/NavigationState/NoticeAdmin 등 + 모바일 퀵스위치바/반응선택)

## 🔴 즉시 조치 권장 (런타임 버그 / 데이터 손상)

| # | 메뉴 | 항목 | 영향 |
|---|------|------|------|
| 1 | 추가기능 | 모바일 인계노트/직원평가 **Rules of Hooks 위반**(조기 return 뒤 useMemo) | 작성 폼 진입 시 React 크래시 |
| 2 | 재고관리 | use-analyze-data가 없는 `amount/qty` 컬럼 참조(정본 quantity) | ABC분석·수요예측 사용량 항상 1, 납기 '-' |
| 3 | 전자결재 | 출결정정양식이 없는 `approval_status` 컬럼 insert | 첫 insert 항상 실패 후 폴백, 이중기록 의심 |
| 4 | 인사관리 | 계약관리 notifications `message` 컬럼(정본 body) | 서명요청 알림 본문 누락 |
| 5 | 채팅 | 모바일 오프라인 업로드 `message_type`만 쓰고 file_kind 누락 | 오프라인 전송 이미지 미표시 |
| 6 | 알림 | 모바일 알림탭 더미 + 실구현 죽은 코드 | 모바일 알림 사실상 미작동 |
| 7 | 관리자 | 워크센터 대시보드 staffs=[]/user=null 전달 | 실사용 경로 KPI degrade |
| 8 | 인사관리 | 연차촉진시스템 upsert가 NOT NULL `target_year`/`step`/`id` 누락 | 연차촉진 기록 INSERT 제약위반 실패 |
| 9 | 인사관리 | 계약서자동생성 `s.base`(정본 base_salary)·의료기기점검 `device_name`(정본 name) | 급여 자동입력 빈값·장비명 미표시 |

> 참고: 인사관리는 2차 정독으로 인사관리서브 전 파일(서브 ~120개) 커버 완료. 위 8·9 외 경조사관리 저장실패 은폐(U), 인사통합설정 무동작(U), 급여명세서.tsx·components/RiskActionDialog 죽은 alias/중복(X) 등 추가.

## 🟠 대규모 죽은 코드 (삭제 후보)

| 메뉴 | 죽은 코드 | 규모 |
|------|----------|------|
| 채팅 | 고아 훅 16 + 모바일 2 | ~3,500줄 |
| 인사관리 | 급여계산기·근무형태변경이력·자동이체·포상징계·전자서명패드·노무준수패널·모바일급여명세서·DocsCertSummary + 죽은 분기 | ~2,900줄+ |
| 게시판 | 업무가이드.tsx.bak(untracked) + getCommentCount 死기능 | ~1,870줄 |
| 관리자 | 모바일 data-hooks(398)+공용UI(169) + barrel/fallback | ~600줄+ |
| 내정보 | 단축키관리.tsx + 모바일 서류제출/이력 + 프로필카드 showSecret | ~1,000줄+ |
| 추가기능 | op-check-types·org-chart-types(거의 통째) + 다수 prop | ~400줄+ |

## 🟠 중복 코드 핵심 (D)

- **게시판**: 게시판공통.ts ↔ view-utils.ts 함수 ~20쌍 중복 + **동작 분기**(PC/모바일 상태색·예약메타 불일치). 업무가이드.tsx가 guide-utils/types inline 재구현(~450줄).
- **채팅**: 메신저방데이터훅 메타데이터 처리 3중 복제. MIME매핑/escapeLikePattern 클라·서버 중복.
- **인사관리**: 근무표 3중 시스템(shift_assignments 2 + nurse_schedules), 연차 통계 3중 계산, 근태유틸 2벌(상수값 불일치).
- **전자결재**: ApprovalMetaPanels JSX 패널 5종(~250줄) 死, buildApprovalHistoryEntry 3중 래퍼.
- **전역**: `KpiCard` 동명 컴포넌트 7곳+ 각자 정의, `extractScheduleMetaFromContent` 3중, `toSafeText`/`normalizeKeywordList` 중복.

## 🔵 보안 (대체로 양호 — 5/30 수정 반영 확인)

- ✅ 데이터초기화 서버 재검증, 직원권한통합 staffPrivilegeGuard, 계약 PII 암호화(과거 '평문' 지적 해소), LIKE 이스케이프, escapeHtml 통합 — 모두 적용 확인.
- ⚠️ 출퇴근 GPS 위치검증이 100% 클라이언트(서버 재검증 없음, 좌표 미저장). 외부 주차링크 평문 HTTP+사내IP를 데스크톱이 무검증 렌더(모바일은 https 강제). 모바일 구성원등록 role 클라 기록(서버 RLS 의존).

## 🟢 한글 깨짐 (K)
- 거의 전무. `명세서자동추출.tsx:268` "이미 지나"(→이미지나) 오타 1건만. (5/29 지적된 쨌·WOM 등은 5/30 수정 확인)

## 메뉴별 보고서
- [01_알림](01_알림.md) · [02_내정보](02_내정보.md) · [03_추가기능](03_추가기능.md) · [04_채팅](04_채팅.md) · [05_게시판](05_게시판.md) · [06_전자결재](06_전자결재.md) · [07_인사관리](07_인사관리.md) · [08_재고관리](08_재고관리.md) · [09_관리자](09_관리자.md)

## 전 메뉴 도달성 스윕 (importer=0 고아 — 전수 확인)
9개 메뉴 전 디렉토리(서브/워크센터/모바일) + 기능부품 루트 + app/components + 모바일 공통/셸을 Grep import 카운트로 전수 스캔. **확정 고아 파일 목록(코드 전체 import 0)**:
- 채팅: useChatRealtimeBridge·useChatReadSync·메신저읽음상태훅·useChatComposerState·useChatRetryQueueState·useChatViewController·useChatArchiveMessages·useChatThreadActions·useChatNavigationState·useChatNoticeAdmin·useChatIdentityDirectory·useChatDrawerController·useChatPreferenceState·메신저워크플로훅·MessengerDateJumpModal·메신저스와이프 + 모바일 퀵스위치바·반응선택 (18개 ~3,500줄)
- 인사관리: 급여계산기·근무형태변경이력·급여자동이체·포상징계관리·전자서명패드·노무준수패널·급여명세서(alias)·모바일 급여명세서·DocsCertSummary (~2,900줄)
- 추가기능: op-check-types.ts(164줄 중 ANESTHESIA_OPTIONS 1개만 생존, 나머지 死)·org-chart-types(getStaffExtensionText 1개만 생존)
- 게시판: 업무가이드.tsx.bak(untracked, 1870줄)
- 관리자: 모바일 data-hooks(398)·공용UI(169)
- **공용/신규 발견 고아(importer=0 재검증 확정)**: `app/components/AppLogo.tsx`·`app/components/DataTable.tsx`·`app/components/useModalFocusTrap.ts` · `app/components/RiskActionDialog.tsx`(인사관리서브판이 정본, 죽은 중복) · `app/main/기능부품/useChatTimelineScroll.ts`(채팅 19번째 고아) · `app/main/모바일/셸/플레이스홀더.tsx`(MobileShell이 실 컴포넌트로 전환, stale)
- 그 외 서브 디렉토리(전자결재서브·게시판서브·재고관리서브·관리자전용서브·전 워크센터): **importer=0 고아 없음(클린)**.
- 비고: `오프라인실패배너.tsx`는 MobileShell이 import(고아 아님). `비품대여물품설정.tsx`는 관리자 폴더에 있으나 재고관리통합이 로드(재고 기능, 양 메뉴 클러스터 미정독 — 저위험 reachable).

> 즉 죽은 "파일"은 위 목록이 전부. 나머지 메뉴 파일은 모두 도달 가능하며, 각 메뉴 보고서의 X 항목은 파일 내부의 죽은 분기/미사용 export·prop을 가리킴.

## ➕ 전 파일 완전 정독 보강 (2026-06-01, 잔여 reachable 전수)
요청에 따라 9개 메뉴의 중·소형 reachable 파일까지 line-by-line 정독 완료(고아 제외 전 파일 커버). 신규 발견:

### S/X 신규
- **[S/채팅]** `메신저메시지서비스.ts:22 insertChatMessageWithFallback`(1~2인자 래퍼)와 `lib/chat-message-write`의 동명 함수(3인자)가 공존 → 워크플로훅은 lib판 직접 사용, 서비스 래퍼는 미사용(혼동).
- **[X/채팅]** `메신저파생훅.ts needsAttention` 항상 false 하드코딩 → 메신저.tsx:2478·타임라인:588의 filter/분기가 영구 死. `ThreadSummary.latestReplySenderId/rootSenderId/latestActivityAt` 계산되나 참조 0(미사용 파생필드). `메신저데이터유틸.ts:172 selectFallbackChatRoomId` 고아 export.
- **[X/게시판·재고]** 모바일 `게시판/첨부카드.tsx`(AttachmentRow/ImageAttachment) import 0 의심(상세는 data-hooks의 SafeAttachment 사용). `재고/납품확인서.tsx:11 showPreview` 미사용 state.
- **[X/추가기능]** `op-check-types.ts`(164줄) 재확인 — ANESTHESIA_OPTIONS 1개만 생존, 나머지 타입15·상수11 死(constants.ts가 실사용본).

### U 신규 (무동작/하드코딩 더미)
- **[U/전자결재]** `양식신청.tsx:74 approver` 변수 계산 후 미사용(죽은 변수)·`:107 cc_departments:['행정팀']` 하드코딩. `관리행정양식.tsx:28 void hospitalGuides`(가이드6종 미렌더)·`:173` 업무보고 input onChange 없음(죽은 입력). `작성하기.tsx:56 void BUILTIN_FORM_TYPE_DEFINITIONS` 잔재. `결재함.tsx:250` 칩 className="on" 하드코딩 항상 활성.
- **[U/관리자]** `AuditWorkcenter/fallback.ts` FALLBACK_ANOMALIES/PAYROLL_OUTLIERS/BACKUPS 전부 빈배열(폴백 효과 없음). `CompanyWorkcenter/fallback-data.ts`·`admin-types.ts ADMIN_WORKCENTERS`·`FormsWorkcenter FORMS/FORMS_KPI` 실명("박철홍"·"수연의원")·고정수치(v2.3·"38회") 하드코딩 데모.
- **[U/재고]** `StatusSubViews.tsx:79,117,148` 발주/설정/자동발주 버튼 onClick 미연결(장식).
- **[보안/전자결재]** `ApprovalDetailModal.tsx:221` iframe srcDoc에 sandbox 속성 없음(동일origin HTML 주입, 방어 권장).
- **[테스트분기 상존]** `ApprovalInboxColumns.tsx:79`·`ApprovalInboxView.tsx:482` `navigator.webdriver` 게이트가 프로덕션 코드에 상존(E2E 전용 인라인 버튼/KPI숨김).

### D/Q 신규
- **[D/전자결재]** `ApprovalWorkflowKpi.tsx:83` ↔ `ApprovalInboxView.tsx:144` myTurn/approvedThisMonth 동일계산 결재함 2중 순회(기 보고 D-4 재확인).
- **[Q/추가기능]** `alphaColor` 이중정의(`전자결재양식관리/design-utils.ts` ↔ `전자결재-utils.ts`). 동명 `PermissionDiffPanel` 2개(시스템마스터센터-modules vs 직원권한통합 — 기능은 다름).
- **[Q/재고]** `재고관리워크센터/stock-types.ts` ↔ `재고관리서브/types.ts` 동일 도메인 타입 2체계 병렬(전환 미완).

### 정정
- 관리행정양식 `bg-X/10/50` 2중 슬래시 지적은 현재 코드에서 단일 슬래시(유효) → **무혐의**(기수정).
- 비품대여물품설정.tsx(71줄)·경영분석 모바일대시보드·charts·시스템마스터-modules·전자결재양식관리 서브 = 전부 reachable·정상(고아/중복 아님).

> **결론: 9개 메뉴 reachable 전 파일 정독 완료.** 죽은 "파일" 인벤토리 + 파일 내부 죽은 분기/미사용 export/무동작 UI/중복까지 6기준 전수 기록됨.

## 권장 처리 순서
1. **런타임 버그 7건**(위 표) — 사용자 영향 직접적.
2. **죽은 코드 일괄 삭제**(채팅 고아 18 + 인사 6 + .bak 등 ~9,000줄) — tsc/build 확인하며 배치 단위.
3. **워크센터↔레거시 / 모바일 목업** — 설계 결정 필요(통합 vs 제거), 별도 작업.
4. **중복 통합**(게시판공통/view-utils, KpiCard, 메신저방데이터훅) — 동작 보존 리팩터.


---

# ① 알림 메뉴 — 재조사 보고서 (현 코드 상태)

> 재조사일: 2026-06-01 · 기준 커밋: 4bd799d2
> 비교 대상: 기존 `docs/전수조사/01_알림.md`(2026-05-29) + `10_수정완료_및_보류.md`(2026-05-30)
> 방식: 알림 메뉴 16개 파일 + API 7개 라우트 + 모바일 3개 파일 현 코드 정독·Grep 검증

## 대상 파일

| 구분 | 파일 |
|------|------|
| 엔진 | `app/main/기능부품/알림시스템.tsx` (1882) |
| 푸시유틸 | `app/main/기능부품/알림푸시.ts` (603) |
| 서브모듈 | `알림시스템/{delivery-log,device-feedback,filter-helpers,push-debug,push-utils,settings,ui-config}.ts` |
| UI | `알림센터.tsx`(577) · `알림인박스.tsx`(1348) · `채팅알림배너.tsx`(260) |
| 공용벨 | `app/components/GlobalNotificationBell.tsx` |
| 서버 | `app/api/notifications/{chat-push,chat-push-flush,mark-read,push-config,push-self-test,push-subscription,repush-unread}/route.ts` · `lib/inapp-notification-jobs.ts` |
| 모바일 | `모바일/내정보/{알림.tsx, 알림탭.tsx, 푸시설정카드.tsx}` |

---

## 🔴 신규 — 심각도 높음

### [X-NEW-1 / 기능불능] 모바일 알림 탭이 하드코딩 더미 데이터만 표시 + 실 DB 화면은 죽은 코드
- **실사용 화면**: `모바일/셸/MobileShell.tsx:81` → `route.tab === 'notif'`일 때 `<알림탭 user={user} />` 렌더.
- **문제**: `모바일/내정보/알림탭.tsx:36-91`은 `INITIAL_NOTIFS`(김민수 대리 연차, A4용지 재고0, 5월 워크숍 등) **6건 고정 더미**만 표시. `staffId`/DB fetch 전혀 없음(`user` prop은 `_user`로 무시). "모두 읽음"·행 탭은 로컬 state만 변경.
- **죽은 정본**: `모바일/내정보/알림.tsx`(SAlert)는 실제 `notifications` 테이블을 fetch하는 정상 구현이나, **어디서도 import 안 됨**(Grep 전체 확인 — 자기참조뿐). 따라서 5/30에 적용한 "SAlert `read`/`is_read`→`read_at`" 수정은 **죽은 파일에 적용**됨.
- **연쇄**: `푸시설정카드.tsx`(모바일 푸시 토글/테스트)도 유일 import처가 죽은 `알림.tsx`(:17) → **함께 도달 불가**. 모바일에서 푸시 설정 UI 진입 경로 없음(내정보 메뉴 재조사에서 교차 확인 필요).
- **영향**: 모바일 사용자는 실제 알림을 못 봄(가짜 목록). 푸시 토글 UI 미노출.
- **권장**: 알림탭.tsx를 SAlert 로직(또는 GlobalNotificationBell 데이터 경로)으로 교체하거나, SAlert를 알림탭이 렌더하도록 연결. 죽은 SAlert/푸시설정카드는 통합 후 정리.

---

## 🟠 기존 항목 — 현 상태 재검증

### [D-1] 푸시 로직 `알림푸시.ts` ↔ `알림시스템.tsx` 통째 중복 → **여전히 열림**
- `initNotificationService`(알림푸시.ts:355~ ↔ 알림시스템.tsx:470~), `getPushConnectionStatus`(289~↔154~), `sendNotification`(530~↔645~), `syncPushSubscriptionOnServer`/`deletePushSubscriptionOnServer`(151~↔417~), `flushPushRetryQueue`(253~↔135~), 디바이스 판별 함수군 12종, `PushConnectionStatus` 타입까지 거의 글자 단위 중복.
- **변경점**: 기존 보고서가 지적한 "아이콘 경로 분기"(sy-logo vs sy-icon-512)는 **현재 양쪽 모두 `/sy-logo.png`·`/badge-72x72.png`로 일치** → 그 부작용은 해소(stale). 코드 중복 자체는 미해소.
- **권장**: 알림푸시.ts를 알림시스템 푸시 함수군 단일 모듈로 합쳐 두 소비자(푸시설정카드, 알림인박스)가 공유.

### [D-2] push-debug/push-utils/settings 3중 정의 → **여전히 열림**
- 알림푸시.ts:188~224·31~46가 `알림시스템/push-debug.ts`와 동일한 `recordPushDebug`/`readPushDebugLog`/`normalizePushDebugDetail`/`PUSH_DEBUG_STORAGE_KEY` 재정의. `urlBase64ToUint8Array`/`uint8ArrayToBase64Url`는 push-utils.ts와 3중.
- **권장**: 알림푸시.ts가 서브모듈을 import해 재사용.

### [D-3] TYPE_CFG(type→표시 메타) 3벌 → **여전히 열림**
- `알림시스템/ui-config.ts:1`(icon/bg/progress/accent) / `알림인박스.tsx:130~`(icon/bg/text/border) / `알림센터.tsx:35~`(Lucide Icon/color/label). `getTypeCfg`도 3군데.

### [D-NEW-2] `normalizeKeywordList` 2중 정의
- `알림시스템/settings.ts:29`(파일 내부 전용) ↔ `알림시스템/filter-helpers.ts:9`(export) — 동일 구현. settings.ts가 filter-helpers 것을 재사용 가능.

### [K-1] `쨌`→`·` → **수정 완료 확인**
- `알림인박스.tsx`에서 깨진 가운뎃점 미검출. 현 코드 정상.

### [P-1] 알림센터 독자 10초 폴링 → **수정 완료 확인**
- `알림센터.tsx`는 `NOTIFICATION_LIST_UPDATED_EVENT` 구독으로 전환됨. `setInterval` 폴링 미검출. (주석 139-141행으로 전환 이력 명시)

### [S-1] 모바일 읽음 컬럼(is_read/read→read_at) → **부분 완료 / 무의미화**
- `GlobalNotificationBell.tsx`는 `read_at` 사용으로 정정 확인. 단 모바일 `알림.tsx` 수정분은 위 X-NEW-1대로 죽은 파일이라 실효 없음.

---

## 🟡 X 죽은 코드 (Grep 검증)

- **[X-1]** `알림푸시.ts` 미사용 export 다수 — 실제 소비 export는 `getPushConnectionStatus`/`initNotificationService`/`deletePushSubscriptionOnServer`/`PushConnectionStatus` 4개뿐. 나머지(`sendNotification`/`flushPushRetryQueue`/`canRequestPushPermissionFromGesture`/`getPushClientPlatform`/`setPushSubscriptionActiveState`/`hasPushSubscriptionActive`/`recordPushDebug`/`readPushDebugLog`/`urlBase64ToUint8Array`/`uint8ArrayToBase64Url`/`PUSH_STATUS_CHANGED_EVENT`/`PUSH_DEBUG_EVENT`/`PushDebugEntry`) 외부 참조 0. (D-1 통합 시 함께 정리)
- **[X-NEW-1]** `모바일/내정보/알림.tsx`(SAlert) + `푸시설정카드.tsx` — 위 상세. import 0.
- **[X-2]** `채팅알림배너.tsx:34-36` props `onOpenApproval`/`onOpenNotifications`/`onOpenInventory` 선언했으나 본문 구조분해·호출부 전달 모두 없음 → 미사용 prop.

## 🟡 S/T 타입 안정성

- **[S-NEW]** `알림인박스.tsx:854` 시그니처가 `{ user, onRefresh }: Record<string, unknown>`인데 호출부(`마이페이지/index.tsx:1136`)는 `onOpenMessage`/`onOpenChatRoom`/`onOpenApproval`/`setMainMenu` 4개를 추가 전달 → 컴포넌트가 무시(router.push 직접 이동). 무시되는 prop 전달.
- **[T-2]** `알림푸시.ts:442` / `알림시스템.tsx:557` `const j: any = sub.toJSON()` → `PushSubscriptionJSON` 사용 권장(JM4).
- **[T-3]** `device-feedback.ts:18-19` `(navigator as any).setAppBadge/clearAppBadge` 강제 캐스팅(파일 내 다른 곳은 `Navigator & {...}` 패턴 — 일관성 결여).

## 🟡 Q 유사기능

- **[Q-fetch]** `알림센터.tsx:105~`(limit 50) ↔ `알림인박스.tsx:869~`(limit 200) `fetchNotifications` 동일 패턴. 공용 훅 후보.
- **[Q-mark]** `알림센터.tsx:209~` ↔ `알림인박스.tsx:964~` markAsRead/markAll/delete + `erp-notification-read` dispatch 복제.
- **[Q-bell]** NotificationCenter ↔ GlobalNotificationBell ↔ 알림탭 — 벨/목록 3종 병렬 재구현. 데이터 훅 통일 후 표현만 분기 후보.

## 🟢 U 불필요/정책

- **[U-1]** `알림시스템.tsx` raw `console.warn/error` 9곳(287,543,548,554,582,626,1396,1480,1537) — 동일 기능 알림푸시.ts는 `@/lib/logger` 사용. 로깅 정책 불일치.
- **[U-2]** `알림시스템.tsx:1543~1548,1573~1577,1834~` 비활성 채널/제거된 repush 설명 장문 주석 잔존.
- **[D-NEW-3]** `lib/chat-push-dispatch.ts:728-729` 동일 주석 2줄 연속 중복. 또한 FCM/WebPush 이중발송 방지 로직이 `lib/notification-repush.ts`(notification-shared 추출본)와 부분 중복(인라인).
- **[U-3]** `api/notifications/push-subscription/route.ts:52-56` `detectExtendedColumnSupport()` 항상 true 반환(Supabase 시절 잔재). 호출부(77,143,190) 무의미.

## 발견 없음
- 하드코딩 시크릿/`dangerouslySetInnerHTML`(SEC): 없음. 한글 깨짐(K): 알림탭/SAlert/배너 포함 추가 미검출. 명백한 컴파일 오류(S): 없음(tsc 통과 전제).

---

## 핵심 결론 (알림)
1. **최우선**: 모바일 알림 탭이 더미 목업(`알림탭.tsx`)이고 실 구현(`SAlert`)이 죽어 있음 → 모바일 알림 기능 사실상 미작동. (신규·고심각)
2. 푸시 로직 2벌 중복(D-1/D-2)·TYPE_CFG 3벌(D-3)·normalizeKeywordList 2중(D-NEW-2)은 5/30 수정에서 미반영, 여전히 열림.
3. 5/30 수정 확인된 것: 쨌→·(K-1), 알림센터 폴링 제거(P-1), GlobalNotificationBell read_at(S-1 일부).


---

# ② 내정보 메뉴 — 재조사 보고서 (현 코드 상태)

> 재조사일: 2026-06-01 · 기준 커밋: 4bd799d2 · 대상 ~12,300줄 (데스크톱 마이페이지 + 모바일 내정보)
> 방식: 31개 파일 4클러스터 병렬 정독 + Grep/스키마 교차검증

## 대상 파일
- 데스크톱 `마이페이지/`: index.tsx(1143)·마이페이지공통섹션·프로필카드(+InfoItems/format-utils/types)·홈탭헤더·역할별대시보드/format-utils·useGlobalShortcuts·단축키관리·단축키설정·즐겨찾기설정·출퇴근기록/{index(1136),checkin-utils,attendance-utils,commute-types,출퇴근모달,출퇴근차트}·나의할일(879)·서류제출(664)·증명서관리·면허보수교육제출·연차휴가내역·certificate-print-utils·급여명세서/{index,모바일명세서}
- 모바일 `내정보/`: 홈(726)·index·정보수정·급여명세·연차·증명서·서류제출·서류제출이력·data-hooks·출퇴근체크인·할일

---

## 🔴 신규 — 심각도 높음 (모바일 내정보가 대부분 목업/죽은코드)

### [Q-NEW-1 / 기능불능] 모바일 연차·급여명세·증명서 화면이 하드코딩 더미 데이터 (live 라우팅됨)
- `모바일/내정보/index.tsx:42-49`가 `leave/payslip/cert` 서브라우트로 **실제 렌더**하는 3화면이 모두 fetch 없는 목업:
  - `연차.tsx:26` `LEAVE_HISTORY` 고정 배열 + 잔여연차 11/15 하드코딩. supabase/useEffect 전무. `_user` 미사용.
  - `급여명세.tsx:21` `PAY_ITEMS`/`DEDUCT_ITEMS`·실수령 3,436,470·"2026년 4월" 하드코딩. fetch 없음. PDF 버튼 무동작.
  - `증명서.tsx:25` `CERT_DOCS`/`RECENT_CERTS` 하드코딩. "발급" 버튼 무동작.
- 데스크톱은 각각 `leave_requests`/`payroll_records`/`certificate_issuances`를 실제 조회. → **모바일 사용자는 가짜 데이터를 봄.** (알림탭과 동일 패턴 — 모바일 전반의 구조적 문제)
- **[U-NEW]** `정보수정.tsx:33` 연락처 초기값 더미('010-1234-5678'), `:37` `handleSave` 빈 본문(`// 추후 연동`) → 저장 버튼 무동작.

### [X-NEW-2] 모바일 서류제출/서류제출이력 — 라우팅 부재로 완전 죽은 코드
- `모바일/내정보/index.tsx`의 서브라우트 분기에 `'docs'` 없음(`MHomeSub` 유니온에도 없음). `서류제출.tsx`(SDocs):57·`서류제출이력.tsx`(SubmissionHistory):34는 셸 어디서도 마운트 불가. (서류제출이력은 죽은 SDocs만 import) → 둘 다 죽은 코드.

### [X-NEW-3] 데스크톱 `단축키관리.tsx` 전체(349줄) 죽은 코드
- `ShortcutManager` — Grep 전체 import 0. 단축키 매핑 picker UI 전체 미사용. (index.tsx 즐겨찾기 picker와 거의 같은 UI를 두 번 작성한 셈 — Q)

---

## 🟠 X 죽은 코드 (Grep 검증)

- **[X-1]** `프로필카드.tsx:23,32,44,46-52` `showSecret`/`setShowSecret` prop·`internalShowSecret` state·파생 `showSecret`·세터 `applyShowSecret` 전부 정의만, JSX/로직 미사용 → 비밀정보 마스킹 기능 미구현/사장. (index.tsx:142 `showSecret`도 카드로 내려보내기만)
- **[X-2]** `프로필카드.tsx:20` `onOpenApproval` 구조분해만 하고 본문 미사용.
- **[X-3]** `역할별대시보드/format-utils.ts:19-46` `formatTodayAttendancePrimary`/`formatTodayAttendanceSecondary`/`resolveEffectiveApproverId` + 타입 `ApprovalRow`/`TodayAttendance` — 외부 import 0(역할별대시보드 컴포넌트 삭제 잔재). 실 소비자는 re-export된 approval-line 3종만 사용.
- **[X-4]** 출퇴근 미사용 export: `checkin-utils.ts:130 fetchLatestStaffShiftContext`, `:80 buildDateWithTime`, `attendance-utils.ts:10 getDisplayStatus` — 모두 export지만 외부 import 0(소비처는 전부 로컬 사본 사용).
- **[X-5]** `인사관리서브/증명서발급.tsx:494` `selectedStaff ? ... : false ? (...)` — `false` 삼항 분기는 영구 도달 불가 죽은 렌더 트리.

## 🟠 D 중복 코드

- **[D-1]** `getDisplayStatus` 3중 정의: `출퇴근기록/index.tsx:70` · `출퇴근차트.tsx:8` · `attendance-utils.ts:10`(export). 로컬 사본 2개가 공용 export를 무시.
- **[D-2]** `출퇴근기록/index.tsx:186-259 fetchLatestStaffShiftContext`(로컬) ↔ `checkin-utils.ts:130`(export) 동일 로직. `buildDateWithTime`도 index.tsx:65 ↔ checkin-utils.ts:80 동일.
- **[D-3]** `프로필카드/format-utils.ts:1 toSafeText` ↔ `조직도서브/org-chart-types.ts:57 toSafeText` 동일 구현 2벌.
- **[D-4]** `index.tsx:52 MYPAGE_RECORDS_VIEW_KEY` 로컬 재정의 ↔ `단축키설정.ts:98` 동일 상수 이미 export.
- **[D-5]** `단축키설정.ts:48-65 normalizeStoredEntry` ↔ `즐겨찾기설정.ts:245-268 normalizeStoredEntry` kind 분기 로직 중복(즐겨찾기 쪽이 레거시 마이그레이션 추가된 상위호환).
- **[D-6]** `인사관리서브/증명서발급.tsx:36 getClosingText`(로컬) ↔ `certificate-print-utils.ts:126`(동일 함수). 같은 파일이 `openIssuedCertificatePrintView`는 import하면서 getClosingText만 로컬 재정의 → 마감문구 분기 어긋날 위험.
- **[D-7]** `서류제출.tsx:37-44 escapeHtml`(로컬) ↔ `lib/escape-html.ts escapeHtml` 동일(주석으로 의도적 복제 명시). 통합 가능.

## 🟠 Q 유사기능 (데스크톱 ↔ 모바일 병렬 재구현 + 동작 비대칭)

- **[Q-1 / 데이터 비대칭]** 출퇴근 토글이 데스크톱(`출퇴근기록/index.tsx:798 handleCommute`)과 모바일(`출퇴근체크인.tsx:128`)에 분리 구현. **결과 데이터가 다름**:
  - 데스크톱: `resolveLateThreshold`로 지각/정상 판정 + `attendances` 연계 + 조퇴(`calculateEarlyLeaveMinutes`) 계산.
  - 모바일 출근: `status:'정상'` 하드코딩(`:152`), 지각판정·attendances 동기화 없음. 퇴근: `check_out`만 update, 조퇴/status 갱신 없음.
  - → 모바일 출근이 데스크톱이 기록한 '지각' 행을 upsert로 '정상' 덮어쓸 위험.
- **[Q-2]** 할일: 데스크톱 `나의할일.tsx`(879, 우선순위/반복/리마인더/담당유형/채팅소스) ↔ 모바일 `할일.tsx`(274, today/open/done 필터·priority만). 모바일은 반복생성/리마인더/repeat_parent_id 전무 → 반복 할일 토글 동작 불일치.
- **[Q-3]** `index.tsx:588-637`(verifyProfilePassword+수정모드 진입) ↔ `프로필카드.tsx:125-167`(verifyPassword+actionButtons) 비번확인 후 수정 로직 중복. index가 `hideActionBar`로 카드 버튼을 숨겨 카드 쪽은 사실상 죽은 중복(X 근접). `/api/auth/verify-password` body도 불일치(password만 vs userId/name/employeeNo).
- **[Q-4]** 즐겨찾기 picker UI(`index.tsx:900-1024`) ↔ 단축키관리 picker UI(`단축키관리.tsx:219-331`) 동일 3종 picker 패턴(단축키관리는 X).
- **[Q-5]** 정보수정.tsx(모바일) ↔ 프로필카드(데스크톱) 동일 프로필 정보 표시.

## 🟡 U 불필요 / 미완성

- **[U-1]** `data-hooks.ts:60-65,96-97` `TodayCounts.unreadChat`/`newBoard` 타입에만 있고 항상 0(미구현 placeholder).
- **[U-2]** `홈.tsx:525-553` "비밀번호 변경" 버튼 onClick 없음(무동작), "알림 설정" 행 "켜짐" 텍스트만. `:347` "이번달 지각 0회" 하드코딩(data-hooks의 useMonthlyAttendance 미사용).
- **[U-3]** 레거시 주석 잔존: `index.tsx:351-353`(제거된 fetchTodayLog effect), `:743-745`(attendance/attendances 혼동 설명). `getToday`/`getTodayKr` "이전 구현 now+9h" 동일 주석 3파일 복붙(나의할일:64, 출퇴근체크인, 할일:32).
- **[U-4]** `급여명세서/index.tsx:43-44` `SalaryRecord.record_type`/`status` 타입에만 있고 미사용.

## 🔵 보안 (메모)

- **[SEC-1 / GPS]** 출퇴근 위치검증이 100% 클라이언트(데스크톱 `resolveCurrentLocation`·모바일 `useGeolocationCheckin`). 서버 재검증 없음, attendance 행에 lat/lng 미저장 → 좌표 위조/devtools 우회 가능, 사후감사 불가. `WORKPLACE_LOCATION`/반경은 번들 노출. (기존 보류 항목과 동일)
- **[SEC-OK]** 모바일 정보수정.tsx는 role/permissions/password 클라 수정 코드 없음(저장 자체 no-op) → 위험 없음.
- **[SEC-OK]** `서류제출.tsx`·`certificate-print-utils.ts` `document.write`/`srcdoc` 모두 `escapeHtml` 적용 확인. dangerouslySetInnerHTML 미사용.

## 🟡 정합성

- **[정합 OK]** `attendance.date`/`staff_id`(단수, UI소스) vs `attendances.work_date`(복수, 급여연계) 별개 테이블로 일관 분리. data-hooks·차트·캘린더 모두 정합.
- **[정합 OK]** data-hooks `notifications.user_id`+`read_at IS NULL`, `approvals.current_approver_id`, `staff_licenses`, `leave_requests`, `payroll_records`, `document_repository` 컬럼 런타임 D1 정본과 일치.
- **[정합 주의]** `index.tsx:323-329` notifications insert `user_id:'system_admin'`(문자열) — 코드 주석도 FK 위반 가능 명시. `마이페이지공통섹션.tsx:62` 알림 type 필터('급여명세') vs index.tsx:327 insert type('SUCCESS') — type 도메인 한/영 혼재.
- **[참고]** base schema SQL(`00_full_schema...`)은 `notifications.is_read`만 있고 `read_at` ADD 마이그레이션 없음 → **스키마 문서가 런타임 D1 정본(read_at)과 stale**. 코드보다 스키마 문서 문제.

## 발견 없음
- K 한글깨짐(mojibake): 31개 파일 0건. S 명백한 컴파일/런타임 오류: 없음(스키마 stale 제외).

---

## 핵심 결론 (내정보)
1. **모바일 내정보의 핵심 화면(연차·급여명세·증명서·정보수정)이 하드코딩 목업** — 알림탭과 동일 패턴. 모바일 내정보 전반이 실데이터 미연동. (최우선)
2. 모바일 서류제출/서류제출이력은 라우팅 부재 죽은 코드. 데스크톱 단축키관리.tsx(349줄) 전체 죽은 코드.
3. 출퇴근 데스크톱↔모바일 처리 비대칭(지각/조퇴/attendances 동기화 누락)으로 데이터 정합성 위험.
4. getDisplayStatus 3중·fetchLatestStaffShiftContext 2중 등 출퇴근 유틸 중복 정리 여지.


---

# ③ 추가기능 메뉴 — 재조사 보고서 (현 코드 상태)

> 재조사일: 2026-06-01 · 기준 커밋: 4bd799d2 · 대상 ~22,000줄 (데스크톱 12개 기능 + 조직도 + 모바일 23화면)
> 방식: 6클러스터 병렬 정독 + Grep/스키마 교차검증. S(런타임) 2건 직접 재확인.

## 대상
- 진입/공통: 추가기능.tsx·추가기능공통.tsx
- 기능: ESL관리·입금실시간조회·마감보고(+Grid)·OP체크(+op-check-*+OP체크/*)·퇴원심사(+규정빌더/패널+퇴원심사/*)·수술상담(+-components/types/utils)·근무현황·인계노트·직원평가시스템·조직도서브/*
- 모바일: 모바일/추가기능/* (index·허브·data-hooks 38KB + 20개 화면)

---

## 🔴 신규 — 심각도 높음

### [S-NEW-1 / 런타임 크래시] 모바일 인계노트·직원평가 Rules of Hooks 위반 (직접 확인)
- `모바일/추가기능/인계노트.tsx:54-55` `useState` → `:57-64` `if (showForm) return <HandoverNoteForm/>` → `:67,69` `useMemo` 2개.
- `모바일/추가기능/직원평가.tsx:41-42` `useState` → `:44-52` `if (evalTarget) return <EvalWriteForm/>` → `:54,60` `useMemo` 2개.
- **조기 return 뒤에 훅 호출** → 폼 진입/복귀 시 훅 개수가 달라져 React19가 "Rendered fewer hooks than expected" 크래시. **인계노트 작성/직원평가 작성 버튼을 누르면 화면이 깨짐.** (확정)
- **권장**: useMemo를 조기 return 위로 이동.

### [Q-NEW / mock] 모바일 추가기능에 데스크톱 기능을 더미로 흉내낸 화면 다수
- `수술상담.tsx` — **읽기(최근 상담) 전부 mock**: 안내배너만 렌더(72-86), `op_consultations` 조회 훅이 data-hooks에 없음. 등록 insert만 진짜 → 등록한 상담이 목록에 안 보임.
- `마감보고.tsx:159-192` — '주 마감' 탭 `Array.from({length:5})` 더미 행, '월 마감' 탭 안내배너만, `:127` "현금 합계 일치" 칩 무조건 하드코딩.
- `인계노트.tsx:84,177-194` — "환자별 인계" 분기 영구 빈 화면(`handover_notes`에 note_scope/patient_name/room_number 컬럼 부재).
- `직원평가.tsx` 평가자명 — `staff_evaluations`에 evaluator_name 컬럼 없어 항상 "평가자" placeholder(조인 필요).
- `퇴원심사상세.tsx:260-276` 코멘트 탭 / `OP체크상세.tsx:99-101` 소모품 — placeholder/빈값(board_posts.supplies 미존재 컬럼).

### [X-NEW / orphan] 모바일 허브 미등록 화면 3종
- `허브.tsx:46-74` GROUPS는 12모듈만 등록. 라우팅(`index.tsx:68 MODULE_TO_VIEW`)엔 있으나 허브 버튼이 없는 **`mri`(MRI일정)·`share`(업무공유)·`guide`(업무가이드)** 3화면은 추가기능 허브에서 진입 경로 없음(Grep 확인). (단 share/guide는 게시판 메뉴 라우팅엔 존재 — 교차 컨텍스트)

---

## 🟠 X 죽은 코드 (Grep 검증)

- **[X-1]** `op-check-types.ts`(164줄) 사실상 죽은 모듈 — 실사용 export는 `op-check-components.tsx:5,503`가 쓰는 `ANESTHESIA_OPTIONS` 1개뿐(재검증 확인). 나머지 타입 15종·상수 전부 미사용(실 소비는 `OP체크/constants.ts`·`*-helpers.ts`). op-check-components의 import를 `./OP체크/constants`로 바꾸면 파일 전체 삭제 가능.
- **[X-2]** `org-chart-types.ts`(71줄) 거의 죽은 모듈 — 실사용은 `getStaffExtensionText` 1개. 나머지(toSafeText 중복 + 미사용 타입 8종 OrgTeam/OrgDepartment/OrgPyramidData/OrgListData/OrgViewData/CanvasLayout/CompanyInfo/OrgChartProps + DEPT_STYLES/DEFAULT_DEPT_STYLE) 코드 참조 0.
- **[X-3]** `추가기능.tsx:273` `card.external === 'iframe'` 분기 도달 불가(FEATURE_CARDS에 'iframe' 없음, 'chart'만).
- **[X-4]** `추가기능공통.tsx:210,215-225` `FeatureCard.accentClass` — 11카드 전부 동일값이나 어디서도 안 읽음(주석으로 legacy 자인).
- **[X-5]** `OP체크.tsx:67` `getScheduleStatusOrder` import 후 호출 0.
- **[X-6]** `op-check-components.tsx` `OpCheckWorkspaceHeader`의 `orderLabel`/`prevDisabled`/`nextDisabled`/`onPrev`/`onNext` prop 5종 destructure만 하고 JSX 미사용 → 부모(`OP체크.tsx:2056,2063-2067`)의 계산·전달도 죽음.
- **[X-7]** `OP체크카드.tsx:227-229` `export function OPCardIcon` import 0.
- **[X-8]** `공통.ts(퇴원심사):140 dischargeBinaryLabel`, `:31 DischargeCheckItem` 타입 — SSOT로 추출했으나 호출/ import 0(호출부는 인라인 삼항·로컬 CheckItem 재선언).
- **[X-9]** `근무현황.tsx:254` work_shifts select가 `company_name/description/weekly_work_days/is_weekend_work` 4컬럼 over-fetch(타입·사용 모두 없음).
- **[U/X]** `OrgChart.tsx:15 depts` prop 받기만 하고 미사용(조직도본문.tsx:280이 넘기지만 무시).

## 🟠 D 중복 코드

- **[D-1]** OP체크 타입/상수 다중 정의: `op-check-types.ts` ↔ `OP체크/constants.ts`(STATUS_OPTIONS 등 글자단위 동일) ↔ `OP체크.tsx:107-124`(WardStaffRow/ChatRoomMemberLookupRow/OpCheckViewUser 로컬 재정의) ↔ `*-helpers.ts`(ChecklistItemDraft/PatientCheckState/LinkedSchedulePost 등).
- **[D-2]** `extractScheduleMetaFromContent` 3중 정의: `OP체크/schedule-helpers.ts:34` · `게시판-view-utils.ts:249` · `게시판공통.ts:263`. (게시판 메뉴와 교차)
- **[D-3]** `org-chart-types.ts:57 toSafeText` ↔ `프로필카드/format-utils.ts:1 toSafeText` 중복(미묘한 trim 동작차).
- **[D-4]** `마감보고Grid.tsx:10-17 ClosureGridItem` ↔ `마감보고.tsx:11-18 ClosureItem` 5필드 동일 인터페이스.
- **[D-5]** `인계노트.tsx:126-131 compareDateKeys` ↔ `:157-162 compareRooms` 본문 완전 동일(이름만 다름).
- **[D-6]** 외부링크 URL을 데스크톱(`추가기능공통.tsx:229-232`)과 모바일(`외부주차.tsx`/`외부웹팩스.tsx`)에 각각 하드코딩(단일 출처 없음).
- **[D-7]** `KpiCard` 동명 컴포넌트 7곳+ 각자 정의(수술상담-components 포함) — 통합 후보.

## 🟠 S/런타임·정합

- **[S-2]** `직원평가시스템.tsx:50-78 fetchEvaluations` 의존성 `[]`인데 본문이 `staffs`/`user` 클로저 참조 → stale closure(staffs 변경 시 evaluator_name 매핑 미갱신).
- **[S-3 의심]** `인계노트.tsx:645`(데스크톱) bedOptions가 `roomConfigs`(모달 편집용 임시 state, 평상시 빈배열)에서 capacity 조회 → 모달 미오픈 시 항상 fallback 4 고정. `effectiveRoomConfigs`(242) 사용해야 할 자리.
- **[S-4 의심]** `퇴원심사.tsx:980` editForm.xxx(string|undefined)를 controlled select value로 — undefined 시 uncontrolled 경고 가능(실사용 시 대부분 string이라 무해 가능).
- **[S-경미]** `수술상담-types.ts:20 SavedRecord`에 chartNumber 누락 → `& { chartNumber?: string }` 인터섹션 우회(타입 비대칭).

## 🟡 U 불필요 / 무동작

- **[U-1]** 모바일 다수 장식 버튼 onClick 없음: `입금조회.tsx:49`(새로고침), `조직도/부서재고/퇴원심사목록/업무공유목록/업무가이드` 헤더 검색·QR, `부서재고.tsx:140-142`(출고/발주), `마감보고.tsx:197`(초안보기), `외부웹팩스/외부주차`(알림받기), `업무공유상세.tsx:156-157,237`(북마크/공유/다운로드), `업무가이드.tsx:137-153`(문서행 chevron).
- **[U-2]** `근무현황.tsx:373-425,436-442` realtime 구독 + 60초 setInterval 중복 갱신, `handleVisible`/`handleFocus`가 `isRealtimeActiveRef` 항상 true라 본문 도달 불가(죽은 폴백 분기).
- **[U-3]** `인계노트.tsx:1007,1124-1146 noteActionValues` Record state가 항상 ''로 리셋 → staff별 보관 불필요(과설계).
- **[U-4]** `입금실시간조회.tsx:331` KPI 매 렌더 IIFE 재계산(useMemo 미적용, 경미).

## 🔵 보안

- **[SEC-1]** `추가기능공통.tsx:230` 주차관제 외부링크 평문 HTTP + 사내 사설IP(`http://kmp0001103.iptime.org/login?redirectTo=undefined`). 데스크톱(`추가기능.tsx:344`)은 무검증 `<a href>` 직접 렌더, 모바일은 `safeExternalUrl`로 https 강제 — **정책 상반**. `redirectTo=undefined` 리터럴 잔존.
- **[SEC-OK]** 입금실시간조회 평문 토큰/시크릿 없음(env 변수 *이름*만 안내). `TOSS_BANK_ACCOUNT`는 공개 수신계좌.
- **[SEC-OK]** 퇴원심사/수술상담 Gemini 키는 서버 라우트(`process.env.GEMINI_API_KEY`)에서만 사용, 클라 프록시 호출 + 인증게이트(`readAuthorizedExtraFeatureUser`)·크기제한. 노출 없음.

## 발견 없음 / 정상 확인
- **K 한글깨짐**: 전 클러스터 0건.
- **정합 OK**: daily_closures/daily_closure_items/daily_checks(마감보고), prep_items/OP_PATIENT_CHECK_*(OP체크, 후속 마이그레이션 timestamps 포함), staff_evaluations(직원평가), leave_requests/discharge_reviews, attendances.work_date(모바일 근무현황 — 과거버그 교정됨). 미존재 컬럼(board_posts.comment_count/tag/is_urgent 등)은 pickText 폴백으로 무음 처리(크래시 없으나 해당 메타 모바일 미표시).
- **SSOT 양호**: 퇴원심사 공통.ts(stayDays/status) 데스크톱·모바일 재사용, 수술상담 types/utils/components 분리 적절.
- **책임 분리 양호(이름만 부적절)**: `조직도측면창.tsx`=실제 글로벌 Sidebar(조직도 무관, export 전부 사용 중), `조직도본문.tsx`=MainContent 라우터(단 `:229-249 checkNotifications` 연차알림 도메인로직 혼입은 배치 부적절 Q).

---

## 핵심 결론 (추가기능)
1. **최우선(크래시)**: 모바일 인계노트·직원평가 Rules of Hooks 위반 → 작성 폼 진입 시 React 크래시.
2. 모바일 수술상담(읽기)/마감보고(주·월탭)/인계노트(환자별)/퇴원심사 코멘트 등 mock·빈화면 다수 — 모바일 전반의 미연동 패턴 지속.
3. op-check-types.ts·org-chart-types.ts 거의 통째 죽은 모듈(각 1개 export만 생존). OP체크 타입 다중 정의 정리 여지 큼.
4. 외부링크 평문 HTTP/사내IP + 데스크톱 무검증 렌더(SEC-1).


---

# ④ 채팅(메신저) 메뉴 — 재조사 보고서 (현 코드 상태)

> 재조사일: 2026-06-01 · 기준 커밋: 4bd799d2 · 대상 ~28,000줄 (데스크톱 65파일 + 모바일 14 + api/lib)
> 방식: 7클러스터 2웨이브 병렬 정독 + Grep import 카운트 직접 검증

## 핵심 결론 (먼저)
**메신저.tsx(3428줄 God 컴포넌트)가 추출된 훅/모듈의 로직을 인라인 재구현하면서, 분리해 둔 훅 파일 16개가 통째로 고아(import 0)가 됨 + 모바일 2개 = 18개 죽은 파일 ~3,500줄.** 채팅 메뉴 최대 부채.

---

## 🔴 X — 죽은 파일 (importers=0, Grep 직접 확인)

| 파일 | 줄 | 비고 (인라인 재구현 위치) |
|------|----|------|
| `useChatRealtimeBridge.ts` | 592 | 메신저.tsx:1955가 `useChatRealtimeSubscriptions` 직접 호출 |
| `메신저워크플로훅.ts` | 363 | 신버전 `메신저입력워크플로훅.ts`(동명함수)만 사용 |
| `useChatNavigationState.ts` | 343 | 메신저.tsx가 `메신저방전환훅` 직접 사용 |
| `useChatIdentityDirectory.ts` | 323 | 메신저.tsx:351/532 인라인 |
| `메신저읽음상태훅.ts`(useChatReadStateSync) | 300 | 메신저.tsx:1345 인라인(개선된 캐시/디바운스가 사장됨) |
| `useChatReadSync.ts` | 228 | 메신저.tsx:685 persistRoomReadCursors 인라인 |
| `useChatDrawerController.ts` | 218 | 메신저.tsx:1268 자체 useState |
| `useChatArchiveMessages.ts` | 152 | import 0 |
| `useChatPreferenceState.ts` | 116 | 메신저.tsx:1185 인라인 |
| `useChatNoticeAdmin.ts` | 125 | 메신저.tsx:2715-2794 1:1 복제 |
| `MessengerDateJumpModal.tsx` | 113 | 실제는 `메신저섹션/DateJumpModal` 렌더 |
| `useChatThreadActions.ts` | 97 | 메신저.tsx 인라인 |
| `useChatComposerState.ts` | 130 | 메신저.tsx 인라인(멘션/IME/높이조절) |
| `useChatRetryQueueState.ts` | 86 | 메신저.tsx:1241 인라인 |
| `메신저스와이프.ts` | 73 | 사이드바는 `components/SwipeableCard` 사용 |
| `useChatViewController.ts` | 10 | import 0(빈 스텁) |
| `useChatTimelineScroll.ts` | 442 | import 0(재검증 추가 확인 — 메신저타임라인이 자체 스크롤 처리) |
| 모바일 `퀵스위치바.tsx`(QuickSwitchBar) | 133 | index.tsx는 주석에서만 언급 |
| 모바일 `반응선택.tsx`(ReactionMenu) | 111 | 신규 반응추가 UI가 어디서도 마운트 안 됨 → UX 누락 |

### 죽은 export (파일은 생존)
- `메신저데이터유틸.ts:172 selectFallbackChatRoomId`, `메신저포매팅.ts:91 wrapSelection`+`:124 FORMATTING_SHORTCUTS`(Ctrl+B/I/E 미배선), `메신저메시지렌더.tsx:139 extractFirstUrl`+`:155 OgLinkPreview`(링크프리뷰 미배선), `메신저메시지서비스.ts:28 buildForwardedMessageContent`+`chatQueryService.ts:141 updateRoomInList`, `emoticon-engine.ts:27/50/73 EMO_OFFICE/HOSPITAL/DEVELOPER`(const 강등 가능).
- 메신저.tsx 미사용 import 10건(:90-118 compareStaffMembers/extractWardMessageMeta/getBookmarkStorageKey/getConversationRoomIdSet/getLatestReadCursor/getPinnedStorageKey/readStoredBookmarks/readStoredStringArray/writeStoredBookmarks/writeStoredPinnedIds).

---

## 🟠 S — 런타임/정합

- **[S-1 / 정합 버그]** 모바일 `채팅/업로드.ts:170,215` 오프라인 큐 적재 시 직접 build한 payload가 `message_type:'image'|'video'|'file'`를 쓰고 `file_kind`는 누락. 데스크톱/온라인 경로(`buildChatMessageInsertPayload`)는 `file_kind`만 설정. 버블 렌더(`메시지버블.tsx isImage`)는 `file_kind` 우선 판정 → **오프라인 큐로 보낸 이미지가 file_kind=null이라 이미지로 안 보이고 URL 확장자 추측에만 의존**.
- **[S-2 / stale closure]** `메신저.tsx:408-432 resolveRoomMemberProfile` deps에 `effectiveChatUserId`(499 정의) 누락. `직원평가시스템`류 stale closure(런타임 에러는 아님).
- **[S-3 / 보안 의심]** 채팅 업로드 MIME "화이트리스트"는 실제 차단 기능이 아님 — `normalizeUploadMimeType`/`getUploadContentType`(`메신저업로드훅.ts`·`api/chat/upload/route.ts`)는 빈 값일 때만 확장자 추론하고 비기본 MIME는 무조건 통과. 크기제한만 유효.
- **Rules of Hooks 위반: 메신저.tsx 포함 데스크톱 클러스터 전수 확인 결과 없음.** (추가기능 모바일과 달리 채팅은 양호)

## 🟠 D — 중복

- **[D-1]** `updateUnreadForRooms` 3중: `메신저읽음상태훅.ts:90` ↔ `메신저방데이터훅.ts:201` ↔ `메신저.tsx:1345`. (앞 둘 중 훅은 고아, 데이터훅+메신저.tsx가 실사용 중복)
- **[D-2]** `메신저방데이터훅.ts`(1797 God 훅) 메타데이터 처리(읽음커서/북마크/반응/고정/투표)가 `fetchData`(1484-1743) ↔ `syncVisibleMessageMetadata`(882-1146) ↔ 개별 refresh 함수(530/618/691)에 **최대 3중 복제**. reaction 집계 루프 완전 복붙.
- **[D-3]** `buildForwardedMessageContent` `메신저메시지서비스.ts:28` ↔ `메신저메시지액션워크플로훅.ts:32` 동일 본문.
- **[D-4]** `MIME_BY_EXTENSION` 16매핑 + getUploadContentType/getFileExtension가 `메신저업로드훅.ts:30-48` ↔ `api/chat/upload/route.ts:22-40` 바이트 동일(클라/서버 미추출).
- **[D-5]** `escapeLikePattern` `메신저검색훅.ts:74` ↔ `GlobalSearch.tsx:11` 동일 복붙.
- **[D-6]** `메신저유틸.ts:186 isActiveChatMember` ↔ `:227 isActiveNoticeMember` 본문 바이트 동일(이름만 다름). `:130 sortChatRoomsWithNoticeFirst` ↔ `:577 sortRoomsForSidebar` 정렬 코어 중복.
- **[D-7]** 모바일 `이모지피커.tsx`(710) — 데스크톱 `EmojiPicker.tsx`의 라벨/엔진은 공유하나 카테고리·정적목록·검색·그리드 로직 통째 재작성(대량 중복, "100% 동기화" 주석).
- **[D-8]** `메신저.tsx:140 formatChatLocalDateKey`(↔ lib/date-utils toDateKey), `:149 UUID_ID_PATTERN`(↔ 메신저유틸 isUuidLike) 로컬 중복.

## 🟡 Q — 유사기능 / 미완

- **[Q-1]** 예약공지: `메신저공지스케줄.ts` 전체가 localStorage 저장 + 클라이언트 타이머 발송. DB `scheduled_messages` 테이블(schema.ts:1929) 존재하나 앱 코드 INSERT/SELECT 0건 → **"앱이 열려있을 때만" 발송되는 한계 + DB 미사용**. 공지 리마인드 경로가 운영센터/예약훅/메신저.tsx 인라인 3곳 분산.
- **[Q-2]** `메신저방데이터훅 fetchData`(1361)가 `.in(room_id).order(asc)` limit 없이 전체 메시지 로드 ↔ `fetchMessagePage`/`loadMessagesAroundMessage` 페이지네이션과 공존(대화량 많으면 성능 위험).
- **[Q-3]** 채팅 통합검색(메시지) ↔ 전역 `GlobalSearch`(메시지) 검색 타입 중복(스코프 다름).
- **[Q-4 / 미완]** 모바일 채팅방 상세시트(`채팅방.tsx:520-636`): 알림토글 핸들러 없음, "상단 공지" 하드코딩 "없습니다", "공유 사진/파일" 빈칸 3개 하드코딩, "채팅방 나가기"가 실제 나가기 아닌 목록 이동. `채팅목록.tsx`의 '채널' 탭은 칩이 안 그려져 도달 불가.

## 🟡 U — 불필요

- `메신저타임라인.tsx:100-131` 데드 prop 10개(isLoadingMessages/activeActionMessageId/pinnedIds/bookmarkedIds/onTogglePin/onStartEdit/onOpenEditHistory/onCopyMessageLink/onOpenReadStatusPanel/onOpenThreadPanel) — 메신저.tsx가 넘기나 타임라인이 안 씀.
- `메신저사이드바.tsx:137` `void [...]` 7개 prop 명시적 미사용(멘션/스레드 인박스 제거 잔재).
- `메신저컴포저.tsx:64 onOpenPollModal`/`selectedRoomName` 데드 prop, `메신저첨부미리보기.tsx:271 onForwardAttachment`/`:57 resetTransform` 미사용.
- 모바일 `채팅방.tsx:59-65 recentRooms`/`onSwitchRoom` destructure만(퀵스위치바 제거 잔재), `채널생성탭.tsx:95` 삼항 양쪽 동일(desc 미반영 무동작).
- `메신저구독훅.ts` Phase 5-C/5-D/6 변경이력 주석 다수, `chatQueryService.ts:106` 주석-코드 불일치.
- `lib/chat-push-health.ts:232 D1MissingTableError` "사용처 없음" 명시 죽은 export.
- 명명 혼동: `메신저액션.tsx`=실제 ReactionDetailModal만, `조직도측면창.tsx`류처럼 파일명-내용 불일치.

## 정상 확인
- **K 한글깨짐**: emoticon-engine(628줄 라벨 포함) 전 클러스터 0건.
- **정합 OK**: `messages`/`chat_rooms`/`room_read_cursors`/`message_reactions` D1 정본 일치. **`chat_room_members` 테이블은 존재하지 않음** — 멤버는 `chat_rooms.members`(JSON) 단일 소스. `.from('chat_messages')` 호출 0(전부 `messages`). 모바일 채팅은 실제 fetch(mock 아님).
- 재시도큐 3종(메시지=localStorage / 첨부=IndexedDB)은 매체·페이로드 달라 분리 정당(공통부 추출됨). 모달 4종·테스트이벤트 모두 실사용.

---

## 권장 우선순위 (채팅)
1. **고아 16+2 파일 삭제 검토(~3,500줄)** — 단, 메신저.tsx 인라인본이 동일 기능 수행 전제. 특히 `메신저읽음상태훅`의 개선된 캐시/디바운스가 사장됐으므로 "삭제 vs 인라인 교체" 결정 필요.
2. 모바일 업로드 `message_type`→`file_kind` 정합(S-1, 실제 이미지 미표시 버그).
3. 메신저방데이터훅 메타데이터 3중 복제 단일화(D-2).
4. 예약공지 localStorage vs `scheduled_messages` DB 정책 결정(Q-1).


---

# ⑤ 게시판 메뉴 — 재조사 보고서 (현 코드 상태)

> 재조사일: 2026-06-01 · 기준 커밋: 4bd799d2 · 대상 ~14,200줄 (데스크톱 + 게시판서브 + 모바일 + api)
> 방식: 4클러스터 병렬 정독 + Grep 검증

---

## 🔴 D — 중복 (핵심)

### [D-1] `게시판공통.ts`(430) ↔ `게시판-view-utils.ts`(427) 대규모 함수 중복 + **동작 분기**
- 거의 동일한 함수/타입 ~20쌍 중복 정의(inferAttachmentType, extract/buildAttachmentMetaContent, normalizeScheduled*, extractScheduleMetaFromContent, extract/buildBoardMetaContent, normalizeBoardPost, normalizeBoardPostStatus, getBoardStatusTone, runBoardPostMutation, getMissingBoardPostColumn, buildSelectColumns 등 + 타입 9종).
- **단순 중복이 아니라 동작이 갈림 → PC(view-utils)/모바일(공통) 불일치**:
  - `normalizeScheduleTimeValue`: 공통(:253) 빈값가드 없음 vs view-utils(:238) 가드 있음 → 결과 다름.
  - `normalizeBoardPostStatus`: 공통(:391) 미정의 상태 '게시중' 강제 vs view-utils(:385) 그대로 통과.
  - `normalizeBoardPost`: view-utils만 `schedule_meta_legacy_missing`/`embedded` 부여 → **PC UI(게시판.tsx:425,2553)가 이 필드에 의존하므로 공통으로 정규화된 모바일엔 경고가 절대 안 뜸**.
  - `getBoardStatusTone`: 색 클래스 PC/모바일 불일치(red-500 vs red-600 등).
  - `getMissingBoardPostColumn`/`isMissingBoardReadStorageError`: 공통은 message만, view-utils는 code+details+hint까지 → 모바일 에러 폴백이 약함.
- **결과 [X]**: 게시판공통.ts의 schedule/meta/status/mutation 계열 export ~15개가 import 0(실 소비자는 view-utils판). → 함수 정본을 view-utils로 두고 게시판공통은 상수+실사용 6개만 남기면 중복+분기 동시 해소.

### [D-2] `extractScheduleMetaFromContent` 3중 정의
- `게시판공통.ts:263` · `게시판-view-utils.ts:249` · `OP체크/schedule-helpers.ts:34`(stripHiddenMetaBlocks 적용 미묘 분기).

### [D-3] `업무가이드.tsx`(2142 God)가 `guide-utils.ts`/`guide-types.ts`를 inline 재구현
- `:35-160`(상수+타입) + `:162-613`(~25개 함수 normalizeText/buildSelectColumns/extractAttachmentMeta/normalizeGuide*/runGuideMutation/matchesTeamScope 등)을 통째 인라인 → guide-utils/guide-types와 거의 1:1 중복. **import로 대체 시 ~450줄 감축.**
- **결과 [X]**: `guide-utils.ts` export ~22개 중 실사용은 GuideDetailPanel이 쓰는 formatDate/getGuideAudienceLabel/getGuideKindLabel 3개뿐. 나머지(buildCompanyScopes/runGuideMutation/parseKeywords 등) import 0 죽은 export. `guide-types.ts:122 GuideLibraryProps`도 미사용(.tsx가 Props 재정의).

### [D-4] `board-poll-prize.ts drawBoardPollPrize`(:86) ↔ `메신저입력워크플로훅.ts:242-294` 추첨 로직 중복
- Fisher-Yates 셔플 + staff_members 이름조회 + 당첨자 선정 흐름 거의 동일. 타입(BoardPollPrizeWinner ↔ PollPrizeWinner)도 평행. 저장위치만 다름(board_posts.poll vs polls). 공통 코어 추출 후보.

### [D-5] `api/board/notice-broadcast/route.ts:225-242` 한 공지에 알림 2건 발송
- 2단계가 `type:'board'` 알림 직접 insert + 3단계 `dispatchChatPushForMessage`가 같은 메시지에 `type:'message'` 알림 별도 insert(`chat-push-dispatch.ts:646-653`) → **공지 1건당 board+message 알림 중복 적재**(코드상 확정).

---

## 🟠 X — 죽은 코드

- **[X-1]** `게시판서브/업무가이드.tsx.bak`(1870줄) — git untracked 백업 파일(`git ls-files` 부재, import 0, 번들 미포함). IDE/Grep 노이즈. 삭제 권장.
- **[X-2]** `post-table-helpers.ts:66-70 getCommentCount` 死기능 — `comments_count`/`comment_count`를 읽지만 데스크톱(`기능부품/`)에서 두 컬럼을 BoardPost에 채우는 코드 0건 → PostTableView 댓글 정렬·💬카운트(`:171,401`) 항상 0. (모바일 data-hooks만 댓글수 산출)
- (D-1/D-3에 따른 게시판공통/guide-utils 죽은 export 다수 — 위 참조)

## 🟠 Q — 유사기능 / 미완

- **[Q-1]** `BoardMobilePostCard.tsx` — 데스크톱 게시판.tsx:2290이 `isMobile && <BoardMobilePostCard>`로 렌더하나, 실제 모바일 셸은 `모바일/게시판`(자체 PostCard 보유)을 마운트 → 진짜 모바일 경로에선 도달 불가 의심 + 모바일 PostCard와 기능 중복.
- **[Q-2]** 모바일 추가기능/업무가이드.tsx — 데스크톱 GuideLibrary와 로직 공유 없는 별개 read-only 뷰. `kind`/`audience`/`keywords` 메타 무시하고 제목·본문 키워드로 카테고리 재추론(`:36-43`) → 동일 글의 분류가 PC와 불일치 가능. meta 미파싱으로 GUIDE_META 마커가 본문에 노출될 의심.
- **[Q-3]** 게시판 10초 폴링 + `fetchPosts limit(500)` 전체 재조회(게시판.tsx:638-660,485) — board_posts 변경 시 최대 500행 재페치. 비관리자도 익명소리함 500행 페치 후 클라이언트에서 버림(트래픽 낭비).

## 🟡 U — 불필요 / 무동작

- **[U-1]** `PostMoreMenu.tsx:14,98 onBookmark` prop — 호출처(PostTableView:456)가 안 넘김 → 북마크 메뉴 영구 비노출.
- **[U-2]** `PostTableView.tsx:57,463 onShareToChat` prop — 게시판.tsx 렌더가 안 넘김 → "채팅으로 공유" 영구 비노출.
- **[U-3]** 모바일 무동작 버튼: `게시판목록.tsx:113,325` 검색 2곳 onClick 없음, `게시판상세.tsx:271` 더보기 onClick 없음.
- **[U-4]** `게시판공통.ts:286-290` 제거된 buildScheduleMetaContent 설명 11줄 NOTE 주석(함수 없음). `게시판.tsx:2617` 주석 위치 오정렬.
- **[U-5]** `guide-types.ts:15-28` `workflowDependencies`/`linkedTaskTemplates`/`collaborativeDepartments` 미사용 스키마 잔재.

## 🟡 S — 문법/명명 (경미)

- **[S-1]** `게시판목록.tsx(모바일):53-54` `countUnread` if 조건 중복(한 줄 죽은 분기) + 함수명 "unread"인데 실제 "최근 7일 글 수"(의미 불일치).
- **[S-2]** `PostTableView.tsx:183 handleSortKeyDown` 명명 오류(실제 읽음필터 탭 핸들러).
- **[S-3]** `게시판.tsx:958 .returns<any>()`, `:741 handleLike`가 `:850 setSelectedPostDetail` 선언 전 참조(런타임 무해, 가독성).

## 정상 확인
- **K 한글깨짐**: 전 클러스터 0건.
- **Rules of Hooks**: 게시판.tsx·업무가이드.tsx·GuideDetailPanel·모바일 전부 위반 없음(조기 return이 모든 훅 이후 또는 동기 호출로 회피).
- **정합 OK**: board_posts(board_type으로 구분, category 컬럼 없음)·board_post_comments(board_comments 아님)·board_post_stars(모바일 별표)·board_post_likes(좋아요) D1 정본 일치. 별표/좋아요는 별개 기능의 별개 테이블(결함 아님). 모바일 게시판 실제 fetch(mock 아님). 첨부업로드는 데스크톱 게시판업로드.ts 재사용(의도된 공유).

---

## 핵심 결론 (게시판)
1. **게시판공통.ts ↔ view-utils.ts 중복 + 동작분기**가 PC/모바일 상태색·예약메타·legacy경고 불일치를 유발(최우선 통합).
2. 업무가이드.tsx가 guide-utils/types를 inline 재구현 → ~450줄 중복 + utils export 대부분 죽음.
3. notice-broadcast 공지 1건당 알림 2건 중복 발송(D-5).
4. .bak 백업파일·getCommentCount 死기능 정리.


---

# ⑥ 전자결재 메뉴 — 재조사 보고서 (현 코드 상태)

> 재조사일: 2026-06-01 · 기준 커밋: 4bd799d2 · 대상 ~18,900줄 (코어+서브 + 모바일 + api)
> 방식: 5클러스터 병렬 정독 + Grep/스키마 직접 검증

---

## 🔴 S — 런타임/정합

### [S-1] 출결정정양식 insert가 존재하지 않는 컬럼 사용 → 항상 1차 실패 후 폴백
- `출결정정양식.tsx:344` insert row에 `approval_status:'대기'`. D1 `attendance_corrections` 스키마(schema.ts:152-161)에는 **`status`만 존재, `approval_status`/`approved_by`/`approved_at` 없음**(직접 확인). → 정상 경로가 항상 missing-column 에러 → `withAttendanceCorrectionsFallback` 재시도에 의존(신청 1건당 쿼리 2회).
- **[S-1b]** `출결정정양식.tsx:563` `selectedDates.sort()` — React state 배열 in-place 변이(312행은 `[...].sort()` 복사 정상, 563만 누락).

### [S-2] 모바일 양식 type 라벨 불일치 → 필터 매칭 실패 의심
- `필터시트.tsx:41-54` TYPE_OPTIONS(`'연장근무'`/`'물품신청'`/`'출결정정'`) vs 폼 저장 type(`작성하기.tsx:30` `'연장근무 신청'`/`'물품구매 신청'`/`'출결정정 신청'`). `결재함.tsx:113`이 `row.type` 완전일치 필터 → 해당 유형 필터 매칭 실패 가능.

### [S-3 경미] 첨부 사양 불일치
- `upload/route.ts`는 비디오 200MB 허용, `AttachmentPicker.tsx:24` 클라 MAX 50MB + ACCEPTED_MIMES에 video 없음 → 모바일 첨부 UI는 비디오 차단(서버는 허용).
- **Rules of Hooks**: 전자결재.tsx·모든 서브·모바일 위반 없음(조기 return이 모든 훅 이후).

---

## 🔴 X — 죽은 코드 (Grep 검증)

- **[X-1]** `ApprovalMetaPanels.tsx`(700줄) JSX 패널 5종 import 0 — `RosterRequestInfoPanel`(:553)·`SupplyRequestItemsPanel`(:251)·`LeaveRequestInfoPanel`(:334)·`ReportInfoPanel`(:365)·`ApprovalAttachmentsPanel`(:402). 실제 렌더는 ApprovalDetailModal이 iframe srcDoc(`render*Html`)로 처리 → **파일의 ~250줄(JSX 패널 절반)이 죽은 코드**.
- **[X-2]** `useApprovalRouting.ts:148-218 syncApprovalDelayNotifications`(70줄) 정의·return되나 호출 0(실제 지연알림은 `syncDelegatedApprovalDelayNotifications`가 수행). `:64 resolveApprovalDelayHoursForStaff`, `:221-225` approvalStaffMap/approverCandidates/resolveApprovalDelayConfigForStaff도 전자결재.tsx 미수신.
- **[X-3]** `useApprovalBulkActions.ts:65-80,200 processFinalApprovalOnServer` 정의·return되나 호출 0.
- **[X-4]** `전자결재.tsx:816-817`이 `normalizeApprovalLineIds`/`resolveApprovalLineIds`를 useApprovalActions로 전달하나 훅은 타입에만 선언·본문 미사용(죽은 prop). `:1324 isApprovalEditLockedItem`→ApprovalInboxView도 동일(죽은 prop).
- **[X-5]** `출결정정양식.tsx:371-393 applyCorrectionToAttendance` 정의 후 호출 0(attendance/attendances upsert 통째 死).
- **[X-6]** `관리행정양식.tsx:21-28 hospitalGuides` 정의 후 `void hospitalGuides`(가이드 6종 미렌더). `roster/approval-request/route.ts:9 rosterApprovalRequestsTable` import 미사용 + `roster_approval_requests` 분기 죽음(항상 'approvals' 하드코딩).

## 🟠 D — 중복

- **[D-1]** `buildApprovalHistoryEntry` useCallback 래퍼 3중 동일: `useApprovalActions.ts:71` · `useApprovalRouting.ts:73` · `useApprovalSubmit.ts:137`(코어는 lib/approval-shared 공유하나 래퍼 중복).
- **[D-2]** `useApprovalRouting.ts:148 syncApprovalDelayNotifications`(死) ↔ `useApprovalDelegation.ts:74 syncDelegatedApprovalDelayNotifications` 지연알림 로직 거의 동일.
- **[D-3]** `ApprovalMetaPanels.tsx:450 renderRosterInfoHtml` ↔ `:553 RosterRequestInfoPanel`(死) staffRowMap/resolveBand 로직 2벌.
- **[D-4]** `ApprovalInboxView.tsx:144 inboxHeaderStats` ↔ `ApprovalWorkflowKpi.tsx:83 stats` myTurn/approvedThisMonth 동일 계산을 결재함에서 2중 렌더.
- **[D-5 모바일]** 결재선 자동매핑 3중: `일반기안폼.tsx:63` · `연차신청폼.tsx:73` · `useApproverLine.ts:35`(훅 추출됐으나 두 폼이 인라인 복붙·미사용). 상신 insert도 `submitApprovalDraft` 공용 vs 일반기안폼/연차신청폼 자체 insert(미이관, 주석에 "다음 배치 이관" 명시).
- **[D-6 모바일]** readAmount(`기안함.tsx:54`↔`결재함-cards.tsx:58`), formatTs/shortTs 날짜포맷 4벌(`기안함:47`/`참조함:20`/`결재상세:70`/`결재함-cards:26`).

## 🟠 Q — 유사기능 / 패턴 이탈

- **[Q-1]** 제출 패턴 이탈: 8개 데스크톱 폼 중 **출결정정양식만** 자체 `attendance_corrections` 직접 insert(`:326`) + setExtraData로 approvals에도 올림 → **동일 신청 2테이블 이중 기록 의심**. `양식신청.tsx:112`도 자체 approvals insert(공통 훅 우회).
- **[Q-2]** `연차사용계획서양식.tsx`(데스크톱, 잔여연차 staffs 직접계산) ↔ 모바일 `연차계획폼.tsx`(useMyLeaveBalance) — 잔여연차 산출 로직 중복·불일치.
- **[Q-3]** 모바일 `결재선피커.tsx`(479) ↔ 데스크톱 결재선 지정 UI — staff fetch+직급정렬 중복(모바일 전용 재구현, 코드 공유 없음).
- **[Q-4]** 비품 단위 선택지 뷰포트 불일치: `비품구매양식Grid.tsx:22` 6종(EA/BOX/박/병/kg/L) vs `SuppliesMobileCard.tsx:126` 2종. 게다가 `normalizeInventoryUnit`이 BOX 외 전부 EA로 환원 → 데스크톱 박/병/kg/L 선택은 저장상 무의미.

## 🟡 U — 불필요 / 보류

- **[U-1]** `useApprovalActions.ts:218-336` 반려·회수 클라이언트 폴백(직접 supabase.update 권한판정) — 승인은 서버 단일경로인데 반려/회수만 클라 폴백 유지(비대칭, 서버화 보류 잔재). `buildNextApprovalMetaData`(:82)는 이 폴백 전용.
- **[U-2]** `ApprovalInboxView.tsx:38-68` 미사용 prop 8개(defaultApprovalMonth/WeekDate/effectiveApprovalDateRange/hasApprovalFilterOverrides/resolveApprovalTemplateDesign/DelegateSnapshot/LockSnapshot/isApprovalEditLockedItem) — 부모가 넘기나 버려짐.
- **[U-3]** `SuppliesSummary.tsx:55 estimatedHours=14` 하드코딩 더미를 "예상 처리 14시간"으로 단정 표기(호출부 미전달이라 항상 14).
- **[U-4]** `관리행정양식.tsx:176` 업무보고 "관련부서" input onChange 없음(죽은 입력). `작성하기.tsx:56 void BUILTIN_FORM_TYPE_DEFINITIONS` 디버그 잔재. `셸/플레이스홀더.tsx:24` "결재함 다음 단계 도입" stale 문구(실제는 마운트됨).
- **[U-5]** `전자결재-utils.ts:162 escapeHtml` re-export 경유 불필요(소비처 print-utils 1곳).

## 정상 확인 / 무혐의
- **K 한글깨짐**: 전 클러스터 0건. 관리행정양식 무효 Tailwind도 무혐의(bg-blue-500/10 등 유효).
- **정합 OK**: `approvals` 정본(approval_documents 직접접근 0, 문서보관함은 lib/approval-document-archive 분리). `current_approver_id`/`approver_line` 컬럼 존재. 첨부 `meta_data.attachments` 정본. 모바일 실 fetch(mock 아님). `purchase_requests`/`supplies` 테이블 없음 — 비품은 approvals(type='물품신청')+meta_data 저장(정상).
- **재사용 양호**: lib/approval-shared(normalizeApprovalLineIds/resolveEffectiveApproverIdCore) 정상 재사용. escapeHtml 단일정의. 서버 권한검증(server-approval-transition.ts:379 actor===current_approver) 정상(클라 canApprove는 UI 게이트).
- **KpiCard**: 코드베이스 전역 7곳 각자 정의(결재함-cards/입출고-form/CompanyCardTab/수술상담-components/stock-workcenter-common 등) — 공용화 미흡(전역 이슈).

---

## 핵심 결론 (전자결재)
1. **출결정정양식**: 미존재 컬럼(approval_status) insert로 항상 폴백 의존(S-1) + state 변이(S-1b) + applyCorrectionToAttendance 死코드 + approvals/attendance_corrections 이중기록 의심.
2. ApprovalMetaPanels JSX 패널 5종(~250줄) 죽은 코드, useApprovalRouting 지연알림 70줄 死.
3. 모바일 결재선 자동매핑·상신 insert가 추출 훅(useApproverLine/submitApprovalDraft) 미사용 인라인 중복(이관 미완).
4. 반려/회수만 클라 폴백 비대칭(서버화 보류).


---

# ⑦ 인사관리 메뉴 — 재조사 보고서 (현 코드 상태)

> 재조사일: 2026-06-01 · 기준 커밋: 4bd799d2 · 대상 ~55,000줄 (서브 41k + 워크센터 13k + 모바일 6.3k)
> 방식: 도달성(reachability) Grep 스캔 + 4클러스터 병렬 정독 + 스키마 직접 검증
> ⚠️ 규모상 가장 방대한 메뉴 — 핵심 클러스터 중심으로 정독, 일부 세부 컴포넌트는 reachability 스캔으로 갈음(아래 "커버리지" 참조)

## 구조 핵심
- `인사관리.tsx`는 **워크센터 라우터**(member/attend/leave/payroll/welfare/docs 6개)를 렌더. 인사관리서브 레거시는 워크센터가 **dynamic import로 재사용하거나(Docs/Welfare) 무시하고 재구현(Leave/Abnormal/Payroll)** 하는 혼재 구조.
- 특이: `인사관리.tsx:458,467` leave·payroll은 **`navigator.webdriver`(테스트)일 때만** 레거시 ClassicLeaveManagement/ClassicPayrollMain 렌더 → 레거시가 사실상 테스트 전용으로만 생존.

---

## 🔴 X — 죽은 코드 (Grep importer=0 직접 확인)

| 파일 | 줄 | 비고 |
|------|----|------|
| `급여명세/급여계산기.tsx` | 823 | netPay/통상임금/일용 3모드 계산기, 마운트 0(기능은 일용근로정산·통상임금계산기가 이미 제공) |
| `근무형태변경이력.tsx` | 561 | WorkTypeChangeHistory import 0(docs/ppt만 참조) |
| `급여명세/급여자동이체.tsx` | 340 | import 0(OpsWorkcenter는 라벨 문자열만) |
| `포상징계관리.tsx` | 286 | import 0, 라우팅 미연결 |
| `계약문서/전자서명패드.tsx` | 169 | import 0(실사용은 전자서명모달의 react-signature-canvas) |
| `급여명세/노무준수패널.tsx` | 65 | import 0 |
| 모바일 `급여명세서.tsx`(SPayroll) | 374 | import 0(payroll 뷰는 급여워크센터 사용) |
| `인사관리워크센터/DocsWorkcenter/DocsCertSummary.tsx` | — | import 0(증명서 탭은 요약카드 미렌더) |

**합계 ~2,900줄+ 죽은 파일.** 추가 죽은 분기:
- **[X]** `증명서발급.tsx:494-663` `selectedStaff ? renderCertificatePaper() : false ? (~170줄) : (...)` — 가운데 `false?` 분기 영구 도달 불가(대형 죽은 JSX).
- **[X]** `근태관리메인.tsx:754-773` `if (false && request.requested_by) {...}` 죽은 알림 블록(~20줄).
- **[X]** `MemberWorkcenter.tsx:65 onOpenNewStaff` prop destructure만, 라우터가 undefined 전달(dead prop).

## 🔴 S — 런타임/컬럼 불일치

- **[S-1 / 알림 누락]** `계약관리.tsx:273` notifications insert가 `message:` 키 사용. **D1 정본 notifications에는 `message` 컬럼 없음, `body`가 정본**(schema.ts 직접 확인). 동일 디렉터리 타 파일(교육관리:465 등)은 모두 `body` 사용 → 계약서 서명요청 알림 본문이 누락/거부.
- **[S-2 / 모바일 의료기기 빈결과]** `모바일 data-hooks.ts:427` `medical_devices` select가 `device_name/vendor/next_check_date` 사용하나 정본은 `name/model/next_inspection_date`(PC WelfareWorkcenter:192는 정상) → 모바일 복지>의료기기 쿼리 에러/빈결과 의심.
- **[S-3 / 합성값]** `모바일 data-hooks.ts:1046` `useTeamAbnormalByDay`가 `late_minutes` 미존재 컬럼을 읽어 항상 0 → 지각분 status='late'시 **고정 5분 플레이스홀더**(근태이상카드 "N분"이 합성값).
- **[S-4 의심]** `근태관리메인.tsx:277` `.eq('status','승인')` 한글 하드코딩 — lib는 `'승인'∪'approved'` 허용 → 영문 status 데이터 누락 가능.
- **[S-5 의심]** `오프보딩.tsx:572` `s.hire_date` 참조(타 파일은 joined_at/join_date 혼용) → 입사일 컬럼명 불일치 시 빈칸.
- **[S-6]** `인사발령관리.tsx:463,482` `bg-blue-500/10/30` 무효 Tailwind(이중 분수, 스타일 미적용). `전자서명모달.tsx:35` 오타 `있임을`→`있음을`.
- **Rules of Hooks 위반: 정독 범위 내 없음.**

## 🟠 D — 중복

- **[D-1 / 급여정산 죽은 legacy 중복]** `급여정산.tsx:701-816` `handleDraftSaveLegacy`/`handleFinalizeLegacy`(~106줄)가 신규 handleDraftSave/Finalize와 완전 중복 + `void`로 무력화(죽은 함수). `연말정산.tsx:255 legacySendCertificateEmail`도 동일.
- **[D-2]** `중간정산.tsx:307-534` 4대보험/소득세 계산 블록이 `급여정산.tsx:528-575`와 거의 동일 인라인 복제(상수는 lib SSOT 쓰나 계산 흐름 중복).
- **[D-3 / 근태유틸 중복]** `근태기록/근태유틸.ts` ↔ `근태관리메인-내부유틸.ts` 동일 함수 다수 중복(resolveLeaveStatusForDate/getAttendanceStatusMeta/buildWeekDates 등). 근태유틸은 일괄수정모달 1곳만 import → 이전 시 삭제 가능. **위험**: 두 파일이 `LEGACY_ROSTER_APPROVAL_TYPE`을 다른 값(`'roster_schedule_approval'` vs `'근무표'`)으로 정의 → 혼용 시 승인조회 깨짐.
- **[D-4 / 근무표 3중 시스템]** `근태근무표뷰`(shift_assignments 월그리드) ↔ `AttendWorkcenter/RosterGrid`(shift_assignments 14일) ↔ `간호근무표`(별도 `nurse_schedules` 테이블) — 동일 도메인 교대근무표가 **3개 UI + 2개 테이블**로 병존.
- **[D-5 / 이중 테이블 기록]** `모바일 data-hooks.ts:926,1187` 및 PC AbnormalWorkcenter — 근태 resolve 시 `attendance`(status='정상')와 `attendances`(status='present')를 항상 동시 갱신(이중 기록 결합).
- **[D-6 의심]** 계약서 본문 파싱(`제N조` 정규식)이 `전자서명모달.tsx:451-567` ↔ `계약서미리보기` ↔ `lib/contract-body-print-html` 3중 잔존(W7 지적 미해소).

## 🟠 Q — 유사기능 (워크센터 ↔ 레거시 병행 재구현)

- **[Q-1]** LeaveWorkcenter — 레거시 휴가관리메인/연차원장 미사용, 자체 fetch+승인모달 전면 재구현(병행 중복).
- **[Q-2]** AbnormalWorkcenter — 레거시 지각조퇴분석/조기퇴근감지 미사용, 자체 재구현. 게다가 `AttendWorkcenter:198`이 abnormal 탭으로 임베드 + 라우터(index.tsx:134)가 standalone으로도 라우팅 → 동일 중량 컴포넌트 2경로 마운트.
- **[Q-3]** payroll/(Mod* 13개) — 레거시 급여정산/급여대장표/퇴직금/4대보험 등과 기능 중첩, PayrollProvider 자체 fetch 전면 재구현(병행 중복).
- **[Q-4 / 연차 통계 3중 계산]** `연차원장`(lib SSOT 정상) vs `연차종합대시보드`(자체 인라인 `total-used`, 소멸·수당 미반영 → 불일치) vs `연차휴가신청내역`(또 다른 인라인) — 연차 통계가 3곳 제각각.
- **[Q-5]** AttendWorkcenter는 근태관리메인을 dynamic import 재사용하나 KPI/대시보드/근무표는 자체 재구현(부분 중복). DocsWorkcenter/WelfareWorkcenter는 레거시 전부 위임(정상 래퍼).

## 🟡 U — 불필요 / 무동작 / 더미

- **[U-1 / 모바일 더미]** `모바일 index.tsx:151-163` 허브 KPI(27명/24명/11일/4.2년) 전부 하드코딩(주석도 더미 인정).
- **[U-2 / 무동작]** `ModSettlement.tsx:90,123` 단계 액션버튼 onClick 없음, "다음단계" 600ms 스피너만. `DocsWorkcenter.tsx:325 ContractGenWizard currentStep={1}` 고정. 모바일 `연차.tsx:69`(휴가계획서)·`근태이상.tsx:244`(사유입력) onClick 없음.
- **[U-3 / 퇴화]** 모바일 `계약문서.tsx SubmitTab` — document_repository 재조회 후 전부 `status:'완료'` 강제 → "제출 필요" 경고 항상 미표시(내문서 탭 라벨만 다른 복제).
- **[U-4]** `AbnormalWorkcenter.tsx:156` "처리완료" KPI가 세션 로컬 resolvedLog.length(remount시 0, "이번달 누적" 라벨과 불일치).
- **[U-5 / audit 누락]** 증명서발급/건강검진/사고보고서/법인카드/비품대여/경조사/스마트서류제출 모두 `logAudit` 0건(발급·기록 추적 누락, 보안 보강 후보).
- **[U-6 / God]** `구성원현황.tsx`(2431줄) any/eslint-disable 55건 — 분할 리팩터 대상(jm 500줄 원칙 위반).
- **[U-7]** `급여정산.tsx:317` attendanceRecordRows 항상 빈배열 → 분단위 보정 dead path. `연말정산.tsx:79` handleOCRScan 인자 미사용.

## 🔵 보안 (확인)

- **[SEC-OK]** 계약 PII 암호화 적용됨 — write(`마이페이지/index.tsx:309 encryptContract`)/read(`문서보관함.tsx:177 decryptContract`) 대칭(AES-GCM, enc:v1:). **과거 "평문 저장" 지적 stale(해소)**.
- **[SEC-주의]** 모바일 `구성원등록.tsx` — 클라에서 `role`(employee/team/manager/admin)을 staff_members에 직접 기록(password/permissions/주민번호는 미기록). `canAccessHrSection`+회사 가드 있으나 role 클라 선택은 서버 RLS 검증 의존.
- **[SEC-주의]** `전자서명모달.tsx:346,357,367` 인쇄 HTML에 `${user?.name}`/`${company?.name}` escapeHtml 없이 삽입(self-XSS 저위험, lib/escape-html 적용 권장).

## 정합 OK / 무혐의
- **K 한글깨짐**: 전 디렉터리 mojibake 0건.
- payroll_records upsert는 `lib/payroll-record-upsert.ts` SSOT 통일(record_type regular/interim/daily/yearend 분리). 4대보험/소득세 요율 `lib/payroll-insurance-rates`·`use-tax-insurance-rates` SSOT.
- 연차 계산 `lib/annual-leave-ledger`(calculateApprovedAnnualLeaveUsage/calculateLeaveDays) 정상 재사용(휴가관리메인/연차원장).
- document-print-utils ↔ lib/escape-html, 증명서발급 ↔ certificate-print-utils 중복 **이미 통합 해소**.
- staff_licenses/personnel_appointments(staff_appointments 아님) 컬럼 정합. 모바일 폼(연차신청/근태조정/구성원등록) 실 insert(offline-queue).

---

## 핵심 결론 (인사관리)
1. **죽은 파일 ~2,900줄+**(급여계산기 823·근무형태변경이력 561·자동이체 340·포상징계 286·전자서명패드 169·노무준수패널 65·모바일 급여명세서 374·DocsCertSummary) + 죽은 분기(증명서발급 false삼항 170줄, 급여정산 legacy 106줄).
2. **워크센터 ↔ 레거시 병행 재구현**(Leave/Abnormal/Payroll)이 최대 구조 부채 — 동일 기능 2벌 유지.
3. 신규 실버그: 계약관리 notifications.message 컬럼(S-1), 모바일 medical_devices 컬럼(S-2), 근태이상 합성 분(S-3).
4. 근무표 3중 시스템·연차 통계 3중 계산·근태유틸 중복 — 통합 검토.

## 커버리지 메모
- 1차 정독: 급여명세 핵심(13)·근태/근무형태/휴가(14)·구성원/계약/문서/면허/교육/복지(25+)·워크센터 전체·모바일 전체.
- 2차 정독(아래 보강): 1차에서 reachability만 본 급여명세 대시보드/노무지표 ~30개 + 상위/휴가/교육/계약 미정독분 ~25개 전부 정독 완료. **인사관리서브 전 파일 커버.**

---

## ➕ 2차 정독 보강 (2026-06-01, 미정독분 완료)

### 🔴 S — 신규 런타임/정합 버그
- **[S-7 / 제약위반]** `휴가신청/연차촉진시스템.tsx:178-188` upsert가 `annual_leave_promotion_logs`의 **NOT NULL 컬럼 `target_year`·`step`·`id`(기본값 없음, 스키마 직접 확인) 누락** → INSERT 제약위반으로 **연차촉진 기록 실패 가능**. (stage/expiry_date만 채움)
- **[S-8]** `의료기기점검.tsx:107` 삭제 확인창이 `target?.device_name` 참조하나 정본 컬럼은 `name` → 장비명 항상 폴백 '선택한 장비'.
- **[S-9]** `계약서자동생성.tsx:70` `s.base` 참조(정본 `base_salary`) → 직원 선택 시 급여 자동입력 항상 빈값(무동작).
- **[S-10]** `퇴직금계산기.tsx:84-85` annual 모드 라벨·수식 불일치(월부담금에 연봉/144 표기).
- **[S-11 / 무효 Tailwind]** `연차소멸알림.tsx:127 bg-red-500/10/60`, `연차촉진시스템.tsx:232 bg-indigo-500/10/30` 이중 불투명도 토큰(스타일 미적용). `4대보험관리.tsx:347`·`교육내역명단.tsx:209` `/100` 잉여 토큰.
- **[S-12 / toast 톤 오기]** `급여명세서발송.tsx:181` 정상 발송요약을 `toast(...,'error')` 톤, `:74` 0명 안내를 success 톤.

### 🔴 X — 신규 죽은 코드
- **[X-7]** `급여명세/급여명세서.tsx` = `export { default } from './급여상세'` 한 줄 re-export, **importer 0**(급여관리·마이페이지 모두 급여상세 직접 import) → 죽은 alias.
- **[X-8]** `app/components/RiskActionDialog.tsx` — 실사용은 `인사관리서브/RiskActionDialog`(계약관리/구성원현황/급여정산 3곳). components판은 import 0 → 죽은 중복본.
- **[X-9]** `교육내역/education-utils.ts:51 getApplicableEducationItems` @deprecated인데 교육내역명단이 여전히 이것만 사용(신규 getEducationItemsForJobCategories 미사용, 전환 미완). `부서별급여분포.tsx:2 useState` 미사용 import.

### 🟠 D/Q — 신규 중복
- **[D-7]** `급여명세/4대보험EDI.tsx` ↔ `원천징수파일생성.tsx` 둘 다 payroll_records(확정)로 4대보험 EDI 파일 생성 — 책임 중복. `급여대장내보내기` SAM 이체CSV ↔ `급여자동이체`(고아) 중복.
- **[D-8 / 인건비 12개월 fetch 중복]** `인건비추이분석.tsx`(net_pay) ↔ `총인건비예측.tsx`(gross_pay) 둘 다 최근 12개월 payroll_records 월별 fetch·합산, 집계컬럼만 다름. 인사대시보드통합에 동시 배치.
- **[Q-6 / 근태이상 3중 소스]** `지각조퇴분석`(attendances.status 파싱) / `조기퇴근감지`(early_leave_records) / `휴가신청/근태이상탐지`(attendances+shift_assignments+leave_requests)가 같은 "조퇴/조기퇴근"을 다른 소스로 3번 계산. `근태이상통합분석`은 앞 둘을 탭으로 묶는 래퍼. 라우팅상 모두 attend 탭 매핑.
- **[Q-7 / 연차촉진 3중 + 잔여기준 이원화]** `연차촉진시스템`(staff.annual_leave_total/used) / `연차소멸알림`(leave_balances.remaining_days) / `알림자동화설정` — 잔여연차 산정 기준 이원화.
- **[Q-8 / 퇴직금 통합함수 死]** `퇴직금계산기`(DC) / `예상퇴직금연차대시보드`(법정) 둘 다 `lib/severance-pay` 쓰나 통합 함수 `calculateRetirementBenefitFromMonthlyWage`(severance-pay.ts:38)는 양쪽 미사용. 통상임금계산기는 hourly-rate SSOT 미사용 자체 나눗셈.

### 🟡 U — 신규 무동작/더미/실패은폐
- **[U-8 / 실패 은폐]** `경조사관리.tsx:135` insert 에러 시 crypto.randomUUID 로컬 fallback 행 추가 + 토스트 없이 폼 닫음 → **저장 실패가 성공처럼 보임(데이터 유실 은폐)**.
- **[U-9]** `인사통합설정.tsx:110 HRPolicies` 정책 토글/저장 버튼에 핸들러 없음, DB 로드/저장 없는 정적 UI(무동작).
- **[U-10 / 하드코딩 더미]** `급여시뮬레이터.tsx:69` 산재요율 'SY INC.' 고정, `무급결근차감.tsx:69` base 3000000·22일 고정, `미지급수당알림.tsx:65`/`임금피크제.tsx:91`/`퇴직연금관리.tsx:42` 기본급 미등록자 fallback 2000000~3000000, `비과세한도체크.tsx:43` childcare/night/overseas 한도 SSOT 외 상수.

### 정상 확인 (2차)
- 대시보드/노무지표 패널 **전부 실 fetch(mock 아님)** — payroll_records/attendances/retirement_pensions 실데이터. 법정기준/비과세/최저임금/주52시간은 lib SSOT(tax-free-limits/use-tax-free-settings) 정상 재사용.
- 백킹 테이블 실재: insurance_records/congratulations_condolences/early_leave_records/generated_contracts/annual_leave_promotion_logs 모두 schema 존재.
- 계약서표준미리보기↔계약서미리보기 = 계층구조(중복 아님), 4대보험관리↔4대보험EDI = 주제만 인접(중복 아님).
- K 한글깨짐: 2차 정독 ~55개 파일 0건.


---

# ⑧ 재고관리 메뉴 — 재조사 보고서 (현 코드 상태)

> 재조사일: 2026-06-01 · 기준 커밋: 4bd799d2 · 대상 ~17,800줄 (통합 558 + 서브 9.4k + 워크센터 3.3k + inventory-utils 1064 + 모바일 3.5k)
> 방식: 도달성 Grep 스캔 + 3클러스터 병렬 정독 + 스키마 직접 검증

## 구조
- `재고관리통합.tsx`가 라우터. 영문 id(status/io/item/analyze)면 `재고관리워크센터`로 위임, 한글 id면 `재고관리서브` 레거시 화면 dynamic import.
- **단, `isStockWorkcenterEnglishId`(통합.tsx:54)가 webdriver(e2e)면 false** → 테스트는 워크센터 경로 미진입. 워크센터/레거시 병행 구조.

---

## 🔴 S — 컬럼 불일치 / 런타임 (데이터 왜곡)

- **[S-1 / 분석 왜곡]** `재고관리워크센터/use-analyze-data.ts:24,82` `pickNumber(l,['amount','qty'],1)` — `inventory_logs`에 `amount`/`qty` 컬럼 없음(정본 `quantity`), **fallback에 quantity 키 누락** → 항상 `1` 반환. ABC분석 value=`1×unit_price`, 수요예측 사용량=로그건수로 왜곡. (status/io 훅은 quantity를 3번째 키로 둬 우연히 동작)
- **[S-2]** `use-io-data.ts:67` 납기 `delivery_date ?? due_date` 참조 — 정본은 `expected_delivery_date` → IO워크센터 납기 항상 '-'. `:58` `purchase_orders.items`는 TEXT(JSON문자열)이라 `Array.isArray` false → 품목수 항상 0.
- **[S-3 / inventory 중복컬럼]** schema.ts:832-860 `quantity`(def 0)/`stock`(def 0), `min_quantity`(def 5)/`min_stock`(def 10)/`safety_stock`, `item_name`/`name`, `unit_price`/`price`, `supplier_name`/`supplier`, `expiry_date`/`expiration_date` 동일의미 컬럼 다중화 + **기본값 불일치(min 5 vs 10)** → 진입경로마다 "최소재고" 판정 상이 위험. 전 컴포넌트가 `??` 폴백으로 양쪽 읽음(유효기간알림은 expiration_date 우선, 재고월마감은 expiry_date).
- **[S-4 / 발주 FK]** `발주관리.tsx:255` purchase_orders.supplier_id FK(onDelete restrict)인데 inventory엔 supplier_id 거의 없고 자유텍스트 supplier_name만 → 자동발주 insert는 사실상 항상 supplier_id null(FK 미활용), 미존재 id 유입 시 restrict 위반 가능.
- **[S-5 / 비원자 잔존]** `재고실사.tsx:99` 실사 조정이 RPC/API 미경유 `inventory.update` 직접 루프(동시성 last-write-wins). `inventory-utils.ts:632 processInventoryIssue`(불출)는 `callAtomicStockTransfer` 실패 시 순차update+수동롤백 fallback 잔존 → **이관(/api/inventory/stock-transfer, D1 batch)만 진짜 원자적, 불출/실사는 비원자**.
- **[S-6]** `거래처관리.tsx:192` 입력 컴포넌트 `F`를 render 본문 내부 정의 → 매 렌더 새 식별자, 입력 시 포커스 유실 위험. `:108` supplierColumns useMemo deps가 매 렌더 재생성(memo 무효). `스캔모듈완성.tsx:76` 오류메시지를 `toast(...,'success')` 성공톤 오용.

## 🟠 X — 죽은 코드 / 숨김 UI (Grep 검증)

- **[X-1]** `재고현황뷰.tsx` 대량 `<div className="hidden">` 블록(컨트롤바 338-435·긴급알림배너 439-473·공급워크플로우 478-580·처리완료히스토리 585-624·유효기간센터 762-781·테이블헤더 630-656). **렌더는 숨김인데 관련 state(sortBy/alertDismissed/showCompleted/expandedApprovalId 등)와 워크플로우 콜백을 통합 라우터가 전부 계산·전달 유지** → 죽은 UI + 미사용 prop 다수.
- **[X-2]** `부서소모기록.tsx:6 withMissingColumnsFallback` import 후 미사용. `납품확인서.tsx:11 showPreview` state 읽기/쓰기 0. `부서소모기록.tsx:132 handleConsume` deps에 미참조 user?.name.
- **[X-3]** `재고현황뷰.tsx:333` "이번 달 입고/출고" 메트릭 `"0건"` 하드코딩.

## 🟠 Q — 유사기능 / mock

- **[Q-1 / 전면 중복]** `재고관리워크센터`(Status/Item/IO/Analyze 4종)가 `재고관리서브` 레거시(재고현황뷰·발주관리·거래처관리·UDI·자산QR·재고실사·재고월마감 등 15+화면)를 **import하지 않고 새 훅으로 전면 재구현**(Grep 0건). 레거시 서브는 여전히 통합 라우터(한글 id)로 살아있음 → **동일 기능 2벌 공존**(인사관리와 동일 패턴).
- **[Q-2 / mock 스캐너]** `자산QR관리.tsx:60` 주석 "Mock Component" — 스캐너가 가짜 UI("카메라 로딩중" 애니만), `scanResult` 설정 코드 전무 → `handleBorrow`(:79) **도달 불가, 대여 처리 미동작**.
- **[Q-3 / QR 3중]** 자산QR관리(mock 미동작) vs UDI관리(스캔 아님, is_udi 선택→CSV) vs 스캔모듈완성(유일 실작동 바코드 입력) — "QR/바코드 자산" UI 3메뉴 분산.
- **[Q-4 / 거래처 CRUD 이중]** `거래처관리.tsx` ↔ `명세서관리.tsx` 둘 다 suppliers CRUD 독립 구현, `SupplierDocumentWorkspace`가 두 탭 동시 노출 → 동일 거래처를 두 곳에서 등록/수정 가능(필드셋 불일치: 거래처관리 category/contract vs 명세서관리 reg_num/ceo 별칭).
- **[Q-5 / 부서별재고 위치]** `부서별물품장비현황.tsx`는 재고관리통합 import 0, 실제 진입은 **추가기능 메뉴 "부서별재고" 카드**(추가기능공통.tsx:186)로만 → 재고 서브 폴더에 있으나 재고메뉴 미연결. 재고통합의 "내부서재고"(부서소모기록)와 별개.
- **[Q-6 / 워크센터 mock]** `분석마감.tsx ClosePane`·`AnalyzeWorkcenter.tsx:37 CLOSE_STEPS/HISTORY` 월마감 단계가 정적 placeholder('-', "2/5 완료" 하드코딩, closing_snapshots 미연동). `use-analyze-data buildInspects(106)` 실사세션 테이블 없어 done=total 강제(진행률 항상 100%).

## 🟡 U — 불필요 / 무동작

- **[U-1 / 무동작 버튼]** 워크센터 다수 onClick 없음: `StatusSubViews.tsx:79`(발주/자동발주/사용우선), `ItemWorkcenter.tsx:139`(물품/자산/UDI등록·일괄가져오기·라벨인쇄), `IOWorkcenter.tsx:171`(수동등록/새발주/상세). 모바일 `자산등록.tsx:70 MOCK_SCAN`(시연용), `발주등록.tsx:71` 거래처/품목 데모 하드코딩.
- **[U-2 / 모바일 더미]** `모바일/재고/index.tsx:149` Hub KPI(847/23/4/8) 하드코딩(주석 더미 인정).
- **[U-3 / 미사용 prop]** `물품등록.tsx:63 fetchSuppliers`(미사용), `카테고리관리.tsx:62 user`(미사용), `소모품통계.tsx:18 user`(미사용).

## 🟠 D — 중복 코드

- **[D-1]** `findDestinationInventoryItem` 2벌(`inventory-utils.ts:528` export ↔ `재고이관.tsx:23` 로컬, 로컬은 serial_number 비교 추가 — 동작 차이).
- **[D-2]** `normalizeInventoryUnit` 2벌(`inventory-utils.ts:244` ↔ `물품등록.tsx:54` 로컬 동일).
- **[D-3]** `qty()/minQty()/name()` `재고현황뷰.tsx:83` ↔ `재고현황뷰Grid.tsx:11` 중복(inventory-utils의 getItemQuantity 미사용).
- **[D-4]** `KpiCard` 동명 6곳(입출고-form/stock-workcenter-common/결재함-cards/CompanyCardTab/수술상담-components) 각자 정의. 모바일 재고 내에서도 KpiCard/MiniKpi/KpiSimple 3종 미니카드 중복. `toISODate` 자산등록/발주등록 중복.

## 정상 / 무혐의
- **K 한글깨짐**: 전 클러스터 0건(초기 정규식 매치는 한글바이트 오탐).
- **정합 OK**: `inventory_logs.quantity`(amount 아님) 일치. `as_repair_records`/`return_records`/`delivery_confirmations`/`inventory_closing_snapshots` 컬럼 일치. `suppliers`는 reg_num/ceo 없음 → 명세서관리가 business_number/contact_name으로 매핑 완료(무음실패 해소). inventory.unit 컬럼 없음 → payload 제외/폴백 처리.
- **보안 OK**: 납품확인서.tsx document.write에 esc() XSS 이스케이프 적용. 명세서관리는 window.print만(XSS 무관).
- **재고이관 원자성**: /api/inventory/stock-transfer D1 batch 경유(재고증발 버그 해소). 단 불출/실사는 비원자(S-5).
- **재고수요예측**: mock 아님(inventory_logs 90일 실데이터). inventory-utils export 순수 죽은 것 없음(단 1064줄 3책임 혼재 — jm 원칙 위반, 분할 후보).

---

## 핵심 결론 (재고관리)
1. **분석/IO 워크센터 데이터 왜곡(S-1/S-2)**: amount/qty 컬럼 오참조로 ABC분석·수요예측·납기·품목수가 항상 1/0/'-'.
2. **워크센터 ↔ 레거시 서브 전면 병행 재구현(Q-1)** — 인사관리와 동일 최대 구조부채.
3. 자산QR 스캐너 mock 미동작(Q-2), 거래처 CRUD 이중(Q-4), 월마감 placeholder(Q-6).
4. inventory 스키마 중복컬럼 기본값 불일치(S-3), 불출/실사 비원자(S-5), 재고현황뷰 숨김 UI 죽은 prop(X-1).


---

# ⑨ 관리자 메뉴 — 재조사 보고서 (현 코드 상태)

> 재조사일: 2026-06-01 · 기준 커밋: 4bd799d2 · 대상 ~20,000줄 (전용 359 + 전용서브 13k + 워크센터 5.8k + 모바일 918)
> 방식: 도달성 Grep 스캔 + 3클러스터 병렬 정독 + 스키마/정책 직접 검증

## 구조
- `관리자전용.tsx`가 라우터 — **영문 id(exec/company/ops/audit/forms/roles)면 관리자워크센터, 한글 id면 관리자전용서브 레거시** dynamic import. 워크센터도 결국 같은 전용서브를 재사용/래핑.
- `admin-menu-config.ts:78` 사이드바는 영문 6종만 visible, 한글 9종 `hidden:true` → **한글 레거시 render 블록은 일반 진입 도달 불가**(알림/즐겨찾기 alias 호환용 잔존).

---

## 🔴 X — 죽은 코드 (Grep 검증)

- **[X-1]** `모바일/관리자/data-hooks.ts`(398줄 전체) import 0 — 유일 참조는 API 라우트 주석 1건. 모바일 관리자 화면들은 PC 워크센터를 dynamic import만 하고 이 훅(useAuditSummary 등)을 안 씀 → 파일 통째 죽음.
- **[X-2]** `모바일/관리자/공용UI.tsx`(169줄) import 0 — PC 워크센터 재사용 정책으로 불필요해진 잔재.
- **[X-3]** `관리자워크센터/OpsWorkcenter/index.ts` barrel export 4종 참조 0(부모가 파일 직접 import). `CompanyWorkcenter/fallback-data.ts:17 FALLBACK_COMPANY`·`:27 FALLBACK_CORPS` 참조 0(CompanyBasicTab은 자체 FALLBACK_COMPANIES 정의).
- **[X-4]** `팝업창관리자.tsx:31 isAllowedFile` export 미사용(실검증은 validatePopupFileSelection). `계약관리도구.tsx:12 ContractClosingData` 미사용 import.
- **[X-5 / 도달불가]** `관리자전용.tsx:295-347` 한글 레거시 render 블록 — 사이드바가 영문만 발행하므로 일반 경로 도달 불가(alias 잔존).

## 🔴 U — 무동작 / 더미 데이터 (관리자 대시보드 신뢰도 문제)

- **[U-1]** `재무대시보드.tsx:14,78` `budgets` state가 set 호출 전무 → "부서별 예산 통제(Budget Burn Rate)" 차트 영구 빈 더미("예산 데이터 없습니다"). cashFlow `out`/`balance`도 항상 0.
- **[U-2]** `경영대시보드.tsx:69-73` notices/stats가 하드코딩 더미('이번 달 입사 2명', '3월 급여 이상치 없음', '정서운 근무 이탈 감지', '매출 집계 준비중'). `:75` quickLinks 6버튼 onClick 없음(무반응, 모바일대시보드도 동일).
- **[U-3]** `ExecDashboard.tsx:289-292`(워크센터 경로)가 BusinessDashboard/BudgetManagement/IntegratedReport/CompanyPnL에 `staffs={[]}`·`user={null}` 빈값 전달 → **워크센터로 진입 시 직원수·재고·휴가 KPI가 0/빈값으로 degrade**. 레거시 경로(관리자전용.tsx:304)는 실값 전달(불일치). 사이드바가 워크센터(영문)만 발행하므로 실사용 경로가 degrade된 쪽.
- **[U-4]** `알림자동화설정.tsx` payrollEnabled/payrollDay가 localStorage에만 저장, 실제 발송 cron 미연결(급여 알림 토글 UI-only). 연차촉진만 실동작.
- **[U-5]** `admin-workcenter-common.tsx:13 WorkcenterHeader`가 mergedCount/mergedTitles 받지만 `_`로 무시(전 워크센터가 채워 전달하나 화면 효과 없음). `직원권한통합.tsx:114 fetchStaffs` no-op(realtime 자동갱신). `시스템마스터센터.tsx:463 visibleChatRooms` 세 분기 모두 같은 값 반환(폴백 무동작).

## 🟠 D — 중복

- **[D-1]** ExecDashboard(워크센터) ↔ 관리자전용 한글 경로가 **6개 대시보드 서브(경영/재무/예산/통합보고서/법인손익/커스텀)를 동일하게 dynamic import** — 동일 화면 2경로 병행 노출.
- **[D-2]** 감사 컴포넌트 이중 배선: `접근감사로그`+`감사로그뷰어`가 데스크톱 별도 탭 2개 + AuditWorkcenter가 한 화면에 stack — 같은 둘을 두 셸이 중복 wiring.
- **[D-3]** `팝업창관리자.tsx:31 isAllowedFile` ↔ `:47 validatePopupFileSelection` 거의 동일(영상 한도 메시지만 25MB vs 200MB로 불일치).
- **[D-4 / 경영대시보드 3종]** `관리자전용서브/경영대시보드`(일부 실데이터) vs `워크센터/ExecDashboard`(overview 더미) vs `모바일/관리자/경영대시.tsx` — 셋 다 "경영 대시보드" 표방.

## 🟠 S / K

- **[S-1 / 기준 분기]** `급여이상치감지`는 `payroll_records.net_pay`(실수령) 비교, `법인손익현황`은 `gross_pay`(총지급) 사용 — 같은 테이블 다른 금액 컬럼(의도된 분리 가능하나 확인 필요).
- **[S-2]** `데이터초기화.tsx:282 .eq('is_active', 0)` boolean 컬럼에 정수 비교(false 명시 권장). `시스템마스터센터.tsx:248 THROTTLE_MS` 상수 선언 후 미사용(하드코딩 60000 사용).
- **[S-3 / 기존]** `전자결재양식관리.tsx:526,753` 동일 `designEditorRef` 중복 부착(scrollIntoView 항상 미리보기로) — 별도 ref 분리 필요.
- **[K-1]** `명세서자동추출.tsx:268` 한글 오타/띄어쓰기 깨짐 "명세서 이미 지나 PDF"(→ "이미지나"). (이전 지적 'WOM' 깨짐은 이 클러스터엔 없음 — 타 파일 기수정)

## 🔵 보안 (점검 — 대부분 양호)

- **[SEC-OK]** `데이터초기화.tsx:215` 파괴적 삭제 직전 `/api/admin/verify-unlock`(세션+bcrypt+rate-limit) 서버 재검증, staff 타입은 `/api/admin/reset-staff` 자체 bcrypt → 클라 isUnlocked 우회 방지.
- **[SEC-OK]** `직원권한통합.tsx:118` role/permissions 직접 update하나 `lib/db/auth/policies.ts:126 staffPrivilegeGuard`가 admin claim일 때만 허용(권한상승 서버 차단).
- **[SEC-OK]** `감사로그뷰어.tsx:16 escapeLikePattern`(\ % _ 이스케이프) 적용. 접근감사로그는 클라 .includes() 필터(SQL 무관).
- **[정책 불일치/주의]** 모바일화 정책 파일별 불일치 — 데이터초기화/데이터백업/접근감사로그/감사로그뷰어/팀관리/팝업창관리자는 `DesktopOnlyNotice`로 모바일 차단, 시스템마스터센터·직원권한통합은 "전부 모바일화"로 차단 해제(의도 확인 필요).

## 🟡 Q — 유사기능 (중복 아님 판별 포함)

- **[Q-1]** 시스템마스터센터 감사탭(audit_logs API) vs AuditWorkcenter/감사로그뷰어(audit_logs 직접) vs 접근감사로그(access_logs) — 셋 다 로그를 다른 각도로 노출(시스템마스터는 master 게이팅으로 일부 정당화).
- **[Q-2]** 연차촉진 즉시실행(`알림자동화설정.tsx:155 triggerPromotionAction`)이 cron(`api/cron/annual-leave-promotion`)과 별도 코드경로로 동일 로직 중복 의심.
- **[Q-3]** 예산관리(localStorage 실동작) vs 재무대시보드 Budget Burn Rate(빈 더미) — 같은 "부서별 예산"을 표현하나 데이터 소스 미연결(통합 미완).
- **[무관]** 명세서자동추출(Gemini OCR→재고입고) vs 재고 명세서관리(거래처 CRUD/인쇄) — 이름만 유사, 다른 기능. 연차수동부여(시스템마스터 전용 서버라우트) vs 인사관리 연차 — 권한/경로 분리 적절. FormsWorkcenter(전자결재양식관리) vs FormBuilder(양식빌더) — 다른 화면.

## 정상 확인
- **정합 OK**: message_templates/external_integrations/audit_logs/approval_form_types/companies/popups/backup_restore_runs 컬럼·정책 등록 일치(403 회귀 없음, withMissingColumnsFallback 방어). 감사 API 5종(summary/anomalies/payroll-outliers/backups/restore) 실재.
- **법인손익 이중집계 해소 확인**: `getPayrollGrossPay`(gross_pay 우선, 미존재 시 taxable+taxfree 폴백, 합산 아님) + interim 제외 적용 → 정합.
- **연결 정상**: 전자결재양식관리↔양식빌더, 급여이상치감지 detectPayrollAnomalies↔알림시스템(canAccessAdminSection 게이트 후 딥링크 알림).
- **Rules of Hooks 위반 없음. mojibake(K) 위 1건 외 없음. KpiCard 동명은 파일-로컬(충돌 없음).**

---

## 핵심 결론 (관리자)
1. **워크센터(영문, 실사용) 경로의 대시보드가 빈값 전달로 KPI degrade(U-3)** + 재무대시보드 예산 영구 빈 더미(U-1) + 경영대시보드 하드코딩/무동작 quickLinks(U-2) → 관리자 대시보드 신뢰도 문제.
2. 모바일 관리자 data-hooks(398줄)+공용UI 통째 죽은 코드(X-1/X-2) — PC 워크센터 래핑 전환 잔재.
3. 영문 워크센터 ↔ 한글 레거시 이중 라우팅(한글 블록 dead alias), 대시보드 6종 2경로 중복.
4. 보안(데이터초기화/권한/LIKE)은 양호. 명세서자동추출 한글 오타(K-1).


---

