# 인사관리 — [구성원현황/직원등록] 전수조사 (W2)

조사일: 2026-05-29 / 작업트리: D:\newmso / 브랜치: main
대상: app/main/기능부품/인사관리서브/구성원현황* + 조직도편집기.tsx

---

## ★특별임무 결론: 두 거대 파일 중복 판정 (확정)

| 파일 | 줄수 | 판정 |
|---|---|---|
| `app/main/기능부품/인사관리서브/구성원현황.tsx` | 2431 | **LIVE (실제 실행됨)** |
| `app/main/기능부품/인사관리서브/구성원현황/index.tsx` | 2414 | **DEAD (그림자 처리되어 죽음)** |

### 근거 (추측 아님, Grep+해석)
- 유일한 import: `app/main/기능부품/인사관리워크센터/MemberWorkcenter.tsx:39`
  ```
  import StaffListManager from '../인사관리서브/구성원현황';
  ```
- 같은 디렉터리에 `구성원현황.tsx` (파일) 과 `구성원현황/` (디렉터리, index.tsx 포함) 가 **공존**.
- 빌드: `package.json` → `"dev": "next dev --webpack"`, `"build": "next build --webpack"` → **webpack(enhanced-resolve) 강제**. (turbopack 아님)
- webpack 모듈 해석 순서: 요청 `./구성원현황` 에 대해 **확장자 부착 파일(`구성원현황.tsx`)을 디렉터리(`구성원현황/index.tsx`)보다 먼저** 해석. → `구성원현황.tsx` 가 승리.
- 두 파일 모두 `export default function StaffListManager(...)` 동일 시그니처:
  - LIVE `구성원현황.tsx:195` (props에 `부서목록` 존재 — 호출부는 안 넘김, 기본값 `[]`)
  - DEAD `index.tsx:258` (props에 `부서목록` 없음)
- 동일 한글 핸들러명 다수 공유: `정보저장`(둘 다), `수정시작`(둘 다), `findDuplicateStaffMember`(둘 다), `닫기함수`(둘 다). → 명백한 쌍둥이.
- 차이: DEAD `index.tsx` 가 **더 새롭고 모듈화된 버전**(LicenseSection/JobCategorySection/ShiftAssignmentSection 분리, staff-form-utils 사용). LIVE `구성원현황.tsx` 는 **구버전 모놀리스**(면허 인라인 `saveStaffLicense` 는 LIVE에만 존재, dead=0).

### 채팅 전례와 동일 패턴 (★)
모놀리스를 디렉터리+섹션으로 리팩토링했으나, **webpack 파일-우선 해석 때문에 옛 모놀리스가 계속 실행**되고 리팩토링 산출물 전체가 죽음. 채팅 메뉴 1575줄 죽은파일과 동형.

### 연쇄 죽은코드 (DEAD index.tsx 만이 import → 전부 죽음)
Grep 참조확인 완료 (`*.tsx` 전역):
- `구성원현황/index.tsx:54` → `LicenseSection.tsx` (237줄) — **DEAD**
- `구성원현황/index.tsx:55` → `JobCategorySection.tsx` (178줄) — **DEAD**
- `구성원현황/index.tsx:56` → `ShiftAssignmentSection.tsx` (241줄) — **DEAD**
- `구성원현황/index.tsx:53` → `staff-form-utils.ts` (159줄) — **DEAD** (다른 importer 없음, Grep 확인)
- `staff-registration-types.ts` (165줄) — **부분 생존**: `LICENSE_TYPE_OPTIONS` 가 LIVE `면허자격증관리.tsx:9` 에서 사용됨. 나머지 타입은 dead index/섹션에서만 사용.
- `staff-license-link.ts` — **LIVE** (LIVE `구성원현황.tsx:30-33` 가 import).

**죽은 줄수 합계(대략): index.tsx 2414 + 656(섹션 3개) + 159(form-utils) ≈ 3229줄.**

---

## X 죽은코드 — 조직도편집기.tsx (이중으로 죽음)

`app/main/기능부품/인사관리서브/조직도편집기.tsx` (export `OrgChartEditor`, `:81`)

1. **import 0건**: `OrgChartEditor`/`조직도편집기` 를 import 하는 `.ts/.tsx` 파일 전무 (Grep 확인, 문서·PPT 참조만 존재). 동적 import도 없음.
2. **존재하지 않는 테이블 타깃**: `org_chart_nodes` 사용 (`:104,137,146,151,175,180,188`) 인데 정본 `lib/db/schema.ts` 에 **`org_chart_nodes` 테이블 없음** (migrations/drizzle/lib 전역 검색 0건, `org_teams`만 존재). `@/lib/supabase` D1 프록시는 잘못된 테이블명에 **무음실패** → 설령 연결돼도 select 빈배열·insert/update/delete no-op.
   → 컴포넌트가 화면에 붙더라도 절대 동작 불가. **완전 죽은 파일.**

---

## 분류별 발견

### SEC (보안/PII)
- **[높음] 주민번호 평문이 감사로그(audit)에 기록됨** — `구성원현황.tsx:1044`
  ```ts
  ...buildAuditDiff(beforeStaff, afterStaff, Object.keys(afterStaff)),
  ```
  `afterStaff` 는 `commonData` 를 포함하고 `commonData.resident_no = 신규직원.주민번호.trim()` (`:940`). 편집 시 주민번호가 바뀌면 BEFORE/AFTER 주민번호 평문이 audit_logs 테이블에 그대로 기록됨. (PII 로깅) → diff 대상에서 `resident_no` 제외 필요.
- **[중간/검토] 신규직원 빈 비밀번호 + 로그인 불가 모순** — `구성원현황.tsx:1102` `password: ''` 로 insert 하지만 `password_reset_required` 플래그를 설정하지 **않음**. master-login(`app/api/auth/master-login/route.ts:346-353`)은 빈 비번 계정을 `password_reset_required` 없으면 로그인 차단. 즉 등록 직후 직원은 로그인 불가인데, 토스트(`:1185-1186`)는 "로그인 아이디: 사번..." 으로 가능하다고 안내 → 오해 유발. (보안상으로는 안전, 기능/E 결함)

### PII 양호 사항 (참고, 결함 아님)
- 주민번호는 부트스트랩 select에서 제외, 편집 진입 시에만 단건 별도 fetch (`:1262-1279`). 목록은 "주민번호 등록/미등록" 마스킹만 표시 (`:1410`). 빈 값으로 덮어쓰기 방지 가드 (`:1011-1014`). — 설계 양호.

### U 불필요/하드코딩
- **[중간] 실명 하드코딩 분기** — `구성원현황.tsx:1066`
  ```ts
  if (신규직원.성명 === '박철홍') { newEmployeeNo = '1'; }
  ```
  특정 실존 인물명으로 사번을 강제 1번 부여. 소스에 개인 실명 노출 + 취약한 비즈니스 로직.

### P 성능
- **[중간] 면허 일괄조회 effect 가 부모 리렌더마다 재실행** — `구성원현황.tsx:643`
  ```ts
  }, [직원목록, 새로고침]);
  ```
  `새로고침` 은 `MemberWorkcenter.tsx:155,174` 에서 **매 렌더 인라인 화살표함수**로 새로 생성되어 전달됨 → 의존성 변화로 effect 재발화 → 전 직원 staff_licenses 재조회 반복. (단, 조회 자체는 batched라 N+1은 아님) → 함수 useCallback 안정화 또는 deps에서 제거 권장.
- **[낮음] initialEditStaff effect** — `구성원현황.tsx:1282` `useEffect([initialEditStaff])` 가 비메모이즈 `수정시작` 호출. deps 누락이지만 동작상 문제는 경미.

### T 타입(any/강제캐스팅)
- LIVE `구성원현황.tsx`: `any` 1건만 — `:432` `data.map((row: any) => row.name)`. (그 외는 `unknown`/`Record<string, unknown>` 사용, 양호)
- DEAD `조직도편집기.tsx`: `staffs: any[]`, `user: any` props (`:86,88`), `staff: any` (`:99`). (죽은 파일이라 영향 없음)

### E 에러처리
- LIVE 파일 전반 양호: try/catch + 무음실패 폴백(withMissingColumnsFallback), 면허/사진 실패는 직원 저장을 막지 않고 경고 토스트 분리 (`saveStaffLicense :846-849`, `:1050-1056`). JM3 준수.
- 조직도편집기 `:158,191` `catch { toast('저장 실패') }` — 에러 삼킴(빈 catch, 로깅 없음). 죽은 파일이라 우선순위 낮음.

### D 중복
- 위 특별임무: `구성원현황.tsx` ↔ `구성원현황/index.tsx` 거의 전체 중복(쌍둥이). 핵심 발견.

### K 한글깨짐
- 대상 9개 파일 전부 mojibake(`�`) 0건. 깨짐 없음.

### S 문법/런타임 / Q 유사기능 / DBG 디버그
- S: LIVE 파일 명백한 런타임 결함 미발견.
- Q: 모듈화 리팩토링본(dead index+섹션)과 모놀리스(live) 가 같은 기능을 중복 구현 — 위 D 참조.
- DBG: `console.error/warn` 다수(정상 에러 로깅 범주). `debugger`/임시 `alert` 없음.

---

## 권장 조치 (요약)
1. **결단**: 둘 중 하나 채택. (a) 리팩토링본 `index.tsx`+섹션을 살리려면 모놀리스 `구성원현황.tsx` 삭제(파일-우선 해석 제거), 또는 (b) 리팩토링 산출물(index.tsx, 3개 섹션, staff-form-utils) 전부 삭제. 현재는 3000+줄 죽은코드 방치.
2. `조직도편집기.tsx` 삭제 또는 `org_chart_nodes` 마이그레이션 추가 후 배선 (현재 완전 무동작).
3. `구성원현황.tsx:1044` 감사로그 diff 에서 `resident_no` 제외 (PII).
4. 신규등록 시 `password_reset_required: 1` 동반 설정 (로그인 불가 모순 해소).
5. `:1066` 실명 하드코딩 제거.
6. `:643` effect 의존성 `새로고침` 안정화.
