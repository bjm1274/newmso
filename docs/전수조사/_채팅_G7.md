# 채팅(메신저) 전수조사 — G7 [모달/액션/드로어 그룹]

작업 디렉터리: D:\newmso · 조사일 2026-05-29 · 정본 스키마 `lib/db/schema.ts` 확인
참고: G1(`_채팅_G1.md`) 선행 보고와 교차확인. `메신저오버레이.tsx`/`MessengerModalHost`는 재조사 제외(교차확인만).

## 0. 파일별 역할 (1줄)

| 파일 | 줄 | 역할 | 사용 |
|---|---|---|---|
| `메신저드로어.tsx` | 849 | 실사용 채팅방 정보 드로어(참여자/알림/공지/스레드/미디어/파일/링크/북마크/방관리). `메신저.tsx:3326` 인라인 렌더. | 사용중 |
| `MessengerDrawerSections.tsx` | 496 | 드로어의 스레드/미디어/파일/링크/북마크 섹션만 분리한 컴포넌트. | **죽은코드(0 import)** |
| `useChatDrawerController.ts` | 218 | 드로어/그룹/멤버추가 모달 상태·파생목록·알림모드 핸들러 훅. `메신저.tsx`가 일부만 사용. | 사용중 |
| `메신저모달.tsx` | 270 | GroupChatModal+ForwardMessageModal+AddMemberModal 정의. | **죽은코드(0 import)** |
| `메신저액션.tsx` | 79 | ReactionDetailModal(반응 상세). | 사용중(`메신저.tsx:31`) |
| `메신저액션훅.ts` | 256 | useChatMessageActions: 반응/고정/북마크/읽음/삭제. | 사용중(`메신저.tsx:32`) |
| `메신저액션서브/EmojiPicker.tsx` | 368 | 이모지 피커 팝오버. | 사용중(타임라인) |
| `메신저액션서브/MessageActionsHost.tsx` | 135 | 행 액션 박스 호스트(컨텍스트메뉴+피커). | 사용중(`메신저타임라인.tsx:616`) |
| `메신저액션서브/MessageContextMenu.tsx` | 246 | 우클릭 컨텍스트 메뉴. | 사용중 |
| `메신저액션서브/index.ts` | 11 | barrel. | 사용중 |
| `메신저그룹생성모달.tsx` | 79 | GroupChatModal(실사용본). | 사용중(`메신저.tsx:68`) |
| `메신저멤버관리모달.tsx` | 141 | ForwardMessageModal+AddMemberModal(실사용본). | 사용중(`메신저.tsx:69`,`MessengerModalHost`) |
| `메신저수정모달.tsx` | 149 | MessageEditModal+MessageEditHistoryModal. | 사용중(`메신저.tsx:70`) |
| `메신저투표모달.tsx` | 253 | PollComposerModal+SlashCommandModal. | 사용중(`메신저.tsx:72`,`MessengerModalHost`) |
| `메신저스레드패널.tsx` | 147 | ThreadPanel(스레드 사이드 패널). | 사용중 |
| `useChatThreadActions.ts` | 98 | 스레드 루트·요약·멘션인박스·메시지링크복사 훅. | 사용중 |
| `MessengerDateJumpModal.tsx` | 114 | 날짜로 이동 모달. | 사용중(`MessengerModalHost`) |
| `메신저스와이프.ts` | 73 | 모바일 스와이프 답글 핸들러. | 사용중(추정) |
| `채팅알림배너.tsx` | 261 | 모바일 채팅 미리보기 배너(알림 메뉴 1차 검토됨). | 사용중 |

---

## X 죽은코드

### X-1 [높음] `메신저모달.tsx` (270줄) 전체 죽은코드 + Q중복
- `메신저모달.tsx:21,109,179` 가 `GroupChatModal`/`ForwardMessageModal`/`AddMemberModal` 3종을 정의하지만 **import 0건**.
  - 근거: `grep "메신저모달"` / `grep "from '\./메신저모달'"` → 0건. 실사용 import는 전부 `메신저그룹생성모달.tsx`(GroupChatModal)와 `메신저멤버관리모달.tsx`(Forward/AddMember)에서 옴(`메신저.tsx:68-69`, `MessengerModalHost.tsx:4`).
- 세 컴포넌트 모두 실사용본과 **거의 바이트 단위로 동일**(예: AddMemberModal `data-testid="chat-add-member-modal"`, 동일 className, 동일 props 시그니처). G1 미수록 신규 발견.
- 조치: `메신저모달.tsx` 삭제(270줄).

### X-2 [높음] `MessengerDrawerSections.tsx` (496줄) 전체 죽은코드 + D중복
- `MessengerDrawerSections.tsx:130` export 되지만 **import 0건**.
  - 근거: `grep "MessengerDrawerSections"` → 자기 정의(16,130,145)뿐. 실사용 드로어는 `메신저드로어.tsx`(`MessengerDrawer`) 인라인.
- 내용은 `메신저드로어.tsx`의 스레드/미디어/파일/링크/북마크 섹션과 **중복 재구현**. 헬퍼(`sortMessagesByRecent`/`getVisibleMessages`/`formatMessageMeta`/`DrawerSectionHeader`)·상수(`COLLAPSED_LIMITS`/`DEFAULT_EXPANDED_SECTIONS`)까지 양쪽에 복붙. G1 미수록 신규 발견.
- 조치: 삭제(496줄). 또는 `메신저드로어.tsx`가 이 섹션 컴포넌트를 import하도록 진짜 분리(D중복 동시 해소). 단 `DeferredAttachmentImage`(지연 로딩 이미지)는 Sections에만 있어 채택 시 성능 이점.

### X-3 [중간] `메신저드로어.tsx:50,182` `onOpenOpsCenter` 죽은 prop (U)
- prop 선언(50)·구조분해(182)되지만 **드로어 body 어디서도 사용 안 됨**.
  - 근거: `grep onOpenOpsCenter 메신저드로어.tsx` → 50,182 두 줄(타입+구조분해)뿐, JSX 사용 0.
  - `메신저.tsx:3354`에서 `setShowDrawer(false)+setShowSettings(true)` 실로직을 넘기지만 호출처가 없어 무의미.
- 조치: prop 삭제(부모 전달부 포함) 또는 운영센터 버튼 실제 추가.

### X-교차확인 [높음·G1수록] 레이아웃 추출 클러스터
- `MessengerLayout.tsx`(42)/`MessengerConversationPanel.tsx`(262)/`MessengerModalHost.tsx`(157) = 죽은 클러스터. `MessengerLayout` import 0건, 진입점은 `메신저.tsx` 인라인(`조직도본문.tsx:14` dynamic import).
  - 확인: `grep MessengerLayout` → 자기 정의 + G1 문서만. `MessengerModalHost`가 렌더하는 모달들(Poll/Slash/Thread/Read/Reaction/Forward/AddMember/DateJump 등)은 전부 `메신저.tsx:3404-3480`에서 **동일하게 live 렌더** → ModalHost 경유분은 사용 안 됨.
- `메신저오버레이.tsx`(`MessageEditModal/MessageEditHistoryModal/PollComposerModal/SlashCommandModal` 재정의)도 교차확인: live import는 `메신저수정모달.tsx`+`메신저투표모달.tsx` → 오버레이는 그 두 파일과 100% 중복 죽은코드(G1 보고 유효).
- 조치: G1 권고대로 클러스터 일괄 결정. **본 G7 신규분(X-1,X-2)과 합산 시 모달/드로어 영역 죽은코드 ≈ 270+496+461 ≈ 1227줄.**

---

## D 책임 중복

### D-1 [높음] 드로어(849) vs DrawerSections(496) 책임 중복
- 동일 섹션(스레드/미디어/파일/링크/북마크) + 동일 헬퍼/상수 양쪽 복제. X-2와 동일 뿌리. `메신저드로어.tsx:84-103`(`sortMessagesByRecent`/`getVisibleMessages`/`formatMessageMeta`) ↔ `MessengerDrawerSections.tsx:63-80` 바이트 동일. `DrawerSectionHeader`(드로어 105 / Sections 81)도 동일.
- 조치: 공용 모듈로 헬퍼/`DrawerSectionHeader`/상수 추출, Sections를 드로어가 실제 사용.

### D-2 [중간] 드로어 vs Controller 책임 경계 모호
- `useChatDrawerController.ts`는 그룹모달·멤버추가모달·알림모드 상태를 보유하나, `메신저.tsx`는 동일 상태(`showAddMemberModal` 등)를 **자체 useState로도 별도 보유**(`메신저.tsx:1267,1298`). 즉 컨트롤러 훅과 메인 컴포넌트가 같은 상태를 이중 관리.
  - 근거: `메신저.tsx:1267 const [showAddMemberModal,setShowAddMemberModal]=useState(false)` 가 존재하면서 `useChatDrawerController`도 동일 state 노출(218:`showAddMemberModal`). 실렌더(`메신저.tsx:3471`)는 메인 컴포넌트 state 사용 → 컨트롤러 훅의 모달 state 일부는 미사용 의심.
- 심각도 중간(혼란/유지보수 비용). 조치: 단일 소유자로 통합.

---

## Q 모달 공통 패턴 중복

### Q-1 [중간] 모달 6종 오버레이 보일러플레이트 반복
- 동일 패턴(고정 오버레이 + `onClick={onClose}` + 내부 `stopPropagation` + `if(!open) return null`)이 최소 8개 모달에 복붙:
  - `메신저모달.tsx:34/118/193`, `메신저그룹생성모달.tsx:29`, `메신저멤버관리모달.tsx:38/89`, `메신저수정모달.tsx:33/70`, `메신저투표모달.tsx:63/191`, `메신저액션.tsx:21`, `MessengerDateJumpModal.tsx:45`.
- z-index도 제각각 하드코딩: `z-[110]`(group/forward/add/poll/slash), `z-[115]`(edit), `z-[116]`(edit-history), `z-[210]`(datejump), `z-[var(--z-modal)]`(reaction). 일관성 없음(S/유지보수).
- 조치: 공용 `<ModalShell open onClose zIndex>` 래퍼로 추출.

### Q-2 [낮음] AddMember/Group 멤버 선택 리스트 중복
- `메신저모달.tsx`/`메신저그룹생성모달.tsx`/`메신저멤버관리모달.tsx`가 거의 동일한 체크박스 멤버 row(`이름 (회사·직급)`)를 각각 구현. 죽은 `메신저모달.tsx` 삭제 후에도 그룹생성 vs 멤버추가 간 미세 중복 잔존.

---

## T 타입(any·강제캐스팅)

### T-1 [높음·교차확인] `MessengerModalHost.tsx:19` `const c = controller as any;`
- 약 50개 컨트롤러 prop을 `any`로 받아 타입 검증 전무. G1 T-2와 동일. 단 ModalHost 자체가 죽은코드라 삭제로 해소.

### T-2 [중간] 드로어/패널의 `message.staff as { name?: string }` 반복 캐스팅
- `메신저드로어.tsx:97,495`, `MessengerDrawerSections.tsx:74,210`, `메신저스레드패널.tsx:101`, `메신저액션.tsx`(간접) 에서 `ChatMessage.staff`를 인라인 `as { name?: string ... }` 캐스팅. `ChatMessage` 타입에 `staff` 형상이 정의되지 않아 매번 캐스팅.
- 조치: `@/types`의 `ChatMessage.staff`를 명시 타입화.

---

## E 에러처리

### E-1 [높음] `메신저액션훅.ts:237` audit_logs `details`에 raw 객체 전달
- `메신저액션훅.ts:230-242` `details: { room_id, content }` 객체를 그대로 insert. 정본 패턴은 `JSON.stringify`(`lib/audit.ts:182`, `app/api/admin/staff-password/route.ts:34`, `app/api/auth/change-password/route.ts:118`).
- `audit_logs.details`는 정본 스키마상 `text()`(schema.ts:208). D1 프록시(`@/lib/supabase`)에 객체를 넘기면 `"[object Object]"` 저장 또는 무음 실패 가능 → 감사로그 무의미화.
- 동일 결함이 `메신저상태훅.ts:217`(message_edit, G7 범위 밖 파일이나 같은 뿌리)에도 존재.
- 조치: `details: JSON.stringify({...})`로 통일.

### E-2 [낮음] `toggleReaction`/`togglePin` 실패 토스트 누락(부분)
- `메신저액션훅.ts:98` `toggleReaction` catch는 `console.error`만, 사용자 피드백 없음(반응은 UX상 무음 허용 가능 — 의심 수준). `togglePin`(134)·`toggleBookmark`는 토스트/낙관적 롤백 처리되어 일관성 부족.

### E-3 [낮음·양호] `deleteMessage` 낙관적 업데이트 후 audit 실패 무음 처리(`메신저액션훅.ts:243`) — 의도된 설계, 정상.

---

## S 문법/런타임

### S-1 [낮음] `MessageContextMenu.tsx:107` effect 의존성 누락
- `useEffect(...,[onClose,onReply,onCopy,onBookmark])` 인데 본문에서 사용하는 `onClose`만 안정적이고 단축키 핸들러는 최신 prop 캡처 의존. `onForward`/`onTask`/`onReact`/`onDelete`/`onAddEmoji`는 의존성 배열에 없으나 클릭 핸들러(렌더 시 바인딩)로 호출되어 실제 버그는 아님. 단 키보드 단축키(R/B/⌘C)는 effect 클로저라 stale 가능(의심, 낮음).

### S-2 [낮음] `메신저드로어.tsx:96` vs `MessengerDrawerSections.tsx:73` `formatMessageMeta` 중복 정의(이미 D-1).

---

## P 성능

### P-1 [중간] DrawerSections의 `DeferredAttachmentImage` 미채택
- 실사용 `메신저드로어.tsx:593` 미디어 그리드는 placeholder 없는 즉시 `<img>`. 죽은 `MessengerDrawerSections.tsx:35`에는 `onLoad` 기반 지연 표시 컴포넌트가 있으나 미사용. 드로어 다중 이미지 동시 로드 시 레이아웃 시프트/체감 저하 가능.
- 조치: D-1 통합 시 Deferred 이미지 채택.

### P-2 [낮음·양호] 드로어 정렬/슬라이스 `useMemo`로 적절히 메모이즈됨(`메신저드로어.tsx:207-239`).

---

## SEC 보안

### SEC-1 [낮음·양호] 링크 섹션 `<a target="_blank" rel="noreferrer">`(`메신저드로어.tsx:675,680`) — reverse tabnabbing 방어 정상.
### SEC-2 [낮음] 공유 링크/북마크 URL을 `extractFirstLinkUrl`로 추출해 `href`에 직접 사용. `javascript:` 스킴 필터 여부 미확인(의심) — `메신저첨부`의 `extractFirstLinkUrl` 구현 확인 권장(본 그룹 범위 밖).

## DBG 디버그
- 해당 없음(콘솔은 `console.error` 에러 로깅뿐, 디버그성 `console.log` 미발견).

## K 한글깨짐
- 해당 없음(전 파일 한글 정상).

---

## 집계표

| 분류 | 건수 | 높음 | 중간 | 낮음 |
|---|---|---|---|---|
| D 중복 | 2 | 1 | 1 | 0 |
| S 문법/런타임 | 2 | 0 | 0 | 2 |
| K 한글깨짐 | 0 | 0 | 0 | 0 |
| X 죽은코드 | 4 | 3 | 1 | 0 |
| U 불필요 | (X-3 포함) | - | - | - |
| Q 유사기능 | 2 | 0 | 1 | 1 |
| T 타입 | 2 | 1 | 1 | 0 |
| E 에러처리 | 3 | 1 | 0 | 2 |
| P 성능 | 2 | 0 | 1 | 1 |
| SEC 보안 | 2 | 0 | 0 | 2 |
| DBG | 0 | 0 | 0 | 0 |
| **합계** | **21** | **6** | **6** | **9** |

### 높음 항목 요약
- X-1 `메신저모달.tsx` 270줄 전체 죽은코드(실사용본과 중복) — 삭제.
- X-2 `MessengerDrawerSections.tsx` 496줄 전체 죽은코드(드로어와 D중복) — 삭제 또는 진짜 분리.
- X-교차확인 레이아웃 클러스터(약 461줄) G1 유효.
- D-1 드로어(849) vs Sections(496) 섹션·헬퍼·상수 복제.
- T-1 `MessengerModalHost.tsx:19 controller as any`(죽은코드, 삭제로 해소).
- E-1 `메신저액션훅.ts:237` audit_logs details에 raw 객체(정본=JSON.stringify) → 감사로그 무음 손실.

**G7 신규 죽은코드 합산: 메신저모달 270 + DrawerSections 496 = 766줄** (G1 클러스터 461줄과 별개).
