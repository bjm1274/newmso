# ① 알림 메뉴 — 3차 전수조사 (현 코드 직접 정독)

> 조사일: 2026-06-01 · 기준 커밋: `922af4cf` (현 HEAD)
> 방식: Sonnet 서브에이전트 6그룹 병렬 line-by-line 정독 → Opus 교차검증(Grep/스키마 실측). 코드 수정 없음(조사 전용).
> 분류: **D** 중복 · **S** 문법/런타임 · **K** 한글깨짐 · **X** 죽은코드 · **U** 불필요 · **Q** 유사기능
> 비고: 기존 보고서 결론에 의존하지 않고 현재 코드 기준 독립 판정. 기존 지적과 달라진 부분은 "정정"으로 명시.

## 대상 파일 (35개 정독)

| 구분 | 파일 |
|------|------|
| 엔진 | `기능부품/알림시스템.tsx`(1883) · `알림시스템/{delivery-log,device-feedback,filter-helpers,push-debug,push-utils,settings,ui-config}.ts` |
| 푸시유틸 | `기능부품/알림푸시.ts`(604) ※죽은 체인 |
| UI | `알림센터.tsx`(577) · `알림인박스.tsx`(1349) · `채팅알림배너.tsx`(261) · `app/components/GlobalNotificationBell.tsx`(435) ※죽음 |
| 모바일 | `모바일/내정보/{알림.tsx(227)※죽음, 알림탭.tsx(324), 푸시설정카드.tsx(306)※죽음}` |
| API | `api/notifications/{chat-push,chat-push-flush,mark-read,push-config,push-self-test,push-subscription,repush-unread}` · `api/admin/notifications/push-health` · `api/cron/inapp-notifications` |
| 발송 lib | `notification-{push-dispatch,repush,shared,metadata,utils}.ts` · `web-push-cloudflare.ts` · `push-quiet-hours.ts` · `chat-push-dispatch.ts`(998) · `public/push-notification-shared.js`(809) |
| 인앱잡 | `lib/inapp-notification-jobs.ts` + `inapp-notification-jobs/{types,check-approval,check-attendance,check-education,check-inventory,check-payroll,check-word-filter}.ts` |

---

## 🔴 X — 죽은 코드 (최대 발견, 삭제 후보 ~1,572줄)

### [X-1] 죽은 푸시 체인 3파일 = ~1,137줄
import 그래프 실측(Grep, docs/handoff/tests 제외):
```
알림.tsx (SAlert, 227줄)  ──import 0──  (어디서도 안 씀)
   └─ import → 푸시설정카드.tsx (306줄)  ← 오직 알림.tsx:17 에서만
        └─ import → 알림푸시.ts (604줄)  ← 오직 푸시설정카드.tsx:21 에서만
```
- **세 파일 전체가 도달 불가능한 죽은 코드.** 살아있는 진입점이 없음.
- `알림푸시.ts`는 `알림시스템.tsx`의 **옛 복사본** — `initNotificationService`/`getPushConnectionStatus`/`sendNotification`/`syncPushSubscription`/`flushPushRetryQueue`/`PushConnectionStatus` 타입/`urlBase64ToUint8Array` 등이 `알림시스템.tsx`(살아있음) 및 `알림시스템/push-*.ts`와 거의 글자 단위로 중복. → 그룹A가 D-1~D-7로 본 "중복"의 실체.
- **권장: 통합이 아니라 3파일 삭제.** 단, `푸시설정카드.tsx`(모바일 푸시 구독 토글/테스트 UI)가 죽으면서 **모바일에 푸시 설정 진입 UI가 사라진 기능 갭**이 남음 → ②내정보 메뉴에서 교차 확인.

### [X-2] `app/components/GlobalNotificationBell.tsx` (435줄) 전체 죽음
- Grep 실측: app/·lib/ 코드에서 import **0건**. 언급은 `알림시스템.tsx:73`의 **주석 한 줄**과 docs/handoff/tests뿐(handoff엔 과거 "layout·조직도측면창 배치" 의도가 있었으나 현 코드엔 렌더 트리 부재).
- **정정**: 기존 재조사 보고서는 이 파일을 살아있는 "공용벨"로 다루고 "`read_at` 사용 정정 확인"이라 기록했으나, **실제로는 죽은 코드**. 따라서 이 파일 안에서 발견되는 D(fetch/markRead 복제)·S(`message` 타입 아이콘 누락 → `Bell` 폴백)·Q(라우팅 if-else 재구현)는 모두 **죽은 파일 내부 문제**로, 삭제 시 일괄 소멸.

### [X-3] 죽은/미사용 API 라우트 2개 (재확인 권장)
- `api/admin/notifications/push-health/route.ts`(119) — `GET /api/admin/notifications/push-health` 호출 Grep 0건. 동일 진단을 `admin/system-master` operations 핸들러가 자체 수집 → 호출되지 않는 엔드포인트.
- `api/notifications/repush-unread/route.ts`(38) — 프론트 fetch 0건. `알림시스템.tsx:1834` 주석 "클라이언트 repush 호출 제거"와 정합. cron(`unread-notification-repush`)이 동일 기능 수행 → 클라 수동 트리거 불요 시 삭제 후보.

### [X-4] 기타 죽은 심볼
- `push-notification-shared.js:525` `erpShowIncomingNotification` — 공개 API(`__erpPushShared`)에 없고 호출 0(실제 노출은 `...Managed`).
- `inapp-notification-jobs/check-education.ts:112` `checkRecentMessages` — 영구 no-op stub인데 오케스트레이터에 등록되어 매 실행 호출(U-6과 연동).
- `filter-helpers.ts:37` `shouldApplyKeywordFilter` — export지만 외부 import 0(파일 내부 호출만).

---

## 🟠 D — 중복 (살아있는 코드 간만, 죽은 체인 제외)

- **[D-1] `TYPE_CFG`/`getTypeCfg` 3벌** — 알림 type→표시메타를 각자 정의:
  `알림시스템/ui-config.ts:1-17`(이모지·bg·progress·accent) ↔ `알림인박스.tsx:130-146`(이모지·bg·text·border) ↔ `알림센터.tsx:35-49`(Lucide·color·label). 키셋 동일(message/approval/inventory/attendance/board/인사/education). 아이콘 스타일 불일치(이모지 vs Lucide)로 UI 비일관. → `ui-config.ts` 단일화 후 import 권장.
- **[D-2] `normalizeKeywordList` 2중 정의** — `알림시스템/settings.ts:29-38`(private) ↔ `알림시스템/filter-helpers.ts:9-18`(export). 구현 동일.
- **[D-3] `toNotificationMetadataRecord` 2중** — `lib/notification-metadata.ts:81-86`(export) ↔ `lib/notification-utils.ts:100-105`(private). 로직 완전 동일(파라미터명만 다름).
- **[D-4] 텍스트 정규화 유사 중복** — `cleanNotificationValue`(notification-metadata.ts:58) ↔ `toNotificationText`(notification-utils.ts:84). boolean 처리·trim 옵션만 차이.
- **[D-5] `chat-push-dispatch.ts` 인라인 중복** — `notification-shared.ts`의 export를 안 쓰고 동일 로직 재작성:
  `selectLatestFcmToken`↔`748-764` · `invalidateExpiredFcmTokens`↔`834-843` · `deleteExpiredWebPushSubscriptions`↔`889-892` · `dedupeWebPushSubscriptions`↔`736-745`. → 공통 함수 위임 권장.
- **[D-6] `PushSubscriptionRow` 타입 3중** — `chat-push-dispatch.ts:47` ↔ `notification-push-dispatch.ts:47` ↔ `notification-repush.ts:36`. → `notification-shared.ts`에 베이스 export 후 확장.
- **[D-7] `errorMessage` 2중** — `inapp-notification-jobs/types.ts:41`(export) ↔ `inapp-notification-jobs.ts:48`(private 동일 구현).
- **[D-8] `groupDuplicateEndpoints` 2중** — `push-health/route.ts:36`(로컬) ↔ `system-master/_shared.ts:317`(export). ※push-health가 죽은 라우트(X-3)면 자동 해소.
- **[D-9] `markAsRead`/`markAll`/`delete` + `erp-notification-read` dispatch** — `알림센터.tsx:209-263` ↔ `알림인박스.tsx:964-982` 각자 구현(둘 다 생존). 알림인박스는 자동읽음 타이머(941)와 수동버튼(970)이 또 내부 2중.

---

## 🟡 S — 문법/런타임 (실제 런타임 결함은 경미)

- **[S-1] check-word-filter가 `.tsx`를 서버 import** — `inapp-notification-jobs/check-word-filter.ts:22` `import { DEFAULT_BANNED } from '../banned-words'` → 실제 파일 `lib/banned-words.tsx`(React/`localStorage` 포함). 서버 전용 cron 잡이 클라이언트 코드 파일을 import → 불필요한 클라 코드가 서버 번들 유입(런타임 오류는 아직 없음). → `banned-words-server.ts`로 상수 분리 권장.
- **[S-2] `j: any = sub.toJSON()`** — `알림시스템.tsx:557`(+죽은 알림푸시.ts:441). `PushSubscriptionJSON` 타입 권장(JM4 any 금지).
- **[S-3] `web-push-cloudflare.ts:64-85`** — `createVapidJwt`가 `getVapidConfig()`를 2~3회 중복 호출(65행 destructure 후 85행 재호출). 결과 동일, 불필요 파싱.
- (경미) `알림탭.tsx:83` `toNotificationText(row.type,'notification',true)` → null 시 `'notification'`이 `mapTypeToVisual` default 분기. 동작 무해하나 fallback 키가 case에 없어 묵시적.
- **정정**: 그룹D가 "rate-limit 런타임 버그(S)"로 본 `recordFailedAttempt` 성공경로 호출은 **버그 아님** → U-5로 강등(아래).

## 🟡 K — 한글 깨짐 (런타임 깨짐 없음)

- **[K-1] 한글이 유니코드 이스케이프로 저장** — `lib/notification-metadata.ts:3-16`(`'채팅'`=채팅 등) · `public/push-notification-shared.js:24-33`. 런타임 정상이나 가독성 0이며 **다른 모든 파일은 한글 리터럴** → 일관성 결여(과거 인코딩 사고 흔적 추정). `push-notification-shared.js:595`는 `'인사'`('인사')를 상수 미추출·인라인 → 더 비일관. → 한글 리터럴 복원 권장.
- 그 외 mojibake·깨진 문자열: **미검출**.

## 🟢 U — 불필요/정책

- **[U-1] `push-subscription/route.ts:54` `detectExtendedColumnSupport()` 항상 `true`** — 호출부 3곳(77/143/190) `if(supportsExtendedColumns && …)` 분기 무의미(Supabase 시절 잔재). 함수+변수 제거 가능.
- **[U-2] 로깅 정책 불일치** — `알림시스템.tsx` raw `console.warn/error` 9곳 vs 다른 알림 lib는 `@/lib/logger`.
- **[U-3] `chat-push-dispatch.ts:728-729` 동일 주석 2줄 연속 중복** + `:787` 위치 어긋난 잔재 주석.
- **[U-4] inapp cron lookback 불일치** — `check-attendance.ts:31`(5분)·`check-word-filter.ts:43`(5분) lookback인데, `cron/inapp-notifications/route.ts:9-18` 주석상 실제 실행은 `push-subscription-cleanup` piggyback **1일 1회(KST 12:00)**. → 하루 5분치만 커버, 23h55m 누락. (외부 5분 cron 설정 시 정상 — 설계상 알려진 한계이나 현 활성 경로 기준 누락.)
- **[U-5] `recordFailedAttempt` 네이밍 혼란** — `mark-read`/`chat-push`/`push-self-test`/`repush-unread` 라우트가 성공 경로에서도 `recordFailedAttempt` 호출. `rate-limit.ts` 실측 결과 `checkRateLimit`은 카운트를 **읽기만**(증가 안 함), `recordFailedAttempt`가 카운트 +1 전담 → 패턴은 "매 요청 +1, 분당 N회 제한"으로 **의도대로 정상 작동**. 단 함수명이 "Failed"라 오해 소지 → `recordAttempt`로 개명 또는 주석 권장. (로그인류 "실패만 카운트" 라우트와 동일 함수 공유 시 의미 충돌 주의.)
- **[U-6] `checkRecentMessages` no-op 파이프라인 등록** — `check-education.ts:112` 영구 빈 결과 stub인데 오케스트레이터가 매 실행 `runIsolated('messages')` 호출(불필요 overhead). 실제 처리는 chat-push-dispatch.
- **[U-7] `notification-repush.ts:325` `const webTargets = webPushTargets`** — 사용 없는 별칭 재할당.
- **[U-8] `check-inventory.ts:89-111`** — inventory `LIMIT 1000` 로드 후 JS에서 `stock<=min_stock` 필터(인덱스 미활용). → WHERE 절 권장.

## 🟢 Q — 유사기능 (재사용 후보)

- **[Q-1] 알림 목록 UI 2종 병렬 재구현** — `알림센터.tsx`(limit 50) ↔ `알림인박스.tsx`(limit 100): fetch + `NOTIFICATION_LIST_UPDATED_EVENT` 구독 + visibilitychange 재fetch + markRead 세트를 각자 구현. → `useNotificationSync(userId)` 공용 훅 후보. (죽은 GlobalNotificationBell 포함 시 3종이었음.)
- **[Q-2] WebPush 발송 패턴** — `chat-push-dispatch.ts` ↔ `notification-push-dispatch.ts`가 `Promise.allSettled(map(sendWebPush)) + 404/410 만료수집 + 삭제` 흐름을 독립 구현. → `dispatchWebPushBatch()` 추출 후보.
- **[Q-3] 각 `check-*` 잡 공통 부트스트랩** — `getD1Binding()→getD1Drizzle()→불가 시 빈 결과` 2-step이 6개 잡 반복. (현 수준 허용 가능, 통합 시 주의.)

---

## 핵심 결론 (알림)

1. **죽은 코드가 알림 메뉴의 최대 이슈** — 죽은 푸시 체인 3파일(`알림.tsx`+`푸시설정카드.tsx`+`알림푸시.ts` ≈1,137줄) + `GlobalNotificationBell.tsx`(435줄) = **~1,572줄 삭제 가능**. 그룹A가 "중복 통합"으로 본 D-1~D-7 푸시 로직은 사실 죽은 `알림푸시.ts`가 살아있는 `알림시스템.tsx`의 옛 복사본 → **삭제로 해소**.
2. **기존 보고서 대비 정정 2건**:
   - (해소) 모바일 `알림탭.tsx`는 이제 **실 notifications fetch + read_at 읽음처리**(106-149행)로 정상 작동 — 기존 "더미 6건(최우선·고심각)" 지적은 **현재 코드에서 완전 해소**.
   - (악화/정정) `GlobalNotificationBell`은 살아있는 "공용벨"이 아니라 **죽은 코드**.
3. **남은 기능 갭**: 죽은 `푸시설정카드` 삭제 시 모바일 푸시 구독 토글/테스트 UI 진입 경로 부재(설계 결정 필요).
4. 살아있는 중복(TYPE_CFG 3벌, normalizeKeywordList 2중, chat-push-dispatch 인라인 4종 등)은 통합 리팩터 대상으로 유효.
5. K(이스케이프 표기 비일관 1건)·S(서버 번들 .tsx 유입 1건) 외 치명적 컴파일/런타임 오류 없음.
