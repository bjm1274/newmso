# ② 내정보(마이페이지) 메뉴 — 3차 전수조사 (현 코드 직접 정독)

> 조사일: 2026-06-01 · 기준 커밋: `922af4cf` (현 HEAD)
> 방식: Sonnet 서브에이전트 4그룹 병렬 정독 → Opus 교차검증(라우터/스키마/Grep 실측).
> 대상: `기능부품/마이페이지/`(26) + `모바일/내정보/`(11, 알림 3파일 제외) = 37파일

---

## 🔴 X — 죽은 코드 (~1,140줄+)

### [X-1] 모바일 미등록 화면 3개 ≈ 790줄
모바일 내정보 라우터 `모바일/내정보/index.tsx`는 `home/attend/leave/payslip/cert/edit` 6 sub만 분기하며 다음을 **import조차 안 함**:
- `모바일/내정보/할일.tsx`(273) — `todos` 실 fetch 로직 완성돼 있으나 라우터·`MHomeSub` 타입에 `'todo'` 없음 → 도달 불가.
- `모바일/내정보/서류제출.tsx`(396) — `document_repository` 실 fetch 완성, `'docs'` 라우트 미등록.
- `모바일/내정보/서류제출이력.tsx`(121) — 죽은 `서류제출.tsx`만 사용 → 연쇄 죽음.
→ **만들다 만(또는 연결 끊긴) 화면.** 라우터에 등록하거나 삭제.

### [X-2] `마이페이지/단축키관리.tsx`(349) 전체 죽음
Grep 실측: 정의부(`:34 ShortcutManagerProps`, `:44 ShortcutManager`) 외 **import 0건**. 실제 단축키/즐겨찾기 UI는 `마이페이지/index.tsx`에 인라인 별도 구현 존재 → 이 파일은 미연결 잔재.

### [X-3] `역할별대시보드/format-utils.ts` 5심볼 죽음
- `formatTodayAttendancePrimary`(19)·`formatTodayAttendanceSecondary`(30)·`resolveEffectiveApproverId`(41) + 타입 `ApprovalRow`(12)·`TodayAttendance`(13) — 외부 import 0. 파일 주석(11행)에 **"역할별대시보드 컴포넌트(삭제됨)"** 명시된 잔재.
- (파일의 나머지 3함수 `normalizeApprovalLineIds`/`resolveApprovalLineIds`/`resolveStoredCurrentApproverId`는 `모바일/결재/data-hooks.ts`가 사용 → 파일 자체는 생존, 5심볼만 삭제 대상.)

### [X-4] `프로필카드` showSecret 체인 미사용
`프로필카드.tsx:32 internalShowSecret`·`:44 showSecret`·`:46 applyShowSecret` — 본문에서 읽거나 호출하는 곳 없음. 상위 `index.tsx:142`가 `showSecret`/`setShowSecret` prop 전달 + `프로필카드/types.ts:13-14` 선언까지 있으나 JSX 미사용.

### [X-5] `MyPageMain` 미사용 prop 2개
`index.tsx:64-65 selectedCo`/`selectedCompanyId` — `조직도본문.tsx:263-264`가 전달하나 함수 구조분해(`:123`)에 없음 → 무시됨.

### [X-6] 죽은 export
- `checkin-utils.ts:parseShiftTime`(외부 0, 내부 `buildShiftBoundary`만) · `attendance-utils.ts:getDisplayStatus`(외부 import 0).

---

## 🟠 D — 중복

- **[D-1] `verifyPassword` 2벌** — `index.tsx:588 verifyProfilePassword` ↔ `프로필카드.tsx:125 verifyPassword`. 동일 `/api/auth/verify-password` POST + toast.
- **[D-2] `getDisplayStatus` 3중** — `출퇴근기록/attendance-utils.ts:10`(export) ↔ `출퇴근기록/index.tsx:70` ↔ `출퇴근차트.tsx:8` (동일 구현 로컬 재정의 2개).
- **[D-3] `buildDateWithTime` 2중** — `checkin-utils.ts:80`(export) ↔ `출퇴근기록/index.tsx:65` (checkin-utils를 import하면서도 재정의).
- **[D-4] `fetchLatestStaffShiftContext` 2중** — `checkin-utils.ts:130` ↔ `출퇴근기록/index.tsx:186`(인라인 재구현).
- **[D-5] `normalizeStoredEntry` 2중** — `단축키설정.ts:48` ↔ `즐겨찾기설정.ts:245`.
- **[D-6] `MYPAGE_RECORDS_VIEW_KEY` 2중** — `단축키설정.ts:98`(export) ↔ `마이페이지/index.tsx:52`(로컬 재선언).
- **[D-7] `formatYearMonthLabel` 2중** — `급여명세서/index.tsx:61` ↔ `급여명세서/모바일명세서.tsx:65`.
- **[D-8] `getToday`/`getTodayKr` 동일 구현** — `나의할일.tsx:63` ↔ `모바일/내정보/할일.tsx:31`(주석까지 동일).
- **[D-9] `getInitial` 2중** — `모바일/홈.tsx:27` ↔ `모바일/정보수정.tsx:20`.
- **[D-10] `escapeHtml` 지역정의** — `서류제출.tsx:37`이 `lib/escape-html.ts`를 import 않고 동일 함수 재정의.
- **[D-11] `document_repository` 조회 중복** — `모바일/서류제출.tsx:77` ↔ `인사관리/계약문서.tsx:282`(동일 `created_by` 조건, status 정규화 불일치).

---

## 🟡 S — 문법/런타임 (보안 1건 중대)

- **[S-1] 🔒 모바일 홈 비밀번호 게이트 무기능** — `홈.tsx:161-171 handlePwConfirm`이 `if (pw.length < 4)` **길이만 검사**. 4자리 아무 값(`1234`)이면 `pwGate`(급여명세·증명서, `:183-184`) 통과. 서버·실비밀번호 대조 없음. **민감정보(급여/증명서) 보호 게이트가 실효 없음.** ↔ 데스크톱 프로필카드는 `/api/auth/verify-password` 사용. → 비번 확인 API 호출로 교체 필요.
- **[S-2] `notifications` insert `user_id: 'system_admin'`** — `index.tsx:323-329`. `notifications.user_id`는 `staff_members.id` FK. `'system_admin'` 행이 없으면 insert 실패(코드 주석도 "추후 실제 HR 담당자 id로 교체 필요" 인지).
- **[S-3] `notifications.type` 영문/한글 혼재** — `index.tsx:327 type:'SUCCESS'` insert ↔ `마이페이지공통섹션.tsx:62 .eq('type','급여명세')` 필터. 도메인 불일치로 `SUCCESS` 알림은 해당 쿼리에 안 잡힘.
- **[S-4] 카메라 트랙 누수 가능** — `서류제출.tsx:497-514` `CameraScanner` `useEffect([])` 클린업이 마운트 시점 `stream`(null) 클로저 캡처 → 트랙 미해제. `useRef` 패턴 권장.
- **[S-5] `any` 다수** — `단축키관리.tsx:72-107 user as any` 4건, `서류제출.tsx:48 useState<any[]>`. (JM4 위반)
- (경미) `출퇴근기록/index.tsx:264,327` useEffect deps 불완전(stale closure 위험).

## 🟡 K — 한글 깨짐
- 없음.

## 🟢 U — 불필요 / 무동작 UI

- **[U-1] 모바일 무동작 UI 버튼 다수(미구현을 완성처럼 표시)**:
  - `홈.tsx:497` 알림설정(div, onClick 無, 항상 '켜짐') · `:525` 비밀번호 변경(onClick 無)
  - `연차.tsx:71` 연차 신청(onClick 無) · `급여명세.tsx:173` PDF 다운로드(onClick 無) · `정보수정.tsx:132` 프로필 사진 변경(onClick 無) · `증명서.tsx:27` 발급 버튼 4개(onClick 無)
- **[U-2] 모바일 홈 수치 비실데이터** — `홈.tsx:347 '지각 0회'` 하드코딩(`useMonthlyAttendance.late` 미사용) · `:206 leaveTotal = ...|| 15` 세션값+fallback(`useMyLeave` 미사용).
- **[U-3] 데스크톱 `홈탭헤더.tsx:89 '정규직'` 하드코딩** — 고용형태 미반영(모바일 홈은 `user.employment_type` 동적 — 대조).
- **[U-4] 별칭 재할당** — `출퇴근기록/index.tsx:38 HOSPITAL_LOCATION`/`ALLOWED_RADIUS_METER` · `증명서관리.tsx:37 IssuedCertificateRow` · `서류제출.tsx:56 REQUIRED_DOCS`.
- **[U-5] `index.tsx:126` 미사용 `isMobile` + eslint-disable 잔재 주석**(2026-05-27 기재).
- **[U-6] `프로필카드.tsx:162 verifyPasswordAndRun`** 단순 래퍼(호출 1곳).
- **[U-7] 🔒 `certificate-print-utils.ts:313` iframe `srcdoc` sandbox 없음** — 결재 미리보기 sandbox 패턴과 불일치.
- **[U-8] `모바일/서류제출.tsx:89 console.error`** 프로덕션 잔존.

## 🟢 Q — 유사기능

- **[Q-1] 할일 CRUD 2벌** — 데스크톱 `나의할일.tsx`(880줄, 필터·반복·리마인더) ↔ 모바일 `할일.tsx`(오프라인 큐) 각자 구현. `useTodos` 훅 통합 후보.
- **[Q-2] 🔒 GPS 위치검증 2경로 모두 클라이언트 전용** — `출퇴근기록/index.tsx:613 resolveCurrentLocation`(PC, 캐시·2분TTL) ↔ `app/hooks/useGeolocationCheckin.ts:53`(모바일, 1회). **서버 재검증 API 없음** → 좌표 조작 시 원격 체크인 가능. 서버 `/api/attendance/checkin` 재검증 레이어 권장.
- **[Q-3] 카메라 스캐너 중복** — 데스크톱 `서류제출.tsx:485 CameraScanner` ↔ 모바일 `서류제출.tsx`(동일 getUserMedia+canvas crop).
- **[Q-4] 연차 훅 2개** — `내정보/data-hooks.ts useMyLeave` ↔ `인사관리/data-hooks.ts useMyLeaveBalance`(동일 staff_members+leave_requests).
- **[Q-5] 화면 2경로** — 서류제출(내정보 SDocs ↔ 인사 계약문서) · 증명서(내정보 ↔ 계약문서 CertTab) · 급여명세(모바일명세서 임베드 ↔ 모바일/급여명세 스크린).
- **[Q-6] 권한목록 중복 계산** — `getPermittedMypageTabs/MainMenus/SubMenuBearers`를 `마이페이지/index.tsx`(즐겨찾기 UI)와 `단축키관리.tsx`가 각자 `useMemo`. (단축키관리가 죽은 코드라 현재 실害 없음.)

---

## 핵심 결론 (내정보)

1. **정정 — 모바일 더미 주장 대부분 해소**: 라우팅된 모바일 화면(연차/급여명세/증명서/출퇴근/정보수정)은 현재 모두 `staffId` 기반 실 DB fetch. 기존 "모바일 내정보 전부 하드코딩" 지적은 **이미 수정됨**(알림탭과 동일 패턴). 잔존 정적: 증명서 발급가능목록(`CERT_DOCS`), 홈 히어로 지각/연차 수치.
2. **🔒 신규 중대 — 모바일 홈 비밀번호 게이트(S-1)가 보안 무기능**: 길이 4자만 통과 → 급여/증명서 보호 실효 없음. (데스크톱은 검증 API 사용.)
3. **죽은 코드 ~1,140줄+**: 모바일 미등록 화면 3개(790) + 단축키관리(349) + 잔재 심볼/prop.
4. **무동작 UI 버튼 다수**(모바일 홈/연차/급여/증명서/정보수정) — 미구현이 완성처럼 보임 → 사용자 혼란.
5. **출퇴근 영역**: GPS 서버 재검증 부재(보안), 출퇴근 유틸 `getDisplayStatus`/`buildDateWithTime`/`fetchLatestStaffShiftContext` 다중 중복.
