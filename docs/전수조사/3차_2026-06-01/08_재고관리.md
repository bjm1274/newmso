# ⑧ 재고관리 메뉴 — 3차 전수조사 (현 코드 직접 정독)

> 조사일: 2026-06-01 · 기준 커밋: `922af4cf` (현 HEAD)
> 방식: Sonnet 서브에이전트 6그룹 병렬 정독 → Opus 교차검증(스키마/Grep 실측). (세션 한도로 1차 실패 후 재실행.)
> 대상(≈65파일): 재고관리통합 + 재고관리서브 30 + 워크센터 14 + 모바일 10 + lib 7 + API 3.

---

## ✅ 정정 — 즉시조치 #2 재검증

기존 "재고 use-analyze-data가 없는 `amount`/`qty` 컬럼 참조(정본 quantity) → ABC분석·수요예측 사용량 항상 1, 납기 '-'"에 대해:
- **정본 컬럼 확정**(schema.ts 대조): `inventory.quantity`·`inventory_logs.quantity`. `amount`/`qty` 컬럼은 스키마에 **없음**.
- `use-analyze-data.ts:22 pickNumber(l,['quantity','amount','qty'],0)`는 quantity가 첫 키라 정상 폴백 → **컬럼 자체는 무해**(불필요한 폴백 후보).
- **실제 "항상 1·100%" 원인**: (a) `use-status-data.ts:105 pickNumber(r,['amount','qty','quantity'],1)` 기본값 **1**(quantity NULL 행을 1로 집계, 부풀림) · (b) `use-analyze-data.ts:107 buildInspects`의 `done=total` 하드코딩(실사 진행률 항상 100%·diff 0). → 컬럼명 수정보다 **기본값/더미 로직**이 핵심.

---

## 🔴 S — 런타임 (잘못된 컬럼/원자성)

- **[S-1] 재고실사 비원자 클라 for-loop** — `재고실사.tsx:99` `for` 루프에서 `inventory.update` 후 `inventory_logs.insert` 순차 → 중간 실패 시 재고만 바뀌고 로그 누락. (재고이관은 서버 `/api/inventory/stock-transfer` 원자 처리와 대조.)
- **[S-2] `부서소모기록.tsx:13,235 inventory_logs.item_name` 없는 컬럼** — 정본은 `item_id`(JOIN 필요) → 품목명 항상 빈값.
- **[S-3] `유효기간알림.tsx:201 expiration_date` 필터** — `getItemExpiryDate`는 `expiration_date||expiry_date` 읽으나 쿼리는 expiration_date만 필터 → `expiry_date`만 채워진 행 **알림 누락**(정본 expiry_date).
- **[S-4] 워크센터 수량 더미** — `use-status-data.ts:105` 기본값 1 + `buildInspects` done=total(실사 100%) → KPI/진행률 왜곡.
- **[S-5] `물품등록.tsx:161 stock+quantity 이중 세팅`**(이중 기록) · `스캔모듈완성.tsx:76` 오류인데 `toast(...,'success')`.
- **[S-6] D1↔Supabase 이중쓰기 fallback 잔존(비원자)** — `inventory-utils.ts processInventoryIssue:632`·`reverseInventoryIssue:921`·`useStockModal.ts:131` D1 route 호출 후 Supabase 직접 UPDATE 경로 잔존 → 재고 손실/스토어 불일치 위험.
- **[S-7] `atomicStockTransfer`(lib/db/functions/inventory.ts:107) SELECT 후 batch UPDATE 간 동시성** — WHERE 가드 없어 동시 요청 시 음수 재고 가능(atomicStockUpdate는 WHERE 가드 있음).

## 🔴 D — 중복 (워크센터↔레거시 전면 병행)

- **[D-1] 워크센터 4종(Status/Item/IO/Analyze) ↔ 재고관리서브 15+화면 재구현** — 워크센터가 `use-*-data` 훅으로 동일 테이블(inventory/inventory_logs/purchase_orders/suppliers/categories) 독립 조회, 레거시 화면(재고현황뷰/발주관리/물품등록/수요예측/실사/카테고리/UDI 등)을 **import하지 않음**. 동일 도메인 2경로(영문id→워크센터, 한글id→레거시). 인사관리 payroll과 동일 패턴.
- **[D-2] 수량 헬퍼/패턴 중복** — `qty`/`minQty` 재정의(재고현황뷰.tsx:83 ↔ Grid:11, `inventory-utils.getItemQuantity` 미사용) · `quantity ?? stock` 패턴 3+곳(재고현황뷰:177/Grid:13/재고월마감:135).
- **[D-3] 발주/거래처 중복** — 발주 insert 3벌(발주관리:254 ↔ PurchaseOrderModal:116 ↔ 모바일 발주등록:164) · 거래처 CRUD 2벌(거래처관리 ↔ 명세서관리:68-170).
- **[D-4] lib 상수/컬럼 중복** — `INVENTORY_SUPPORT_COMPANY`/`DEPARTMENT` 2중(inventory-constants ↔ inventory-utils:54) · `INVENTORY_SELECT_COLUMNS`(utils) ↔ `INVENTORY_LIST_COLUMNS`(query-columns) 컬럼목록 중복.
- **[D-5] 화면↔워크센터패널** — 물품등록 ↔ ItemWorkcenter CatalogPanel · 재고수요예측 ↔ AnalyzeWorkcenter ForecastPanel · 카테고리관리 ↔ CategoryPanel · 부서별물품장비현황 ↔ 부서소모기록.
- **[D-6] 모바일** — `toISODate` 2벌(자산등록:77 ↔ 발주등록:57) · `KpiCard`(입출고-form) 재고 내 중복(전역 KpiCard 7곳 계열).

## 🟠 X — 죽은 코드

- **[X-1] 고아 lib** — `lib/inventory-constants.ts` 전체 import 0 · `inventory-query-columns.ts` SELECT 상수 **12개 미사용**(PURCHASE_ORDER_LIST_SELECT 등) + `INVENTORY_LIST_SELECT` 미사용 · `lib/db/functions/inventory.ts atomicStockTransfer` export됐으나 route는 executiveTransfer 자체구현(미사용).
- **[X-2] `재고현황뷰.tsx` hidden UI 8섹션** — `className="hidden"` 컨트롤바(:338)/긴급알림(:439)/공급워크플로우(:478)/히스토리(:585)/테이블헤더(:630)/유효기간센터(:762) + 구동 state 4개(`sortBy`/`showCompleted`/`expandedApprovalId`/`alertDismissed`) 死.
- **[X-3] 모바일** — `data-hooks.ts:70 useShortageTop` 미사용(재고현황은 인라인 재구현) · `입출고-form.tsx:148 KpiCard` 미사용 · `물품등록.tsx:58 imgAttached` DB 미저장(토글만).
- **[X-4] 기타** — `납품확인서.tsx:11 showPreview` 미사용(여전) · `명세서관리.tsx:37 customPresets` stale(localStorage 미저장, 항상 빈배열) · `stock-workcenter-common.tsx:257 WorkcenterBack` 미사용 · `부서소모기록.tsx:6 withMissingColumnsFallback` import 미사용.

## 🟢 U — 불필요 / 무동작 / 더미

- **[U-1] 워크센터 무동작 버튼 12개+** — StatusSubViews 발주/설정/자동발주(:79/117/149) · ItemWorkcenter 일괄가져오기/물품등록/자산등록/UDI등록/QR라벨(:140/304/444/290) · IOWorkcenter 수동등록/새발주/상세(:171/266/312) · AnalyzeWorkcenter 예측리포트/실사시작(:237/341) — 전부 onClick 없음.
- **[U-2] 워크센터 더미** — `AnalyzeWorkcenter.tsx:37 CLOSE_STEPS`(월마감 항상 "1단계 진행중")·`:45 CLOSE_HISTORY`(amt '-') 하드코딩.
- **[U-3] 모바일 더미(주석도 "더미" 명시)** — `index.tsx:163 Hub KPI 4종`(847종/23건/4건/8건) · `:61 STOCK_MENU 배지`(부족23/발주3) · 발주등록 거래처('동인의료')/품목/날짜('2026.05.30')/주소 하드코딩 · 자산등록 `MOCK_SCAN`(C-Arm…) · 분석마감 5단계 정적.
- **[U-4] 모바일 무동작 버튼** — 재고현황 입고/출고/검색/QR(:228/85/88) · 발주등록 품목추가(:243) · 물품자산 스캔(:298) · 분석마감 대표승인요청(:465).
- **[U-5] 무동작 목업** — `자산QR관리.tsx:62 scannerActive`(실제 카메라 API 없음, 애니메이션만) · `dept-week-settings.ts` localStorage(서버 미동기화, inventory에 weekly/min_weeks 컬럼 없음).
- **[U-6] 하드코딩 'SY INC.'** — inventory-constants/utils.

## 🟡 K — 한글 깨짐
- 없음.

## 🟢 Q — 유사기능 / 🔵 보안

- **[Q-1] 워크센터↔레거시 전면 병행** — 인사관리와 동일 최대 구조 이슈.
- **[Q-2] QR/바코드 스캔 3중** — `자산QR관리`(카메라 목업) ↔ `스캔모듈완성`(키보드 스캐너) ↔ ItemWorkcenter UdiPanel. UDI 진입점 3곳(물품등록 is_udi/UDI관리/UdiPanel).
- **[Q-3] 유효기간 알림 경로** — `유효기간알림.tsx` 클라 `setInterval(24h)`(앱 열려야 동작) ↔ `check-inventory.ts` cron(재고부족만, 유효기간 미포함) → 유효기간 알림은 클라 폴링이 유일. cron에 유효기간 추가 권장.
- **[Q-4] 데스크톱↔모바일 insert 중복** — 물품/발주.
- **[🔵 SEC] 회사격리** — `use-status/io/item/analyze-data` 대부분 `company` 필터 없이 RLS 의존. `purchase_orders`는 company 컬럼 없어 notes 태깅만(쿼리 격리 불가).

---

## 핵심 결론 (재고관리)

1. **정정 — 즉시조치 #2**: `amount/qty` 컬럼은 무해(quantity 폴백). 실제 사용량 왜곡 원인은 `use-status-data` 기본값 1 + `buildInspects` done=total 더미. 정본 `quantity` 확정.
2. **워크센터↔레거시 전면 병행(최대 구조 이슈)**: 워크센터 4종이 레거시 15+화면 재구현 + **무동작 버튼 12개+** + 월마감/실사 더미. 인사관리 payroll과 동일.
3. **모바일 재고**: Hub KPI·분석마감·발주/자산등록 **대량 하드코딩 더미**(주석도 인정) + 무동작 버튼 다수.
4. **원자성/잘못된 컬럼**: 재고실사 클라 for-loop · processInventoryIssue/reverseInventoryIssue D1↔Supabase 이중쓰기 fallback · 부서소모기록 item_name · 유효기간알림 expiration_date(알림 누락).
5. **고아 lib**: inventory-constants + query-columns 상수 12개 + atomicStockTransfer. 재고현황뷰 hidden 8섹션.
