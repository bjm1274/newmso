# ⑦ 인사관리 메뉴 — 3차 전수조사 (현 코드 직접 정독)

> 조사일: 2026-06-01 · 기준 커밋: `922af4cf` (현 HEAD)
> 방식: Sonnet 서브에이전트 10그룹(2배치) 병렬 정독 → Opus 교차검증(스키마/Grep 실측). 단일 메뉴 최대(171파일).
> 대상: 인사관리.tsx + 인사관리서브 107 + 인사관리워크센터 63 + 급여/연차 lib.

---

## ✅ 정정 — 기존 즉시조치 해소 확인

- **[해소] 연차촉진 upsert NOT NULL** — `휴가신청/연차촉진시스템.tsx:180-199`에서 `id`/`target_year`/`step` 모두 명시 채움. 제약위반 없음.
- **[해소] 계약관리 notifications 컬럼** — `계약관리.tsx:273`은 `body` 사용(message 아님), `read_at:null`도 D1 `schema.ts:1327`에 존재 → 정상. (기존 "message 컬럼" 지적 해소. *그룹 G가 구 마이그레이션 기준 "read_at 없음"으로 본 것은 오판 — D1 정본엔 body·read_at 모두 존재.*)
- **[해소] RiskActionDialog 중복** — `app/components/RiskActionDialog.tsx`는 worktree에만 존재, 메인 트리엔 정본 `인사관리서브/RiskActionDialog.tsx`만. 3개 소비자 모두 정본 참조.

---

## 🔴 S — 런타임 (잘못된 D1 컬럼/테이블 — 즉시조치급)

- **[S-1] 급여정산 지각차감 무력화** — `급여명세/급여정산.tsx:317` `attendanceRecordRows` 빈 배열 선언 후 push 코드 없음 → `:396 attendanceMinuteMap` 항상 빈 Map → `late_minutes`/`early_leave_minutes` 기반 지각 차감 미적용.
- **[S-2] `s.base` 잘못된 컬럼(여전, 즉시조치 #9)** — `계약서자동생성.tsx:70 salary: s.base`(정본 `base_salary`) → 급여 자동입력 빈값. `급여명세/무급결근차감.tsx:69 staff.base || 3000000` 동일(항상 fallback).
- **[S-3] `device_name` 잘못된 컬럼(여전, 즉시조치 #9)** — `의료기기점검.tsx:107 target?.device_name`(D1 `medical_devices`는 `name`만) → 삭제확인 장비명 항상 '선택한 장비'.
- **[S-4] `employ_type` 잘못된 컬럼(신규)** — `MemberWorkcenter/data.ts:141`·`StaffTable.tsx:202`·`StaffDrawer.tsx:97` `employ_type`(정본 `employment_type`) → 정규/계약/수습 분류 항상 ''→**모든 직원 "정규" 집계**.
- **[S-5] `staff_certifications` 없는 테이블(신규)** — `교육자격인사이동패널.tsx:12 from('staff_certifications')`(정본 `staff_licenses`) → table not found, 자격 섹션 비동작.
- **[S-6] `certificate_issues` 없는 테이블(신규)** — `DocsWorkcenter/DocsCertSummary.tsx:48 from('certificate_issues')`(정본 `certificate_issuances`, 컬럼도 불일치) → 항상 catch fallback 정적데이터.
- **[S-7] 연말정산 계산 오류** — `급여명세/연말정산.tsx:105 year_month='YYYY-YE'` 비표준 포맷 · `:294 incomeTax = taxable*0.03` flat · `:152 기본공제 150만` 하드코딩 → 간이세율 부정확.
- **[S-8] 증명서발급 키 불일치** — `증명서발급.tsx:41 getClosingText` map 키(`급여지급증명서`/`소득금액증명서`)가 `CERTIFICATE_TYPES` id(`급여인증서`/`소득금액증명원`)와 불일치 → 기본문구 반환 + `:174 isSalaryCert` 항상 false(급여행 미표시).
- **[S-9] 지각조퇴분석 데이터 합성** — `지각조퇴분석.tsx:63` `attendances`에서 `late_minutes` 미조회, status 문자열로 1/0 합성 → 평균지각분 무의미(실제 컬럼 존재).
- **[S-10] 간호근무표 `s.shiftType` 타입 부재** — `간호근무표.tsx:180`(StaffMember에 shiftType 없음) → `day_fixed` 제외 필터 무동작(상근직도 표시).
- **[S-11] Tailwind 2중 슬래시 → 배경 투명** — `bg-orange-500/20/50` 등: `근태유틸.ts:313,315`·`근태관리메인-내부유틸.ts:275,277`·`근태근무표뷰.tsx:276`·`근태달력뷰.tsx:293,326`.
- **[S-12] lib 계산 오류** — `withholding-tax-file.ts:207 resident_number`(정본 `resident_no`)→빈값 · `퇴직금계산기.tsx:25 monthlyContribution = avg/12` 단위 중복(/144 효과) · `payroll-fetch.ts:307` 퇴직금 공식이 `severance-pay.ts`와 불일치(30일계수·365.25).
- **[S-13] 저장 error 미확인** — `의료기기점검.tsx:74`·`비품장비대여관리.tsx:243,275` supabase update/insert `{error}` 미확인 후 UI 선반영.

## 🔴 D — 중복 (워크센터↔레거시 전면 병행 + 유틸 중복)

- **[D-1] 근태유틸 2벌 ~700줄 50%+ 중복** — `근태유틸.ts` ↔ `근태관리메인-내부유틸.ts`(padDay/buildAttendanceKey/resolveAttendanceStatus/ShiftBand/ROSTER_*_POSITIONS 등 다수 복제). 진짜 차이는 `LEGACY_ROSTER_APPROVAL_TYPE`('roster_schedule_approval' vs '근무표') 1개.
- **[D-2] 근무표 3중 시스템** — `간호근무표.tsx`(nurse_schedules) ↔ `근태근무표뷰.tsx`(shift_assignments) ↔ `AttendWorkcenter/RosterGrid.tsx`(shift_assignments). 후자 2개는 동일 테이블에 독립 write(RosterGrid가 승인 워크플로 우회).
- **[D-3] payroll 워크센터 Mod\*13 레거시 재구현** — ModOrdinary↔통상임금계산기, ModRetirement↔퇴직금계산기, ModMinWage↔최저임금체크, ModInsurance↔4대보험EDI, ModSettlement↔급여정산 등 13쌍. **계산 lib은 공유**(수치 중복 없음)하나 UI 골격 재구현 + 대부분 실기능 버튼 무동작(실기능은 레거시에). → 워크센터=골격, 레거시=실기능 2경로.
- **[D-4] 함수 N벌** — `resolveAttendanceStatus` 3벌 · 연차사용 계산 3곳(annual-leave-ledger ↔ 연차원장 ↔ 내정보 data-hooks) · `JobCategorySchema` 3곳(교육관리/면허자격증관리/JobCategoryTrainingMatrix) · `daysUntil` 3곳(Welfare*) · `formatPercent`(4대보험EDI/세율보험요율) · `SalaryChangeHistoryRow` 타입 2중.
- **[D-5] 계산 인라인 중복** — `중간정산.tsx` 4대보험/소득세 ↔ `급여정산 calculateSalary` 동일 · `통상임금계산기`가 `lib/payroll-working-hours.calculateHourlyRateFromMonthlySalary` 미사용 인라인(고정 209h).
- **[D-6] 기능 2중** — 연차소멸알림(`연차소멸알림.tsx` ↔ `LeaveExpiryBoard`) · 구성원목록(`구성원현황` ↔ `MemberWorkcenter/StaffTable`) · 서류제출 `REQUIRED_DOCS` 3곳(스마트서류제출/마이페이지 서류제출/DocsSubmSummary).

## 🟠 X — 죽은 코드

- **[X-1] payroll 워크센터 더미** — `payroll-data.ts` 더미 상수 10개(SETTLEMENT_STEPS/PAYROLL_KPIS/LEDGER_ROWS 등) + `payroll-types.ts` 더미 타입 8개 전부 미사용(PAYROLL_MODULES만 사용).
- **[X-2] 죽은 함수/블록** — `구성원현황.tsx:561 handleApproveEss`(Safe판만 사용)+`:219` 주석블록 · `급여정산.tsx:701 handleDraftSaveLegacy/handleFinalizeLegacy`(void 억제) · `연말정산.tsx:255 legacySendCertificateEmail`+`:50 isScanning`+`:478 scanResult OCR 블록`(死) · `증명서발급.tsx:494 false? 분기 170줄` · `근태관리메인.tsx:754 if(false&&) 반려알림` · `스마트서류제출.tsx:56 limit(0) 미사용 쿼리` · `lib/annual-leave-promotion.ts:91 filterStaffsNeedingPromotion`(고아).
- **[X-3] 미사용 user prop 다수** — 지각조퇴분석/조기퇴근감지/근태이상통합분석/통상임금계산기/무급결근차감/총인건비예측 등.

## 🟢 U — 불필요 / 무동작 / 하드코딩

- **[U-1] 하드코딩 회사명/이름(다중 테넌트 위험)** — `LeaveWorkcenter/data.ts:298 'SY INC.'`·`급여시뮬레이터.tsx:69 'SY INC.'`·`구성원현황.tsx:1219 '박철홍정형외과'`·`구성원현황.tsx:1064 성명==='박철홍'→사번'1'`.
- **[U-2] toast tone 오류 대량(success↔error 뒤바뀜)** — `급여정산.tsx:289`(오류인데 success)·`연말정산.tsx:427`·`급여명세서발송.tsx:181`(성공인데 error)·`교육내역명단.tsx:158,165`·`법인카드사용내역.tsx:278,311`.
- **[U-3] 저장실패 은폐** — `경조사관리.tsx:134` insert 실패 시 로컬 fallback으로 UI 반영(toast 없음, 리로드 시 소멸) · `인사통합설정.tsx:186 policy 탭 "저장하기" 버튼 onClick/upsert 없음`(다른 탭은 저장 보유).
- **[U-4] 무동작 버튼** — payroll Mod*(ModInsurance EDI 3개·ModPension 2개·ModWithholding 연말정산 4개·ModRetirement/ModWagePeak/ModUnpaid/ModAbsence 등) · `근태대시보드뷰.tsx:42 면담요청` · `인사발령관리` 관보생성 이중 진입.
- **[U-5] 하드코딩 요율/타입약화** — `payroll-insurance-rates.ts` `_2026` 연도 하드코딩(yearMonth 파라미터 미사용) · 계약관리/통상임금계산기/자격면허대시보드 등 `Record<string,unknown>` props 캐스팅.

## 🟡 K — 한글 깨짐
- 없음(근태유틸 유니코드 이스케이프는 가독성 이슈, 런타임 정상).

## 🟢 Q — 유사기능 / 🔵 보안

- **[Q-1] 워크센터 ↔ 레거시 전 영역 병행** — payroll/leave/member/attend/docs 워크센터가 각 레거시(급여관리/휴가관리메인/구성원현황/근태관리메인/계약관리)와 동일 도메인 2경로. (`인사관리.tsx:467` navigator.webdriver로 일부 레거시 분기.)
- **[Q-2] 이상탐지 3중** — 지각조퇴분석 ↔ AbnormalWorkcenter/data ↔ 근태이상통합분석. AbnormalWorkcenter가 완성도 높음(컬럼 정상).
- **[Q-3] 정책값 불일치** — 임금피크 시작나이 55(임금피크제.tsx) vs 60(ModWagePeak/payroll-policy) · 건강검진 `scheduled_date`(WelfareCheckupSummary) vs `completed_date`(건강검진관리) 집계 불일치.
- **[🔵 SEC]** 계약 PII는 `contract-crypto` 복호화를 인쇄 직전에만 수행, `ArchivedDocumentView.tsx:167 sandbox=""` 적용 — 양호.

---

## 핵심 결론 (인사관리 — 최대 메뉴)

1. **정정 3건 해소**: 연차촉진 upsert · RiskActionDialog 중복 · 계약관리 notifications(body 사용, 정상).
2. **런타임 즉시조치(잘못된 컬럼/테이블) 다수**: `s.base`·`device_name`·`employ_type`(모든직원 정규)·`staff_certifications`·`certificate_issues`·`resident_number` + 급여정산 지각차감 무력화 + 연말정산 간이세율 오류.
3. **워크센터↔레거시 전면 병행(최대 구조 이슈)**: payroll Mod*13 = 골격 재구현 + 무동작 버튼, 실기능은 레거시. payroll-data 더미 18개 死.
4. **근태 영역**: 근태유틸 2벌 ~700줄 중복 + 근무표 3중 시스템 + Tailwind 2중슬래시(배경 투명) + 지각분 합성/shiftType 무동작.
5. **U**: 하드코딩 회사명/이름('SY INC.'/'박철홍정형외과'/'박철홍') · toast tone 오류 대량 · 경조사/인사통합설정 저장실패 은폐·무동작.
