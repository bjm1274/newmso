# ④ 채팅(메신저) 메뉴 — 3차 전수조사 (현 코드 직접 정독)

> 조사일: 2026-06-01 · 기준 커밋: `922af4cf` (현 HEAD)
> 방식: Sonnet 서브에이전트 8그룹 병렬 정독 → Opus 교차검증(import그래프/Hooks순서/스키마/Grep 실측).
> 대상(≈76파일): 메신저 루트 44 + `메신저섹션/`5 + chatQueryService + useChatTypingD1 + 모바일채팅 12 + lib/chat 6 + API 7.

---

## ✅ 최대 정정 — 기존 "고아 훅 18개 ~3,500줄" 해소

기존 재조사 보고서의 채팅 핵심 지적은 "메신저.tsx가 추출 훅을 인라인 재구현 → 고아 18개(useChatRealtimeBridge/useChatReadSync/메신저읽음상태훅/useChatComposerState/useChatViewController/… + 모바일 퀵스위치바/반응선택)"였음. **현재 코드 실측 결과:**
- `useChat*` 파일은 `useChatTypingD1.ts`(사용됨) 1개만 잔존 — 나머지 영문 고아 훅 17개는 **코드베이스에서 삭제됨**.
- 메신저.tsx import 헤더(L20-83)가 추출 훅(방데이터/구독/전송/파생/상태/업로드/사이드바/검색/메시지액션워크플로/입력워크플로/방관리/방전환/예약공지/방환경설정)을 **실제 import·사용**. 그룹 C 실측: "7개 워크플로훅 전원 메신저.tsx에서 정상 사용, 고아 없음".
- 새 디렉토리 `메신저섹션/`(StaffDetailModal/ChatRoomHeader/DateJumpModal/RetryQueueBanner/TypingNotice)로 추가 분리됨.
→ **추출훅 리팩터 완료**. 단 메신저.tsx 자체는 여전히 3,428줄(추가 분할 여지, U).

---

## 🟠 여전히 열림 (기존 지적 잔존)

- **[X-1] `needsAttention` 항상 false 하드코딩** — `메신저파생훅.ts:256`(useThreadSummaries)·`:287`(useThreadOverviews) `const needsAttention = false`. 소비처 `메신저.tsx:2478 .filter(t=>t.needsAttention)`(항상 빈배열)·`메신저타임라인.tsx:588` 분기 **영구 死**. 동반: `rootSenderId`(:254,285) 계산되나 참조 0.
- **[S/D-1] `insertChatMessageWithFallback` 2벌 공존** — `lib/chat-message-write.ts:17`(3인자 client,payload,select) ↔ `메신저메시지서비스.ts:22`(2인자 래퍼, supabase 고정). 호출 분산: 전송훅·입력워크플로훅=래퍼 / 메시지액션워크플로훅·예약공지훅·모바일 data-hooks·업로드.ts=lib직접. 신규 작성 시 오선택 위험. → lib 단일화 권장.
- **[D-2] `MIME_BY_EXTENSION`+`getUploadContentType` 3중** — `메신저업로드훅.ts:30` ↔ `게시판업로드.ts:10` ↔ `api/chat/upload/route.ts:22`. → `lib/upload-mime.ts` 추출.

## 🔴 신규 — 보안

- **[S-SEC] og-preview SSRF** — `api/chat/og-preview/route.ts:80`은 **최초 입력 URL만** `isPublicHttpUrl` 검증, `:97 fetch(url,{redirect:'follow'})`로 리다이렉트 추종 후 **최종 도달 URL 재검증 없음**. 공개 서버가 302로 내부IP(`169.254.169.254` 메타데이터 등)로 리다이렉트하면 SSRF 우회 가능(응답은 OG 태그·50KB 제한이라 유출면은 작으나 내부 요청 자체 성립). → `redirect:'manual'` 후 Location 재검증 권장.
- **[U-SEC] 모바일 채팅 회사격리 없음** — `chatQueryService.ts:79 fetchAllChatRooms`(chat_rooms 전체) · 모바일 `data-hooks.ts:61 useChatStaffDirectory`(staff_members 전체) WHERE company 없음. 단일 회사 SaaS면 무해, 다중 테넌트면 데이터 혼재. (추가기능 `void company`와 동일 계열.)

## 🟡 S — 문법/런타임

- **[S-2] `fetchData` force 옵션 무시** — `메신저방데이터훅.ts:1361 fetchData = useCallback(async () => {...})` 무인자인데 `메신저구독훅.ts:114`는 `(options?:{force?})` 타입 선언 + `:250/274/444/451 fetchDataLatestRef.current({force:true})` 호출 → **강제 재조회 의도가 구현에 미반영**.
- **[S-3] `chat-query-columns.ts:7 is_announcement 누락`** — D1 `chat_rooms`에 `is_announcement` 존재(`schema.ts:382`)·`ChatRoomRow` 사용하나 `CHAT_ROOM_OPTIONAL_COLUMNS`에 빠짐 → `buildChatRoomSelect()` SELECT에 미포함, Supabase 경유 조회 시 항상 null.
- **[S-4] 오프라인 업로드 `file_kind` 부분 누락** — `모바일/채팅/업로드.ts:213-235`(R2 PUT 실패 + 서버 fallback 실패 후 큐잉 경로)만 `file_kind` 누락(`message_type`만). 정상 경로(:171,250)는 포함. → 해당 경로 적재분은 확장자 정규식 폴백에 의존.
- **[S-5] deps/타입** — 메신저.tsx:1555 `updateUnreadForRooms` 미사용 deps · 메신저구독훅:349 과잉 deps · 메신저운영센터 `any[]` 다수 · 모바일 `as any` 3건(이모지피커:226/456, data-hooks:70/446).

## 🟡 K — 한글 깨짐/혼용

- **[K-1] `메신저유틸.ts:20` 유니코드 이스케이프** — `RESIGNED_STATUSES = new Set(['퇴사','퇴직'])`('퇴사'/'퇴직') + `:23-27` JSDoc 전체 이스케이프 → 리뷰/편집 판독 불가(런타임 정상). UTF-8 한글 복원 권장.
- **[K-2] 'Unknown'↔'알 수 없음' 혼용** — `메신저방데이터훅.ts:560,571,998,1009` 영문 'Unknown' ↔ `:1664,1675` 한글. UI 불일치.

## 🟢 X — 죽은 코드 (미사용 import/export)

- **메신저.tsx**: `getBookmarkStorageKey`(:92)·`getConversationRoomIdSet`(:94)·`getPinnedStorageKey`(:98)·`POLL_SELECT`(:18) import만, 호출 0.
- **메신저메시지렌더.tsx**: `extractFirstUrl`(:139)·`OgLinkPreview`(:155) export 참조 0(OG 프리뷰 미연결).
- **메신저타임라인.tsx**: `INLINE_ACTION_REACTIONS`(:54)·`inlineActionButtonClass`(:55)·`inlineDangerActionButtonClass`(:57) 미사용 + 미사용 prop 다수(isLoadingMessages/onOpenBoardPost/onTogglePin/onStartEdit 등 구조분해 후 JSX 미참조).
- **메신저컴포저.tsx**: `QUICK_EMOJIS`(:23) 미사용 + `showScrollToLatest`/`onScrollToLatest` prop 미사용(부모는 전달).
- **메신저데이터유틸.ts:172 selectFallbackChatRoomId** · **chatQueryService.ts:141 updateRoomInList** · **메신저포매팅.ts:91 wrapSelection/:124 FORMATTING_SHORTCUTS** · **메신저검색훅.ts:11/13 export 타입 2개** · **메신저멤버관리모달.tsx:5 ForwardRoomItem** · **메신저첨부미리보기.tsx:271 onForwardAttachment** · **lib/chat-push-health.ts:22 D1MissingTableError** — 모두 외부 import 0(Grep).
- **모바일**: `채팅목록.tsx:31 'channel'` 탭(ChipBtn 없어 도달 불가) · `채널생성탭.tsx:95 desc` 무시.

## 🟢 D — 중복 (살아있는 코드)

- **[D-3] 메신저방데이터훅 메타 파싱 3중 복제** — reaction(`:543/981/1647`)·pinned(`:618/1033/1584`)·polls(`:691/1096/1695`) 빌드 블록(각 40~45줄)이 refreshXxx/syncVisibleMessageMetadata/fetchData 3곳에 verbatim. → `buildReactionMaps`/`applyPinnedMessages` 추출. (Q: syncVisibleMessageMetadata와 fetchData가 90% 동일.)
- **[D-4] 이모지피커 모바일↔PC 100% 중복** — `모바일/채팅/이모지피커.tsx:30-193`(EMOTICONS/STICKERS/CATEGORIES) ≡ `메신저액션서브/EmojiPicker.tsx:20-244`. 카테고리 데이터+검색+서브탭 동일.
- **[D-5] `getKoreanTodayString` 3중** — `메신저유틸.ts:478` ↔ `lib/date-utils.ts:33` ↔ `lib/seoul-time.ts:13`(Intl 정본). · **`buildForwardedMessageContent` 2벌**(메신저메시지서비스:28 ↔ 메시지액션워크플로훅:32) · **`isActiveChatMember`≡`isActiveNoticeMember`**(메신저유틸:186≡227 완전복제) · **userId() helper 2벌**(chat-rooms route ↔ [id] route).

## 🟢 U — 불필요 / 무동작 UI

- **[U-1] 모바일 채팅 무동작/더미 UI** — `채팅방.tsx:539` 알림토글(onChange 없음, defaultChecked) · `:610` 공유파일 갤러리 "비어있음" 3칸 하드코딩 · `:317` "실시간 연결됨" 고정(연결상태 무관) · `:635` "채팅방 나가기"가 onBack만(DB 미반영) · `채팅목록.tsx:417 OrgPlaceholder`("곧 추가").
- **[U-2] rate-limit `recordFailedAttempt`(upload:177)** — 성공 경로도 카운트(알림 메뉴와 동일, 동작은 의도된 총량제한, 네이밍 혼란).
- **[U-3] 사소** — 첨부미리보기:299 sleep(400) 매직넘버 · 첨부재시도큐:90 불필요 alias · chatQueryService:111 JSDoc 위치오류 · 메신저공통:60 동일분기 삼항 · 메신저테스트이벤트(NODE_ENV 가드, 번들 잔류).

## 🟢 Q — 유사기능

- **[Q-1] 데스크톱↔모바일 reaction toggle** — `반응.ts:toggleMobileReaction` ↔ `메신저액션훅.ts:72 toggleReaction`(동일 message_reactions select→delete/insert). → `lib/chat-reactions.ts` 통합.
- **[Q-2] 날짜 포맷 4곳 분산** — 메신저사이드바:95 / 스레드패널:113 / 드로어:98 / 타임라인(인라인). `toChatDate` 있으나 미통일. 스레드패널·드로어는 검증 없는 `new Date()`(Invalid Date 노출 위험).
- **[Q-3] `ReadStatusModal` 동명 2개** — `메신저읽음모달.tsx`(채팅) ↔ `게시판/ReadStatusModal.tsx`(게시판). props 다름, 혼동 위험. rename 권장.
- **[Q-4] 입력워크플로훅↔메시지액션워크플로훅 책임 혼재** — 둘 다 supabase 직접 쓰기 포함, 훅 이름과 책임 불일치.

---

## 핵심 결론 (채팅)

1. **정정(대규모 해소)**: 기존 "고아 훅 18개 ~3,500줄"은 현재 **삭제·리팩터 완료**. 추출 훅 전부 메신저.tsx에서 실사용(고아 없음). useChat*는 useChatTypingD1만 잔존.
2. **여전히 열림**: `needsAttention` 항상 false(死 분기) · `insertChatMessageWithFallback` 2벌 · MIME맵 3중.
3. **신규 보안**: og-preview SSRF(리다이렉트 후 재검증 없음) · 모바일 채팅 회사격리 없음(단일회사 전제 시 무해).
4. **D 핫스팟**: 메신저방데이터훅 메타파싱 3중 복제(reaction/pinned/polls) · 이모지피커 모바일↔PC 100% 중복.
5. **X**: 메신저.tsx 미사용 import 4 + 다수 미참조 export/prop/상수(타임라인·렌더·컴포저·데이터유틸·포매팅 등).
6. **S**: fetchData force 무시 · is_announcement SELECT 누락 · 오프라인 file_kind 부분 누락.
7. **U**: 모바일 채팅 무동작/더미 UI 다수(알림토글/공유갤러리/실시간표시/나가기).
8. 메신저.tsx 3,428줄 — 추가 책임 분할 여지.
