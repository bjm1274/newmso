# ③ 추가기능 메뉴 — 3차 전수조사 (현 코드 직접 정독)

> 조사일: 2026-06-01 · 기준 커밋: `922af4cf` (현 HEAD)
> 방식: Sonnet 서브에이전트 6그룹 병렬 정독 → Opus 교차검증(라우터/스키마/Hooks순서/Grep 실측).
> 대상: 허브(추가기능.tsx/추가기능공통.tsx) + 데스크톱 12기능(Gemini/근무현황/인계노트/직원평가/퇴원심사/수술상담/OP체크/입금/마감/ESL) + OP체크 13파일 + 모바일 추가기능 23파일. (조직도 OrgChart·부서별재고는 타 메뉴 소속이라 진입점만 확인)

---

## 🔴 X — 죽은 코드 / 도달 불가

### [X-1] OP체크 `dayWorkspaceOpen` 기능 전체 데드 (~150줄 분기)
- `OP체크.tsx:964-978` `handleScheduleSelection`이 `openWorkspace` 파라미터를 받지만 본문에서 `applyDateAndScheduleSelection(..., { openWorkspace: false })` **항상 false 하드코딩**. → `setDayWorkspaceOpen(true)` 경로가 코드 전체에 없음.
- 연쇄 데드: `handleWorkspaceOpen`(:1260, 미호출) · useEffect 3개(:1009 스크롤락, :1015·:1273 키보드 네비) · `workspaceSelectedIndex`/`workspaceVisibleScheduleCount`/`workspaceSelectedOrderLabel` 파생값 · `workspaceSections.notes`(:206) · `OpCheckWorkspaceNotesSection`(import만 :47, JSX 미사용) · `notesSummaryText`(:1152) · `datalist#op-check-anesthesia-options`(:2554, 연결 input 없음).
- **권장**: 워크스페이스 기능을 완성하거나 인라인 방식으로 대체 후 데드 분기 일괄 제거.

### [X-2] 모바일 허브 누락 3화면 — 도달 불가
- `모바일/추가기능/허브.tsx:46-74 GROUPS`는 12개 모듈만 노출. `AddonModuleKey` 타입(:30-32)·`index.tsx`의 `case 'mri'/'share'/'guide'`는 존재하나 **허브 그리드에 진입 버튼 없음** → `MRI일정`/`업무공유목록`/`업무가이드` 화면 + 그 data-hooks(useMriSchedules/업무공유/업무가이드)가 허브 경유로 도달 불가. (다른 진입점 없으면 숨겨진 화면.)

### [X-3] 죽은 export/심볼
- `op-check-utils.ts:40 filterWardStaffsByCompany` — `resolveWardStaffCandidates` 내부 전용인데 export(외부 0).
- `모바일/추가기능/OP체크카드.tsx:227 OPCardIcon` — 코드 소비자 0(docs만).
- `추가기능공통.tsx:216 FeatureCard.accentClass` — 파일 주석도 "레거시 미사용", FEATURE_CARDS 12항목 전부 동일값, 참조 0(dead field).
- `퇴원심사.tsx` ↔ `api/discharge-review/route.ts:253` — 서버가 `ruleAnalysis` 응답하나 클라(L534-542)는 `analysis`만 소비 → `ruleAnalysis` 미사용(서버 중복 연산).
- `인계노트.tsx:133 previousDateKey` — 파일 내 호출 0(dead function).

---

## 🟠 D — 중복

- **[D-1] `extractScheduleMetaFromContent` + `SCHEDULE_META_PREFIX/SUFFIX` 3중** — `OP체크/schedule-helpers.ts:34`·`constants.ts:3` ↔ `게시판-view-utils.ts:249`·`:4` ↔ `게시판공통.ts:265`·`:50`. 동일 PREFIX/SUFFIX·파싱. → `lib/meta-constants.ts` 집중화.
- **[D-2] `WARD_MESSAGE_META_PREFIX/SUFFIX` 2중** — `OP체크/constants.ts:5-6` ↔ `메신저유틸.ts:16-17`.
- **[D-3] `STATUS_OPTIONS` ↔ `WORKSPACE_STATUS_STEPS`** — `OP체크/constants.ts:8`(export) ↔ `op-check-components.tsx:88`(별도 선언, 동일 배열 `['준비중','준비완료','수술중','완료']`).
- **[D-4] `isMissingRelationError` 2중** — `op-check-utils.ts:218`(배열판) ↔ `lib/supabase-compat.ts:20`(단수판). 시그니처만 다름.
- **[D-5] OP체크 페이로드/템플릿 로직 중복** — `OP체크.tsx:1408 savePatientCheck` ↔ `:1598 quickStatusChange`(15+ 동일필드 upsert) · `:839 buildDefaultPatientCheck` ↔ `:1334 applyTemplatesToFormState`(템플릿 매칭 동일) · `OP체크/모바일상세시트.tsx:275 handleSave` ↔ `OP체크.tsx:1408`(동일 op_patient_checks upsert).
- **[D-6] 모바일 외부링크 헬퍼 중복** — `safeExternalUrl`/`isAdmin`가 `외부주차.tsx:20,34` ↔ `외부웹팩스.tsx:19,33` 완전 동일. (Q-3 외부런처 통합과 연결)
- **[D-7] `syncLabel` 2중** — `모바일/추가기능/OP체크보드.tsx:30` ↔ `근무현황.tsx:21`.

## 🔴 Q — 데스크톱↔모바일 병렬 재구현 (4쌍, 동일 테이블)

- **[Q-1] 인계노트** — `기능부품/인계노트.tsx`(직접 fetch + withMissingColumnsFallback) ↔ `모바일/추가기능/인계노트.tsx`(useHandoverNotes + 오프라인큐). 동일 `handover_notes`, 컬럼 방어 전략 상이(데스크톱 fallback vs 모바일 컬럼 제외).
- **[Q-2] 직원평가** — `직원평가시스템.tsx` ↔ `모바일/추가기능/직원평가.tsx`. 동일 `staff_evaluations` INSERT 컬럼셋.
- **[Q-3] 퇴원심사** — `기능부품/퇴원심사/모바일상세.tsx`(퇴원심사.tsx:554 isMobile 분기) ↔ `모바일/추가기능/퇴원심사상세.tsx`. **모바일 상세 컴포넌트 2개 병존**(동일 discharge_reviews 승인/반려).
- **[Q-4] 수술상담** — `수술상담.tsx`(데스크톱, 녹음+AI) ↔ `모바일/추가기능/수술상담.tsx`(등록만).
- **[Q-5] 외부앱 런처** — `외부주차.tsx` ≈ `외부웹팩스.tsx` 95% 동일 구조 → `ExternalAppLauncher` props 통합 후보.
- **[Q-6] 근무 밴드 계산** — `근무현황.tsx` ↔ `인사관리워크센터/AttendWorkcenter/RosterGrid.tsx` 동일 `shift_assignments`에서 D/E/N 각자 집계(공용 `shift-resolution.ts` 있으나 RosterGrid는 별도). (인사관리 메뉴 교차)

---

## 🟡 S — 문법/런타임

- **[S-1] Gemini 모델명 오류** — `api/ai/chat/route.ts:50 model: 'gemini-3.5-flash'` — 존재하지 않는 모델명(타 라우트 transcribe/퇴원심사는 `gemini-2.5-flash`/`2.5-pro`). 실 키 환경에서 404. 단 `:41 apiKey.startsWith('AIzaSyBGqA18')` mock 분기로 평소 가려짐. `Gemini어시스턴트.tsx:164` 헤더도 "3.5 Flash" 오표시.
- **[S-2] 수술상담 MediaStream 미정리** — `수술상담.tsx:82-129` `streamRef` 없어 녹음 중 언마운트 시 마이크 트랙 미해제(표시등 지속). cleanup에서 `getTracks().stop()` 필요.
- **[S-3] useCallback/useEffect deps 누락 다수** — `직원평가시스템.tsx:50 fetchEvaluations` deps `[]`인데 `staffs`/`user` 캡처(평가자 이름 stale) · `퇴원심사.tsx:168 loadData` · `근무현황.tsx:485 getExtraShiftChips`(useCallback 없이 매 렌더 재생성) · `입금실시간조회.tsx:159` 필터 변경마다 폴링 interval 재등록.
- **[S-4] 스키마 불일치 — 과다 fallback** — `인계노트.tsx`는 `handover_notes`에 없는 11개 컬럼(note_scope/handover_date/patient_name 등)을 매 insert/update마다 `withMissingColumnsFallback` 최대 8회 retry(:829-872 등) → 마이그레이션 미적용 시 성능 문제. `마감보고.tsx:38 created_by_name`도 `daily_closures` 스키마에 없음(폴백 의존).

## 🟡 K — 한글 깨짐
- 없음(6그룹 전부 미검출).

## 🟢 U — 불필요 / 무동작 UI

- **[U-1] 🔒 모바일 회사격리 미동작** — `모바일/추가기능/data-hooks.ts` `void company` 4곳: `:368 useHandoverNotes` · `:446 useStaffEvaluations` · `:911 useDeposits`(주석: 정본 company_id) · `:983 useClosingToday`. → 인계노트/직원평가/입금/마감이 **전사 데이터 노출**(RLS/서버 경유 의존, 코드가 한계 인지·주석화). 타 훅(:101,170,271,590,651,708)은 정상 `.eq('company')`.
- **[U-2] api/ai/chat mock 레거시** — `:41 apiKey.startsWith('AIzaSyBGqA18')` 하드코딩 분기 + `getMockResponseForUserMessage`(~140줄). 실키 미설정 시 오류 없이 mock 반환 → 상태 은폐.
- **[U-3] 무동작 UI 버튼 대량(미구현을 완성처럼 표시)**:
  - 모바일: `입금조회.tsx:49`/`허브.tsx:99`/`조직도.tsx:46`/`업무공유목록.tsx:71`/`업무가이드.tsx:74`/`퇴원심사목록.tsx:65` 검색·새로고침 onClick 無 · `부서재고.tsx:44,140` QR/출고/발주 · `업무공유상세.tsx:156-237` 북마크/공유/다운로드 · `외부주차.tsx:95`/`외부웹팩스.tsx:94` 알림버튼 · `마감보고.tsx:202` 초안보기.
  - `OP체크/모바일상세시트.tsx:324,471` 사진 저장 "곧 지원" 미구현 UI 노출.
- **[U-4] 하드코딩 안내/칩** — `입금조회.tsx:75` "Chart 이관 2026 Q4" · `마감보고.tsx:128` "현금 합계 일치"(검증 없이 고정) · `직원평가시스템.tsx:281` "(부서장)" 라벨(실제 admin 체크만).
- **[U-5] 모바일 제한 화면(데스크톱 안내만)** — `수술상담.tsx`(모바일 목록 조회 없음, 등록만) · `마감보고.tsx:159-197`(주/월탭 fetch 없이 "데스크톱에서") · `퇴원심사상세.tsx:260` 코멘트탭 공껍데기 · `업무가이드.tsx:138` 행 클릭 불가. (의도적 제한일 수 있으나 빈 UI 노출.)
- **[U-6] 사소** — `근무현황.tsx:208 cloneCounts`(스프레드 대체 가능)·`:437` 1분 타이머 realtime과 중복 · `ESL관리.tsx:54 user` prop 미사용·`:64 editDoctor` 영속화 없음 · `Gemini어시스턴트.tsx:75 scrollToBottom` 인라인화 가능 · `마감보고.tsx:34` 제출 후 `day.submitted` 미갱신(버튼 상태 불일치).

---

## 핵심 결론 (추가기능)

1. **정정 — 기존 최우선 버그 해소**: 모바일 인계노트/직원평가 "Rules of Hooks 위반(크래시)"은 현재 **모든 hook 호출 후 조건부 return**으로 수정됨(주석 명시). 모바일 추가기능 화면도 대부분 실 DB fetch.
2. **신규 — 모바일 회사격리 미동작(U-1, 보안)**: 인계노트/직원평가/입금/마감 4훅이 `void company` → 전사 데이터 노출. RLS/서버 격리 확인 필요.
3. **죽은 코드**: OP체크 `dayWorkspaceOpen` 기능 전체(~150줄 분기) + 허브 누락 3화면 + 다수 dead 심볼/prop.
4. **D/Q 중복 핵심**: 데스크톱↔모바일 4쌍 병렬 재구현(인계/평가/퇴원/수술상담), SCHEDULE_META/extractScheduleMeta 3중, OP체크 페이로드 빌더 미추출.
5. **S**: Gemini 모델명 오류(mock에 가려짐), 다수 useEffect deps 누락, handover_notes/daily_closures 스키마 미적용으로 fallback 과다 의존.
6. **무동작 UI 버튼이 모바일 전반에 다수** — 미구현 기능이 완성처럼 보여 사용자 혼란.
