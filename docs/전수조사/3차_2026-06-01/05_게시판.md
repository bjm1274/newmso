# ⑤ 게시판 메뉴 — 3차 전수조사 (현 코드 직접 정독)

> 조사일: 2026-06-01 · 기준 커밋: `922af4cf` (현 HEAD)
> 방식: Sonnet 서브에이전트 4그룹 병렬 정독 → Opus 교차검증(import그래프/스키마/Grep 실측).
> 대상(≈32파일): 루트 5(게시판.tsx 2854줄 등) + 게시판서브 11 + 모바일 13 + API 3.
> 참고: 최근 커밋 `b1fb9285`("게시판공통↔view-utils 동작 분기 통일")의 반영 상태를 재검증.

---

## 🔴 D — 중복 (게시판 최대 이슈)

### [D-1] `게시판공통.ts` ↔ `게시판-view-utils.ts` 거의 전체(28심볼) 중복 + 이중 import
- 두 파일에 동일 함수/타입 **약 28개**가 각각 정의(`buildSelectColumns`·`inferAttachmentType`·`extractAttachmentMetaFromContent`·`normalizeBoardPost`·`extractScheduleMetaFromContent`·`runBoardPostMutation` 등 + 타입 10여 개). 그룹 A 대조 결과 **대부분 완전 동일**.
- **이중 구조 확정**: `게시판.tsx:38 } from './게시판공통'` + `:65 } from './게시판-view-utils'` — 한 파일이 **양쪽을 동시 import**. 모바일 `data-hooks.ts`는 게시판공통 사용.
- **최근 통일 커밋 반영**: 동작 분기는 대체로 통일됐으나(상태색 등), **코드 중복 자체는 미해소**. 한쪽만 수정 시 재불일치 위험 상존.
- → 한 파일을 폐기하고 re-export로 단일화 권장.

### [D-2] `업무가이드.tsx`(2143줄)가 `guide-utils.ts`/`guide-types.ts`를 inline 재구현 (~580줄)
- `업무가이드.tsx`는 `guide-utils`/`guide-types`를 **import 0**(Grep). L35~614 구간에서 상수 6·함수 25·타입 10여 개를 **독립 재선언**(guide-utils.ts와 라인 대응 동일).
- `guide-utils.ts`(483줄)·`guide-types.ts`(127줄)는 `GuideDetailPanel.tsx`만 소비 → `업무가이드.tsx` 관점에선 사실상 분리 미완(기존 지적 **여전히 열림**, 규모 더 큼).

### [D-3] 메타/MIME/타입 N중 중복
- `MIME_BY_EXTENSION` 3중: `게시판업로드.ts:10` ↔ `api/board/upload/route.ts:20` ↔ `메신저업로드훅.ts:30`.
- `buildAttachmentMetaContent` 4중: 게시판공통:178 ↔ view-utils:163 ↔ guide-utils:120 ↔ 업무가이드.tsx:235(inline).
- `SCHEDULE_META_PREFIX/SUFFIX` 3중: 게시판공통:50 ↔ view-utils:4 ↔ OP체크/constants:3.
- `QueryResult<T>` 5중(업무가이드/게시판공통/view-utils/op-check-utils/게시판.tsx), `formatShortDate` 3벌(post-table-helpers:72 / 모바일 data-hooks:442 / DocsCertSummary:32 — 출력 포맷 상이).
- `board-poll-prize.ts:12 BoardPollPrize`/`BoardPollPrizeWinner` ≡ `메신저유틸.ts:501 PollPrize`/`PollPrizeWinner`(필드 동일).
- 좋아요 토글 PC(`게시판.tsx:728 handleLike`) ↔ 모바일(`좋아요훅.ts:61`) 동일 3단계 패턴. `ANONYMOUS_ALLOWED_CATS` 2중(글작성.tsx:38 ↔ 글작성옵션.tsx:12).

---

## 🟡 S — 문법/런타임

- **[S-1] `buildBoardMetaContent` guard 동작 차이(잔존)** — `게시판공통.ts:317`(`Object.keys(meta).length===0` 키 존재 검사) vs `게시판-view-utils.ts:303`(`!meta.scheduled_publish_at && !meta.status` 값 검사). 빈 문자열 메타 시 게시판공통 버전은 불필요한 `[[BOARD_META]]` 블록 삽입. `게시판.tsx:42`는 게시판공통 버전 import(:1328,1377 사용).
- **[S-2] 업무가이드 inline `normalizeGuideResource`(L323) 필드 strip** — `guide-types`의 넓은 타입(`mobileSummary`/`collaborativeDepartments`/`workflowDependencies` 등)을 inline 좁은 타입(L68)이 누락 → extra 필드 보존 안 됨. `GuideDetailPanel.tsx:161 selectedResource.mobileSummary` 렌더가 항상 falsy(잠재 버그).
- **[S-3] `게시판업로드.ts:1 'use client'` 오용** — 컴포넌트 아닌 `.ts` 유틸에 지시어. + `notice-broadcast/route.ts:147,170` 동일 staff 쿼리 조건부 2회.
- **[S-4] 모바일 `index.tsx:108` setLikeSet IIFE** — 함수형 업데이터 미사용(deps에 likeSet, stale 위험은 회피됨). + `board-poll-prize.ts:121 staffRows as any`(JM4).

## 🟢 X — 죽은 코드

- **[X-1] `getCommentCount` 死(여전)** — `post-table-helpers.ts:66` `comments_count`/`comment_count` 둘 다 `board_posts` D1 스키마에 **없음** → 항상 0. `PostTableView.tsx:30,171,343`이 정렬/표시에 사용. 기존 지적 잔존.
- **[X-2] `게시판메뉴.ts:19 BOARD_META_MAP`** 참조 0(Grep, docs 제외).
- **[X-3] 업무가이드.tsx** — `parseKeywords`(L318) 정의 후 호출 0 + lucide 미사용 import 5개(`Edit`/`ChevronRight`/`AlertTriangle`/`MoreHorizontal`/`FolderOpen`). `guide-types.ts:122 GuideLibraryProps` 참조 0.
- **[X-4] 모바일** — `별표훅.ts:171 useStarSet` 고아(data-hooks 재export만) · `게시판목록.tsx:34 BoardCategory.subtitle` 선언·세팅되나 JSX 미렌더.
- **[X-5] `PostMoreMenu.tsx:14 onBookmark` prop** — 호출처 전달 0(PostTableView·게시판.tsx 모두 미전달).

## 🟢 U — 불필요 / 무동작 UI

- **[U-1] 모바일 `게시판목록.tsx:321 sub="박철홍정형외과"` 하드코딩** — 전 회사 사용자에게 특정 병원명 고정 표시. `user.company`로 교체 필요.
- **[U-2] 무동작 버튼** — `게시판목록.tsx:113,325` 검색(onClick 無) · `게시판상세.tsx:271` 더보기(onClick 無) · `업무가이드.tsx:1446` "새 팀 추가"(onClick 無).
- **[U-3] `게시판업로드.ts:87` fallback 무효화** — multipart 서버 업로드 fallback이 `route.ts:237` 50MB 상한에 걸림 → 200MB 의도인 동영상 직접업로드 실패 시 서버 fallback도 413.
- **[U-4] `게시판목록.tsx:53-54` 중복 조건**(dead branch) · `업무가이드.tsx:672 toastMessage` ↔ `toast()` 이중 토스트 혼용.

## 🟡 K — 한글 깨짐
- 없음(4그룹 미검출).

## 🟢 Q — 유사기능 / 🔵 보안

- **[Q-1] PostTableView ↔ BoardMobilePostCard 상태배지** — `normalizeBoardPostStatus`+중요 배지 로직 중복, 색 토큰 상이(`var(--danger)` vs `red-500`). 예약공지 처리도 PC는 숨김(`PostTableView:137 return false`)/모바일은 "예약" 뱃지 — 분기 차이(주석 없음).
- **[Q-2] PC↔모바일 업무가이드 카테고리 분류 독립** — PC는 meta marker(`kind`/`teamName`), 모바일 `data-hooks.ts:705 detectCategory`는 제목+본문 키워드 재추론 → 분류 불일치 가능.
- **[🔵 SEC] 게시판 회사격리 없음** — `useBoardPosts`/`useBoardDetail`(모바일) + `게시판.tsx fetchPosts`(PC) 모두 `board_posts`에 `company_id` 필터 없음(board_type만). 멀티테넌트 시 타사 게시글 노출. RLS 의존. (채팅·추가기능과 동일 계열.)

---

## 핵심 결론 (게시판)

1. **최대 이슈 — `게시판공통.ts` ↔ `게시판-view-utils.ts` 28심볼 중복 + `게시판.tsx` 이중 import(L38+L65)**. 최근 커밋이 동작은 통일했으나 코드 중복·일부 guard 미세차(buildBoardMetaContent)는 잔존 → 단일화 미완.
2. **`업무가이드.tsx` inline 재구현 ~580줄**(guide-utils/types 미사용, GuideDetailPanel만 소비) — 기존 지적 여전, normalizeGuideResource 필드 strip 잠재버그.
3. **`getCommentCount` 死**(스키마에 없는 컬럼, 항상 0).
4. **회사격리 없음**(PC·모바일 공통) — 멀티테넌트면 보안 이슈, 단일회사면 무해.
5. **무동작 UI**(검색/더보기/새 팀) + 모바일 "박철홍정형외과" 하드코딩 + 동영상 서버 업로드 fallback 무효화.
6. MIME/메타/QueryResult 등 N중 중복은 채팅·OP체크와 공유되는 전역 패턴.
