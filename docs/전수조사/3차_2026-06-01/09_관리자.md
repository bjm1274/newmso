# ⑨ 관리자 메뉴 — 3차 전수조사 (현 코드 직접 정독)

> 조사일: 2026-06-01 · 기준 커밋: `922af4cf` (현 HEAD)
> 방식: Sonnet 서브에이전트 6그룹 병렬 정독 → Opus 교차검증(권한경로/스키마/Grep 실측).
> 대상(≈90파일): 관리자전용.tsx + 관리자전용서브 ~50 + 관리자워크센터 ~16 + 모바일 관리자 8 + api/admin 24 + lib(access-control/audit/admin-credentials/server-session).

---

## 🔴 보안 — 최우선 (신규 다수)

- **[SEC-1 CRITICAL] 권한·역할 변경이 클라이언트 Supabase SDK 직접 실행** — `직원권한통합.tsx:120 updateStaffRecord`가 `supabase.from('staff_members').update(updates).eq('id',staffId)`를 **서버 라우트 없이** 실행. `handleRoleChange:197`(role: 'admin' 포함)·`togglePermission`·`applyGroupPermission`·`copyPermissionsToStaff` 모두 이 경로. **서버에서 호출자가 관리자인지 재검증 안 함** → Supabase RLS가 staff_members.role/permissions 쓰기를 막지 않으면 임의 staffId로 권한 상승(escalation) 가능. (대조: `resetPassword`→`/api/admin/staff-password`, force-logout→서버 라우트로 `isAdminSession` 검증.) → role/permissions 전용 서버 API + 권한 가드 필수.
- **[SEC-2] 데이터초기화 클라 직접 delete** — `데이터초기화.tsx`의 chat/inventory/board/schedule/system_logs/expired_* 타입이 `verifyServerAuthorization()`(verify-unlock, 서버 bcrypt+rate-limit) 통과 후에도 **실제 삭제는 브라우저 Supabase SDK**(`supabase.from('messages').delete()...`). RLS 미설정 시 세션 조작 우회 가능. (staff 타입만 서버 라우트.)
- **[SEC-3] 감사 로그 클라 직접 조회 + 행위자 위조 가능** — `접근감사로그.tsx:100`이 `access_logs`를 클라 직접 select(서버 세션 검증 없음, `user` prop 미사용). `audit.ts:117 readClientAuditActor`가 감사 actor를 **localStorage에서 읽음** → 권한 변경 감사 기록의 "누가"가 조작 가능.
- **[SEC-4] admin-credentials 하드코딩 레거시 ID** — `admin-credentials.ts:3 LEGACY_ADMIN_LOGIN_IDS=['1']` 폴백 → id가 '1'인 계정이 `ADMIN_NAME` 환경변수와 무관하게 관리자로 매칭.
- **[SEC-5] 기타** — `staff-password/route.ts:53` 비밀번호 최소 길이 미검증(1자 가능) · `회사관리.tsx:188` 빈 DB 시 실법인명 seed(박철홍정형외과/서연요양원) 클라 insert · `팝업창관리자.tsx:170` 팝업 insert 클라 직접(관리자 검증 없음).

## 🔴 X / D — 워크센터↔레거시 병행 (즉시조치 #7 여전)

- **[정정/X-W] 관리자 워크센터 staffs=[]/user=null 여전히 열림** — 기존 보고서의 ExecDashboard/AuditWorkcenter/CompanyWorkcenter/FormsWorkcenter/OpsWorkcenter/RolesWorkcenter는 **`관리자워크센터/`에 실재**(모바일 관리자 8화면이 dynamic import 래핑). `ExecDashboard.tsx:289-292`가 `<IntegratedReport staffs={[]} />`·`<CompanyPnL staffs={[]} user={null} />`·`<CustomDashboard user={{}} />` → 실사용 시 인건비 0원·인원 0명 degrade. (즉시조치 #7 미해소.)
- **[D-1] `groupDuplicateEndpoints`/`pickPreferredSubscription` 중복** — `system-master/_shared.ts:317,402`(export) ↔ `push-health/route.ts:36`·`cron/push-subscription-cleanup/route.ts:56`(자체 복사본). + push-health ↔ system-master operations 푸시진단 2중 집계.
- **[D-2] `PermissionDiffPanel` 동명 2개** — `시스템마스터센터-modules/`(감사이력) ↔ `직원권한통합/`(권한 diff). 기능 다름, rename 권장.
- **[D-3] `KpiCard` 동명 5곳+** — 수술상담-components / CompanyWorkcenter / stock-workcenter-common / 결재함-cards / 입출고-form. (경영/커스텀대시보드는 SwipeableKpiCards 정상 사용.) → `app/components/KpiCard` 일원화.
- **[D-4] 인건비 집계 2소스 불일치** — `법인손익현황.tsx:79`(payroll_records 실집행) ↔ `통합보고서.tsx:97`(staff_members.base_salary) → 화면 수치 상이.
- **[D-5] 심야 판정 임계값 불일치** — `접근감사로그.tsx:27`(0~5시) ↔ `audit/_shared.ts:227`(0~4시).
- **[D-6] KPI 3중** — 경영대시보드 ↔ 커스텀대시보드(다른 fetch) ↔ 모바일 허브. 재무대시보드 budget ↔ 예산관리.

## 🟠 X — 죽은 코드

- **[X-1] 연차 manual-grant 死 로직** — `api/admin/annual-leave/manual-grant/route.ts:126 computeUsedDays`+`:217 leaveByStaff` — DB 전건 조회·분류 후 갱신 루프에서 미사용(`usedDays=update.used` 프런트값 사용). DB 조회+함수 통째 死.
- **[X-2] `전자결재양식관리.tsx:526,753 designEditorRef`** — 2개 section에 동시 바인딩 → 마지막(753)이 ref 덮어씀, 526 무동작·scrollIntoView 항상 753로.
- **[X-3] 직원권한통합/permission-review.ts** — `formatPermissionValue`·`normalizeApprovalReferenceUser`·`normalizeApprovalReferenceDefaults` 외부 import 0(전자결재 타입이 권한 디렉토리에 오배치) + `types.ts ApprovalReferenceSettingUser`.
- **[X-4] no-op/미사용** — `직원권한통합.tsx:114 fetchStaffs`(빈 본문) · `팝업창관리자.tsx:31 isAllowedFile`(export 미사용) · 시스템마스터센터 `visibleChatRooms`(:463 양 분기 동일 반환, 필터 미구현).
- **[X-5] 모바일** — `AuditWorkcenter.tsx:160 PREVIEW_LOG_ROWS` 영구 빈배열(map 死) · 결재양식/운영설정/회사관리.tsx `ErpUser` import·`user` prop 미사용(구조분해 제외).
- **[X-6] 대시보드** — `재무대시보드.tsx:14 setBudgets` 미호출(budgets 항상 []) · `커스텀대시보드` monthly_revenue 위젯 switch 누락(항상 '-').
- **[X-7] system-master 타입 미동기** — overview handler `chatRooms`/`recentMessages`(:120), operations `latestBackup`/`pushFailures.recent`(:233) 응답하나 `types.ts` 미선언 → 클라 사용 불가(사장).

## 🟢 U — 불필요 / 무동작 / 더미 (대량)

- **[U-1] 하드코딩 실명/수치(프로덕션 노출)** — `경영대시보드.tsx:72 "정서운 근무 이탈 감지"`(실명)·`:49 "이번 달 입사 2명"` · 모바일 `index.tsx:133` 매출 182,420,000원/+4.2%/달성률 38%·`:60 "요청 2"` 배지 · `ExecDashboard` KPI_ITEMS · 회사관리 seed.
- **[U-2] 더미 패널** — `재무대시보드.tsx:104 AI 분석 패널`("데이터 부족" 고정) · `알림자동화설정.tsx:57 급여알림`(localStorage만, 발송 API 없음) · `예산관리.tsx:84` 전체 localStorage(DB 미저장).
- **[U-3] 무동작 UI** — 모바일 허브 벨 버튼(onClick 없음) · `근태차감규칙설정.tsx:142` 저장버튼 type 누락+hover 무효.
- **[U-4] 팝업 DB 혼용** — `팝업창관리자` Supabase insert(is_active:true) ↔ 데이터초기화 D1 delete(is_active:0).

## 🟡 S — 문법/런타임

- **[S-1] 팀관리 `전략기획본부` 저장 오류** — `팀관리.tsx:142,210` SY INC. 매핑에서 전략기획본부→'진료부'로 저장(분기 누락), `startEdit:191` 역매핑도 불일치 → 왕복 깨짐.
- **[S-2] 시스템마스터센터 메시지 삭제만 Supabase** — `:396 supabase.from('messages').delete()`(나머지 채팅 삭제는 D1 API) → D1 환경서 무동작 가능.
- **[S-3] 재무대시보드** — `:6 period`에 dead union '초기화', `:33 period` 변경이 fetch 미반영(today만 사용).

## 🟡 K — 한글 깨짐

- **[K-1] `명세서자동추출.tsx:268 "명세서 이미 지나 PDF"`** — '이미지나'가 '이미 지나'로 잘못 분리(공백 오류). 기존 유일 K 지적 **여전히 열림**.

## 🟢 Q — 유사기능

- **[Q-1] 접근감사로그 ↔ 감사로그뷰어** — access_logs vs audit_logs, 날짜필터/페이지네이션 공통훅 후보(관리자전용.tsx·AuditWorkcenter 나란히 마운트).
- **[Q-2] 모바일 시스템마스터만 관리자워크센터 미경유** — `관리자전용서브/시스템마스터센터` 직접 import(나머지 6은 관리자워크센터/*). 일관성 결여.

---

## 핵심 결론 (관리자)

1. **🔴 보안 최우선**: 권한·역할 변경(SEC-1 CRITICAL)·데이터초기화(SEC-2)·감사로그(SEC-3)가 **클라이언트 Supabase SDK 직접 실행**(서버 재검증 없음, RLS 의존). admin '1' 레거시 폴백. → 권한성 작업은 서버 라우트 + 가드로 이전 필요.
2. **워크센터↔레거시 병행(즉시조치 #7 여전)**: 관리자워크센터 ExecDashboard 등이 `staffs=[]/user=null` degrade. 데스크톱(관리자전용서브) + 모바일(관리자워크센터 래핑) 2체계.
3. **하드코딩 더미 대량**: 실명("정서운")·매출(182M)·달성률(38%)·seed 법인명이 프로덕션 노출.
4. **X**: 연차 manual-grant 死로직 · designEditorRef 중복 바인딩 · PREVIEW_LOG_ROWS 死 · system-master 응답 타입 미동기.
5. **D**: KpiCard 5곳 · PermissionDiffPanel 동명 · groupDuplicateEndpoints · 인건비 2소스.
6. **K**: 명세서자동추출 "이미 지나" 오타 여전(전 메뉴 통틀어 유일 K).
