# ⑥ 전자결재 메뉴 — 3차 전수조사 (현 코드 직접 정독)

> 조사일: 2026-06-01 · 기준 커밋: `922af4cf` (현 HEAD)
> 방식: Sonnet 서브에이전트 7그룹 병렬 정독 → Opus 교차검증(스키마/Grep/iframe 실측).
> 대상(≈74파일): 루트 3 + 전자결재서브 37 + 모바일 결재 21 + lib 7 + API 6.

---

## ✅ 정정 — 기존 지적 해소 확인

- **[해소] `buildApprovalHistoryEntry` 3중 래퍼 통합**(커밋 e5cf57b0) — 현재 `buildApprovalHistoryEntryCore`(lib/approval-shared) → `useApprovalHistoryEntry`(훅 단일 래퍼) 구조로 정리. 클라 측 잔존 중복 없음. (서버 `server-approval-transition.ts:113` 1줄 래퍼만 미세 잔존 — U.)
- **[해소] ApprovalDetailModal iframe sandbox**(커밋 6881c3b5) — `ApprovalDetailModal.tsx:221-226` `<iframe srcDoc={detailPreviewHtml} sandbox="">` 확인. 가장 제한적 sandbox 적용으로 HTML 주입 방어됨.

---

## 🔴 S — 런타임 (D1 컬럼/제약 — 즉시조치급)

- **[S-1] 출결정정 `approval_status` 컬럼 insert (클라+서버, 여전히 열림)** — `출결정정양식.tsx:342 approval_status:'대기'` insert인데 `attendance_corrections` 스키마에 컬럼 없음(`schema.ts:152-166`). `withAttendanceCorrectionsFallback`이 legacy 에러 감지 후 컬럼 제거 재시도 → **첫 primary insert 항상 실패 → 폴백 재시도(이중 왕복)**. 서버 `server-approval-processing.ts:453`도 동일 `approval_status:'승인'`.
- **[S-2] 근태신청 `work_schedules` 테이블 조회 (신규)** — `근태신청양식.tsx:120 supabase.from('work_schedules')` — 스키마에 없는 테이블(실제: `work_shifts`/`shift_assignments`). `schedules=[]` → `calculateOT`(:133) `end_time` 항상 undefined → **연장근무 계산 전체 무동작**.
- **[S-3] `onConflictDoUpdate` unique index 부재 (신규)** — `server-approval-processing.ts:101` upsert target `[staff_id, attendance_date]`인데 스키마(`:163`)는 **비유니크 index**(주석 명시). D1 ON CONFLICT는 unique/PK 대상 필요 → SQL 오류 또는 조용한 INSERT 실패 가능. (delete+insert 패턴 권장.)
- **[S-4] `approval/recall/route.ts:119` 알림 read 처리에 `user_id` 필터 없음** — `type='approval' AND read_at IS NULL` 전체 500건 로드 후 JS 매칭. `server-approval-transition.ts:240`은 user_id 필터 있음(불일치). 성능/전체스캔.
- **[S-5] WorkflowBoard 'draft' 영구 빈 컬럼** — `WorkflowBoard.tsx:83 classifyStage`가 `'draft'`를 반환하는 경로 없음 → `buckets.draft` 항상 빈 배열, '기안 작성' 컬럼 항상 "문서 없음".
- **[S-6] 반려 fallback 히스토리 유실** — `useApprovalActions.ts:226-248` 서버 transition 실패 시 2차 update가 `nextRejectedMetaData`(히스토리 포함) 대신 원본 메타 사용 → 히스토리 엔트리 유실.

## 🟠 X — 죽은 코드

- **[X-1] `ApprovalMetaPanels.tsx` JSX 패널 5종 死 (여전히 열림)** — `SupplyRequestItemsPanel`(:251)·`LeaveRequestInfoPanel`(:334)·`ReportInfoPanel`(:365)·`ApprovalAttachmentsPanel`(:402)·`RosterRequestInfoPanel`(:553) 전부 외부 import 0(Grep). HTML 렌더 함수(`renderXxxHtml`, print-utils가 사용)만 생존. → 패널 탭 계획 없으면 삭제.
- **[X-2] `전자결재-utils.ts` 출결정정 관련 export 4개 참조 0** — `isAttendanceCorrectionApprovalSchemaError`/`withAttendanceCorrectionApprovalFallback`/`isAttendanceCorrectionApprovalItem`(:270~) + `resolveAttendanceCorrectionStatusPair`(:304, 서버는 독자 정의).
- **[X-3] `전자결재.tsx`** — `savedApproverLine`(:123, 읽기 참조 0) · `visibleApprovals`(:857 `useMemo(()=>approvals,[approvals])` 무동작 래핑).
- **[X-4] 훅 미사용 export** — `useApprovalRouting syncApprovalDelayNotifications`(전자결재.tsx 미사용) · `useApprovalBulkActions processFinalApprovalOnServer`(미사용) · `useApprovalActions`가 받는 `resolveApprovalLineIds`/`normalizeApprovalLineIds` 파라미터 본문 미사용.
- **[X-5] lib/API 미사용** — `approval-query-columns.ts` SELECT 상수 5개 import 0 · `approval-workflow.ts:344 formatApprovalHistoryActionLabel` 미사용 · `roster/approval-request/route.ts:9 rosterApprovalRequestsTable` import 미사용(전부 approvalsTable로 insert).
- **[X-6] 모바일** — `결재함.tsx:37 rows` prop 미사용(index가 전달) · `작성하기.tsx:56 void BUILTIN_FORM_TYPE_DEFINITIONS` 잔재.

## 🟠 D — 중복

- **[D-1] myTurn/approvedThisMonth 2중 순회** — `ApprovalWorkflowKpi.tsx:83` ↔ `ApprovalInboxView.tsx:144`(주석도 "KPI와 동일"). 같은 배열 2회.
- **[D-2] `ApprovalMetaPanels.tsx` 내 `resolveBand`/`getStyleForBand` 2중**(:482/491 HTML용 ↔ :631/640 JSX용).
- **[D-3] 지연알림 동기화 복제** — `useApprovalRouting.ts:139 syncApprovalDelayNotifications` ↔ `useApprovalDelegation.ts:74 syncDelegatedApprovalDelayNotifications`(차이는 approver 해석 1줄).
- **[D-4] `resolveApprovalLineIds` 서버 로컬 재구현** — `server-approval-transition.ts:61` ↔ `approval-shared.ts:47`(export).
- **[D-5] 모바일 유틸 재선언** — `formatTs`(기안함:47·참조함:20 ↔ 결재함-cards:26 export)·`readAmount`(기안함:54 ↔ cards:58)·`pickAvatarTone`(결재상세:51 ↔ cards:19).
- **[D-6] 모바일 결재선 로직 인라인** — `일반기안폼.tsx:62`·`연차신청폼.tsx:72`가 `useApproverLine` 훅 미사용·인라인 재구현(연차계획폼/연장근무폼은 훅 사용).

## 🟢 U — 불필요 / 무동작 UI

- **[U-1] `navigator.webdriver` 게이트 프로덕션 상존(3곳)** — `ApprovalInboxView.tsx:482`(WorkflowBoard↔테이블 분기)·`ApprovalInboxColumns.tsx:79`(승인/반려 버튼 중복)·`전자결재.tsx:939`(날짜필터 강제 off). E2E 전용 분기가 프로덕션에.
- **[U-2] 양식 하드코딩/무동작 input** — `양식신청.tsx:73 approver` 죽은 변수·`:107 cc_departments:['행정팀']` 하드코딩 · `관리행정양식.tsx:21 void hospitalGuides`·`:173` 업무보고 input onChange 없음·`:187` 기안성격 select onChange 없음.
- **[U-3] 모바일 무동작 UI** — `결재함.tsx:252 className="on"` 하드코딩(seg 무관 항상 활성)·`결재상세.tsx:317` 더보기·`작성하기.tsx:72` 검색·`참조함.tsx:107` 필터 onClick 없음 · `연차신청폼.tsx:207 cc_departments:['행정팀']` 하드코딩.
- **[U-4] 타입 약화** — `수리요청서양식.tsx:6 Record<string,unknown>` props 캐스팅 + 다수 `any`. `ApprovalMetaPanels` `a:any`·`approval-print-utils.tsx:18 Record<string,any>`.
- **[U-5] `approval-print-utils.tsx:438` 모바일 자동인쇄 누락** — `buildHtml(item)`(autoPrint 옵션 없이) 재호출 → `window.print()` 미삽입, 모바일 자동인쇄 무반응(L403의 html 재사용해야).

## 🟡 K — 한글 깨짐
- **[K-1] `roster/approval-request/route.ts:16` 유니코드 이스케이프** — `['간호과장',...]`(간호과장/간호부장/실장). 런타임 정상, 가독성 손실(타 라우트와 불일치). UTF-8 복원 권장.

## 🟢 Q — 유사기능 / 🔵 보안

- **[Q-1] `approval/upload`(presigned, 오프라인큐 전용) ↔ `approvals/upload`(multipart, 5개 컴포넌트 사용)** — 기능은 다르나 경로 거의 동명(혼동 위험).
- **[Q-2] `supplies-helpers StockTone` ↔ `재고관리워크센터 stock-types Tone`** 2체계 병존(모바일은 3단 변환).
- **[Q-3] 모바일 `문서조회.tsx:33 periodCutoff` ↔ `필터시트.tsx:71 resolvePeriodCutoff`**(타입명만 달라 미공유).
- **[🔵 SEC] 결재 회사격리 쿼리 필터 없음** — `useApprovalList`(모바일)·`전자결재.tsx`(PC) 모두 approvals에 `company_id` 필터 없이 RLS 의존. 방어 쿼리 권장. (✅ iframe sandbox는 해소됨.)

---

## 핵심 결론 (전자결재)

1. **정정 2건 해소**: buildApprovalHistoryEntry 3중 래퍼 통합 + ApprovalDetailModal iframe `sandbox=""` 추가(보안).
2. **런타임 핫스팟(즉시조치)**: 출결정정 `approval_status`(클라+서버, 첫 insert 항상 실패) · 근태신청 `work_schedules` 없는 테이블(연장근무 무동작) · `onConflictDoUpdate` unique index 부재(DB 실패 가능).
3. **여전히 열림**: ApprovalMetaPanels JSX 패널 5종 死 · navigator.webdriver 게이트 프로덕션 3곳 · 양식 하드코딩/무동작 input.
4. **D**: KPI myTurn 2중순회 · 지연알림 동기화 복제 · 모바일 formatTs/readAmount/pickAvatarTone 재선언 · 결재선 로직 인라인.
5. **U**: 모바일 무동작 UI 다수(칩/더보기/검색/필터) · cc_departments 하드코딩.
6. WorkflowBoard 'draft' 컬럼 영구 빈 상태 · 모바일 자동인쇄 누락.
