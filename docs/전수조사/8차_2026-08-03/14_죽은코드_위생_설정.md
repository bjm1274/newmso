# 8차 전수조사 — 죽은코드·위생·설정

> 감사일: 2026-08-03 · 대상 리비전: 7a7b7c21 · 판정: R1 감사 → R2 적대적 검증 완료

## 요약

이 도메인의 결함은 런타임을 거의 건드리지 않는다 — 20건 중 19건이 P2 다. 그러나 두 가지가 예외적으로 무겁다. 첫째, **직원 실명 인사 데이터 8개 파일이 git 에 커밋돼 있다**(D13-007, P1). `.gitignore:107` 주석이 스스로 "스크립트 산출물은 직원 데이터 포함 — 커밋 금지"를 선언해 놓고 루트 산출물은 그 규칙의 사거리 밖에 있다(`query_result*` 패턴 부재). 둘째, **품질 게이트가 사실상 존재하지 않는다** — `.github/workflows` 3종(deploy/e2e/hygiene) 어디에도 lint·tsc 스텝이 없고 hygiene 은 jscpd 만 `continue-on-error` 로 돌린다. 로컬 `npm run lint` 는 잡음 디렉터리가 문제의 97.8% 를 만들어 상시 exit 1 이고, 보안 회귀 스펙 2개는 Playwright 프로젝트에 매칭되지 않아 영구 미실행이다.

세 번째 축은 **Supabase 컷오버 잔재**다. `lib/db/dual-write.ts`(158줄) 전체가 죽은 코드고, `DataBackend` 유니온의 `'supabase'|'dual-write'` 분기가 전부 도달 불가이며, 특히 `lib/db/types.ts:28` 주석이 안내하는 **"장애 시 `DATA_BACKEND` 를 dual-write/supabase 로 설정해 롤백"이라는 절차는 값을 읽는 코드가 하나도 없어 아무 일도 일어나지 않는 거짓 안내**다. `next.config.ts`·`e2e.yml`·`package.json`·`.gitignore`·`tsconfig.json` 에도 같은 계열 사문 설정이 흩어져 있다.

R2 검증은 원 감사가 P1 로 본 3건(e2e.yml Supabase secrets, lint 게이트, 미실행 보안 스펙)을 전부 P2 로 내렸다. 이유는 공통적이다 — **런타임·데이터 무결성 영향이 0이거나, 주장한 공백을 다른 경로가 이미 메우고 있었다.** 다만 세 건 모두 수정 비용이 매우 낮아 우선 처리 권장은 유지된다.

| 구분 | CONFIRMED | PLAUSIBLE | REFUTED |
|---|---|---|---|
| P0 | 0 | 0 | 0 |
| P1 | 1 | 0 | 0 |
| P2 | 19 | 0 | 0 |

(총 20건. `merged_into` 가 채워진 D13-006→D11-015, D13-008→D11-012, D13-009→D11-011 포함.)

---

## 지적사항

### D13-007 · P1 · 직원 실명 개인정보 산출물 8개가 git 에 커밋되어 있다

- **근본원인 축**: S12 운영 스크립트·저장소 위생 — 구 DB 조준·실행 게이트 누락·자격증명/실명 PII 커밋
- **위치**: `annual_leave_status_2026.json:1-14022`, `query_result_leave_accruals.txt:1-2`, `patch_remote.sql:1-8` (+ `annual_leave_status_2026.csv`, `연차오류_및_정정내역.xlsx`, `연차자동부여_소급내역.xlsx`, `연차정정_및_소급대상자_명단.xlsx`)
- **결함**: `git ls-files` 로 8개 파일 전량이 tracked 임을 확인했다. 내용은 실무 인사 데이터다.
  - `annual_leave_status_2026.json` — 48행 배열, `TEST_` 접두 2건을 뺀 **실명 46건**. 필드는 `name, department, company, status, hire_date, year, total_days, used_days, expired_days, compensated_days, remaining_days` 로 **소속·입사일·연차잔여가 결합**돼 있다.
  - `query_result_leave_accruals.txt:2` — 실명 + `joined_at`/`hire_date` + 발생일이 평문.
  - 연차 xlsx 3종 — 파일명부터 정정 대상자 명단.

  자기모순도 명시적이다 — `.gitignore:107` 은 "스크립트 산출물은 직원 데이터 포함 — 커밋 금지"라고 선언하지만 루트 산출물을 거르는 `query_result*`·`annual_leave_status*` 패턴이 **없다**(D13-014 가 직접 원인).
- **도달 경로**: 저장소 clone / fork / 백업 경로 전부. **git 이력에도 남아 작업트리 삭제만으로는 제거되지 않는다.**
- **영향**: 저장소 접근 권한이 곧 직원 연차·인사 데이터 접근 권한이 된다.
- **검증**: falsifier("전원 가상/테스트 인물") 반증 — `node` 집계로 rows=48 / `TEST_`=2 / real=46 확인(감사자 수치 일치). **부분 반박**: `patch_remote.sql` 은 헤더가 "대상 직원 수: 0명" 이고 `staff_id` UUID 벌크 INSERT 291건이라 **실명을 포함하지 않는다** — 이 파일을 '실명 산출물'로 묶은 것은 부정확하다(가명화 식별자). 심각도 분기점은 저장소 가시성이다 — 주민번호·계좌 같은 최고민감 항목은 없으므로(스키마 확인) **비공개 리포면 P1, 공개 리포면 P0**. `git remote` 는 `https://github.com/bjm1274/newmso.git` 이나 공개 여부를 정적으로 확정할 수 없어 P1 로 유지했다.
- **수정 방향**: 8개 파일 `git rm` 후 `.gitignore` 에 `/query_result*`, `/annual_leave_status*`, `/*.xlsx` 루트 앵커 패턴 추가. 이력 제거가 필요하면 `git filter-repo`/BFG 로 재작성(원격 강제푸시 정책 확인 필요). **선행 조건: 소유자에게 저장소 가시성 확인.** (난이도 S · 회귀위험 낮음 · 묶음 FB11)

---

### P2 항목 요약

| ID | 제목 | 위치 | 수정 방향 |
|---|---|---|---|
| D13-001 | `lib/db/dual-write.ts` 전체가 죽은 코드 — `dualInsert`/`dualWrite` 호출부 0곳 | `lib/db/dual-write.ts:1-158`, `lib/db/index.ts:20-22` | `lib/db/index.ts` 재수출 2줄 제거 후 파일 삭제. `DualWriteContext`/`DualWriteResult` 타입 소비자도 0이므로 함께 제거 |
| D13-002 | `lib/system-master-staff-query.ts` 는 프로덕션 참조 0 — 테스트만 소비하는 PostgREST 잔재 | `lib/system-master-staff-query.ts:1-120`, `tests/e2e/system-master-route-compat.desktop.spec.ts:2` | 모듈과 소비 테스트를 함께 삭제하거나, 테스트가 실제 라우트 핸들러를 직접 검증하도록 재작성 |
| D13-003 | `DataBackend` `'supabase'`/`'dual-write'` 분기 전체 사문화 — 롤백 안내 주석이 거짓 | `lib/db/types.ts:15-32`, `lib/db/get-binding.ts:164-166`, `lib/db/mirror.ts:62-63` | `DataBackend` 타입을 `'d1'` 단일화(또는 제거), `mirror.ts` 의 supabase 분기·오도성 롤백 주석 삭제, `mirrorRowsToD1` 을 'D1 upsert 헬퍼'로 개명 문서화 |
| D13-004 | `next.config.ts` `images.remotePatterns` 에 `supabase.co` 스토리지 호스트 잔재 | `next.config.ts:52-59` | `remotePatterns` 항목 삭제 (아래 '삭제 안전성' 절 참조) |
| D13-005 | `e2e.yml` 에 `SUPABASE_SERVICE_ROLE_KEY` 등 Supabase secrets 주입 잔존 | `.github/workflows/e2e.yml:33-42`, `:79-82` | 워크플로에서 Supabase env 3종 제거 + GitHub 저장소 secrets 에서 키 삭제(가능하면 Supabase 쪽 키 rotate/프로젝트 폐기 확인) |
| D13-006 | 루트 `backfill-sync.mjs`·`check-sync.mjs` — 커밋된 Supabase 조작 스크립트 (→ D11-015 병합) | `backfill-sync.mjs:1-17`, `check-sync.mjs:1-17` | `git rm` (보존 가치 있으면 `scripts/legacy-supabase/` 로 이동) |
| D13-008 | `scripts/deploy-worker.mjs` 가 FIREBASE_SERVICE_ACCOUNT 를 `cmd.exe` echo 파이프로 업로드 (→ D11-012 병합) | `scripts/deploy-worker.mjs:30-42` | echo 파이프 제거, `upload-secrets.mjs` 방식(stdin `input`)으로 통일하거나 폐기 |
| D13-009 | `upload-secrets.mjs` — 죽은 Supabase 시크릿 목록 + 실패 시 평문 tmp 잔존 + 멀티라인 절단 (→ D11-011 병합) | `upload-secrets.mjs:8-45` | `SUPABASE_*` 2종 제거, tmp 대신 `input` 직접 전달 + `finally` 에서 `unlink`, 값 파싱을 공용 파서로 교체 |
| D13-010 | `.env.local` 파서가 스크립트마다 4계열(실제 5계열)로 갈라짐 — 따옴표/`$` 처리 불일치 | `scripts/export-dns.mjs:5-7`, `scripts/deploy.mjs:75-82`, `scripts/run-backup-now.mjs:19-25`, `upload-secrets.mjs:18-28` | `scripts/lib/parse-env-local.mjs` 하나로 통일(양끝 따옴표 제거 + `$` 비확장 + 멀티라인 미지원 명시)하고 `.env.local` 규약(값에 `$` 포함 시 작은따옴표)을 README 에 문서화 |
| D13-011 | 머신 고정 경로 `npm.cmd` 래퍼와 0바이트 `npx` 파일이 git 에 커밋됨 | `npm.cmd:1`, `npx:1`, `auto-deploy.mjs:18-21` | `git rm npm.cmd npx` + `auto-deploy.mjs` 를 `'npm run …'`(셸 PATH 해석)으로 수정하거나 `scripts/deploy.mjs` 로 일원화 후 삭제 |
| D13-012 | tsconfig `exclude` 죽은 항목 4건 + `scratch/` 미제외 구멍 | `tsconfig.json:39-57` | `exclude` 에서 죽은 4항목(`supabase/functions`·`_*.ts`·`_*.tsx`·`board`) 제거, `scratch`·`scratch_zip`·`handoff`·`electron-app`·`outputs` 추가(후자 4개는 **예방 목적** — 현재 tsc 에 포함돼 있지 않음) |
| D13-013 | `npm run lint` 게이트 사망 — `globalIgnores` 누락 6개 디렉터리가 문제의 97.8% 생성 | `eslint.config.mjs:66-86`, `:29-51` | `globalIgnores` 에 6개 디렉터리 추가(또는 lint 스크립트를 `eslint app lib scripts tests` 로 한정), 죽은 항목 3건 제거, `no-unused-vars` 를 `warn`+옵션으로 교정, supabase 룰 블록 삭제 |
| D13-014 | `.gitignore` 결함 묶음 — `query_result*` 패턴 부재, ignore-after-commit 2건, 죽은 패턴 2건, 비앵커 위험 패턴 | `.gitignore:27-28`, `:107-114`, `:144-148` | `query_result*`·`annual_leave_status*` 루트 앵커 패턴 추가, tracked 잔존물 `git rm --cached`, 죽은 패턴 2건 삭제, `cd`/`git`/`tmp*/` 앵커링 |
| D13-015 | 빈 디렉터리 3개 잔존 | `app/main/기능부품/근태기록`, `app/main/기능부품/알림센터`, `app/main/기능부품/마이페이지/역할별대시보드` | `rmdir` 3건 |
| D13-016 | `lib/db/local.sqlite` 0바이트 고아 파일 + 존재하지 않는 모듈을 가리키는 주석 잔존 | `lib/db/local.sqlite:0`, `lib/db/index.ts:89-91`, `lib/db/client-d1.ts:10` | `local.sqlite` 삭제, 두 주석을 `get-binding` 로컬 폴백 설명으로 교체, `.gitignore:147` 주석 갱신 |
| D13-017 | 루트 임시파일 전수 분류 — tracked / untracked / ignored | (아래 3분류 표) | tracked `git rm`(개인정보 포함분은 D13-007 절차), `scratch_query_img.mjs` 는 `/scratch_query*` 패턴 추가 후 삭제, ignored 잔존은 로컬 일괄 삭제 목록으로 제안 |
| D13-018 | `package.json` 깨진 스크립트 2건 — legacy-supabase 이동 미반영 경로 | `package.json:16-17` | 두 scripts 항목 삭제(Supabase 영구 배제 방침상 경로 수정이 아니라 삭제가 정합) |
| D13-019 | 보안 회귀 스펙 2개가 어떤 Playwright 프로젝트에도 매칭되지 않아 영구 미실행 (내용 중복 이중 작성) | `tests/security.spec.ts:1`, `tests/security/d1-policies.spec.ts:1`, `playwright.config.ts:4-74` | 두 스위트를 병합해 `tests/e2e/security.desktop.spec.ts` 로 이동(또는 security 전용 프로젝트 추가), 중복본 삭제 |
| D13-100 | [Pass B] 7차 P0 42건 중 D13 배정 행 0건 — 판정 대상 없음 | (장부 기록) | 해당 없음 |

**P2 중 특히 주목할 4건**

- **D13-003(롤백 절차 무동작)** — 실사용 버그는 없지만 성격이 다르다. `lib/db/types.ts:28` 은 "롤백 시 `DATA_BACKEND` 를 `'dual-write'`/`'supabase'` 로 명시 설정한다"고 안내하는데, `getDataBackend(envOverride?)` 는 인자를 무시하고 `return 'd1'` 이고(`:30-32`) `resolveDataBackend()` 도 마찬가지다(`get-binding.ts:164-166`). `wrangler.toml:25` 에 `DATA_BACKEND = "d1"` 이 선언돼 있으나 **이 값을 읽는 코드가 0건**이다. 즉 장애 시 문서대로 롤백을 시도하면 아무 일도 일어나지 않고 대응 시간만 잃는다. 부수적으로 `mirror.ts:62-63` 의 supabase 분기와 `:68-73` 의 `logD1BindingMissing` 분기도 영구 도달 불가다.
- **D13-013(lint 게이트)** — 전체 3,254 problems(에러 2,034) 중 잡음 6개 디렉터리(`handoff/`·`scratch/`·`scratch_zip/`·`tmp/`·`tmp_orphan/`·`backups/`)가 3,181건(97.8%)을 만든다. app+lib 기여는 에러 0·경고 44 뿐이다. ESLint 는 `.gitignore` 를 읽지 않는데 `globalIgnores` 에 이 6개가 없다. 반면 등재된 `main/**`·`tmp_apk_extract/**`·`.codex-temp/**` 는 실물 부재고, `no-unused-vars` 는 `["off", {argsIgnorePattern…}]` 라는 무의미 조합이며, supabase `.from()` 제한 룰은 코드베이스에 `supabase` 식별자가 0건이라 완전 사문이다.
- **D13-010(.env 파서 5계열)** — 실해가 이미 1회 발생한 이력이 있다(따옴표 없는 값의 `$` 확장으로 bcrypt 해시가 잘려 마스터 비밀번호가 어떤 값으로도 통과 못 한 사고). 소비자 코드가 값의 형태를 규정한다 — `lib/fcm-http.ts:15` 가 `JSON.parse` 를 하므로 `FIREBASE_SERVICE_ACCOUNT` 는 따옴표·중괄호·공백을 반드시 포함한다. "모든 값이 따옴표 없고 `$` 가 없다"는 전제는 이미 깨져 있다. 심지어 `upload-secrets.mjs` 는 **한 파일 안에서도** 파싱 규칙이 갈린다(`:20` 전역 replace / `:28` 양끝 앵커).
- **D13-011(npm.cmd 셰도잉)** — "PowerShell/Git Bash 만 쓰면 회피 가능"이 성립하지 않는다. `auto-deploy.mjs:18,21` 은 Node `execSync` 를 쓰고 **Windows 의 `execSync` 는 항상 `cmd.exe /d /s /c` 로 실행**되므로 사용자가 어떤 셸을 쓰든 내부에서는 cmd 해석이 강제된다. 게다가 명령어가 `npm.cmd`(확장자 명시)라 PATH 이전에 cwd 의 래퍼가 확정적으로 선택된다.

---

## 삭제 후보 — 안전성 근거의 구분

"삭제하면 된다"는 처방이 실제로 안전한지를 항목별로 구분했다. **A군은 근거가 검증돼 즉시 실행 가능하고, B군은 선행 조건이나 동반 수정이 있어 단독 삭제가 위험하다.**

### A. 삭제해도 안전한 근거가 검증된 항목

| ID | 대상 | 검증된 안전 근거 |
|---|---|---|
| D13-015 | 빈 디렉터리 3개 | `find app -type d -empty` 재실행으로 정확히 3개 재현. **파일 0개, git 은 빈 디렉터리를 미추적하므로 git 영향 0.** 최저 위험 항목 |
| D13-016 | `lib/db/local.sqlite` (0바이트) | `grep 'local\.sqlite'` app/lib/scripts/tests/docs → **0건**. `git check-ignore` → IGNORED(untracked). 로컬 D1 폴백은 `get-binding.ts` 의 `.wrangler/state` 경로(`getLocalD1Mock`)를 쓰므로 이 파일을 여는 코드가 없다 |
| D13-001 | `lib/db/dual-write.ts`(158줄) + 배럴 재수출 2줄 | 전 저장소 정규식 `dualInsert\|dualWrite\b` 전량 스캔(head_limit 0) 매치 **정확히 4건** — 정의 2건(`:48,:120`), 배럴 재수출 1건(`index.ts:21`), 문서 언급 1건. `import('@/lib/db/dual-write')` 형태의 동적 import·require·`next/dynamic` 매치 **0**. 타입(`DualWriteContext`/`DualWriteResult`) 소비자도 0. **4차 전수조사에서 이미 동일 판정(lib-14)됐으나 미조치된 재발 항목** |
| D13-018 | `package.json:16-17` 스크립트 2건 | `ls` 실측으로 `scripts/cleanup-supabase-chat-storage.mjs`·`scripts/migrate-chat-storage-to-r2.mjs` 부재 확인, 실파일은 `scripts/legacy-supabase/` 하위 존재. **실행하면 즉시 MODULE_NOT_FOUND 이고 자동 호출 경로 0** 이므로 삭제로 잃을 동작이 없다 |
| D13-012 | tsconfig `exclude` 죽은 4항목 | `ls` 실측: `supabase/`·`board/` 디렉터리 부재, 루트 `_*.ts(x)` 부재. 현재 tsc 에러 0(b1)이라 실해 없음 |
| D13-004 | `next.config.ts` `remotePatterns` 의 supabase.co 항목 | **감사자의 falsifier 는 실제로 트리거된다** — 로컬 D1 163테이블 텍스트컬럼 `LIKE '%supabase.co%'` 스캔 결과 **373행 히트**(audit_logs 369, board_posts 1, document_repository 1, messages 1, notifications 1). 그러나 더 강한 근거가 이를 무력화했다: `remotePatterns` 는 `next/image` 최적화 경로에만 적용되는데 app+lib 전체에서 `next/image` 사용처는 `BoardBodyPickerModal.tsx:61-62` **단 1곳**이고 그 src 는 로컬 정적 자산 `/human-body-mri.png` 다. **원격 호스트를 `next/image` 로 렌더하는 코드가 0이므로 항목 제거가 어떤 이미지도 깨뜨릴 수 없다.** (V12 는 "실 데이터 확인 후 결정"으로 유보했으나 V10 근거를 채택) |
| D13-003 | `DataBackend` supabase/dual-write 분기 | 전 저장소 `DATA_BACKEND` 스캔 결과 값을 읽는 코드 **0건** — `get-binding.ts:16` 은 타입 선언뿐이고 나머지는 전부 주석·문서·에러 메시지 문자열. 분기가 도달 불가임이 확정 (난이도만 M) |

### B. 단독 삭제가 위험하거나 선행 조건이 있는 항목

| ID | 대상 | 왜 단독 삭제가 안 되는가 |
|---|---|---|
| D13-007 | 실명 PII 8개 파일 | **작업트리 삭제만으로는 제거되지 않는다** — git 이력에 남는다. `git filter-repo`/BFG 이력 재작성과 원격 강제푸시 정책 확인이 선행돼야 하고, 그 전에 저장소 가시성(공개/비공개) 확인이 심각도 분기점이다 |
| D13-002 | `lib/system-master-staff-query.ts` | **테스트가 실제로 소비 중**이다 — `tests/e2e/system-master-route-compat.desktop.spec.ts:2,30,72,99` 가 유일한 소비자이며 이 스펙은 Playwright desktop 프로젝트가 실제로 실행한다(`.desktop.spec.ts` 매치). 모듈만 지우면 테스트가 깨진다. 죽은 모듈을 검증하는 테스트가 통과해 "라우트 호환성 보증" 착시를 만드는 구조 자체를 함께 정리해야 한다. ⚠ **원장 불일치**: 같은 모듈을 다룬 D08-020 은 "테스트가 참조하므로 오탐"이라는 이유로 **REFUTED** 인 반면 D13-002 는 "프로덕션 참조 0"으로 **CONFIRMED** 다. V12 가 병합을 권고했으나 병합되지 않았다 |
| D13-011 | `npm.cmd`, `npx` | `auto-deploy.mjs:18,21` 이 `npm.cmd run …` 을 **명시 호출**해 이 래퍼에 의존한다. 삭제 시 두 줄을 `'npm run …'` 으로 동시 수정해야 안전하다 |
| D13-006 | 루트 `backfill-sync.mjs`·`check-sync.mjs` | tracked 라 커밋이 필요하고, `package-lock.json:33` 의 루트 devDependencies 에 `@supabase/supabase-js ^2.108.1` 이 남아 있어 **node_modules 에 실제로 설치돼 있다** — 실행하면 크래시하지 않고 `.env.local` 의 자격증명으로 폐기 프로젝트에 접속을 시도한다(방어막 없음). ★ 부수 발견: `package.json`(dbd68141 에서 제거)과 `package-lock.json`(31f3a3a4 이후 미갱신)이 **비동기화 상태** — CI 3종의 `npm ci` 가 `EUSAGE` 로 실패할 수 있다(D13 배정 외, 별도 조사 필요) |
| D13-019 | `tests/security.spec.ts`, `tests/security/d1-policies.spec.ts` | **삭제가 아니라 병합이 정답**이다. 두 스펙 내용의 2/3(4건)는 실행 중인 `tests/e2e/security-authorization.desktop.spec.ts:4-40` 이 이미 커버하지만, **고유 커버 2건은 단순 삭제 시 손실**된다 — 비인증 `/api/d1/query` `payroll_records` select, 비인증 `/api/board/notice-broadcast`. 이 2건을 `security-authorization.desktop.spec.ts` 에 추가하는 것이 정확한 처방 |
| D13-005 | `e2e.yml` Supabase env 3종 | 워크플로 수정만으로는 절반이다 — **GitHub 저장소 secrets 에서 키를 삭제하고 가능하면 Supabase 쪽 키를 rotate/프로젝트 폐기**해야 실효가 있다. 이는 저장소 밖 작업이므로 코드 수정과 분리해 추적해야 한다 |
| D13-014 | `.gitignore` ignore-after-commit 2건 | 패턴 추가만으로는 tracked 잔존물이 사라지지 않는다 — `.jscpd-report/jscpd-report.json` 과 `scripts/legacy-supabase/backfill-d1/.backfill-progress.json` 은 `git rm --cached` 가 별도로 필요하다 |
| D13-017 | 루트 tracked 산출물 | **개인정보 포함분(`annual_leave_status_*`, `query_result_leave_accruals.txt`, xlsx 3)은 D13-007 절차(이력 재작성 검토)를 따라야 하므로 단순 `git rm` 처방과 분리해야 한다** |

---

## 루트 임시파일 3분류 (D13-017)

`git ls-files` 로 루트 tracked 파일 40개를 확인했고, `git check-ignore` 로 전수 판정했다.

### ① tracked — git 에 커밋된 산출물 (삭제/이동 후보)

| 파일 | 성격 | 처방 |
|---|---|---|
| `query_result.json`, `query_result2.json`, `query_result3.json`, `query_result4.json`, `query_result.txt`, `query_result_rates.json` | 쿼리 산출물 | `git rm` + `/query_result*` 패턴 추가 |
| `query_result_leave_accruals.txt` | **실명+입사일+발생내역 평문** | **D13-007 절차** (이력 재작성 검토) |
| `annual_leave_status_2026.csv`, `annual_leave_status_2026.json` | **실명 46건 인사 데이터** | **D13-007 절차** |
| `연차오류_및_정정내역.xlsx`, `연차자동부여_소급내역.xlsx`, `연차정정_및_소급대상자_명단.xlsx` | **정정 대상자 명단** | **D13-007 절차** |
| `patch_remote.sql` | 연차 소급 INSERT 291건 (헤더 "대상 직원 수: 0명", staff_id UUID — **실명 없음**) | `git rm` |
| `scratch_query.js`, `scratch_query2.js` | 일회성 조회 | `git rm` |
| `claude-usage-log_2026-06-16_to_2026-06-23.md` | 기록물 | `git rm` 또는 `docs/` 이하 이동 |
| `npm.cmd`(38B, 머신 고정 절대경로), `npx`(0바이트) | 래퍼/빈 파일 | `git rm` + `auto-deploy.mjs:18,21` 동시 수정 (D13-011) |
| `backfill-sync.mjs`, `check-sync.mjs` | Supabase 레거시 | `git rm` 또는 `scripts/legacy-supabase/` 이동 (D13-006 / D11-015) |
| `run-wrangler.js` | 구 `pchos-d1` 생일봇 메시지 1회성 조회(`:2-3` 하드코딩) | `git rm` (D11-015) |

> ⚠ **원장 불일치**: 이 목록은 원장에 "tracked 삭제후보 **20개**"로 기재돼 있으나 실제 열거 항목은 **21개**다(query_result* 7 + patch_remote 1 + scratch_query 2 + annual_leave csv/json 2 + xlsx 3 + claude-usage-log 1 + npm.cmd/npx 2 + backfill/check-sync 2 + run-wrangler 1). 원 감사 claim 은 여기에 `auto-deploy.mjs`·`upload-secrets.mjs` 를 "운영 스크립트라면 `scripts/` 이동" 항목으로 추가 언급하므로 총계 해석이 세 갈래로 갈린다. **9차에서는 목록을 기계 생성해 수치와 항목을 일치시킬 것.**

| 별도 분류 — 이동 검토 | 성격 |
|---|---|
| `auto-deploy.mjs` | 운영 배포 래퍼 — 삭제가 아니라 `scripts/` 이동 또는 `deploy.mjs` 로 일원화 (D11-021) |
| `upload-secrets.mjs` | 운영 시크릿 업로드 — `scripts/` 이동 (D11-011) |

### ② untracked·미차단 — `git status` 상시 오염 (1건)

| 파일 | 판정 | 처방 |
|---|---|---|
| `scratch_query_img.mjs` | `git check-ignore` 전수 판정에서 **유일하게 NOT-IGNORED** | `/scratch_query*` 패턴 추가 후 삭제 |

### ③ ignored 채 로컬 잔존 — 커밋 위험은 없으나 디스크·탐색 노이즈

| 파일 | 비고 |
|---|---|
| `__skel.txt` | V10 표본 검증에서 EXISTS+IGNORED 확인 |
| `_tmp_*.txt` ×5 | |
| `test_output.txt` | 표본 확인 |
| `submissions_output*` ×4 | |
| `single_test_output*` ×2 | |
| `walkthrough_output.txt` | 표본 확인 |
| `test-insert-out.txt`, `test-insert.mjs` | 표본 확인 |
| `test_db.js`, `test_emoticon.js` | 표본 확인 |
| `query_poll.py` | 표본 확인 |
| `.codex-chat-ttl.patch` | |
| `tsconfig.tsbuildinfo` | `*.tsbuildinfo` ignore 정상 작동, 미커밋 확인 |
| `lib/db/local.sqlite` | 0바이트 고아 (D13-016) |

> ⚠ **원장 불일치**: 원장은 "ignored 잔존 **23개**"로 기재하나 열거 항목은 **22개**다. 또 V10 은 "표본 검증한 **11개**"라 적으면서 실제로는 10건을 명시 열거했다. 열거된 항목은 전부 EXISTS+IGNORED 로 판정 일치했으므로 **결론(전부 ignored)은 유효**하나 수치는 재확인 필요.

---

## 타 도메인 소관 죽은코드·위생 항목 — 교차 참조

`kind:"dead"` / `kind:"hygiene"` 중 D13 배정이 아닌 항목이다. 상세 판정과 수정 계획은 각 도메인 문서에 있다.

### dead

| ID | 판정/심각도 | 제목 | 축 |
|---|---|---|---|
| D08-008 | CONFIRMED **P1** | `access_logs` 에 기록하는 코드가 저장소에 전혀 없어 '접근 감사 로그' 화면과 KPI 가 영구히 0 | S10 |
| D03-025 | CONFIRMED P2 | `annual-leave-expiry.ts` 의 `calculateAnnualLeaveExpiryDate` import 미사용 | S8 |
| D06-013 | CONFIRMED P2 | chat-rooms PATCH 의 notice 분기와 헤더 주석이 실제 동작과 어긋난 죽은 코드 | S14 |
| D09-008 | CONFIRMED P2 | `initOfflineQueueFlush`·`enqueueSupabaseTransaction` 죽은 코드 — 이중 flush·프록시 직렬화 사고 잠복 | S14 |
| D08-020 | **REFUTED** P2 | `lib/system-master-staff-query.ts` 는 어디서도 쓰이지 않는 Postgres 시절 잔재 | S14 |

### hygiene

| ID | 판정/심각도 | 제목 | 축 |
|---|---|---|---|
| D01-019 | CONFIRMED P2 | `/api/consultation/analyze` 는 세션만 검사 — `extra_수술상담` 권한 없이 Gemini 분석 호출 가능(transcribe 와 게이트 불일치) | S3 |
| D01-021 | CONFIRMED P2 | `/api/payments/virtual-account-webhook` 이 여전히 query string 토큰 폴백 허용(경고만) | S3 |
| D02-008 | CONFIRMED P2 | `sendAdminNotifications` 가 `status='재직'` 필터 없이 부서 기준으로만 수신자 선정 — 퇴사자에게도 알림 생성 | S6 |
| D02-010 | CONFIRMED P2 | 크론 18개 라우트 전부 `CRON_SECRET` 을 비상수시간 문자열 비교(`!==`)로 검증 | S6 |
| D03-019 | CONFIRMED P2 | `useAnnualLeaveSummary` 가 API 실패 시 `error:null` + 전량 0 표시 — 오류가 정상값으로 위장 | S8 |
| D03-021 | CONFIRMED P2 | export-csv 가 `year=2026` 하드코딩 — 2027년부터 연도 라벨 오기 | S8 |
| D03-022 | CONFIRMED P2 | diagnose 라우트: 미사용 import + 'staff_members 미수정' 계약이 실제와 불일치 | S8 |
| D03-024 | CONFIRMED P2 | substitute-holiday: 원장 기록에 `company_id` null 고정 + 헤더 주석이 폐기된 동작 설명 | S8 |
| D04-018 | CONFIRMED P2 | 2026 건강보험 보수월액 상한이 2025 고시값으로 TODO 상태 | S13 |
| D04-021 | CONFIRMED P2 | payroll-record-upsert 의 에러 판별이 Postgres 전용 코드(22001/42P10)로 반쪽 사문 | S5 |
| D05-017 | CONFIRMED P2 | `/api/approvals/process-final` 이 기안자·참조자에게도 후속 처리 실행 권한 부여 | S9 |
| D07-017 | CONFIRMED P2 | notice-broadcast: 동일 staff 조회 코드 사본 2개 + 중복 발송 방지 부재 | S13 |
| D07-020 | CONFIRMED P2 | board/upload 서명 URL 경로: 이미지 무제한 크기 + 클라이언트 신고 `fileSize` + magic-byte 검사 부재 | S11 |
| D07-021 | CONFIRMED P2 | stock-post: 이관출고/이관입고를 단독 전표로 허용 — 반쪽 이관 로그 위조 가능 | S2 |
| D09-015 | CONFIRMED P2 | 체크인 성공 시 attendances 이중 기록 + 오프라인 체크인 지각판정 '정상' 폴백 영구화 | S10 |
| D09-017 | CONFIRMED P2 | 오프라인 체크아웃 0행 UPDATE 시 낙관적 성공 처리 | S10 |
| D10-013 | CONFIRMED P2 | 24h 백업 폴더키에 KST 날짜와 UTC 시각 병기 — KST 00:00 크론에서 두 날짜가 항상 하루 어긋남 | S7 |
| D01-020 | **REFUTED** P2 | `/api/notifications/push-config` 는 유일하게 게이트가 없음 | S11 |
| D01-022 | **REFUTED** P2 | `/api/notifications/chat-push-flush` 가 임의 인증 사용자에게 전역 푸시 큐 드레인 허용 | S4 |
| D10-010 | **REFUTED** P2 | `updated_at` 헬퍼 이중화: `withUpdatedAt` 은 ISO, `nowSqlite` 는 `CURRENT_TIMESTAMP` | S7 |

---

## 반증된 주장 (REFUTED)

이 도메인(D13-*)에서 레코드 전체가 오탐으로 판정된 항목은 **없다**(REFUTED 0건). 다만 CONFIRMED 레코드 안에서 반증된 하위주장이 다수 있으며, 9차에서 같은 오탐을 반복하지 않기 위해 기록한다.

| ID | 반증된 하위주장 | 반증 근거 |
|---|---|---|
| D13-004 | "DB 에 supabase.co URL 이 남아 있으면 제거 시 이미지가 깨진다"(감사자 falsifier) | falsifier 는 **실제로 트리거된다**(로컬 D1 실측 373행 히트). 그러나 `next/image` 원격 src 사용처가 0이라(유일 사용처 `BoardBodyPickerModal.tsx:61-62` 는 로컬 정적 자산) `remotePatterns` 는 어떤 요청 경로에서도 평가되지 않는다. 잔존 URL 은 `<img>` 등 비-`next/image` 경로로 렌더된다. **fix_sketch 의 '저장된 URL 잔존 행 수 확인' 전제는 불필요.** |
| D13-005 | "포크 PR 컨텍스트에 전권 키가 노출된다" | **GitHub 은 fork 발 `pull_request` 트리거 워크플로에 secrets 를 전달하지 않는다.** 실제 노출 경로는 "워크플로 스텝 중 하나가 임의 코드를 실행할 때"(`npm ci` 의 postinstall 등)로 한정된다. 여기에 옛 Supabase 프로젝트 생존 여부가 미확인이라 실제 유출면은 이론 수준 → **P1→P2 강등 사유** |
| D13-006 | "미설치 의존성으로 실행 즉시 `ERR_MODULE_NOT_FOUND`" | `package-lock.json:33` 루트 devDependencies 에 `@supabase/supabase-js ^2.108.1` 이 남아 있고 `node_modules/@supabase/{supabase-js,auth-js,postgrest-js,realtime-js,storage-js,functions-js,phoenix}` 가 실존한다. **두 스크립트는 실제로 로드된다** — 오히려 위험이 미세하게 커진다 |
| D13-007 | "`patch_remote.sql` 이 실명 산출물" | 헤더가 "대상 직원 수: 0명" 이고 `staff_id` UUID 벌크 INSERT 291건이라 **실명을 포함하지 않는다**(가명화 식별자). 또 감사자가 적은 "TEST_ 가상직원 4건"은 실측 **2건**이 정확(실명 46건은 일치) |
| D13-008 | "P1 급 — FCM 인증이 조용히 깨져 푸시 전면 실패" | 결함 자체는 확정(`lib/fcm-http.ts:12-18` 이 `JSON.parse(raw)` 로 raw JSON 을 파싱하므로 base64 규약이 없고, 값에 `{`·`}`·`"`·공백이 반드시 포함돼 cmd 인용이 깨진다). 그러나 `git ls-files scripts/deploy-worker.mjs` → 매치 없음(**untracked·미참조**)이고 `package.json`·워크플로·docs 어디에서도 호출되지 않으며 **안전한 대체 경로(`upload-secrets.mjs`)가 이미 존재**한다. 도달 경로가 수동 실행 1가지뿐 → **P1→P2 강등 사유** |
| D13-009 | "죽은 Supabase 키를 프로덕션 워커 시크릿으로 **계속 밀어넣는다**" | `:27` `if (match && match[1])` 가드가 있어 `.env.local` 에 해당 키가 없으면 업로드가 일어나지 않는다. 목록 존재까지만 확정이고 워커 상주 여부는 런타임 확인 사항. 확정된 결함은 **(b) 실패 시 평문 tmp 잔존**(`:30-44` try 안에서만 `unlinkSync`, catch 는 `console.error` 만, `finally` 부재) |
| D13-013 | "회귀 감지 수단이 0 이 된다" | `.github/workflows` 3종(deploy/e2e/hygiene)에 lint·eslint·tsc 스텝이 **0건**이고 hygiene 은 jscpd 만 `continue-on-error` 로 돌린다. 즉 lint 는 애초에 CI 게이트가 아니었으며, "깨진 게이트"가 아니라 **"처음부터 없는 게이트"** 다. 런타임·데이터 무결성 영향 0 → **P1→P2 강등 사유** |
| D13-019 | "보안 게이트 부재" | 과장이다. `tests/e2e/security-authorization.desktop.spec.ts:4-40` 이 비인증 `d1/mutate`·approval update·chat-rooms·quick-reply 를 동일하게 검증하며 desktop-chromium 프로젝트로 **실제 실행된다**. 6건 중 4건은 이미 커버되고 **실제 공백은 2건뿐**(비인증 `/api/d1/query` payroll_records select, 비인증 `/api/board/notice-broadcast`) → **P1→P2 강등 사유** |
| D13-012 | "handoff·electron-app·outputs 를 exclude 에 추가해야 한다" | 이 4개 디렉터리는 `.ts` 실물이 `node_modules` 하위뿐이라 **현재 tsc 에 포함되지 않는다**(중첩 node_modules 가 exclude 로 걸림). 추가는 **예방 목적**임을 명시해야 한다. 실제 구멍은 `scratch/` 하나(`test-birthday-cron.ts`·`test-payroll-load.ts` 2개가 tsc 루트 파일로 포함 중) |
| D13-100 | evidence 의 도메인별 P0 건수 | 감사자 세부 집계(D01 4, D03 11)가 실측(D01 6, D03 12)과 어긋난다. 전체 42행·D13=0 이라는 **핵심 결론은 유지** |

---

## 7차 대비 델타

| 7차 ID | 제목 | 판정 | 근거 |
|---|---|---|---|
| (P0 표 배정 0건) | 7차 P0 42건 중 죽은코드·설정·위생 도메인 배정 행 없음 | — | `b7-delta-index.md:24` 담당 도메인 정의가 D01~D11 만 열거하며 D13 을 포함하지 않는다. §1 표 담당 컬럼 기계 집계 결과 총 42행이고 값은 D01~D09 만 나타난다(D03 12, D01 6, D07 6, D08 5, D02 3, D04 3, D05 3, D06 2, D09 2). **판정 대상 0건**(D13-100). 7차 P0 중 위생·설정 성격 항목이 없어 정합하며, 8차 베이스라인(b6-wiring)의 위생 결함들은 Pass A 신규 레코드로 처리했다 |

### 참고 — 이 도메인에 배치된 7차 델타 REFUTED 항목 (S14 축)

`root-causes.md` S14 축에는 다른 도메인이 수행한 7차 델타 판정 중 **CLOSED 로 확인돼 REFUTED 로 정규화된** 항목 4건이 함께 묶여 있다. 판정 소관은 각 도메인이며 여기서는 축 소속만 기록한다.

| 7차 ID | 제목 | 판정 | 근거 |
|---|---|---|---|
| A2-02 (D03-D02) | 서류제출/계약서명 불능(`document_repository` ADMIN_ONLY_ALL) | **CLOSED** | `document_repository` SELF_OR_SAME_COMPANY 명시 등록으로 해소 |
| A2-03 (D03-D03) | 할일(todos) 전면 불능 | **CLOSED** | `todos` SELF_ONLY 명시 정책 등록, 소비처 6곳 네임스페이스 일치 |
| A2-04 (D03-D04) | 본인 증명서 조회 불능(`certificate_issuances`) | **CLOSED** | STAFF_IN_SCOPE / SELF insert 로 해소 |
| A7-01 (D03-D06) | export-csv 무인증 PII 유출 | **CLOSED** | 관리자 게이트 추가로 해소 |
