# MSO 디자인 진단 보고서 (심층 2차)
> 작성일: 2026-05-10 | 대상: `app/` 전체 333개 TSX 파일 | 1차 대비 신규 발견 포함

---

## 핵심 요약

| 항목 | 심각도 | 파일수 | 건수 | 상태 |
|------|--------|--------|------|------|
| `slate/gray/zinc` 라이트 하드코딩 | 🔴 높음 | ~80 | 382 | 신규 확인 |
| 다크모드 미대응 상태 색상 (`bg-*-50`) | 🔴 높음 | 182 | 900+ | 광범위 |
| 다크모드 미대응 테두리 (`border-*-200`) | 🔴 높음 | 101 | 400+ | 광범위 |
| `--radius-sm`=`--radius-md`=6px 버그 | 🟠 중간 | 1 (globals.css) | 1 | **확인됨** |
| `bg-white` 비인쇄용 사용 | 🟠 중간 | 8 | 20 | 신규 확인 |
| aria-label 누락 | 🟠 중간 | ~15 | ~30 | 신규 확인 |
| `shadow-xl/lg` 과도 사용 | 🟡 낮음 | 12 | 21 | 신규 확인 |
| `rounded-full` 버튼 사용 | 🟡 낮음 | 3 | 6 | 신규 확인 |
| `rounded-[px]` 하드코딩 | ✅ 없음 | 0 | 0 | 양호 |
| z-index 하드코딩 | ✅ 없음 | 0 | 0 | 양호 (CSS 변수 사용) |

---

## 1. CSS 변수 미대응 — 다크모드 전반 🔴

### 1-1. `slate/gray/zinc` 라이트 계열 직접 사용 (382건)

가장 광범위한 문제. 다크모드에서 텍스트·배경이 배경색에 묻힘.

**주요 파일별**:
| 파일 | 건수 | 예시 |
|------|------|------|
| `인사관리서브/계약문서/계약서미리보기.tsx` | 20+ | `bg-slate-50`, `bg-slate-100`, `border-slate-200` |
| `퇴원심사규정빌더.tsx` | 18+ | `slate-*`, `gray-*` 혼용 |
| `OP체크.tsx` | 8+ | `gray-50`, `gray-300`, `gray-500` |
| `메신저드로어.tsx` | 3+ | `zinc-300` |
| `메신저패널.tsx` | 2+ | `zinc-100`, `zinc-500` |
| `roster/RosterFairnessBoard.tsx` | 3 | `zinc-100`, `zinc-200`, `zinc-700` |

**수정 매핑**:
```
slate-50 / gray-50 / zinc-50   → bg-[var(--tab-bg)] 또는 bg-[var(--muted)]
slate-100 / gray-100           → bg-[var(--muted)]
slate-200 / gray-200 (테두리)  → border-[var(--border)]
slate-500 / gray-500 (텍스트)  → text-[var(--toss-gray-3)]
slate-600 / gray-600 (텍스트)  → text-[var(--toss-gray-4)]
slate-700 / gray-700 (텍스트)  → text-[var(--toss-gray-5)]
slate-900 / gray-900 (텍스트)  → text-[var(--foreground)]
```

### 1-2. 상태 색상 `bg-*-50` 라이트 전용 (182개 파일)

globals.css 패치가 `bg-white`, `bg-gray-50`만 커버 — `bg-blue-50`, `bg-red-50` 등은 **미패치**.

| 패턴 | 발견 파일 수 | 다크모드 영향 |
|------|-----------|------------|
| `bg-blue-50` | ~45 | 정보 섹션 배경이 라이트모드 색으로 고정 |
| `bg-red-50` | ~30 | 경고 영역 배경 문제 |
| `bg-orange-50` | ~25 | 진행 중 상태 배경 문제 |
| `bg-emerald-50` | ~15 | 성공 상태 배경 문제 |
| `bg-amber-50` | ~20 | 주의 상태 배경 문제 |

**단기 해결책** — globals.css에 패치 추가:
```css
/* globals.css에 추가 (다크모드 패치 확장) */
:root.dark .bg-blue-50 { background-color: color-mix(in srgb, #1d4ed8 15%, transparent) !important; }
:root.dark .bg-red-50 { background-color: color-mix(in srgb, #dc2626 15%, transparent) !important; }
:root.dark .bg-orange-50 { background-color: color-mix(in srgb, #ea580c 15%, transparent) !important; }
:root.dark .bg-emerald-50 { background-color: color-mix(in srgb, #059669 15%, transparent) !important; }
:root.dark .bg-amber-50 { background-color: color-mix(in srgb, #d97706 15%, transparent) !important; }
:root.dark .border-blue-100, :root.dark .border-blue-200 { border-color: #1e3a5f !important; }
:root.dark .border-red-100, :root.dark .border-red-200 { border-color: #5c1a1a !important; }
```

**장기 해결책** — 파일별 `dark:` modifier 추가:
```tsx
// 현재
<div className="bg-blue-50 text-blue-700">

// 수정
<div className="bg-blue-500/10 dark:bg-blue-950/40 text-blue-700 dark:text-blue-300">
```

### 1-3. 라이트 전용 테두리 (101개 파일)

`border-*-100`, `border-*-200`이 `dark:` 없이 사용 → 다크모드에서 테두리 소멸.

**즉시 수정 필요 파일**:
- `RiskActionDialog.tsx`: `border-red-200`, `border-blue-200`, `border-amber-200`
- `게시판.tsx`: `border-blue-100` (dark 없음)
- `인사관리서브/계약문서/계약서미리보기.tsx`: `border-slate-200` 전체

---

## 2. `--radius-sm` = `--radius-md` = 6px 버그 🟠

**파일**: `app/globals.css` 줄 57-58

```css
/* 현재 — sm과 md가 동일, 의미 없음 */
--radius-xs: 4px;
--radius-sm: 6px;
--radius-md: 6px;  ← 버그: sm과 동일
--radius-lg: 8px;
--radius-xl: 10px;
```

`rounded-[var(--radius-sm)]`과 `rounded-[var(--radius-md)]`이 시각적으로 완전히 동일.

**수정 제안**:
```css
/* 수정 후 - 시각적 계층 구분 */
--radius-xs: 4px;
--radius-sm: 6px;
--radius-md: 8px;   ← lg와 swap, md를 의미있게
--radius-lg: 10px;
--radius-xl: 14px;
```

> ⚠️ 변경 시 전체 UI에 영향. 빌드·스냅샷 비교 후 적용.

---

## 3. `bg-white` 비인쇄용 사용 🟠 (20건)

| 파일 | 줄번호 | 용도 | 수정 방향 |
|------|-------|------|---------|
| `StatePanel.tsx` | 24, 28, 32, 36 | 배지 배경 | `bg-[var(--card)]` |
| `ApprovalDetailModal.tsx` | 118, 123, 127, 242 | 모달 내부 | `bg-[var(--card)]` |
| `계약서미리보기.tsx` | 162, 458, 468 | 문서 뷰어 | 인쇄용만 유지, 뷰어→`var(--card)` |
| `ESL관리.tsx` | 550 | 컨테이너 배경 | `bg-[var(--muted)]` |
| `수술상담.tsx` | 431, 440 | 상태 점 | `bg-[var(--card)]` |

**ApprovalDetailModal.tsx 헤더는 이번 CODEX_TASKS에서 1곳 수정됨** — 나머지 3곳 잔존.

---

## 4. 접근성 문제 🟠 (신규 발견)

### aria-label 누락 버튼 (~30곳)

| 파일 | 줄번호 | 현재 | 수정 |
|------|-------|------|------|
| `마이페이지/서류제출.tsx` | 500 | `<button>✕</button>` | `aria-label="닫기"` 추가 |
| `공휴일달력.tsx` | 343-345 | `<button>◀</button>`, `<button>▶</button>` | `aria-label="이전 달"`, `"다음 달"` |
| `인사관리서브/구성원현황.tsx` | 1370 | `<button>✕</button>` | `aria-label="닫기"` 추가 |
| `알림시스템.tsx` | 여러 곳 | `<button>✕</button>` | `aria-label="알림 닫기"` 추가 |
| `법인카드사용내역.tsx` | 여러 곳 | `<button>✕</button>` | `aria-label="닫기"` 추가 |

**z-index**: CSS 변수로 완전 관리됨 (`var(--z-modal)` 등) ✅

### 미완성 UI (숨겨진 기능)

| 파일 | 줄번호 | 내용 | 조치 |
|------|-------|------|------|
| `roster/RosterWizardRuleSetupSection.tsx` | 286 | `<label className="hidden">` — 미구현 설정 필드 | 구현 또는 제거 |
| `메신저사이드바.tsx` | 138 | `<button className="hidden">` — 레거시 버튼 | 제거 |

---

## 5. `shadow-xl/lg` 과도 사용 🟡 (21건)

모달/드롭다운의 `shadow-lg`는 허용. 수정이 필요한 것:

| 파일 | 클래스 | 수정 방향 |
|------|-------|---------|
| `마이페이지/서류제출.tsx:278` | `shadow-xl` (카드에 직접 사용) | `shadow-sm`으로 교체 |
| `조직도그림.tsx` | `hover:shadow-xl` (transition) | `hover:shadow-md`로 교체 |

---

## 6. `rounded-full` 버튼 사용 🟡 (6건)

배지·점·인디케이터는 허용. 실제 클릭 버튼에만 금지:

| 파일 | 줄번호 | 유형 | 수정 |
|------|-------|------|------|
| `op-check-components.tsx` | 631, 640, 666 | pill 필터 버튼 | `rounded-[var(--radius-md)]` |
| `OP체크.tsx` | 2718, 2976, 3126 | 액션 버튼 | `rounded-[var(--radius-md)]` |

---

## 7. 디자인 시스템 CSS 변수 현황 (확인 완료)

### radius 변수 (globals.css 기준)

```css
--radius-xs: 4px
--radius-sm: 6px   ← sm=md 버그
--radius-md: 6px   ← sm=md 버그
--radius-lg: 8px
--radius-xl: 10px
--radius-2xl: 12px
--radius-popover: 14px
--radius-3xl: 16px
--radius-dialog: 18px
--radius-sheet: 20px
--radius-display: 24px
--radius-modal: 28px
```

### 양호한 것들 ✅

- `rounded-[var(--radius-*)]` 사용 (하드코딩 0건)
- z-index CSS 변수 완전 관리
- CSS utility class (`.app-card`, `.erp-stat-card`, `.badge-*`) 정상 사용
- globals.css 기존 패치 (`bg-white`, `bg-gray-50`) 작동 중
- 상태 색상 일관성 (부분): StatePanel.tsx에서 `dark:` modifier 추가됨 (이번 CODEX_TASKS에서 수정)

---

## 우선순위 조치 계획

### 즉시 (1-2일, 영향도 높음)
1. **globals.css 다크모드 패치 확장** — `bg-blue-50/red-50/orange-50/emerald-50/amber-50` + 테두리 (파일 1개)
2. **aria-label 일괄 추가** — icon-only 버튼 30곳 (30분 작업)
3. **`--radius-md` 수정** — 6px → 8px (globals.css 1줄)

### 단기 (1주일)
4. **`ApprovalDetailModal.tsx` 내부 bg-white 제거** — 3곳
5. **`slate/gray/zinc` → CSS 변수 교체** — 계약서미리보기, 퇴원심사규정 우선 (50건)
6. **`rounded-full` 버튼 6곳 교체**

### 중기 (1-2개월)
7. **182개 파일 상태 색상 `dark:` modifier 추가** — 롤링 업데이트
8. **101개 파일 테두리 `dark:` modifier 추가**

---

*1차 보고서 대비 추가: slate/gray/zinc 382건(신규), bg-white 20건(신규), shadow-xl 21건(신규), aria-label 30건(신규), radius-sm=radius-md=6px 버그 확인(수정: 이전 보고서에서 radius-md=radius-lg=8px로 잘못 기재되었음).*
