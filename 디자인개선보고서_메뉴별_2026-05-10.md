# MSO 세부 메뉴 디자인 개선 보고서
> 작성일: 2026-05-10 | 에이전트 6개 병렬 분석 통합  
> 기준: 2025~2026 최신 UI/UX 트렌드 (Slack·Linear·Notion·Figma·Vercel)

---

## 전체 개선 우선순위 맵

| 순위 | 메뉴 | 핵심 문제 | 난이도 | 효과 |
|------|------|----------|--------|------|
| 🔴1 | 사이드바 네비게이션 | 아이콘만, 라벨 없음 | 낮음 | 신규 사용자 온보딩 |
| 🔴2 | 전역 검색 | 드롭다운 방식, 한글 1글자 불가 | 중간 | 생산성 핵심 |
| 🔴3 | 서류제출 | 16개 카드 과밀, 모바일 2열 찌그러짐 | 낮음 | 모바일 사용성 |
| 🔴4 | 재고 현황 | 수량 숫자만, 부족도 게이지 없음 | 중간 | 재고 관리 직관성 |
| 🔴5 | 전자결재 결재라인 | 단계별 타임라인 없음 | 중간 | 결재 흐름 파악 |
| 🟡6 | 출퇴근기록 | CTA 버튼 가시성 낮음, 차트 56px | 낮음 | 일일 핵심 기능 |
| 🟡7 | 관리자 재무대시보드 | 차트 없음, DB 연동 안 됨 | 높음 | 경영 의사결정 |
| 🟡8 | 게시판 | 작성자 아바타·본문 미리보기 없음 | 낮음 | 정보 스캔 속도 |
| 🟡9 | 알림 시스템 | 읽음/안읽음 구분 약함, 그룹화 없음 | 중간 | 알림 신뢰도 |
| 🟡10 | 로그인 | 비밀번호 토글 없음, 로딩 스피너 없음 | 낮음 | 첫인상·신뢰도 |
| 🟡11 | 월간근태달력 | 모바일 대응 없음, 색각이상자 고려 없음 | 중간 | 근태 관리 |
| 🟡12 | OP체크 진행률 | 체크 완료율 시각화 없음 | 낮음 | 수술 전 안전 |
| 🟢13 | 메신저 | 이모지 반응 없음, 멤버 아바타 없음 | 높음 | 협업 경험 |
| 🟢14 | 간호근무표 | 셀 터치 타겟 30px (44px 필요) | 낮음 | 모바일 사용성 |
| 🟢15 | 역할별 대시보드 | 긴급도 강조 약함 | 낮음 | 관리자 스캔 |

---

## 1. 공통/전역 컴포넌트

### 1-1. 사이드바 네비게이션 🔴

**현재 문제**:
- 72px 아이콘만 표시 → 신규 사용자가 메뉴 역할 파악 불가
- 호버 시 라벨 팝업 없음
- 현재 위치 브레드크럼 없음

**개선안**:

```
① 호버 툴팁 라벨 (즉시 적용 가능)
   - 마우스 호버 시 좌측으로 라벨 말풍선 표시
   - ml-2, z-40, bg-foreground text-white text-xs

② 사이드바 확장 토글 (선택)
   - 고정 버튼으로 72px → 192px 확장
   - transition-all으로 부드러운 전환

③ 상단 브레드크럼 추가
   - "홈 / 인사관리 / 근태기록" 형태
   - 8px 텍스트, border-b로 분리
```

**레퍼런스**: Slack 사이드바(호버 툴팁), Figma(토글 확장), GitHub(브레드크럼)

---

### 1-2. 전역 검색 (GlobalSearch) 🔴

**현재 문제** (`app/components/GlobalSearch.tsx:110-121`):
- 드롭다운 방식 — Spotlight 패턴 아님
- 한글 2글자 이상만 검색 가능 (1글자 불가)
- 결과 항목에 타입 아이콘 없음
- "검색 결과 없음" Empty state 없음

**개선안**:

```
① Spotlight 패턴으로 전환 (Cmd+K)
   - 전체화면 오버레이 + 중앙 검색창
   - backdrop-blur-sm + max-w-xl 카드

② 한글 1글자 검색 지원
   - 한글/영문 1글자도 허용 (정규식으로 구분)

③ 결과 항목 강화
   - 타입별 아이콘: 👤직원 📝게시글 📋결재 💬메시지
   - 키보드 ↑↓ 네비게이션 유지

④ Empty state
   - "'{query}'에 대한 검색 결과가 없습니다"
```

**레퍼런스**: Linear(Cmd+K Spotlight), VS Code(Cmd+P), Vercel Dashboard(타입 아이콘)

---

### 1-3. 알림벨 (GlobalNotificationBell) 🟡

**현재 문제** (`app/components/GlobalNotificationBell.tsx`):
- 알림 배지(빨간 카운트 뱃지) UI 코드 부재
- 타입별 아이콘이 이모지 — 다크모드 렌더링 불안정
- "모두 읽음" 버튼 없음
- Toast 5초 자동 닫힘, 수동 닫기 불가

**개선안**:

```
① 알림 배지 추가
   - 벨 아이콘 우상단 -top-1 -right-1
   - 99+ 초과 처리

② SVG 아이콘으로 교체
   - 이모지 → Heroicons/Lucide SVG
   - 다크모드 색상 자동 대응

③ "모두 읽음" 버튼
   - 드롭다운 헤더 우측에 텍스트 버튼

④ Toast 수동 닫기
   - X 버튼 추가, 진행바(5초) 시각화
```

---

### 1-4. 로그인 페이지 🟡

**현재 문제** (`app/login/page.tsx`):
- 비밀번호 보기/숨기기 토글 없음 (줄 141)
- 로그인 중 버튼에 CSS 스피너 없음 (줄 162)
- 에러 메시지에 아이콘 없음 (줄 152~156)
- 로고/브랜딩 아이콘 없음 (텍스트만)

**개선안**:

```
① 비밀번호 토글 버튼
   - input 우측에 절대 위치 눈 아이콘
   - type="password" ↔ type="text" 전환

② 로딩 스피너
   - border-2 border-white border-t-transparent animate-spin
   - "인증 중..." 텍스트 병행 표시

③ 에러 메시지 강화
   - 왼쪽 ⚠️ 아이콘 + bg-danger-light

④ 브랜딩 로고
   - 상단 accent 색 사각 아이콘 "SY" 이니셜
```

**레퍼런스**: Vercel(로고+로딩), GitHub(에러 아이콘), Supabase(비밀번호 토글)

---

## 2. 마이페이지

### 2-1. 출퇴근기록 🟡

**현재 문제** (`app/main/기능부품/마이페이지/출퇴근기록.tsx`):
- 출근/퇴근 버튼이 우측 끝 — 모바일에서 스크롤 후 탭해야 함 (줄 1125~1145)
- 근무시간 차트 높이 **56px** — 추이 파악 불가 (줄 1379)
- 달력 셀 크기 너무 작음, 상태 텍스트 5px 수준 (줄 1303~1318)
- 데이터 없을 때 차트 미렌더 + 안내 없음 (줄 1185)

**개선안**:

```
① 출근/퇴근 버튼 → Sticky Footer 고정
   - 모바일: 화면 하단 고정, 항상 접근 가능
   - 데스크톱: 카드 상단 고정 유지

② 차트 높이 확대
   - 56px → 최소 120px
   - 평균 근무시간 점선 참고선 추가

③ 달력 셀 개선
   - aspect-square → min-h-[48px]
   - 상태 아이콘 추가 (✓지각⚠ 결근✕)

④ 빈 상태 안내
   - "이번 달 근무 기록이 없습니다"
   - 첫 출근 버튼으로 CTA 연결
```

---

### 2-2. 서류제출 🔴

**현재 문제** (`app/main/기능부품/마이페이지/서류제출.tsx`):
- 16개 카드 `xl:grid-cols-6` — 모바일 2열에서 찌그러짐 (줄 283~340)
- 카드 높이 유동적 — 버튼이 아래로 밀림
- 제출 완료 상태가 배경색만 (체크마크 10px로 너무 작음)
- 카메라 촬영 가이드 불명확 (줄 490~555)

**개선안**:

```
① 카드 그리드 반응형
   - mobile: grid-cols-2
   - tablet: grid-cols-3  
   - desktop: grid-cols-4 (6열 → 4열로 줄임)

② 카드 높이 고정
   - h-[140px] 고정 + overflow 처리

③ 제출 완료 배지
   - 카드 좌상단에 초록 ✓ 배지 (절대 위치)
   - 카드 전체 opacity-60 + pointer-events-none

④ 서류 메타 정보 추가
   - "✓ 필수" / "⏰ 유효기간 1년" 작은 배지

⑤ 촬영 가이드 개선
   - 영역 음영 + "여기에 맞춰 촬영하세요" 문구
   - 버튼 "추가" / "제출"로 축약
```

---

### 2-3. 급여명세서 🟡

**현재 문제** (`app/main/기능부품/마이페이지/급여명세서/index.tsx`):
- 최근 6개월 차트에 평균선·참고선 없음 (줄 77~106)
- 월 선택 드롭다운 연도별 그룹핑 없음 (줄 362~376)
- 빈 상태 안내 문구 모호 (줄 321~329)

**개선안**:

```
① 차트 강화
   - 평균 실지급액 점선 참고선 추가
   - 호버 시 해당 월 상세 툴팁

② 월 선택 UX 개선
   - 좌/우 화살표 월 이동 버튼
   - 또는 연도별 그룹 드롭다운

③ 빈 상태 문구
   - "명세서가 아직 발송되지 않았습니다"
   - "인사팀에 문의: 내선 000" CTA 추가
```

---

## 3. 인사관리

### 3-1. 월간근태달력 🔴

**현재 문제** (`app/main/기능부품/인사관리서브/근태기록/월간근태달력.tsx`):
- 7일 고정 테이블 — 모바일 수평 스크롤 필수 (줄 78~128)
- 상태가 배경색만 — 색각이상자 구분 불가 (줄 92~95)
- 주차 선택 버튼이 날짜 텍스트만 (줄 36~49)

**개선안**:

```
① 반응형 전환
   - PC: 현재 7일 테이블 유지
   - 모바일: 개인별 일별 카드 (스와이프 네비게이션)

② 상태 3중 표현 (색상 + 아이콘 + 텍스트)
   - ✓ 초록: 정상
   - ⚠️ 주황: 지각 (09:15 시간 표시)
   - ✕ 빨강: 결근 (사유 표시)
   - 🏖️ 파랑: 휴가 (연차/병가 구분)

③ 셀 인터랙션 강화
   - 호버 시 파란 그림자
   - 선택 주차 배경 accent/5 미세 표시
```

---

### 3-2. 급여대장표 🔴

**현재 문제** (`app/main/기능부품/인사관리서브/급여명세/급여대장표.tsx`):
- 모바일 완전 숨김 `hidden md:block` (줄 102~103)
- 컨트롤 영역(선택·통계·버튼)이 3개 섹션 분산 (줄 76~99)
- 합계 행이 스크롤과 함께 사라짐

**개선안**:

```
① 모바일 카드 그리드
   - hidden md:block 유지 + 별도 모바일 카드 뷰
   - 직원명 + 실지급액만 카드로 표시

② 컨트롤 영역 통합
   - 3단 그리드: 선택 | 통계 | 액션 버튼

③ Sticky 합계 행
   - <tfoot className="sticky bottom-0">
   - 배경 muted + 상단 border-2 accent
```

---

### 3-3. 구성원현황 🟡

**현재 문제** (`app/main/기능부품/인사관리서브/구성원현황.tsx` — 2,079줄):
- 신규/편집 폼이 파일 절반 이상 차지 → 별도 분리 필요
- 헤더 영역이 모바일에서 5줄 이상 차지 (줄 1107~1118)
- 행 클릭 영역 불명확

**개선안**:

```
① 파일 분리 (JM 규칙)
   - 구성원현황.tsx → 목록 뷰만 (500줄 이내)
   - 구성원편집모달.tsx → 신규/편집 폼 분리

② 헤더 재설계
   - sticky top-0 헤더
   - 제목 + 카운트 요약 1줄
   - 탭/필터/검색 1줄

③ 행 호버 인터랙션
   - group-hover:bg-accent/5
   - 우측 액션 버튼 opacity-0 → group-hover:opacity-100
```

---

### 3-4. 간호근무표 🟢

**현재 문제** (`app/main/기능부품/인사관리서브/간호근무표.tsx`):
- 셀 크기 w-8 h-9 = **30px** — 터치 타겟 최소 44px 미달 (줄 400~439)
- 생성 버튼 로딩이 ⏳ 이모지만 (줄 843~847)
- 테이블 좌측 직원명 고정 없음 (긴 표에서 컬럼 헤더 매칭 불가)

**개선안**:

```
① 셀 크기 확대
   - w-8 h-9 → w-10 h-10 (md: w-12 h-12)
   - rounded-lg + font-bold text-sm 유지

② 로딩 스피너
   - ⏳ → CSS animate-spin 원형 스피너

③ Sticky 헤더 + 좌측 고정 열
   - thead sticky top-0 z-20
   - 직원명 컬럼 sticky left-0 z-30
```

---

## 4. 재고관리

### 4-1. 재고 현황 게이지 🔴

**현재 문제** (`재고관리서브/재고현황뷰.tsx`):
- 재고 수량이 숫자 텍스트만 (줄 671~771)
- 부족 품목 긴급 배너가 `className="hidden"` 으로 완전 숨겨짐 (줄 429~466)
- InventorySummaryStrip의 tone 이름 비일관성 (줄 305~312)

**개선안**:

```
① 재고 수량 → 진행률 바
   현재재고/최소재고 비율로 바 표시
   - 100%+: emerald (정상)
   - 50~100%: amber (주의)
   - 50% 미만: red (부족)
   옆에 "현재 23개 / 최소 30개" 텍스트 병행

② 긴급 배너 활성화
   - className="hidden" 제거
   - sticky top-0 z-40 배치
   - 부족 품목 최대 3개 chip으로 표시

③ 카테고리 색상 CSS 오류 수정
   - 'bg-blue-500/100' → 'bg-blue-500'
```

---

### 4-2. 발주 워크플로우 시각화 🔴

**현재 문제** (`재고관리서브/발주관리.tsx`):
- 발주 단계(요청→승인→입고)가 배지 텍스트만
- D-day 계산 함수의 CSS 문법 오류 (줄 57~69: `bg-red-500/100/10`)
- 발주 자동 생성 모달에 부족 품목 미리보기 없음 (줄 214~268)

**개선안**:

```
① 발주 단계 타임라인 UI
   - ① 요청 → ② 승인 → ③ 발송 → ④ 입고
   - 완료 단계: emerald 원형 체크
   - 현재 단계: accent + animate-pulse
   - 미완료: border gray

② CSS 문법 오류 수정
   - 'bg-red-500/100/10' → 'bg-red-500/10'

③ 발주 생성 모달 미리보기
   - 부족 품목 목록 + 자동 계산 수량 표시
   - "발주 수량 수정" 인라인 가능하게
```

---

## 5. 전자결재

### 5-1. 결재라인 타임라인 🔴

**현재 문제** (`전자결재서브/ApprovalInboxView.tsx`, `ApprovalDetailModal.tsx`):
- 결재자 순서·상태가 단순 텍스트 나열
- 필터 UI가 4개 select 수직 나열 — 모바일 UX 불량 (줄 189~253)
- 일괄 처리 UI가 모달 위에 모달 (중복 레이어)

**개선안**:

```
① 결재라인 수평 타임라인
   기안자 → 팀장 → 부서장 → 최종 승인
   - 완료: ✓ emerald 원
   - 현재: → amber animate-pulse
   - 대기: 회색 빈 원
   - 연결선: 완료 구간은 채워진 선

② 필터 Collapsible 패널
   - 기본 접힘 상태
   - "필터 3개 적용 중" 요약 표시
   - 그리드 2열(모바일) / 4열(PC)

③ 일괄 결재 → 우측 사이드 패널
   - fixed right-0 top-0 h-full w-[400px]
   - 반려 사유 textarea 포함
   - 기존 모달 중첩 제거
```

---

### 5-2. 전자서명 캔버스 🟡

**현재 문제** (`인사관리서브/계약문서/전자서명패드.tsx`):
- 캔버스 고정 크기 400×200px — 모바일 서명 정확도 낮음 (줄 108)
- 색상이 `#000000` 하드코딩 — 디자인 시스템 미연동 (줄 12~13)
- 버튼 영역 flex-wrap 없음 — 모바일 겹침 (줄 166~187)

**개선안**:

```
① 반응형 캔버스
   - containerRef로 실제 너비 감지
   - width: Math.min(containerWidth, 500)px
   - height: 200px 유지

② 색상 변수 연동
   - '#000000' → 'var(--foreground)'
   - '#2563EB' → 'var(--accent)'
   - '#EF4444' → 'var(--danger)'

③ 버튼 wrap 처리
   - flex-wrap 추가 또는 grid-cols-3
```

---

## 6. 게시판·메신저·알림

### 6-1. 게시판 목록 🟡

**현재 문제** (`app/main/기능부품/게시판.tsx`):
- 목록이 제목만 표시 — 내용 미리보기 없음
- 작성자 아바타 없음
- 첨부 이미지 썸네일 없음
- 카드형/목록형 토글 없음

**개선안**:

```
① 목록 아이템 구조 개선
   [아바타 44px] [제목 bold] [첫 100자 미리보기 gray]
                [부서 배지] [날짜] [조회수 👁 댓글 💬]

② 상태 배지
   - [공지] [수정됨] [첨부] 등 배지 추가

③ 첨부 이미지 썸네일
   - 첫 이미지 56×56px 우측 표시

④ 레이아웃 토글 (선택)
   - 우측 상단 리스트/카드 아이콘 버튼
```

**레퍼런스**: Notion(아바타+요약), Linear(상태 배지)

---

### 6-2. 메신저 🟢

**현재 문제**:
- 메시지에 읽음 체크마크 없음
- 이모지 반응 기능 없음
- 그룹 채팅 헤더에 멤버 아바타 없음
- 이미지 직접 업로드 시 인라인 썸네일 불명확

**개선안**:

```
① 읽음 표시
   - 내 메시지 우측 하단에 ✓✓ (읽음/안읽음)
   - 회색(전송) → 파랑(읽음) 색상 전환

② 이모지 반응 (Phase 2)
   - 메시지 호버 시 + 버튼
   - 이모지 피커 → 반응 저장/표시

③ 그룹 헤더 멤버 아바타
   - 최대 3개 겹침 + "+N" 표시

④ 인라인 이미지 미리보기
   - 업로드 이미지 200×150px 썸네일
```

**레퍼런스**: KakaoTalk(읽음), Slack(이모지+스레드)

---

### 6-3. 알림 시스템 🟡

**현재 문제** (`app/main/기능부품/알림시스템.tsx`):
- Toast 카드 아이콘·제목·시간·액션 배치 불명확
- 읽음/안읽음 시각적 구분 약함
- 알림 유형별·시간별 그룹화 없음
- 액션 버튼 ("열기" 등) 없음

**개선안**:

```
① Toast 카드 재설계
   [🔔 아이콘] [제목 bold 14px] [닫기 ✕]
   [내용 12px gray] [7초 진행바]

② 읽음/안읽음 구분
   - 안읽음: 좌측 4px 파란 막대 + bold 제목
   - 읽음: 회색 배경 + 일반 굵기

③ 시간별 그룹화
   - "오늘" / "어제" / "이번 주" 섹션 헤더

④ 액션 버튼
   - 우측: "열기" 버튼 (accent 색)
   - 모바일: 좌측 스와이프 → 삭제
```

---

## 7. OP체크·대시보드

### 7-1. OP체크 진행률 🟡

**현재 문제** (`app/main/기능부품/OP체크.tsx` — 4,262줄):
- 체크리스트 완료율 시각화 없음
- 필수 항목 강조 없음
- 재고 차감이 별도 비동기 모달로 흐름 단절

**개선안**:

```
① 체크리스트 진행률 바
   상단에 "준비 체크: 5/8 (62%)"
   - 0~50%: red 바
   - 50~80%: amber 바
   - 80%+: green 바

② 필수 항목 배지
   - [필수] 빨간 배지 + 미체크 시 항목 강조

③ 재고 차감 인라인 통합
   - 상태 변경 모달 내 체크박스로 통합
   - "완료 처리 + 재고 차감" 원클릭

> 추가: 4,262줄 파일 분리 필요 (JM 규칙)
```

---

### 7-2. 관리자 재무대시보드 🟡

**현재 문제** (`app/main/기능부품/관리자전용서브/재무대시보드.tsx`):
- 현금 흐름 카드가 숫자만 (차트 없음, 줄 75~124)
- 기간 선택 Q1~Q4가 DB 연동 안 됨 (줄 6, 34)
- AI 인사이트가 "데이터 부족" 플레이스홀더만 (줄 109~122)

**개선안**:

```
① 차트 추가 (Recharts 또는 내장 SVG)
   - 월별 현금 흐름 선/면적 차트
   - 부서별 예산 가로 막대 차트
   - 분기 비교 차트

② 기간 필터 DB 연동
   - Q1~Q4 선택 시 실제 Supabase date_range 쿼리

③ AI 인사이트 처리
   - 데이터 없을 때: "데이터 부족" 대신 숨김 처리
   - 실제 데이터 있을 때만 AI 인사이트 표시

④ KPI 카드 트렌드
   - 금액 옆에 ↑+5% / ↓-3% 추세 표시
```

---

### 7-3. 커스텀 대시보드 🟡

**현재 문제** (`app/main/기능부품/관리자전용서브/커스텀대시보드.tsx`):
- 위젯 위치 저장 안 됨 (줄 97~99)
- 캐시 없음 — 매번 refetch
- 로딩 스켈레톤 단순

**개선안**:

```
① 위젯 배치 저장
   - localStorage 또는 Supabase user_settings 테이블
   - 자동 저장 + 실행취소 (Ctrl+Z)

② 데이터 캐시
   - 5분 캐시 정책 + 수동 새로고침 버튼
   - 마지막 업데이트 시간 표시

③ 스켈레톤 개선
   - .skeleton 유틸리티 클래스 활용 (globals.css 정의됨)
   - 카드 모양 맞춤 스켈레톤
```

---

## 즉시 실행 로드맵

### Sprint 1 — 1주 (빠른 효과)
| 작업 | 파일 | 시간 |
|------|------|------|
| 로그인 비밀번호 토글 + 스피너 | `app/login/page.tsx` | 2h |
| 사이드바 호버 툴팁 | `조직도측면창.tsx` | 3h |
| 재고 긴급 배너 활성화 (`hidden` 제거) | `재고현황뷰.tsx` | 1h |
| CSS 색상 문법 오류 수정 | `카테고리관리.tsx`, `발주관리.tsx` | 1h |
| OP체크 진행률 바 추가 | `OP체크.tsx` | 3h |
| 알림 배지 카운트 | `GlobalNotificationBell.tsx` | 2h |

### Sprint 2 — 2주 (핵심 UX)
| 작업 | 파일 | 시간 |
|------|------|------|
| GlobalSearch → Spotlight 패턴 | `GlobalSearch.tsx` | 6h |
| 재고 수량 진행률 바 | `재고현황뷰.tsx` | 4h |
| 전자결재 결재라인 타임라인 | `ApprovalDetailModal.tsx` | 4h |
| 출퇴근 버튼 Sticky Footer | `출퇴근기록.tsx` | 3h |
| 서류제출 카드 그리드 반응형 | `서류제출.tsx` | 3h |

### Sprint 3 — 3~4주 (품질 개선)
| 작업 | 파일 | 시간 |
|------|------|------|
| 월간근태달력 반응형 | `월간근태달력.tsx` | 5h |
| 게시판 목록 아바타 + 미리보기 | `게시판.tsx` | 4h |
| 알림 시간별 그룹화 | `알림시스템.tsx` | 4h |
| 재무대시보드 차트 연동 | `재무대시보드.tsx` | 8h |
| 전자서명 캔버스 반응형 | `전자서명패드.tsx` | 3h |

---

*실제 코드 분석 기반 — 구체적인 줄번호와 현재 구현 방식 검증 후 작성됨.*
